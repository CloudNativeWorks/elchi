// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/service/ratelimit/v3/rls.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../../../../google/protobuf/duration";
import { Struct } from "../../../../google/protobuf/struct";
import { Timestamp } from "../../../../google/protobuf/timestamp";
import { messageTypeRegistry } from "../../../../typeRegistry";
import { HeaderValue } from "../../../config/core/v3/base";
import { RateLimitDescriptor } from "../../../extensions/common/ratelimit/v3/ratelimit";

export const protobufPackage = "envoy.service.ratelimit.v3";

/**
 * Main message for a rate limit request. The rate limit service is designed to be fully generic
 * in the sense that it can operate on arbitrary hierarchical key/value pairs. The loaded
 * configuration will parse the request and find the most specific limit to apply. In addition,
 * a RateLimitRequest can contain multiple "descriptors" to limit on. When multiple descriptors
 * are provided, the server will limit on *ALL* of them and return an OVER_LIMIT response if any
 * of them are over limit. This enables more complex application level rate limiting scenarios
 * if desired.
 */
export interface RateLimitRequest {
  $type: "envoy.service.ratelimit.v3.RateLimitRequest";
  /**
   * All rate limit requests must specify a domain. This enables the configuration to be per
   * application without fear of overlap. E.g., "envoy".
   */
  domain?:
    | string
    | undefined;
  /**
   * All rate limit requests must specify at least one RateLimitDescriptor. Each descriptor is
   * processed by the service (see below). If any of the descriptors are over limit, the entire
   * request is considered to be over limit.
   */
  descriptors?:
    | RateLimitDescriptor[]
    | undefined;
  /**
   * Rate limit requests can optionally specify the number of hits a request adds to the matched
   * limit. If the value is not set in the message, a request increases the matched limit by 1.
   * This value can be overridden by setting filter state value ``envoy.ratelimit.hits_addend``
   * to the desired number. Invalid number (< 0) or number will be ignored.
   */
  hits_addend?: number | undefined;
}

/**
 * A response from a ShouldRateLimit call.
 * [#next-free-field: 8]
 */
export interface RateLimitResponse {
  $type: "envoy.service.ratelimit.v3.RateLimitResponse";
  /**
   * The overall response code which takes into account all of the descriptors that were passed
   * in the RateLimitRequest message.
   */
  overall_code?:
    | RateLimitResponse_Code
    | undefined;
  /**
   * A list of DescriptorStatus messages which matches the length of the descriptor list passed
   * in the RateLimitRequest. This can be used by the caller to determine which individual
   * descriptors failed and/or what the currently configured limits are for all of them.
   */
  statuses?:
    | RateLimitResponse_DescriptorStatus[]
    | undefined;
  /** A list of headers to add to the response */
  response_headers_to_add?:
    | HeaderValue[]
    | undefined;
  /** A list of headers to add to the request when forwarded */
  request_headers_to_add?:
    | HeaderValue[]
    | undefined;
  /** A response body to send to the downstream client when the response code is not OK. */
  raw_body?:
    | Uint8Array
    | undefined;
  /**
   * Optional response metadata that will be emitted as dynamic metadata to be consumed by the next
   * filter. This metadata lives in a namespace specified by the canonical name of extension filter
   * that requires it:
   *
   * - :ref:`envoy.filters.http.ratelimit <config_http_filters_ratelimit_dynamic_metadata>` for HTTP filter.
   * - :ref:`envoy.filters.network.ratelimit <config_network_filters_ratelimit_dynamic_metadata>` for network filter.
   * - :ref:`envoy.filters.thrift.rate_limit <config_thrift_filters_rate_limit_dynamic_metadata>` for Thrift filter.
   */
  dynamic_metadata?:
    | { [key: string]: any }
    | undefined;
  /**
   * Quota is available for a request if its entire descriptor set has cached quota available.
   * This is a union of all descriptors in the descriptor set. Clients can use the quota for future matches if and only if the descriptor set matches what was sent in the request that originated this response.
   *
   * If quota is available, a RLS request will not be made and the quota will be reduced by 1.
   * If quota is not available (i.e., a cached entry doesn't exist for a RLS descriptor set), a RLS request will be triggered.
   * If the server did not provide a quota, such as the quota message is empty then the request admission is determined by the
   * :ref:`overall_code <envoy_v3_api_field_service.ratelimit.v3.RateLimitResponse.overall_code>`.
   *
   * If there is not sufficient quota and the cached entry exists for a RLS descriptor set is out-of-quota but not expired,
   * the request will be treated as OVER_LIMIT.
   * [#not-implemented-hide:]
   */
  quota?: RateLimitResponse_Quota | undefined;
}

export enum RateLimitResponse_Code {
  /** UNKNOWN - The response code is not known. */
  UNKNOWN = "UNKNOWN",
  /** OK - The response code to notify that the number of requests are under limit. */
  OK = "OK",
  /** OVER_LIMIT - The response code to notify that the number of requests are over limit. */
  OVER_LIMIT = "OVER_LIMIT",
}

export function rateLimitResponse_CodeFromJSON(object: any): RateLimitResponse_Code {
  switch (object) {
    case 0:
    case "UNKNOWN":
      return RateLimitResponse_Code.UNKNOWN;
    case 1:
    case "OK":
      return RateLimitResponse_Code.OK;
    case 2:
    case "OVER_LIMIT":
      return RateLimitResponse_Code.OVER_LIMIT;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum RateLimitResponse_Code");
  }
}

export function rateLimitResponse_CodeToJSON(object: RateLimitResponse_Code): string {
  switch (object) {
    case RateLimitResponse_Code.UNKNOWN:
      return "UNKNOWN";
    case RateLimitResponse_Code.OK:
      return "OK";
    case RateLimitResponse_Code.OVER_LIMIT:
      return "OVER_LIMIT";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum RateLimitResponse_Code");
  }
}

export function rateLimitResponse_CodeToNumber(object: RateLimitResponse_Code): number {
  switch (object) {
    case RateLimitResponse_Code.UNKNOWN:
      return 0;
    case RateLimitResponse_Code.OK:
      return 1;
    case RateLimitResponse_Code.OVER_LIMIT:
      return 2;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum RateLimitResponse_Code");
  }
}

/** Defines an actual rate limit in terms of requests per unit of time and the unit itself. */
export interface RateLimitResponse_RateLimit {
  $type: "envoy.service.ratelimit.v3.RateLimitResponse.RateLimit";
  /** A name or description of this limit. */
  name?:
    | string
    | undefined;
  /** The number of requests per unit of time. */
  requests_per_unit?:
    | number
    | undefined;
  /** The unit of time. */
  unit?: RateLimitResponse_RateLimit_Unit | undefined;
}

/**
 * Identifies the unit of of time for rate limit.
 * [#comment: replace by envoy/type/v3/ratelimit_unit.proto in v4]
 */
export enum RateLimitResponse_RateLimit_Unit {
  /** UNKNOWN - The time unit is not known. */
  UNKNOWN = "UNKNOWN",
  /** SECOND - The time unit representing a second. */
  SECOND = "SECOND",
  /** MINUTE - The time unit representing a minute. */
  MINUTE = "MINUTE",
  /** HOUR - The time unit representing an hour. */
  HOUR = "HOUR",
  /** DAY - The time unit representing a day. */
  DAY = "DAY",
  /** MONTH - The time unit representing a month. */
  MONTH = "MONTH",
  /** YEAR - The time unit representing a year. */
  YEAR = "YEAR",
}

export function rateLimitResponse_RateLimit_UnitFromJSON(object: any): RateLimitResponse_RateLimit_Unit {
  switch (object) {
    case 0:
    case "UNKNOWN":
      return RateLimitResponse_RateLimit_Unit.UNKNOWN;
    case 1:
    case "SECOND":
      return RateLimitResponse_RateLimit_Unit.SECOND;
    case 2:
    case "MINUTE":
      return RateLimitResponse_RateLimit_Unit.MINUTE;
    case 3:
    case "HOUR":
      return RateLimitResponse_RateLimit_Unit.HOUR;
    case 4:
    case "DAY":
      return RateLimitResponse_RateLimit_Unit.DAY;
    case 5:
    case "MONTH":
      return RateLimitResponse_RateLimit_Unit.MONTH;
    case 6:
    case "YEAR":
      return RateLimitResponse_RateLimit_Unit.YEAR;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum RateLimitResponse_RateLimit_Unit");
  }
}

export function rateLimitResponse_RateLimit_UnitToJSON(object: RateLimitResponse_RateLimit_Unit): string {
  switch (object) {
    case RateLimitResponse_RateLimit_Unit.UNKNOWN:
      return "UNKNOWN";
    case RateLimitResponse_RateLimit_Unit.SECOND:
      return "SECOND";
    case RateLimitResponse_RateLimit_Unit.MINUTE:
      return "MINUTE";
    case RateLimitResponse_RateLimit_Unit.HOUR:
      return "HOUR";
    case RateLimitResponse_RateLimit_Unit.DAY:
      return "DAY";
    case RateLimitResponse_RateLimit_Unit.MONTH:
      return "MONTH";
    case RateLimitResponse_RateLimit_Unit.YEAR:
      return "YEAR";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum RateLimitResponse_RateLimit_Unit");
  }
}

export function rateLimitResponse_RateLimit_UnitToNumber(object: RateLimitResponse_RateLimit_Unit): number {
  switch (object) {
    case RateLimitResponse_RateLimit_Unit.UNKNOWN:
      return 0;
    case RateLimitResponse_RateLimit_Unit.SECOND:
      return 1;
    case RateLimitResponse_RateLimit_Unit.MINUTE:
      return 2;
    case RateLimitResponse_RateLimit_Unit.HOUR:
      return 3;
    case RateLimitResponse_RateLimit_Unit.DAY:
      return 4;
    case RateLimitResponse_RateLimit_Unit.MONTH:
      return 5;
    case RateLimitResponse_RateLimit_Unit.YEAR:
      return 6;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum RateLimitResponse_RateLimit_Unit");
  }
}

/**
 * Cacheable quota for responses.
 * Quota can be granted at different levels: either for each individual descriptor or for the whole descriptor set.
 * This is a certain number of requests over a period of time.
 * The client may cache this result and apply the effective RateLimitResponse to future matching
 * requests without querying rate limit service.
 *
 * When quota expires due to timeout, a new RLS request will also be made.
 * The implementation may choose to preemptively query the rate limit server for more quota on or
 * before expiration or before the available quota runs out.
 * [#not-implemented-hide:]
 */
export interface RateLimitResponse_Quota {
  $type: "envoy.service.ratelimit.v3.RateLimitResponse.Quota";
  /** Number of matching requests granted in quota. Must be 1 or more. */
  requests?: number | undefined;
  expiration_specifier?:
    | //
    /** Point in time at which the quota expires. */
    { $case: "valid_until"; valid_until: Date }
    | undefined;
  /**
   * The unique id that is associated with each Quota either at individual descriptor level or whole descriptor set level.
   *
   * For a matching policy with boolean logic, for example, match: "request.headers['environment'] == 'staging' || request.headers['environment'] == 'dev'"),
   * the request_headers action produces a distinct list of descriptors for each possible value of the ‘environment’ header even though the granted quota is same.
   * Thus, the client will use this id information (returned from RLS server) to correctly correlate the multiple descriptors/descriptor sets that have been granted with same quota (i.e., share the same quota among multiple descriptors or descriptor sets.)
   *
   * If id is empty, this id field will be ignored. If quota for the same id changes (e.g. due to configuration update), the old quota will be overridden by the new one. Shared quotas referenced by ID will still adhere to expiration after `valid_until`.
   */
  id?: string | undefined;
}

/** [#next-free-field: 6] */
export interface RateLimitResponse_DescriptorStatus {
  $type: "envoy.service.ratelimit.v3.RateLimitResponse.DescriptorStatus";
  /** The response code for an individual descriptor. */
  code?:
    | RateLimitResponse_Code
    | undefined;
  /** The current limit as configured by the server. Useful for debugging, etc. */
  current_limit?:
    | RateLimitResponse_RateLimit
    | undefined;
  /** The limit remaining in the current time unit. */
  limit_remaining?:
    | number
    | undefined;
  /** Duration until reset of the current limit window. */
  duration_until_reset?:
    | Duration
    | undefined;
  /**
   * Quota is available for a request if its descriptor set has cached quota available for all
   * descriptors.
   * This is for each individual descriptor in the descriptor set. The client will perform matches for each individual descriptor against available per-descriptor quota.
   *
   * If quota is available, a RLS request will not be made and the quota will be reduced by 1 for
   * all matching descriptors.
   *
   * If there is not sufficient quota, there are three cases:
   * 1. A cached entry exists for a RLS descriptor that is out-of-quota, but not expired.
   *    In this case, the request will be treated as OVER_LIMIT.
   * 2. Some RLS descriptors have a cached entry that has valid quota but some RLS descriptors
   *    have no cached entry. This will trigger a new RLS request.
   *    When the result is returned, a single unit will be consumed from the quota for all
   *    matching descriptors.
   *    If the server did not provide a quota, such as the quota message is empty for some of
   *    the descriptors, then the request admission is determined by the
   *    :ref:`overall_code <envoy_v3_api_field_service.ratelimit.v3.RateLimitResponse.overall_code>`.
   * 3. All RLS descriptors lack a cached entry, this will trigger a new RLS request,
   *    When the result is returned, a single unit will be consumed from the quota for all
   *    matching descriptors.
   *    If the server did not provide a quota, such as the quota message is empty for some of
   *    the descriptors, then the request admission is determined by the
   *    :ref:`overall_code <envoy_v3_api_field_service.ratelimit.v3.RateLimitResponse.overall_code>`.
   * [#not-implemented-hide:]
   */
  quota?: RateLimitResponse_Quota | undefined;
}

function createBaseRateLimitRequest(): RateLimitRequest {
  return { $type: "envoy.service.ratelimit.v3.RateLimitRequest" };
}

export const RateLimitRequest: MessageFns<RateLimitRequest, "envoy.service.ratelimit.v3.RateLimitRequest"> = {
  $type: "envoy.service.ratelimit.v3.RateLimitRequest" as const,

  encode(message: RateLimitRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== undefined && message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    if (message.descriptors !== undefined && message.descriptors.length !== 0) {
      for (const v of message.descriptors) {
        RateLimitDescriptor.encode(v!, writer.uint32(18).fork()).join();
      }
    }
    if (message.hits_addend !== undefined && message.hits_addend !== 0) {
      writer.uint32(24).uint32(message.hits_addend);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RateLimitRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRateLimitRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          if (message.descriptors === undefined) {
            message.descriptors = [];
          }
          const el = RateLimitDescriptor.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.descriptors!.push(el);
          }
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.hits_addend = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RateLimitRequest {
    return {
      $type: RateLimitRequest.$type,
      domain: isSet(object.domain) ? globalThis.String(object.domain) : undefined,
      descriptors: globalThis.Array.isArray(object?.descriptors)
        ? object.descriptors.map((e: any) => RateLimitDescriptor.fromJSON(e))
        : undefined,
      hits_addend: isSet(object.hits_addend) ? globalThis.Number(object.hits_addend) : undefined,
    };
  },

  toJSON(message: RateLimitRequest): unknown {
    const obj: any = {};
    if (message.domain !== undefined) {
      obj.domain = message.domain;
    }
    if (message.descriptors?.length) {
      obj.descriptors = message.descriptors.map((e) => RateLimitDescriptor.toJSON(e));
    }
    if (message.hits_addend !== undefined) {
      obj.hits_addend = Math.round(message.hits_addend);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RateLimitRequest>, I>>(base?: I): RateLimitRequest {
    return RateLimitRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RateLimitRequest>, I>>(object: I): RateLimitRequest {
    const message = createBaseRateLimitRequest();
    message.domain = object.domain ?? undefined;
    message.descriptors = object.descriptors?.map((e) => RateLimitDescriptor.fromPartial(e)) || undefined;
    message.hits_addend = object.hits_addend ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(RateLimitRequest.$type, RateLimitRequest);

function createBaseRateLimitResponse(): RateLimitResponse {
  return { $type: "envoy.service.ratelimit.v3.RateLimitResponse" };
}

export const RateLimitResponse: MessageFns<RateLimitResponse, "envoy.service.ratelimit.v3.RateLimitResponse"> = {
  $type: "envoy.service.ratelimit.v3.RateLimitResponse" as const,

  encode(message: RateLimitResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.overall_code !== undefined && message.overall_code !== RateLimitResponse_Code.UNKNOWN) {
      writer.uint32(8).int32(rateLimitResponse_CodeToNumber(message.overall_code));
    }
    if (message.statuses !== undefined && message.statuses.length !== 0) {
      for (const v of message.statuses) {
        RateLimitResponse_DescriptorStatus.encode(v!, writer.uint32(18).fork()).join();
      }
    }
    if (message.response_headers_to_add !== undefined && message.response_headers_to_add.length !== 0) {
      for (const v of message.response_headers_to_add) {
        HeaderValue.encode(v!, writer.uint32(26).fork()).join();
      }
    }
    if (message.request_headers_to_add !== undefined && message.request_headers_to_add.length !== 0) {
      for (const v of message.request_headers_to_add) {
        HeaderValue.encode(v!, writer.uint32(34).fork()).join();
      }
    }
    if (message.raw_body !== undefined && message.raw_body.length !== 0) {
      writer.uint32(42).bytes(message.raw_body);
    }
    if (message.dynamic_metadata !== undefined) {
      Struct.encode(Struct.wrap(message.dynamic_metadata), writer.uint32(50).fork()).join();
    }
    if (message.quota !== undefined) {
      RateLimitResponse_Quota.encode(message.quota, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RateLimitResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRateLimitResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.overall_code = rateLimitResponse_CodeFromJSON(reader.int32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          if (message.statuses === undefined) {
            message.statuses = [];
          }
          const el = RateLimitResponse_DescriptorStatus.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.statuses!.push(el);
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          if (message.response_headers_to_add === undefined) {
            message.response_headers_to_add = [];
          }
          const el = HeaderValue.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.response_headers_to_add!.push(el);
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          if (message.request_headers_to_add === undefined) {
            message.request_headers_to_add = [];
          }
          const el = HeaderValue.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.request_headers_to_add!.push(el);
          }
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.raw_body = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.dynamic_metadata = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.quota = RateLimitResponse_Quota.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RateLimitResponse {
    return {
      $type: RateLimitResponse.$type,
      overall_code: isSet(object.overall_code) ? rateLimitResponse_CodeFromJSON(object.overall_code) : undefined,
      statuses: globalThis.Array.isArray(object?.statuses)
        ? object.statuses.map((e: any) => RateLimitResponse_DescriptorStatus.fromJSON(e))
        : undefined,
      response_headers_to_add: globalThis.Array.isArray(object?.response_headers_to_add)
        ? object.response_headers_to_add.map((e: any) => HeaderValue.fromJSON(e))
        : undefined,
      request_headers_to_add: globalThis.Array.isArray(object?.request_headers_to_add)
        ? object.request_headers_to_add.map((e: any) => HeaderValue.fromJSON(e))
        : undefined,
      raw_body: isSet(object.raw_body) ? bytesFromBase64(object.raw_body) : undefined,
      dynamic_metadata: isObject(object.dynamic_metadata) ? object.dynamic_metadata : undefined,
      quota: isSet(object.quota) ? RateLimitResponse_Quota.fromJSON(object.quota) : undefined,
    };
  },

  toJSON(message: RateLimitResponse): unknown {
    const obj: any = {};
    if (message.overall_code !== undefined) {
      obj.overall_code = rateLimitResponse_CodeToJSON(message.overall_code);
    }
    if (message.statuses?.length) {
      obj.statuses = message.statuses.map((e) => RateLimitResponse_DescriptorStatus.toJSON(e));
    }
    if (message.response_headers_to_add?.length) {
      obj.response_headers_to_add = message.response_headers_to_add.map((e) => HeaderValue.toJSON(e));
    }
    if (message.request_headers_to_add?.length) {
      obj.request_headers_to_add = message.request_headers_to_add.map((e) => HeaderValue.toJSON(e));
    }
    if (message.raw_body !== undefined) {
      obj.raw_body = base64FromBytes(message.raw_body);
    }
    if (message.dynamic_metadata !== undefined) {
      obj.dynamic_metadata = message.dynamic_metadata;
    }
    if (message.quota !== undefined) {
      obj.quota = RateLimitResponse_Quota.toJSON(message.quota);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RateLimitResponse>, I>>(base?: I): RateLimitResponse {
    return RateLimitResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RateLimitResponse>, I>>(object: I): RateLimitResponse {
    const message = createBaseRateLimitResponse();
    message.overall_code = object.overall_code ?? undefined;
    message.statuses = object.statuses?.map((e) => RateLimitResponse_DescriptorStatus.fromPartial(e)) || undefined;
    message.response_headers_to_add = object.response_headers_to_add?.map((e) => HeaderValue.fromPartial(e)) ||
      undefined;
    message.request_headers_to_add = object.request_headers_to_add?.map((e) => HeaderValue.fromPartial(e)) || undefined;
    message.raw_body = object.raw_body ?? undefined;
    message.dynamic_metadata = object.dynamic_metadata ?? undefined;
    message.quota = (object.quota !== undefined && object.quota !== null)
      ? RateLimitResponse_Quota.fromPartial(object.quota)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(RateLimitResponse.$type, RateLimitResponse);

function createBaseRateLimitResponse_RateLimit(): RateLimitResponse_RateLimit {
  return { $type: "envoy.service.ratelimit.v3.RateLimitResponse.RateLimit" };
}

export const RateLimitResponse_RateLimit: MessageFns<
  RateLimitResponse_RateLimit,
  "envoy.service.ratelimit.v3.RateLimitResponse.RateLimit"
> = {
  $type: "envoy.service.ratelimit.v3.RateLimitResponse.RateLimit" as const,

  encode(message: RateLimitResponse_RateLimit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(26).string(message.name);
    }
    if (message.requests_per_unit !== undefined && message.requests_per_unit !== 0) {
      writer.uint32(8).uint32(message.requests_per_unit);
    }
    if (message.unit !== undefined && message.unit !== RateLimitResponse_RateLimit_Unit.UNKNOWN) {
      writer.uint32(16).int32(rateLimitResponse_RateLimit_UnitToNumber(message.unit));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RateLimitResponse_RateLimit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRateLimitResponse_RateLimit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.requests_per_unit = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.unit = rateLimitResponse_RateLimit_UnitFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RateLimitResponse_RateLimit {
    return {
      $type: RateLimitResponse_RateLimit.$type,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      requests_per_unit: isSet(object.requests_per_unit) ? globalThis.Number(object.requests_per_unit) : undefined,
      unit: isSet(object.unit) ? rateLimitResponse_RateLimit_UnitFromJSON(object.unit) : undefined,
    };
  },

  toJSON(message: RateLimitResponse_RateLimit): unknown {
    const obj: any = {};
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.requests_per_unit !== undefined) {
      obj.requests_per_unit = Math.round(message.requests_per_unit);
    }
    if (message.unit !== undefined) {
      obj.unit = rateLimitResponse_RateLimit_UnitToJSON(message.unit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RateLimitResponse_RateLimit>, I>>(base?: I): RateLimitResponse_RateLimit {
    return RateLimitResponse_RateLimit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RateLimitResponse_RateLimit>, I>>(object: I): RateLimitResponse_RateLimit {
    const message = createBaseRateLimitResponse_RateLimit();
    message.name = object.name ?? undefined;
    message.requests_per_unit = object.requests_per_unit ?? undefined;
    message.unit = object.unit ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(RateLimitResponse_RateLimit.$type, RateLimitResponse_RateLimit);

function createBaseRateLimitResponse_Quota(): RateLimitResponse_Quota {
  return { $type: "envoy.service.ratelimit.v3.RateLimitResponse.Quota", expiration_specifier: undefined };
}

export const RateLimitResponse_Quota: MessageFns<
  RateLimitResponse_Quota,
  "envoy.service.ratelimit.v3.RateLimitResponse.Quota"
> = {
  $type: "envoy.service.ratelimit.v3.RateLimitResponse.Quota" as const,

  encode(message: RateLimitResponse_Quota, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requests !== undefined && message.requests !== 0) {
      writer.uint32(8).uint32(message.requests);
    }
    switch (message.expiration_specifier?.$case) {
      case "valid_until":
        Timestamp.encode(toTimestamp(message.expiration_specifier.valid_until), writer.uint32(18).fork()).join();
        break;
    }
    if (message.id !== undefined && message.id !== "") {
      writer.uint32(26).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RateLimitResponse_Quota {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRateLimitResponse_Quota();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.requests = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.expiration_specifier = {
            $case: "valid_until",
            valid_until: fromTimestamp(Timestamp.decode(reader, reader.uint32())),
          };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RateLimitResponse_Quota {
    return {
      $type: RateLimitResponse_Quota.$type,
      requests: isSet(object.requests) ? globalThis.Number(object.requests) : undefined,
      expiration_specifier: isSet(object.valid_until)
        ? { $case: "valid_until", valid_until: fromJsonTimestamp(object.valid_until) }
        : undefined,
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
    };
  },

  toJSON(message: RateLimitResponse_Quota): unknown {
    const obj: any = {};
    if (message.requests !== undefined) {
      obj.requests = Math.round(message.requests);
    }
    if (message.expiration_specifier?.$case === "valid_until") {
      obj.valid_until = message.expiration_specifier.valid_until.toISOString();
    }
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RateLimitResponse_Quota>, I>>(base?: I): RateLimitResponse_Quota {
    return RateLimitResponse_Quota.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RateLimitResponse_Quota>, I>>(object: I): RateLimitResponse_Quota {
    const message = createBaseRateLimitResponse_Quota();
    message.requests = object.requests ?? undefined;
    if (
      object.expiration_specifier?.$case === "valid_until" &&
      object.expiration_specifier?.valid_until !== undefined &&
      object.expiration_specifier?.valid_until !== null
    ) {
      message.expiration_specifier = { $case: "valid_until", valid_until: object.expiration_specifier.valid_until };
    }
    message.id = object.id ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(RateLimitResponse_Quota.$type, RateLimitResponse_Quota);

function createBaseRateLimitResponse_DescriptorStatus(): RateLimitResponse_DescriptorStatus {
  return { $type: "envoy.service.ratelimit.v3.RateLimitResponse.DescriptorStatus" };
}

export const RateLimitResponse_DescriptorStatus: MessageFns<
  RateLimitResponse_DescriptorStatus,
  "envoy.service.ratelimit.v3.RateLimitResponse.DescriptorStatus"
> = {
  $type: "envoy.service.ratelimit.v3.RateLimitResponse.DescriptorStatus" as const,

  encode(message: RateLimitResponse_DescriptorStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== undefined && message.code !== RateLimitResponse_Code.UNKNOWN) {
      writer.uint32(8).int32(rateLimitResponse_CodeToNumber(message.code));
    }
    if (message.current_limit !== undefined) {
      RateLimitResponse_RateLimit.encode(message.current_limit, writer.uint32(18).fork()).join();
    }
    if (message.limit_remaining !== undefined && message.limit_remaining !== 0) {
      writer.uint32(24).uint32(message.limit_remaining);
    }
    if (message.duration_until_reset !== undefined) {
      Duration.encode(message.duration_until_reset, writer.uint32(34).fork()).join();
    }
    if (message.quota !== undefined) {
      RateLimitResponse_Quota.encode(message.quota, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RateLimitResponse_DescriptorStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRateLimitResponse_DescriptorStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.code = rateLimitResponse_CodeFromJSON(reader.int32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.current_limit = RateLimitResponse_RateLimit.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.limit_remaining = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.duration_until_reset = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.quota = RateLimitResponse_Quota.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RateLimitResponse_DescriptorStatus {
    return {
      $type: RateLimitResponse_DescriptorStatus.$type,
      code: isSet(object.code) ? rateLimitResponse_CodeFromJSON(object.code) : undefined,
      current_limit: isSet(object.current_limit)
        ? RateLimitResponse_RateLimit.fromJSON(object.current_limit)
        : undefined,
      limit_remaining: isSet(object.limit_remaining) ? globalThis.Number(object.limit_remaining) : undefined,
      duration_until_reset: isSet(object.duration_until_reset)
        ? Duration.fromJSON(object.duration_until_reset)
        : undefined,
      quota: isSet(object.quota) ? RateLimitResponse_Quota.fromJSON(object.quota) : undefined,
    };
  },

  toJSON(message: RateLimitResponse_DescriptorStatus): unknown {
    const obj: any = {};
    if (message.code !== undefined) {
      obj.code = rateLimitResponse_CodeToJSON(message.code);
    }
    if (message.current_limit !== undefined) {
      obj.current_limit = RateLimitResponse_RateLimit.toJSON(message.current_limit);
    }
    if (message.limit_remaining !== undefined) {
      obj.limit_remaining = Math.round(message.limit_remaining);
    }
    if (message.duration_until_reset !== undefined) {
      obj.duration_until_reset = Duration.toJSON(message.duration_until_reset);
    }
    if (message.quota !== undefined) {
      obj.quota = RateLimitResponse_Quota.toJSON(message.quota);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RateLimitResponse_DescriptorStatus>, I>>(
    base?: I,
  ): RateLimitResponse_DescriptorStatus {
    return RateLimitResponse_DescriptorStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RateLimitResponse_DescriptorStatus>, I>>(
    object: I,
  ): RateLimitResponse_DescriptorStatus {
    const message = createBaseRateLimitResponse_DescriptorStatus();
    message.code = object.code ?? undefined;
    message.current_limit = (object.current_limit !== undefined && object.current_limit !== null)
      ? RateLimitResponse_RateLimit.fromPartial(object.current_limit)
      : undefined;
    message.limit_remaining = object.limit_remaining ?? undefined;
    message.duration_until_reset = (object.duration_until_reset !== undefined && object.duration_until_reset !== null)
      ? Duration.fromPartial(object.duration_until_reset)
      : undefined;
    message.quota = (object.quota !== undefined && object.quota !== null)
      ? RateLimitResponse_Quota.fromPartial(object.quota)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(RateLimitResponse_DescriptorStatus.$type, RateLimitResponse_DescriptorStatus);

export interface RateLimitService {
  /** Determine whether rate limiting should take place. */
  ShouldRateLimit(request: RateLimitRequest): Promise<RateLimitResponse>;
}

export const RateLimitServiceServiceName = "envoy.service.ratelimit.v3.RateLimitService";
export class RateLimitServiceClientImpl implements RateLimitService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || RateLimitServiceServiceName;
    this.rpc = rpc;
    this.ShouldRateLimit = this.ShouldRateLimit.bind(this);
  }
  ShouldRateLimit(request: RateLimitRequest): Promise<RateLimitResponse> {
    const data = RateLimitRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ShouldRateLimit", data);
    return promise.then((data) => RateLimitResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { $type: "google.protobuf.Timestamp", seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
