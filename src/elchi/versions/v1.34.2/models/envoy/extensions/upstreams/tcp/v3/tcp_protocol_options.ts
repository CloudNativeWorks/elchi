// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/upstreams/tcp/v3/tcp_protocol_options.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../../../../../google/protobuf/duration";
import { messageTypeRegistry } from "../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.upstreams.tcp.v3";

export interface TcpProtocolOptions {
  $type: "envoy.extensions.upstreams.tcp.v3.TcpProtocolOptions";
  /**
   * The idle timeout for the connection. The idle timeout is defined as the period in which
   * the connection is not associated with a downstream connection. When the idle timeout is
   * reached, the connection will be closed.
   *
   * If not set, the default idle timeout is 10 minutes. To disable idle timeouts, explicitly set this to 0.
   *
   * .. warning::
   *   Disabling this timeout has a highly likelihood of yielding connection leaks due to lost TCP
   *   FIN packets, etc.
   */
  idle_timeout?: Duration | undefined;
}

function createBaseTcpProtocolOptions(): TcpProtocolOptions {
  return { $type: "envoy.extensions.upstreams.tcp.v3.TcpProtocolOptions" };
}

export const TcpProtocolOptions: MessageFns<
  TcpProtocolOptions,
  "envoy.extensions.upstreams.tcp.v3.TcpProtocolOptions"
> = {
  $type: "envoy.extensions.upstreams.tcp.v3.TcpProtocolOptions" as const,

  encode(message: TcpProtocolOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.idle_timeout !== undefined) {
      Duration.encode(message.idle_timeout, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TcpProtocolOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTcpProtocolOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.idle_timeout = Duration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TcpProtocolOptions {
    return {
      $type: TcpProtocolOptions.$type,
      idle_timeout: isSet(object.idle_timeout) ? Duration.fromJSON(object.idle_timeout) : undefined,
    };
  },

  toJSON(message: TcpProtocolOptions): unknown {
    const obj: any = {};
    if (message.idle_timeout !== undefined) {
      obj.idle_timeout = Duration.toJSON(message.idle_timeout);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TcpProtocolOptions>, I>>(base?: I): TcpProtocolOptions {
    return TcpProtocolOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TcpProtocolOptions>, I>>(object: I): TcpProtocolOptions {
    const message = createBaseTcpProtocolOptions();
    message.idle_timeout = (object.idle_timeout !== undefined && object.idle_timeout !== null)
      ? Duration.fromPartial(object.idle_timeout)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(TcpProtocolOptions.$type, TcpProtocolOptions);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
