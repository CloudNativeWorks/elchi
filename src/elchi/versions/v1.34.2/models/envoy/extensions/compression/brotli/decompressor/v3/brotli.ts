// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/compression/brotli/decompressor/v3/brotli.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { UInt32Value } from "../../../../../../google/protobuf/wrappers";
import { messageTypeRegistry } from "../../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.compression.brotli.decompressor.v3";

export interface Brotli {
  $type: "envoy.extensions.compression.brotli.decompressor.v3.Brotli";
  /**
   * If true, disables "canny" ring buffer allocation strategy.
   * Ring buffer is allocated according to window size, despite the real size of the content.
   */
  disable_ring_buffer_reallocation?:
    | boolean
    | undefined;
  /** Value for decompressor's next output buffer. If not set, defaults to 4096. */
  chunk_size?: number | undefined;
}

function createBaseBrotli(): Brotli {
  return { $type: "envoy.extensions.compression.brotli.decompressor.v3.Brotli" };
}

export const Brotli: MessageFns<Brotli, "envoy.extensions.compression.brotli.decompressor.v3.Brotli"> = {
  $type: "envoy.extensions.compression.brotli.decompressor.v3.Brotli" as const,

  encode(message: Brotli, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.disable_ring_buffer_reallocation !== undefined && message.disable_ring_buffer_reallocation !== false) {
      writer.uint32(8).bool(message.disable_ring_buffer_reallocation);
    }
    if (message.chunk_size !== undefined) {
      UInt32Value.encode({ $type: "google.protobuf.UInt32Value", value: message.chunk_size! }, writer.uint32(18).fork())
        .join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Brotli {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBrotli();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.disable_ring_buffer_reallocation = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.chunk_size = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Brotli {
    return {
      $type: Brotli.$type,
      disable_ring_buffer_reallocation: isSet(object.disable_ring_buffer_reallocation)
        ? globalThis.Boolean(object.disable_ring_buffer_reallocation)
        : undefined,
      chunk_size: isSet(object.chunk_size) ? Number(object.chunk_size) : undefined,
    };
  },

  toJSON(message: Brotli): unknown {
    const obj: any = {};
    if (message.disable_ring_buffer_reallocation !== undefined) {
      obj.disable_ring_buffer_reallocation = message.disable_ring_buffer_reallocation;
    }
    if (message.chunk_size !== undefined) {
      obj.chunk_size = message.chunk_size;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Brotli>, I>>(base?: I): Brotli {
    return Brotli.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Brotli>, I>>(object: I): Brotli {
    const message = createBaseBrotli();
    message.disable_ring_buffer_reallocation = object.disable_ring_buffer_reallocation ?? undefined;
    message.chunk_size = object.chunk_size ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(Brotli.$type, Brotli);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
