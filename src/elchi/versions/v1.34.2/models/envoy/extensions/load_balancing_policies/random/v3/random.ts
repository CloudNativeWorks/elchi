// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/load_balancing_policies/random/v3/random.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../typeRegistry";
import { LocalityLbConfig } from "../../common/v3/common";

export const protobufPackage = "envoy.extensions.load_balancing_policies.random.v3";

/**
 * This configuration allows the built-in Random LB policy to be configured via the LB policy
 * extension point. See the :ref:`load balancing architecture overview
 * <arch_overview_load_balancing_types>` for more information.
 */
export interface Random {
  $type: "envoy.extensions.load_balancing_policies.random.v3.Random";
  /** Configuration for local zone aware load balancing or locality weighted load balancing. */
  locality_lb_config?: LocalityLbConfig | undefined;
}

function createBaseRandom(): Random {
  return { $type: "envoy.extensions.load_balancing_policies.random.v3.Random" };
}

export const Random: MessageFns<Random, "envoy.extensions.load_balancing_policies.random.v3.Random"> = {
  $type: "envoy.extensions.load_balancing_policies.random.v3.Random" as const,

  encode(message: Random, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.locality_lb_config !== undefined) {
      LocalityLbConfig.encode(message.locality_lb_config, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Random {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRandom();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.locality_lb_config = LocalityLbConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Random {
    return {
      $type: Random.$type,
      locality_lb_config: isSet(object.locality_lb_config)
        ? LocalityLbConfig.fromJSON(object.locality_lb_config)
        : undefined,
    };
  },

  toJSON(message: Random): unknown {
    const obj: any = {};
    if (message.locality_lb_config !== undefined) {
      obj.locality_lb_config = LocalityLbConfig.toJSON(message.locality_lb_config);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Random>, I>>(base?: I): Random {
    return Random.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Random>, I>>(object: I): Random {
    const message = createBaseRandom();
    message.locality_lb_config = (object.locality_lb_config !== undefined && object.locality_lb_config !== null)
      ? LocalityLbConfig.fromPartial(object.locality_lb_config)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(Random.$type, Random);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
