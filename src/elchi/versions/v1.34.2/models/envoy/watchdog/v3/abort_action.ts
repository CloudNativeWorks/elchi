// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/watchdog/v3/abort_action.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../../../google/protobuf/duration";
import { messageTypeRegistry } from "../../../typeRegistry";

export const protobufPackage = "envoy.watchdog.v3";

/**
 * A GuardDogAction that will terminate the process by killing the
 * stuck thread. This would allow easier access to the call stack of the stuck
 * thread since we would run signal handlers on that thread. By default
 * this will be registered to run as the last watchdog action on KILL and
 * MULTIKILL events if those are enabled.
 */
export interface AbortActionConfig {
  $type: "envoy.watchdog.v3.AbortActionConfig";
  /**
   * How long to wait for the thread to respond to the thread kill function
   * before killing the process from this action. This is a blocking action.
   * By default this is 5 seconds.
   */
  wait_duration?: Duration | undefined;
}

function createBaseAbortActionConfig(): AbortActionConfig {
  return { $type: "envoy.watchdog.v3.AbortActionConfig" };
}

export const AbortActionConfig: MessageFns<AbortActionConfig, "envoy.watchdog.v3.AbortActionConfig"> = {
  $type: "envoy.watchdog.v3.AbortActionConfig" as const,

  encode(message: AbortActionConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.wait_duration !== undefined) {
      Duration.encode(message.wait_duration, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AbortActionConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAbortActionConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.wait_duration = Duration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AbortActionConfig {
    return {
      $type: AbortActionConfig.$type,
      wait_duration: isSet(object.wait_duration) ? Duration.fromJSON(object.wait_duration) : undefined,
    };
  },

  toJSON(message: AbortActionConfig): unknown {
    const obj: any = {};
    if (message.wait_duration !== undefined) {
      obj.wait_duration = Duration.toJSON(message.wait_duration);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AbortActionConfig>, I>>(base?: I): AbortActionConfig {
    return AbortActionConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AbortActionConfig>, I>>(object: I): AbortActionConfig {
    const message = createBaseAbortActionConfig();
    message.wait_duration = (object.wait_duration !== undefined && object.wait_duration !== null)
      ? Duration.fromPartial(object.wait_duration)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(AbortActionConfig.$type, AbortActionConfig);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
