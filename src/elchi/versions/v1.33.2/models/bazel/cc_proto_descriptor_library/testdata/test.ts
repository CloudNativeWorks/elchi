// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: bazel/cc_proto_descriptor_library/testdata/test.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Any } from "../../../google/protobuf/any";
import { messageTypeRegistry } from "../../../typeRegistry";

export const protobufPackage = "testdata.dynamic_descriptors";

export interface Foo {
  $type: "testdata.dynamic_descriptors.Foo";
  bar?: string | undefined;
  baz?: Any | undefined;
}

function createBaseFoo(): Foo {
  return { $type: "testdata.dynamic_descriptors.Foo" };
}

export const Foo: MessageFns<Foo, "testdata.dynamic_descriptors.Foo"> = {
  $type: "testdata.dynamic_descriptors.Foo" as const,

  encode(message: Foo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.bar !== undefined && message.bar !== "") {
      writer.uint32(10).string(message.bar);
    }
    if (message.baz !== undefined) {
      Any.encode(message.baz, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Foo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFoo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.bar = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.baz = Any.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Foo {
    return {
      $type: Foo.$type,
      bar: isSet(object.bar) ? globalThis.String(object.bar) : undefined,
      baz: isSet(object.baz) ? Any.fromJSON(object.baz) : undefined,
    };
  },

  toJSON(message: Foo): unknown {
    const obj: any = {};
    if (message.bar !== undefined) {
      obj.bar = message.bar;
    }
    if (message.baz !== undefined) {
      obj.baz = Any.toJSON(message.baz);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Foo>, I>>(base?: I): Foo {
    return Foo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Foo>, I>>(object: I): Foo {
    const message = createBaseFoo();
    message.bar = object.bar ?? undefined;
    message.baz = (object.baz !== undefined && object.baz !== null) ? Any.fromPartial(object.baz) : undefined;
    return message;
  },
};

messageTypeRegistry.set(Foo.$type, Foo);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
