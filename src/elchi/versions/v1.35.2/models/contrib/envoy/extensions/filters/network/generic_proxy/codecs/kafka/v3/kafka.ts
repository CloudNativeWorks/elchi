// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: contrib/envoy/extensions/filters/network/generic_proxy/codecs/kafka/v3/kafka.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.filters.network.generic_proxy.codecs.kafka.v3";

/**
 * Configuration for Kafka codec. This codec gives the generic proxy the ability to proxy
 * Kafka traffic. But note any route configuration for Kafka traffic is not supported yet.
 * The generic proxy can only used to generate logs or metrics for Kafka traffic but cannot
 * do matching or routing.
 *
 * .. note::
 *   The codec can currently only be used in the sidecar mode. And to ensure the codec works
 *   properly, please make sure the following conditions are met:
 *
 *   1. The generic proxy must be configured with a wildcard route that matches all traffic.
 *   2. The target cluster must be configured as a original destination cluster.
 *   3. The :ref:`bind_upstream_connection
 *      <envoy_v3_api_field_extensions.filters.network.generic_proxy.router.v3.Router.bind_upstream_connection>`
 *      of generic proxy router must be set to true to ensure same upstream connection is used
 *      for all traffic from same downstream connection.
 */
export interface KafkaCodecConfig {
  $type: "envoy.extensions.filters.network.generic_proxy.codecs.kafka.v3.KafkaCodecConfig";
}

function createBaseKafkaCodecConfig(): KafkaCodecConfig {
  return { $type: "envoy.extensions.filters.network.generic_proxy.codecs.kafka.v3.KafkaCodecConfig" };
}

export const KafkaCodecConfig: MessageFns<
  KafkaCodecConfig,
  "envoy.extensions.filters.network.generic_proxy.codecs.kafka.v3.KafkaCodecConfig"
> = {
  $type: "envoy.extensions.filters.network.generic_proxy.codecs.kafka.v3.KafkaCodecConfig" as const,

  encode(_: KafkaCodecConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KafkaCodecConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKafkaCodecConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): KafkaCodecConfig {
    return { $type: KafkaCodecConfig.$type };
  },

  toJSON(_: KafkaCodecConfig): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<KafkaCodecConfig>, I>>(base?: I): KafkaCodecConfig {
    return KafkaCodecConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KafkaCodecConfig>, I>>(_: I): KafkaCodecConfig {
    const message = createBaseKafkaCodecConfig();
    return message;
  },
};

messageTypeRegistry.set(KafkaCodecConfig.$type, KafkaCodecConfig);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
