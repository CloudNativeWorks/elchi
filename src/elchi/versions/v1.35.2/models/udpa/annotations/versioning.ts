// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: udpa/annotations/versioning.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../typeRegistry";

export const protobufPackage = "udpa.annotations";

export interface VersioningAnnotation {
  $type: "udpa.annotations.VersioningAnnotation";
  /**
   * Track the previous message type. E.g. this message might be
   * udpa.foo.v3alpha.Foo and it was previously udpa.bar.v2.Bar. This
   * information is consumed by UDPA via proto descriptors.
   */
  previous_message_type?: string | undefined;
}

function createBaseVersioningAnnotation(): VersioningAnnotation {
  return { $type: "udpa.annotations.VersioningAnnotation" };
}

export const VersioningAnnotation: MessageFns<VersioningAnnotation, "udpa.annotations.VersioningAnnotation"> = {
  $type: "udpa.annotations.VersioningAnnotation" as const,

  encode(message: VersioningAnnotation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.previous_message_type !== undefined && message.previous_message_type !== "") {
      writer.uint32(10).string(message.previous_message_type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VersioningAnnotation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVersioningAnnotation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.previous_message_type = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VersioningAnnotation {
    return {
      $type: VersioningAnnotation.$type,
      previous_message_type: isSet(object.previous_message_type)
        ? globalThis.String(object.previous_message_type)
        : undefined,
    };
  },

  toJSON(message: VersioningAnnotation): unknown {
    const obj: any = {};
    if (message.previous_message_type !== undefined) {
      obj.previous_message_type = message.previous_message_type;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VersioningAnnotation>, I>>(base?: I): VersioningAnnotation {
    return VersioningAnnotation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VersioningAnnotation>, I>>(object: I): VersioningAnnotation {
    const message = createBaseVersioningAnnotation();
    message.previous_message_type = object.previous_message_type ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(VersioningAnnotation.$type, VersioningAnnotation);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
