// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/type/matcher/v3/address.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../typeRegistry";
import { CidrRange } from "../../../../xds/core/v3/cidr";

export const protobufPackage = "envoy.type.matcher.v3";

/**
 * Match an IP against a repeated CIDR range. This matcher is intended to be
 * used in other matchers, for example in the filter state matcher to match a
 * filter state object as an IP.
 */
export interface AddressMatcher {
  $type: "envoy.type.matcher.v3.AddressMatcher";
  ranges?: CidrRange[] | undefined;
}

function createBaseAddressMatcher(): AddressMatcher {
  return { $type: "envoy.type.matcher.v3.AddressMatcher" };
}

export const AddressMatcher: MessageFns<AddressMatcher, "envoy.type.matcher.v3.AddressMatcher"> = {
  $type: "envoy.type.matcher.v3.AddressMatcher" as const,

  encode(message: AddressMatcher, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ranges !== undefined && message.ranges.length !== 0) {
      for (const v of message.ranges) {
        CidrRange.encode(v!, writer.uint32(10).fork()).join();
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AddressMatcher {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddressMatcher();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          if (message.ranges === undefined) {
            message.ranges = [];
          }
          const el = CidrRange.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.ranges!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AddressMatcher {
    return {
      $type: AddressMatcher.$type,
      ranges: globalThis.Array.isArray(object?.ranges)
        ? object.ranges.map((e: any) => CidrRange.fromJSON(e))
        : undefined,
    };
  },

  toJSON(message: AddressMatcher): unknown {
    const obj: any = {};
    if (message.ranges?.length) {
      obj.ranges = message.ranges.map((e) => CidrRange.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AddressMatcher>, I>>(base?: I): AddressMatcher {
    return AddressMatcher.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AddressMatcher>, I>>(object: I): AddressMatcher {
    const message = createBaseAddressMatcher();
    message.ranges = object.ranges?.map((e) => CidrRange.fromPartial(e)) || undefined;
    return message;
  },
};

messageTypeRegistry.set(AddressMatcher.$type, AddressMatcher);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
