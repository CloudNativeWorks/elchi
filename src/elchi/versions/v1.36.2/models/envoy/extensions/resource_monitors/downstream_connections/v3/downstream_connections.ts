// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/resource_monitors/downstream_connections/v3/downstream_connections.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.resource_monitors.downstream_connections.v3";

/** The downstream connections resource monitor tracks the global number of open downstream connections. */
export interface DownstreamConnectionsConfig {
  $type: "envoy.extensions.resource_monitors.downstream_connections.v3.DownstreamConnectionsConfig";
  /**
   * Maximum threshold for global open downstream connections, defaults to 0.
   * If monitor is enabled in Overload manager api, this field should be explicitly configured with value greater than 0.
   */
  max_active_downstream_connections?: number | undefined;
}

function createBaseDownstreamConnectionsConfig(): DownstreamConnectionsConfig {
  return { $type: "envoy.extensions.resource_monitors.downstream_connections.v3.DownstreamConnectionsConfig" };
}

export const DownstreamConnectionsConfig: MessageFns<
  DownstreamConnectionsConfig,
  "envoy.extensions.resource_monitors.downstream_connections.v3.DownstreamConnectionsConfig"
> = {
  $type: "envoy.extensions.resource_monitors.downstream_connections.v3.DownstreamConnectionsConfig" as const,

  encode(message: DownstreamConnectionsConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.max_active_downstream_connections !== undefined && message.max_active_downstream_connections !== 0) {
      writer.uint32(8).int64(message.max_active_downstream_connections);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DownstreamConnectionsConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDownstreamConnectionsConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.max_active_downstream_connections = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DownstreamConnectionsConfig {
    return {
      $type: DownstreamConnectionsConfig.$type,
      max_active_downstream_connections: isSet(object.max_active_downstream_connections)
        ? globalThis.Number(object.max_active_downstream_connections)
        : undefined,
    };
  },

  toJSON(message: DownstreamConnectionsConfig): unknown {
    const obj: any = {};
    if (message.max_active_downstream_connections !== undefined) {
      obj.max_active_downstream_connections = Math.round(message.max_active_downstream_connections);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DownstreamConnectionsConfig>, I>>(base?: I): DownstreamConnectionsConfig {
    return DownstreamConnectionsConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DownstreamConnectionsConfig>, I>>(object: I): DownstreamConnectionsConfig {
    const message = createBaseDownstreamConnectionsConfig();
    message.max_active_downstream_connections = object.max_active_downstream_connections ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(DownstreamConnectionsConfig.$type, DownstreamConnectionsConfig);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
