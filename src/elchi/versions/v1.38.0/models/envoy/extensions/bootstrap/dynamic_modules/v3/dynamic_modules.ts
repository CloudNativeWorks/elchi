// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/bootstrap/dynamic_modules/v3/dynamic_modules.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Any } from "../../../../../google/protobuf/any";
import { messageTypeRegistry } from "../../../../../typeRegistry";
import { DynamicModuleConfig } from "../../../dynamic_modules/v3/dynamic_modules";

export const protobufPackage = "envoy.extensions.bootstrap.dynamic_modules.v3";

/**
 * Configuration for the Dynamic Modules bootstrap extension. This extension allows loading shared
 * object files that can be loaded via ``dlopen`` to extend Envoy's bootstrap behavior.
 *
 * A module can be loaded by multiple bootstrap extensions; the module is loaded only once and shared
 * across multiple extensions.
 *
 * Bootstrap extensions run on the main thread and are initialized when Envoy starts. They can:
 *
 * * Perform initialization tasks when the server is initialized.
 * * Perform per-worker thread initialization when worker threads start.
 * * Access server-level resources like the cluster manager and dispatcher.
 */
export interface DynamicModuleBootstrapExtension {
  $type: "envoy.extensions.bootstrap.dynamic_modules.v3.DynamicModuleBootstrapExtension";
  /** Specifies the shared-object level configuration. */
  dynamic_module_config?:
    | DynamicModuleConfig
    | undefined;
  /**
   * The name for this extension configuration.
   *
   * This can be used to distinguish between different extension implementations inside a dynamic
   * module. For example, a module can have completely different extension implementations. When Envoy
   * receives this configuration, it passes the ``extension_name`` to the dynamic module's bootstrap
   * extension config init function together with the ``extension_config``. That way a module can
   * decide which in-module extension implementation to use based on the name at load time.
   *
   * If not specified, defaults to an empty string.
   */
  extension_name?:
    | string
    | undefined;
  /**
   * The configuration for the extension chosen by ``extension_name``.
   *
   * This is passed to the module's bootstrap extension initialization function. Together with the
   * ``extension_name``, the module can decide which in-module extension implementation to use and
   * fine-tune the behavior of the extension.
   *
   * For example, if a module has two extension implementations, one for configuration loading and
   * one for metric initialization, ``extension_name`` is used to choose the implementation. The
   * ``extension_config`` can be used to configure the specific behavior of each implementation.
   *
   * ``google.protobuf.Struct`` is serialized as JSON before passing it to the module.
   * ``google.protobuf.BytesValue`` and ``google.protobuf.StringValue`` are passed directly
   * without the wrapper.
   *
   * .. code-block:: yaml
   *
   *  # Passing a string value
   *  extension_config:
   *    "@type": "type.googleapis.com/google.protobuf.StringValue"
   *    value: hello
   *
   *  # Passing raw bytes
   *  extension_config:
   *    "@type": "type.googleapis.com/google.protobuf.BytesValue"
   *    value: aGVsbG8=  # echo -n "hello" | base64
   */
  extension_config?: Any | undefined;
}

function createBaseDynamicModuleBootstrapExtension(): DynamicModuleBootstrapExtension {
  return { $type: "envoy.extensions.bootstrap.dynamic_modules.v3.DynamicModuleBootstrapExtension" };
}

export const DynamicModuleBootstrapExtension: MessageFns<
  DynamicModuleBootstrapExtension,
  "envoy.extensions.bootstrap.dynamic_modules.v3.DynamicModuleBootstrapExtension"
> = {
  $type: "envoy.extensions.bootstrap.dynamic_modules.v3.DynamicModuleBootstrapExtension" as const,

  encode(message: DynamicModuleBootstrapExtension, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dynamic_module_config !== undefined) {
      DynamicModuleConfig.encode(message.dynamic_module_config, writer.uint32(10).fork()).join();
    }
    if (message.extension_name !== undefined && message.extension_name !== "") {
      writer.uint32(18).string(message.extension_name);
    }
    if (message.extension_config !== undefined) {
      Any.encode(message.extension_config, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DynamicModuleBootstrapExtension {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDynamicModuleBootstrapExtension();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dynamic_module_config = DynamicModuleConfig.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.extension_name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.extension_config = Any.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DynamicModuleBootstrapExtension {
    return {
      $type: DynamicModuleBootstrapExtension.$type,
      dynamic_module_config: isSet(object.dynamic_module_config)
        ? DynamicModuleConfig.fromJSON(object.dynamic_module_config)
        : undefined,
      extension_name: isSet(object.extension_name) ? globalThis.String(object.extension_name) : undefined,
      extension_config: isSet(object.extension_config) ? Any.fromJSON(object.extension_config) : undefined,
    };
  },

  toJSON(message: DynamicModuleBootstrapExtension): unknown {
    const obj: any = {};
    if (message.dynamic_module_config !== undefined) {
      obj.dynamic_module_config = DynamicModuleConfig.toJSON(message.dynamic_module_config);
    }
    if (message.extension_name !== undefined) {
      obj.extension_name = message.extension_name;
    }
    if (message.extension_config !== undefined) {
      obj.extension_config = Any.toJSON(message.extension_config);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DynamicModuleBootstrapExtension>, I>>(base?: I): DynamicModuleBootstrapExtension {
    return DynamicModuleBootstrapExtension.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DynamicModuleBootstrapExtension>, I>>(
    object: I,
  ): DynamicModuleBootstrapExtension {
    const message = createBaseDynamicModuleBootstrapExtension();
    message.dynamic_module_config =
      (object.dynamic_module_config !== undefined && object.dynamic_module_config !== null)
        ? DynamicModuleConfig.fromPartial(object.dynamic_module_config)
        : undefined;
    message.extension_name = object.extension_name ?? undefined;
    message.extension_config = (object.extension_config !== undefined && object.extension_config !== null)
      ? Any.fromPartial(object.extension_config)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(DynamicModuleBootstrapExtension.$type, DynamicModuleBootstrapExtension);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
