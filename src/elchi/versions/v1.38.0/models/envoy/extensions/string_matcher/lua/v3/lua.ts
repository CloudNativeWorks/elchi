// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/string_matcher/lua/v3/lua.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../typeRegistry";
import { DataSource } from "../../../../config/core/v3/base";

export const protobufPackage = "envoy.extensions.string_matcher.lua.v3";

export interface Lua {
  $type: "envoy.extensions.string_matcher.lua.v3.Lua";
  /** The Lua code that Envoy will execute */
  source_code?: DataSource | undefined;
}

function createBaseLua(): Lua {
  return { $type: "envoy.extensions.string_matcher.lua.v3.Lua" };
}

export const Lua: MessageFns<Lua, "envoy.extensions.string_matcher.lua.v3.Lua"> = {
  $type: "envoy.extensions.string_matcher.lua.v3.Lua" as const,

  encode(message: Lua, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.source_code !== undefined) {
      DataSource.encode(message.source_code, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Lua {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLua();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.source_code = DataSource.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Lua {
    return {
      $type: Lua.$type,
      source_code: isSet(object.source_code) ? DataSource.fromJSON(object.source_code) : undefined,
    };
  },

  toJSON(message: Lua): unknown {
    const obj: any = {};
    if (message.source_code !== undefined) {
      obj.source_code = DataSource.toJSON(message.source_code);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Lua>, I>>(base?: I): Lua {
    return Lua.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Lua>, I>>(object: I): Lua {
    const message = createBaseLua();
    message.source_code = (object.source_code !== undefined && object.source_code !== null)
      ? DataSource.fromPartial(object.source_code)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(Lua.$type, Lua);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
