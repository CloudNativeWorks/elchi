// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/http/stateful_session/header/v3/header.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.http.stateful_session.header.v3";

/**
 * This extension allows the session state to be tracked via request headers.
 *
 * This extension encodes the address of the upstream host selected by the load balancer
 * into a response header with the :ref:`header configuration
 * <envoy_v3_api_msg_extensions.http.stateful_session.header.v3.HeaderBasedSessionState>`.
 * When new requests are incoming, this extension will try to parse the specific upstream host
 * address by header name. If the address parsed from the header corresponds to a valid
 * upstream host, this upstream host will be selected first. See :ref:`stateful session filter
 * <envoy_v3_api_msg_extensions.filters.http.stateful_session.v3.StatefulSession>`.
 *
 * For example, if the header name is set to ``session-header``, Envoy will prefer ``1.2.3.4:80``
 * as the upstream host when the request contains the following header:
 *
 * .. code-block:: none
 *
 *     session-header: "MS4yLjMuNDo4MA=="
 *
 * When processing the upstream response, if ``1.2.3.4:80`` is indeed the final choice the extension
 * does nothing. If ``1.2.3.4:80`` is not the final choice, the new selected host will be set to
 * response headers (via the ``session-header`` response header).
 *
 * [#extension: envoy.http.stateful_session.header]
 */
export interface HeaderBasedSessionState {
  $type: "envoy.extensions.http.stateful_session.header.v3.HeaderBasedSessionState";
  /**
   * The name that will be used to obtain header value from downstream HTTP request or generate
   * new header for downstream.
   */
  name?: string | undefined;
}

function createBaseHeaderBasedSessionState(): HeaderBasedSessionState {
  return { $type: "envoy.extensions.http.stateful_session.header.v3.HeaderBasedSessionState" };
}

export const HeaderBasedSessionState: MessageFns<
  HeaderBasedSessionState,
  "envoy.extensions.http.stateful_session.header.v3.HeaderBasedSessionState"
> = {
  $type: "envoy.extensions.http.stateful_session.header.v3.HeaderBasedSessionState" as const,

  encode(message: HeaderBasedSessionState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HeaderBasedSessionState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHeaderBasedSessionState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HeaderBasedSessionState {
    return {
      $type: HeaderBasedSessionState.$type,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
    };
  },

  toJSON(message: HeaderBasedSessionState): unknown {
    const obj: any = {};
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HeaderBasedSessionState>, I>>(base?: I): HeaderBasedSessionState {
    return HeaderBasedSessionState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HeaderBasedSessionState>, I>>(object: I): HeaderBasedSessionState {
    const message = createBaseHeaderBasedSessionState();
    message.name = object.name ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(HeaderBasedSessionState.$type, HeaderBasedSessionState);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
