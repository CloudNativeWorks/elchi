// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/network/dns_resolver/hickory/v3/hickory_dns_resolver.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../../../../../../google/protobuf/duration";
import { BoolValue, UInt32Value } from "../../../../../../google/protobuf/wrappers";
import { messageTypeRegistry } from "../../../../../../typeRegistry";
import { Address } from "../../../../../config/core/v3/address";

export const protobufPackage = "envoy.extensions.network.dns_resolver.hickory.v3";

/** Configuration for DNS-over-TLS (DoT) servers. */
export interface DnsOverTlsConfig {
  $type: "envoy.extensions.network.dns_resolver.hickory.v3.DnsOverTlsConfig";
  /** The list of DNS-over-TLS server addresses. The port should typically be 853. */
  servers?:
    | Address[]
    | undefined;
  /** The SNI hostname to use for TLS verification. Required when ``servers`` are specified. */
  tls_server_name?: string | undefined;
}

/** Configuration for DNS-over-HTTPS (DoH) servers. */
export interface DnsOverHttpsConfig {
  $type: "envoy.extensions.network.dns_resolver.hickory.v3.DnsOverHttpsConfig";
  /** The list of DNS-over-HTTPS endpoint URLs (e.g., ``https://dns.google/dns-query``). */
  server_urls?: string[] | undefined;
}

/**
 * Configuration for the Hickory DNS resolver. This resolver uses the Hickory DNS library,
 * a pure Rust DNS implementation, for DNS resolution. It supports standard DNS (UDP/TCP),
 * DNS-over-TLS (DoT), DNS-over-HTTPS (DoH), and ``DNSSEC`` validation.
 *
 * The resolver runs asynchronously on its own ``Tokio`` runtime threads, separate from Envoy's
 * event loop threads. Results are delivered back to the calling dispatcher thread.
 * [#next-free-field: 10]
 */
export interface HickoryDnsResolverConfig {
  $type: "envoy.extensions.network.dns_resolver.hickory.v3.HickoryDnsResolverConfig";
  /**
   * A list of DNS resolver addresses for standard UDP/TCP resolution.
   * If not specified and ``use_system_config`` is not explicitly set to ``false``, the system
   * configuration (``/etc/resolv.conf`` on Unix) will be used.
   */
  resolvers?:
    | Address[]
    | undefined;
  /**
   * Configuration for DNS-over-TLS (DoT). When specified, queries will be sent over TLS
   * to the configured servers.
   */
  dns_over_tls?:
    | DnsOverTlsConfig
    | undefined;
  /**
   * Configuration for DNS-over-HTTPS (DoH). When specified, queries will be sent over
   * HTTPS to the configured endpoints.
   */
  dns_over_https?:
    | DnsOverHttpsConfig
    | undefined;
  /**
   * Enables ``DNSSEC`` validation for DNS responses. When enabled, the resolver will validate
   * ``DNSSEC`` signatures and reject responses that fail validation.
   *
   * Defaults to ``false``.
   */
  enable_dnssec?:
    | boolean
    | undefined;
  /**
   * Maximum number of entries in the DNS response cache. The cache uses an LRU eviction
   * policy and supports negative caching (caching of ``NXDOMAIN``/``NODATA`` responses).
   *
   * Defaults to ``1024``.
   */
  cache_size?:
    | number
    | undefined;
  /**
   * Number of threads in the ``Tokio`` runtime used for asynchronous DNS resolution.
   * Each resolver instance runs its own ``Tokio`` runtime.
   *
   * Defaults to ``2``. Maximum is ``16``.
   */
  num_resolver_threads?:
    | number
    | undefined;
  /**
   * If ``true``, read the system DNS configuration (``/etc/resolv.conf`` on Unix) for name server
   * addresses and search domains. When ``resolvers`` are also specified, they take precedence
   * over the system configuration.
   *
   * If not specified, defaults to ``true`` when no ``resolvers``, ``dns_over_tls``, or
   * ``dns_over_https`` are configured.
   */
  use_system_config?:
    | boolean
    | undefined;
  /**
   * Timeout for each individual DNS query attempt.
   *
   * Defaults to ``5`` seconds.
   */
  query_timeout?:
    | Duration
    | undefined;
  /**
   * Maximum number of query attempts before the resolver gives up. Each attempt may use
   * a different name server.
   *
   * Defaults to ``3``.
   */
  query_tries?: number | undefined;
}

function createBaseDnsOverTlsConfig(): DnsOverTlsConfig {
  return { $type: "envoy.extensions.network.dns_resolver.hickory.v3.DnsOverTlsConfig" };
}

export const DnsOverTlsConfig: MessageFns<
  DnsOverTlsConfig,
  "envoy.extensions.network.dns_resolver.hickory.v3.DnsOverTlsConfig"
> = {
  $type: "envoy.extensions.network.dns_resolver.hickory.v3.DnsOverTlsConfig" as const,

  encode(message: DnsOverTlsConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.servers !== undefined && message.servers.length !== 0) {
      for (const v of message.servers) {
        Address.encode(v!, writer.uint32(10).fork()).join();
      }
    }
    if (message.tls_server_name !== undefined && message.tls_server_name !== "") {
      writer.uint32(18).string(message.tls_server_name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DnsOverTlsConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDnsOverTlsConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          if (message.servers === undefined) {
            message.servers = [];
          }
          const el = Address.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.servers!.push(el);
          }
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tls_server_name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DnsOverTlsConfig {
    return {
      $type: DnsOverTlsConfig.$type,
      servers: globalThis.Array.isArray(object?.servers)
        ? object.servers.map((e: any) => Address.fromJSON(e))
        : undefined,
      tls_server_name: isSet(object.tls_server_name) ? globalThis.String(object.tls_server_name) : undefined,
    };
  },

  toJSON(message: DnsOverTlsConfig): unknown {
    const obj: any = {};
    if (message.servers?.length) {
      obj.servers = message.servers.map((e) => Address.toJSON(e));
    }
    if (message.tls_server_name !== undefined) {
      obj.tls_server_name = message.tls_server_name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DnsOverTlsConfig>, I>>(base?: I): DnsOverTlsConfig {
    return DnsOverTlsConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DnsOverTlsConfig>, I>>(object: I): DnsOverTlsConfig {
    const message = createBaseDnsOverTlsConfig();
    message.servers = object.servers?.map((e) => Address.fromPartial(e)) || undefined;
    message.tls_server_name = object.tls_server_name ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(DnsOverTlsConfig.$type, DnsOverTlsConfig);

function createBaseDnsOverHttpsConfig(): DnsOverHttpsConfig {
  return { $type: "envoy.extensions.network.dns_resolver.hickory.v3.DnsOverHttpsConfig" };
}

export const DnsOverHttpsConfig: MessageFns<
  DnsOverHttpsConfig,
  "envoy.extensions.network.dns_resolver.hickory.v3.DnsOverHttpsConfig"
> = {
  $type: "envoy.extensions.network.dns_resolver.hickory.v3.DnsOverHttpsConfig" as const,

  encode(message: DnsOverHttpsConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.server_urls !== undefined && message.server_urls.length !== 0) {
      for (const v of message.server_urls) {
        writer.uint32(10).string(v!);
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DnsOverHttpsConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDnsOverHttpsConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          if (message.server_urls === undefined) {
            message.server_urls = [];
          }
          const el = reader.string();
          if (el !== undefined) {
            message.server_urls!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DnsOverHttpsConfig {
    return {
      $type: DnsOverHttpsConfig.$type,
      server_urls: globalThis.Array.isArray(object?.server_urls)
        ? object.server_urls.map((e: any) => globalThis.String(e))
        : undefined,
    };
  },

  toJSON(message: DnsOverHttpsConfig): unknown {
    const obj: any = {};
    if (message.server_urls?.length) {
      obj.server_urls = message.server_urls;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DnsOverHttpsConfig>, I>>(base?: I): DnsOverHttpsConfig {
    return DnsOverHttpsConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DnsOverHttpsConfig>, I>>(object: I): DnsOverHttpsConfig {
    const message = createBaseDnsOverHttpsConfig();
    message.server_urls = object.server_urls?.map((e) => e) || undefined;
    return message;
  },
};

messageTypeRegistry.set(DnsOverHttpsConfig.$type, DnsOverHttpsConfig);

function createBaseHickoryDnsResolverConfig(): HickoryDnsResolverConfig {
  return { $type: "envoy.extensions.network.dns_resolver.hickory.v3.HickoryDnsResolverConfig" };
}

export const HickoryDnsResolverConfig: MessageFns<
  HickoryDnsResolverConfig,
  "envoy.extensions.network.dns_resolver.hickory.v3.HickoryDnsResolverConfig"
> = {
  $type: "envoy.extensions.network.dns_resolver.hickory.v3.HickoryDnsResolverConfig" as const,

  encode(message: HickoryDnsResolverConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.resolvers !== undefined && message.resolvers.length !== 0) {
      for (const v of message.resolvers) {
        Address.encode(v!, writer.uint32(10).fork()).join();
      }
    }
    if (message.dns_over_tls !== undefined) {
      DnsOverTlsConfig.encode(message.dns_over_tls, writer.uint32(18).fork()).join();
    }
    if (message.dns_over_https !== undefined) {
      DnsOverHttpsConfig.encode(message.dns_over_https, writer.uint32(26).fork()).join();
    }
    if (message.enable_dnssec !== undefined && message.enable_dnssec !== false) {
      writer.uint32(32).bool(message.enable_dnssec);
    }
    if (message.cache_size !== undefined) {
      UInt32Value.encode({ $type: "google.protobuf.UInt32Value", value: message.cache_size! }, writer.uint32(42).fork())
        .join();
    }
    if (message.num_resolver_threads !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.num_resolver_threads! },
        writer.uint32(50).fork(),
      ).join();
    }
    if (message.use_system_config !== undefined) {
      BoolValue.encode(
        { $type: "google.protobuf.BoolValue", value: message.use_system_config! },
        writer.uint32(58).fork(),
      ).join();
    }
    if (message.query_timeout !== undefined) {
      Duration.encode(message.query_timeout, writer.uint32(66).fork()).join();
    }
    if (message.query_tries !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.query_tries! },
        writer.uint32(74).fork(),
      ).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HickoryDnsResolverConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHickoryDnsResolverConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          if (message.resolvers === undefined) {
            message.resolvers = [];
          }
          const el = Address.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.resolvers!.push(el);
          }
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.dns_over_tls = DnsOverTlsConfig.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.dns_over_https = DnsOverHttpsConfig.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.enable_dnssec = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.cache_size = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.num_resolver_threads = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.use_system_config = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.query_timeout = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.query_tries = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HickoryDnsResolverConfig {
    return {
      $type: HickoryDnsResolverConfig.$type,
      resolvers: globalThis.Array.isArray(object?.resolvers)
        ? object.resolvers.map((e: any) => Address.fromJSON(e))
        : undefined,
      dns_over_tls: isSet(object.dns_over_tls) ? DnsOverTlsConfig.fromJSON(object.dns_over_tls) : undefined,
      dns_over_https: isSet(object.dns_over_https) ? DnsOverHttpsConfig.fromJSON(object.dns_over_https) : undefined,
      enable_dnssec: isSet(object.enable_dnssec) ? globalThis.Boolean(object.enable_dnssec) : undefined,
      cache_size: isSet(object.cache_size) ? Number(object.cache_size) : undefined,
      num_resolver_threads: isSet(object.num_resolver_threads) ? Number(object.num_resolver_threads) : undefined,
      use_system_config: isSet(object.use_system_config) ? Boolean(object.use_system_config) : undefined,
      query_timeout: isSet(object.query_timeout) ? Duration.fromJSON(object.query_timeout) : undefined,
      query_tries: isSet(object.query_tries) ? Number(object.query_tries) : undefined,
    };
  },

  toJSON(message: HickoryDnsResolverConfig): unknown {
    const obj: any = {};
    if (message.resolvers?.length) {
      obj.resolvers = message.resolvers.map((e) => Address.toJSON(e));
    }
    if (message.dns_over_tls !== undefined) {
      obj.dns_over_tls = DnsOverTlsConfig.toJSON(message.dns_over_tls);
    }
    if (message.dns_over_https !== undefined) {
      obj.dns_over_https = DnsOverHttpsConfig.toJSON(message.dns_over_https);
    }
    if (message.enable_dnssec !== undefined) {
      obj.enable_dnssec = message.enable_dnssec;
    }
    if (message.cache_size !== undefined) {
      obj.cache_size = message.cache_size;
    }
    if (message.num_resolver_threads !== undefined) {
      obj.num_resolver_threads = message.num_resolver_threads;
    }
    if (message.use_system_config !== undefined) {
      obj.use_system_config = message.use_system_config;
    }
    if (message.query_timeout !== undefined) {
      obj.query_timeout = Duration.toJSON(message.query_timeout);
    }
    if (message.query_tries !== undefined) {
      obj.query_tries = message.query_tries;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HickoryDnsResolverConfig>, I>>(base?: I): HickoryDnsResolverConfig {
    return HickoryDnsResolverConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HickoryDnsResolverConfig>, I>>(object: I): HickoryDnsResolverConfig {
    const message = createBaseHickoryDnsResolverConfig();
    message.resolvers = object.resolvers?.map((e) => Address.fromPartial(e)) || undefined;
    message.dns_over_tls = (object.dns_over_tls !== undefined && object.dns_over_tls !== null)
      ? DnsOverTlsConfig.fromPartial(object.dns_over_tls)
      : undefined;
    message.dns_over_https = (object.dns_over_https !== undefined && object.dns_over_https !== null)
      ? DnsOverHttpsConfig.fromPartial(object.dns_over_https)
      : undefined;
    message.enable_dnssec = object.enable_dnssec ?? undefined;
    message.cache_size = object.cache_size ?? undefined;
    message.num_resolver_threads = object.num_resolver_threads ?? undefined;
    message.use_system_config = object.use_system_config ?? undefined;
    message.query_timeout = (object.query_timeout !== undefined && object.query_timeout !== null)
      ? Duration.fromPartial(object.query_timeout)
      : undefined;
    message.query_tries = object.query_tries ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(HickoryDnsResolverConfig.$type, HickoryDnsResolverConfig);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
