// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/tracers/dynamic_modules/v3/dynamic_modules.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Any } from "../../../../../google/protobuf/any";
import { messageTypeRegistry } from "../../../../../typeRegistry";
import { DynamicModuleConfig } from "../../../dynamic_modules/v3/dynamic_modules";

export const protobufPackage = "envoy.extensions.tracers.dynamic_modules.v3";

/**
 * Configuration for the Dynamic Modules Tracer. This tracer allows loading shared object
 * files via ``dlopen`` to implement custom distributed tracing backends.
 *
 * A module can be loaded by multiple tracer configurations; the module is loaded only once
 * and shared across multiple tracer instances.
 *
 * The tracer receives trace context from incoming requests and can inject trace context into
 * outgoing requests for propagation. It supports the full span lifecycle: creation, tagging,
 * logging, child spans, and reporting.
 */
export interface DynamicModuleTracer {
  $type: "envoy.extensions.tracers.dynamic_modules.v3.DynamicModuleTracer";
  /** Specifies the shared-object level configuration. This field is required. */
  dynamic_module_config?:
    | DynamicModuleConfig
    | undefined;
  /**
   * The name for this tracer configuration. If not specified, defaults to an empty string.
   *
   * This can be used to distinguish between different tracer implementations inside a dynamic
   * module. For example, a module can have completely different tracer implementations (e.g.,
   * Zipkin-compatible, OpenTelemetry-compatible). When Envoy receives this configuration, it
   * passes the ``tracer_name`` to the dynamic module's tracer config init function together with
   * the ``tracer_config``. That way a module can decide which in-module tracer implementation to
   * use based on the name at load time.
   */
  tracer_name?:
    | string
    | undefined;
  /**
   * The configuration for the tracer chosen by ``tracer_name``. If not specified, an empty
   * configuration is passed to the module.
   *
   * This is passed to the module's tracer initialization function. Together with the
   * ``tracer_name``, the module can decide which in-module tracer implementation to use and
   * fine-tune the behavior of the tracer.
   *
   * ``google.protobuf.Struct`` is serialized as JSON before passing it to the module.
   * ``google.protobuf.BytesValue`` and ``google.protobuf.StringValue`` are passed directly
   * without the wrapper.
   *
   * .. code-block:: yaml
   *
   *  # Passing a JSON struct configuration
   *  tracer_config:
   *    "@type": "type.googleapis.com/google.protobuf.Struct"
   *    value:
   *      endpoint: "http://tracing-backend:9411/api/v2/spans"
   *      sample_rate: 0.1
   *
   *  # Passing a simple string configuration
   *  tracer_config:
   *    "@type": "type.googleapis.com/google.protobuf.StringValue"
   *    value: "http://tracing-backend:9411"
   */
  tracer_config?: Any | undefined;
}

function createBaseDynamicModuleTracer(): DynamicModuleTracer {
  return { $type: "envoy.extensions.tracers.dynamic_modules.v3.DynamicModuleTracer" };
}

export const DynamicModuleTracer: MessageFns<
  DynamicModuleTracer,
  "envoy.extensions.tracers.dynamic_modules.v3.DynamicModuleTracer"
> = {
  $type: "envoy.extensions.tracers.dynamic_modules.v3.DynamicModuleTracer" as const,

  encode(message: DynamicModuleTracer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dynamic_module_config !== undefined) {
      DynamicModuleConfig.encode(message.dynamic_module_config, writer.uint32(10).fork()).join();
    }
    if (message.tracer_name !== undefined && message.tracer_name !== "") {
      writer.uint32(18).string(message.tracer_name);
    }
    if (message.tracer_config !== undefined) {
      Any.encode(message.tracer_config, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DynamicModuleTracer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDynamicModuleTracer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dynamic_module_config = DynamicModuleConfig.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tracer_name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tracer_config = Any.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DynamicModuleTracer {
    return {
      $type: DynamicModuleTracer.$type,
      dynamic_module_config: isSet(object.dynamic_module_config)
        ? DynamicModuleConfig.fromJSON(object.dynamic_module_config)
        : undefined,
      tracer_name: isSet(object.tracer_name) ? globalThis.String(object.tracer_name) : undefined,
      tracer_config: isSet(object.tracer_config) ? Any.fromJSON(object.tracer_config) : undefined,
    };
  },

  toJSON(message: DynamicModuleTracer): unknown {
    const obj: any = {};
    if (message.dynamic_module_config !== undefined) {
      obj.dynamic_module_config = DynamicModuleConfig.toJSON(message.dynamic_module_config);
    }
    if (message.tracer_name !== undefined) {
      obj.tracer_name = message.tracer_name;
    }
    if (message.tracer_config !== undefined) {
      obj.tracer_config = Any.toJSON(message.tracer_config);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DynamicModuleTracer>, I>>(base?: I): DynamicModuleTracer {
    return DynamicModuleTracer.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DynamicModuleTracer>, I>>(object: I): DynamicModuleTracer {
    const message = createBaseDynamicModuleTracer();
    message.dynamic_module_config =
      (object.dynamic_module_config !== undefined && object.dynamic_module_config !== null)
        ? DynamicModuleConfig.fromPartial(object.dynamic_module_config)
        : undefined;
    message.tracer_name = object.tracer_name ?? undefined;
    message.tracer_config = (object.tracer_config !== undefined && object.tracer_config !== null)
      ? Any.fromPartial(object.tracer_config)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(DynamicModuleTracer.$type, DynamicModuleTracer);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
