// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/load_balancing_policies/random_subsetting/v3/random_subsetting.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { UInt32Value } from "../../../../../google/protobuf/wrappers";
import { messageTypeRegistry } from "../../../../../typeRegistry";
import { LoadBalancingPolicy } from "../../../../config/cluster/v3/cluster";

export const protobufPackage = "envoy.extensions.load_balancing_policies.random_subsetting.v3";

/**
 * Configuration for the Random Subsetting Load Balancing Policy
 *
 * This policy selects a subset of endpoints and passes them to the child LB policy.
 * It maintains 2 important properties:
 * 1. The policy tries to distribute connections among servers as equally as possible. The higher
 * ``(N_clients*subset_size)/N_servers`` ratio is, the closer the resulting server connection
 * distribution is to uniform.
 * 2. The policy minimizes the amount of connection churn generated during server scale-ups by
 * using rendezvous hashing
 *
 * See the :ref:`load balancing architecture
 * overview<arch_overview_load_balancing_types>` for more information.
 *
 * [#not-implemented-hide:]
 */
export interface RandomSubsetting {
  $type: "envoy.extensions.load_balancing_policies.random_subsetting.v3.RandomSubsetting";
  /**
   * subset_size indicates how many backends every client will be connected to.
   * The value must be greater than 0.
   */
  subset_size?:
    | number
    | undefined;
  /**
   * The config for the child policy.
   * The value is required.
   */
  child_policy?: LoadBalancingPolicy | undefined;
}

function createBaseRandomSubsetting(): RandomSubsetting {
  return { $type: "envoy.extensions.load_balancing_policies.random_subsetting.v3.RandomSubsetting" };
}

export const RandomSubsetting: MessageFns<
  RandomSubsetting,
  "envoy.extensions.load_balancing_policies.random_subsetting.v3.RandomSubsetting"
> = {
  $type: "envoy.extensions.load_balancing_policies.random_subsetting.v3.RandomSubsetting" as const,

  encode(message: RandomSubsetting, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.subset_size !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.subset_size! },
        writer.uint32(10).fork(),
      ).join();
    }
    if (message.child_policy !== undefined) {
      LoadBalancingPolicy.encode(message.child_policy, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RandomSubsetting {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRandomSubsetting();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.subset_size = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.child_policy = LoadBalancingPolicy.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RandomSubsetting {
    return {
      $type: RandomSubsetting.$type,
      subset_size: isSet(object.subset_size) ? Number(object.subset_size) : undefined,
      child_policy: isSet(object.child_policy) ? LoadBalancingPolicy.fromJSON(object.child_policy) : undefined,
    };
  },

  toJSON(message: RandomSubsetting): unknown {
    const obj: any = {};
    if (message.subset_size !== undefined) {
      obj.subset_size = message.subset_size;
    }
    if (message.child_policy !== undefined) {
      obj.child_policy = LoadBalancingPolicy.toJSON(message.child_policy);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RandomSubsetting>, I>>(base?: I): RandomSubsetting {
    return RandomSubsetting.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RandomSubsetting>, I>>(object: I): RandomSubsetting {
    const message = createBaseRandomSubsetting();
    message.subset_size = object.subset_size ?? undefined;
    message.child_policy = (object.child_policy !== undefined && object.child_policy !== null)
      ? LoadBalancingPolicy.fromPartial(object.child_policy)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(RandomSubsetting.$type, RandomSubsetting);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
