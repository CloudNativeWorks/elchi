// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/internal_redirect/previous_routes/v3/previous_routes_config.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.internal_redirect.previous_routes.v3";

/**
 * An internal redirect predicate that rejects redirect targets that are pointing
 * to a route that has been followed by a previous redirect from the current route.
 * [#extension: envoy.internal_redirect_predicates.previous_routes]
 */
export interface PreviousRoutesConfig {
  $type: "envoy.extensions.internal_redirect.previous_routes.v3.PreviousRoutesConfig";
}

function createBasePreviousRoutesConfig(): PreviousRoutesConfig {
  return { $type: "envoy.extensions.internal_redirect.previous_routes.v3.PreviousRoutesConfig" };
}

export const PreviousRoutesConfig: MessageFns<
  PreviousRoutesConfig,
  "envoy.extensions.internal_redirect.previous_routes.v3.PreviousRoutesConfig"
> = {
  $type: "envoy.extensions.internal_redirect.previous_routes.v3.PreviousRoutesConfig" as const,

  encode(_: PreviousRoutesConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PreviousRoutesConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePreviousRoutesConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PreviousRoutesConfig {
    return { $type: PreviousRoutesConfig.$type };
  },

  toJSON(_: PreviousRoutesConfig): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<PreviousRoutesConfig>, I>>(base?: I): PreviousRoutesConfig {
    return PreviousRoutesConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PreviousRoutesConfig>, I>>(_: I): PreviousRoutesConfig {
    const message = createBasePreviousRoutesConfig();
    return message;
  },
};

messageTypeRegistry.set(PreviousRoutesConfig.$type, PreviousRoutesConfig);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
