// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/internal_redirect/safe_cross_scheme/v3/safe_cross_scheme_config.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.internal_redirect.safe_cross_scheme.v3";

/**
 * An internal redirect predicate that checks the scheme between the
 * downstream url and the redirect target url and allows a) same scheme
 * redirect and b) safe cross scheme redirect, which means if the downstream
 * scheme is HTTPS, both HTTPS and HTTP redirect targets are allowed, but if the
 * downstream scheme is HTTP, only HTTP redirect targets are allowed.
 * [#extension: envoy.internal_redirect_predicates.safe_cross_scheme]
 */
export interface SafeCrossSchemeConfig {
  $type: "envoy.extensions.internal_redirect.safe_cross_scheme.v3.SafeCrossSchemeConfig";
}

function createBaseSafeCrossSchemeConfig(): SafeCrossSchemeConfig {
  return { $type: "envoy.extensions.internal_redirect.safe_cross_scheme.v3.SafeCrossSchemeConfig" };
}

export const SafeCrossSchemeConfig: MessageFns<
  SafeCrossSchemeConfig,
  "envoy.extensions.internal_redirect.safe_cross_scheme.v3.SafeCrossSchemeConfig"
> = {
  $type: "envoy.extensions.internal_redirect.safe_cross_scheme.v3.SafeCrossSchemeConfig" as const,

  encode(_: SafeCrossSchemeConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SafeCrossSchemeConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSafeCrossSchemeConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): SafeCrossSchemeConfig {
    return { $type: SafeCrossSchemeConfig.$type };
  },

  toJSON(_: SafeCrossSchemeConfig): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<SafeCrossSchemeConfig>, I>>(base?: I): SafeCrossSchemeConfig {
    return SafeCrossSchemeConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SafeCrossSchemeConfig>, I>>(_: I): SafeCrossSchemeConfig {
    const message = createBaseSafeCrossSchemeConfig();
    return message;
  },
};

messageTypeRegistry.set(SafeCrossSchemeConfig.$type, SafeCrossSchemeConfig);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
