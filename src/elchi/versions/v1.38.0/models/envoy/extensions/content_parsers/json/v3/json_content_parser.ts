// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/content_parsers/json/v3/json_content_parser.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../typeRegistry";
import { JsonToMetadata_Rule } from "../../../filters/http/json_to_metadata/v3/json_to_metadata";

export const protobufPackage = "envoy.extensions.content_parsers.json.v3";

/** Configuration for the JSON content parser. */
export interface JsonContentParser {
  $type: "envoy.extensions.content_parsers.json.v3.JsonContentParser";
  /**
   * The rules to apply for extracting values from JSON content.
   * Rules are evaluated in order for each content item provided.
   * At least one rule must be specified.
   */
  rules?: JsonContentParser_RuleConfig[] | undefined;
}

/** Configuration for a single rule with its processing behavior. */
export interface JsonContentParser_RuleConfig {
  $type: "envoy.extensions.content_parsers.json.v3.JsonContentParser.RuleConfig";
  /**
   * The json-to-metadata rule to apply for extracting values from JSON content.
   * See :ref:`json_to_metadata rule <envoy_v3_api_msg_extensions.filters.http.json_to_metadata.v3.JsonToMetadata.Rule>`
   * for available configuration options including selectors, on_present, on_missing, on_error actions.
   */
  rule?:
    | JsonToMetadata_Rule
    | undefined;
  /**
   * Controls how many times this rule should successfully match before stopping evaluation
   * of this rule for subsequent content items.
   *
   * - If set to 0 (default): This rule is evaluated against all content items provided.
   *   Later matches may overwrite earlier values (unless
   *   :ref:`preserve_existing_metadata_value <envoy_v3_api_field_extensions.filters.http.json_to_metadata.v3.JsonToMetadata.KeyValuePair.preserve_existing_metadata_value>`
   *   is set in the rule), effectively extracting the LAST occurrence.
   *
   * - If set to 1: Stop evaluating this rule after the first successful match.
   *   This is useful for extracting values that appear early in the stream
   *   to avoid unnecessary processing of subsequent content.
   *
   * - If set to N > 1: Reserved for future use (e.g., aggregating multiple values).
   *   Values > 1 are currently rejected to prevent behavioral changes when this feature is implemented.
   *
   * Example use cases:
   *
   * - Extract model name from early content: ``stop_processing_after_matches: 1``
   *   (Stops checking this rule after first match, doesn't process remaining content for this rule)
   *
   * - Extract token usage from final content: ``stop_processing_after_matches: 0``
   *   (Processes all content items, extracts value from the last one that contains it)
   */
  stop_processing_after_matches?: number | undefined;
}

function createBaseJsonContentParser(): JsonContentParser {
  return { $type: "envoy.extensions.content_parsers.json.v3.JsonContentParser" };
}

export const JsonContentParser: MessageFns<
  JsonContentParser,
  "envoy.extensions.content_parsers.json.v3.JsonContentParser"
> = {
  $type: "envoy.extensions.content_parsers.json.v3.JsonContentParser" as const,

  encode(message: JsonContentParser, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rules !== undefined && message.rules.length !== 0) {
      for (const v of message.rules) {
        JsonContentParser_RuleConfig.encode(v!, writer.uint32(10).fork()).join();
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JsonContentParser {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJsonContentParser();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          if (message.rules === undefined) {
            message.rules = [];
          }
          const el = JsonContentParser_RuleConfig.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.rules!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JsonContentParser {
    return {
      $type: JsonContentParser.$type,
      rules: globalThis.Array.isArray(object?.rules)
        ? object.rules.map((e: any) => JsonContentParser_RuleConfig.fromJSON(e))
        : undefined,
    };
  },

  toJSON(message: JsonContentParser): unknown {
    const obj: any = {};
    if (message.rules?.length) {
      obj.rules = message.rules.map((e) => JsonContentParser_RuleConfig.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JsonContentParser>, I>>(base?: I): JsonContentParser {
    return JsonContentParser.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JsonContentParser>, I>>(object: I): JsonContentParser {
    const message = createBaseJsonContentParser();
    message.rules = object.rules?.map((e) => JsonContentParser_RuleConfig.fromPartial(e)) || undefined;
    return message;
  },
};

messageTypeRegistry.set(JsonContentParser.$type, JsonContentParser);

function createBaseJsonContentParser_RuleConfig(): JsonContentParser_RuleConfig {
  return { $type: "envoy.extensions.content_parsers.json.v3.JsonContentParser.RuleConfig" };
}

export const JsonContentParser_RuleConfig: MessageFns<
  JsonContentParser_RuleConfig,
  "envoy.extensions.content_parsers.json.v3.JsonContentParser.RuleConfig"
> = {
  $type: "envoy.extensions.content_parsers.json.v3.JsonContentParser.RuleConfig" as const,

  encode(message: JsonContentParser_RuleConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rule !== undefined) {
      JsonToMetadata_Rule.encode(message.rule, writer.uint32(10).fork()).join();
    }
    if (message.stop_processing_after_matches !== undefined && message.stop_processing_after_matches !== 0) {
      writer.uint32(16).uint32(message.stop_processing_after_matches);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JsonContentParser_RuleConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJsonContentParser_RuleConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rule = JsonToMetadata_Rule.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.stop_processing_after_matches = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JsonContentParser_RuleConfig {
    return {
      $type: JsonContentParser_RuleConfig.$type,
      rule: isSet(object.rule) ? JsonToMetadata_Rule.fromJSON(object.rule) : undefined,
      stop_processing_after_matches: isSet(object.stop_processing_after_matches)
        ? globalThis.Number(object.stop_processing_after_matches)
        : undefined,
    };
  },

  toJSON(message: JsonContentParser_RuleConfig): unknown {
    const obj: any = {};
    if (message.rule !== undefined) {
      obj.rule = JsonToMetadata_Rule.toJSON(message.rule);
    }
    if (message.stop_processing_after_matches !== undefined) {
      obj.stop_processing_after_matches = Math.round(message.stop_processing_after_matches);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JsonContentParser_RuleConfig>, I>>(base?: I): JsonContentParser_RuleConfig {
    return JsonContentParser_RuleConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JsonContentParser_RuleConfig>, I>>(object: I): JsonContentParser_RuleConfig {
    const message = createBaseJsonContentParser_RuleConfig();
    message.rule = (object.rule !== undefined && object.rule !== null)
      ? JsonToMetadata_Rule.fromPartial(object.rule)
      : undefined;
    message.stop_processing_after_matches = object.stop_processing_after_matches ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(JsonContentParser_RuleConfig.$type, JsonContentParser_RuleConfig);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
