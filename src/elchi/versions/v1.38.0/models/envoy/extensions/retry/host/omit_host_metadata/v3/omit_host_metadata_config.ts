// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/retry/host/omit_host_metadata/v3/omit_host_metadata_config.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../../typeRegistry";
import { Metadata } from "../../../../../config/core/v3/base";

export const protobufPackage = "envoy.extensions.retry.host.omit_host_metadata.v3";

/**
 * A retry host predicate that can be used to reject a host based on
 * predefined metadata match criteria.
 * [#extension: envoy.retry_host_predicates.omit_host_metadata]
 */
export interface OmitHostMetadataConfig {
  $type: "envoy.extensions.retry.host.omit_host_metadata.v3.OmitHostMetadataConfig";
  /**
   * Retry host predicate metadata match criteria. The hosts in
   * the upstream cluster with matching metadata will be omitted while
   * attempting a retry of a failed request. The metadata should be specified
   * under the ``envoy.lb`` key.
   */
  metadata_match?: Metadata | undefined;
}

function createBaseOmitHostMetadataConfig(): OmitHostMetadataConfig {
  return { $type: "envoy.extensions.retry.host.omit_host_metadata.v3.OmitHostMetadataConfig" };
}

export const OmitHostMetadataConfig: MessageFns<
  OmitHostMetadataConfig,
  "envoy.extensions.retry.host.omit_host_metadata.v3.OmitHostMetadataConfig"
> = {
  $type: "envoy.extensions.retry.host.omit_host_metadata.v3.OmitHostMetadataConfig" as const,

  encode(message: OmitHostMetadataConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.metadata_match !== undefined) {
      Metadata.encode(message.metadata_match, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OmitHostMetadataConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOmitHostMetadataConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.metadata_match = Metadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OmitHostMetadataConfig {
    return {
      $type: OmitHostMetadataConfig.$type,
      metadata_match: isSet(object.metadata_match) ? Metadata.fromJSON(object.metadata_match) : undefined,
    };
  },

  toJSON(message: OmitHostMetadataConfig): unknown {
    const obj: any = {};
    if (message.metadata_match !== undefined) {
      obj.metadata_match = Metadata.toJSON(message.metadata_match);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OmitHostMetadataConfig>, I>>(base?: I): OmitHostMetadataConfig {
    return OmitHostMetadataConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OmitHostMetadataConfig>, I>>(object: I): OmitHostMetadataConfig {
    const message = createBaseOmitHostMetadataConfig();
    message.metadata_match = (object.metadata_match !== undefined && object.metadata_match !== null)
      ? Metadata.fromPartial(object.metadata_match)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(OmitHostMetadataConfig.$type, OmitHostMetadataConfig);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
