// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/clusters/composite/v3/cluster.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.clusters.composite.v3";

/**
 * Configuration for the composite cluster. See the :ref:`architecture overview
 * <arch_overview_composite_cluster>` for more information. This cluster type enables retry-aware
 * cluster selection, allowing different retry attempts to automatically target
 * different upstream clusters. Unlike the standard aggregate cluster which uses
 * health-based selection, the composite cluster uses the retry attempt count to
 * deterministically select which sub-cluster to route to.
 *
 * When retry attempts exceed the number of configured clusters, requests will fail with no
 * host available.
 *
 * Example configuration:
 *
 * .. code-block:: yaml
 *
 *     name: composite_cluster
 *     connect_timeout: 0.25s
 *     lb_policy: CLUSTER_PROVIDED
 *     cluster_type:
 *       name: envoy.clusters.composite
 *       typed_config:
 *         "@type": type.googleapis.com/envoy.extensions.clusters.composite.v3.ClusterConfig
 *         clusters:
 *         - name: primary_cluster
 *         - name: secondary_cluster
 *         - name: fallback_cluster
 *
 * [#extension: envoy.clusters.composite]
 */
export interface ClusterConfig {
  $type: "envoy.extensions.clusters.composite.v3.ClusterConfig";
  /**
   * List of clusters to use for request routing. The first cluster is used for the
   * initial request (attempt 1), the second cluster for the first retry (attempt 2),
   * and so on. Must contain at least one cluster. When retry attempts exceed the number
   * of configured clusters, requests will fail with no host available.
   */
  clusters?: ClusterConfig_ClusterEntry[] | undefined;
}

/** Configuration for an individual cluster entry. */
export interface ClusterConfig_ClusterEntry {
  $type: "envoy.extensions.clusters.composite.v3.ClusterConfig.ClusterEntry";
  /** Name of the cluster. This cluster must be defined elsewhere in the configuration. */
  name?: string | undefined;
}

function createBaseClusterConfig(): ClusterConfig {
  return { $type: "envoy.extensions.clusters.composite.v3.ClusterConfig" };
}

export const ClusterConfig: MessageFns<ClusterConfig, "envoy.extensions.clusters.composite.v3.ClusterConfig"> = {
  $type: "envoy.extensions.clusters.composite.v3.ClusterConfig" as const,

  encode(message: ClusterConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clusters !== undefined && message.clusters.length !== 0) {
      for (const v of message.clusters) {
        ClusterConfig_ClusterEntry.encode(v!, writer.uint32(10).fork()).join();
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClusterConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClusterConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          if (message.clusters === undefined) {
            message.clusters = [];
          }
          const el = ClusterConfig_ClusterEntry.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.clusters!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClusterConfig {
    return {
      $type: ClusterConfig.$type,
      clusters: globalThis.Array.isArray(object?.clusters)
        ? object.clusters.map((e: any) => ClusterConfig_ClusterEntry.fromJSON(e))
        : undefined,
    };
  },

  toJSON(message: ClusterConfig): unknown {
    const obj: any = {};
    if (message.clusters?.length) {
      obj.clusters = message.clusters.map((e) => ClusterConfig_ClusterEntry.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ClusterConfig>, I>>(base?: I): ClusterConfig {
    return ClusterConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ClusterConfig>, I>>(object: I): ClusterConfig {
    const message = createBaseClusterConfig();
    message.clusters = object.clusters?.map((e) => ClusterConfig_ClusterEntry.fromPartial(e)) || undefined;
    return message;
  },
};

messageTypeRegistry.set(ClusterConfig.$type, ClusterConfig);

function createBaseClusterConfig_ClusterEntry(): ClusterConfig_ClusterEntry {
  return { $type: "envoy.extensions.clusters.composite.v3.ClusterConfig.ClusterEntry" };
}

export const ClusterConfig_ClusterEntry: MessageFns<
  ClusterConfig_ClusterEntry,
  "envoy.extensions.clusters.composite.v3.ClusterConfig.ClusterEntry"
> = {
  $type: "envoy.extensions.clusters.composite.v3.ClusterConfig.ClusterEntry" as const,

  encode(message: ClusterConfig_ClusterEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClusterConfig_ClusterEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClusterConfig_ClusterEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClusterConfig_ClusterEntry {
    return {
      $type: ClusterConfig_ClusterEntry.$type,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
    };
  },

  toJSON(message: ClusterConfig_ClusterEntry): unknown {
    const obj: any = {};
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ClusterConfig_ClusterEntry>, I>>(base?: I): ClusterConfig_ClusterEntry {
    return ClusterConfig_ClusterEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ClusterConfig_ClusterEntry>, I>>(object: I): ClusterConfig_ClusterEntry {
    const message = createBaseClusterConfig_ClusterEntry();
    message.name = object.name ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(ClusterConfig_ClusterEntry.$type, ClusterConfig_ClusterEntry);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
