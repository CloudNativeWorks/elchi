// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/clusters/dynamic_modules/v3/cluster.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Any } from "../../../../../google/protobuf/any";
import { messageTypeRegistry } from "../../../../../typeRegistry";
import { DynamicModuleConfig } from "../../../dynamic_modules/v3/dynamic_modules";

export const protobufPackage = "envoy.extensions.clusters.dynamic_modules.v3";

/**
 * Configuration for the dynamic modules cluster.
 *
 * This cluster type delegates host discovery and load balancing to a dynamic module. The module
 * manages hosts via callbacks such as ``envoy_dynamic_module_callback_cluster_add_host`` and
 * ``envoy_dynamic_module_callback_cluster_remove_host``. The cluster must use
 * ``lb_policy: CLUSTER_PROVIDED`` since the module provides its own load balancer.
 *
 * [#extension: envoy.clusters.dynamic_modules]
 */
export interface ClusterConfig {
  $type: "envoy.extensions.clusters.dynamic_modules.v3.ClusterConfig";
  /** The dynamic module configuration for the cluster. */
  dynamic_module_config?:
    | DynamicModuleConfig
    | undefined;
  /**
   * The name to identify the cluster implementation within the module.
   * This is passed to the module's ``envoy_dynamic_module_on_cluster_config_new`` function.
   */
  cluster_name?:
    | string
    | undefined;
  /**
   * The configuration for the module's cluster implementation.
   * This is passed to the module's ``envoy_dynamic_module_on_cluster_config_new`` function.
   * The configuration can be any protobuf message. However, it is recommended to use
   * ``google.protobuf.Struct``, ``google.protobuf.StringValue``, or ``google.protobuf.BytesValue``.
   * These types are passed directly as bytes to the module, so the module does not need to have
   * knowledge of protobuf encoding. Otherwise, the serialized bytes of the type are passed.
   * If not specified, an empty configuration is passed.
   */
  cluster_config?: Any | undefined;
}

function createBaseClusterConfig(): ClusterConfig {
  return { $type: "envoy.extensions.clusters.dynamic_modules.v3.ClusterConfig" };
}

export const ClusterConfig: MessageFns<ClusterConfig, "envoy.extensions.clusters.dynamic_modules.v3.ClusterConfig"> = {
  $type: "envoy.extensions.clusters.dynamic_modules.v3.ClusterConfig" as const,

  encode(message: ClusterConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dynamic_module_config !== undefined) {
      DynamicModuleConfig.encode(message.dynamic_module_config, writer.uint32(10).fork()).join();
    }
    if (message.cluster_name !== undefined && message.cluster_name !== "") {
      writer.uint32(18).string(message.cluster_name);
    }
    if (message.cluster_config !== undefined) {
      Any.encode(message.cluster_config, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClusterConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClusterConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dynamic_module_config = DynamicModuleConfig.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cluster_name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.cluster_config = Any.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClusterConfig {
    return {
      $type: ClusterConfig.$type,
      dynamic_module_config: isSet(object.dynamic_module_config)
        ? DynamicModuleConfig.fromJSON(object.dynamic_module_config)
        : undefined,
      cluster_name: isSet(object.cluster_name) ? globalThis.String(object.cluster_name) : undefined,
      cluster_config: isSet(object.cluster_config) ? Any.fromJSON(object.cluster_config) : undefined,
    };
  },

  toJSON(message: ClusterConfig): unknown {
    const obj: any = {};
    if (message.dynamic_module_config !== undefined) {
      obj.dynamic_module_config = DynamicModuleConfig.toJSON(message.dynamic_module_config);
    }
    if (message.cluster_name !== undefined) {
      obj.cluster_name = message.cluster_name;
    }
    if (message.cluster_config !== undefined) {
      obj.cluster_config = Any.toJSON(message.cluster_config);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ClusterConfig>, I>>(base?: I): ClusterConfig {
    return ClusterConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ClusterConfig>, I>>(object: I): ClusterConfig {
    const message = createBaseClusterConfig();
    message.dynamic_module_config =
      (object.dynamic_module_config !== undefined && object.dynamic_module_config !== null)
        ? DynamicModuleConfig.fromPartial(object.dynamic_module_config)
        : undefined;
    message.cluster_name = object.cluster_name ?? undefined;
    message.cluster_config = (object.cluster_config !== undefined && object.cluster_config !== null)
      ? Any.fromPartial(object.cluster_config)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(ClusterConfig.$type, ClusterConfig);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
