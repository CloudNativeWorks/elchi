// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/clusters/mcp_multicluster/v3/cluster.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../../../../../google/protobuf/duration";
import { messageTypeRegistry } from "../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.clusters.mcp_multicluster.v3";

/**
 * Configuration for the MCP multi cluster. See the :ref:`architecture overview
 * <arch_overview_mcp_multicluster>` for more information. This cluster type allows aggregation of
 * multiple clusters into one, providing metadata with the list of aggregated clusters.
 * Use the ``attemptCount`` property of the request ``StreamInfo`` object to select host in a specific subcluster.
 * If the ``attemptCount`` value is greater than the number of aggregated clusters, the host selection will fail.
 *
 * The primary purpose of this cluster extension is to provide the list of servers for the MCP router
 * configured for tool and resource aggregation. For details of how tools and resource are aggregated see
 * :ref:`MCP router documentation<config_http_filters_mcp_router>`.
 *
 * Example configuration:
 *
 * .. code-block:: yaml
 *
 *     name: mcp_multicluster
 *     connect_timeout: 0.25s
 *     lb_policy: CLUSTER_PROVIDED
 *     cluster_type:
 *       name: envoy.clusters.mcp_multicluster
 *       typed_config:
 *         "@type": type.googleapis.com/envoy.extensions.clusters.mcp_multicluster.v3.ClusterConfig
 *         servers:
 *         - name: build_tools
 *           mcp_cluster:
 *             cluster: build_tools
 *         - name: review_tools
 *            mcp_cluster:
 *              cluster: review_tools
 *              host_rewrite_literal: "mcp.review_tools.acme.com"
 *
 * [#extension: envoy.clusters.mcp_multicluster]
 */
export interface ClusterConfig {
  $type: "envoy.extensions.clusters.mcp_multicluster.v3.ClusterConfig";
  /**
   * A list of remote MCP servers.
   * Based on the MCP multi cluster configuration the MCP router aggregates capabilities, tools and resources from remote MCP servers
   * and presents itself as single MCP server to the client. All remote MCP servers are sent the same capabilities
   * that the client presented to Envoy. MCP router prefixes tool names and resource path with the server name to resolve
   * naming collisions.
   */
  servers?: ClusterConfig_McpBackend[] | undefined;
}

/** Cluster-based backend configuration. */
export interface ClusterConfig_McpCluster {
  $type: "envoy.extensions.clusters.mcp_multicluster.v3.ClusterConfig.McpCluster";
  /** Cluster name to route requests to. */
  cluster?:
    | string
    | undefined;
  /** Path to use for MCP requests. Defaults to "/mcp". */
  path?:
    | string
    | undefined;
  /**
   * Request timeout.
   * If not set, uses cluster's timeout configuration.
   */
  timeout?:
    | Duration
    | undefined;
  /**
   * Indicates that during forwarding, the host header will be swapped with
   * this value.
   */
  host_rewrite_literal?: string | undefined;
}

/** Specification of the MCP server. */
export interface ClusterConfig_McpBackend {
  $type: "envoy.extensions.clusters.mcp_multicluster.v3.ClusterConfig.McpBackend";
  /**
   * Unique name for this backend. Used for:
   * - Tool name prefixing (e.g., "time__get_current_time")
   * - Session ID composition
   * - Logging and error messages.
   * Default will be the cluster name if not specified.
   */
  name?:
    | string
    | undefined;
  /** Backend target specification. */
  mcp_cluster?: ClusterConfig_McpCluster | undefined;
}

function createBaseClusterConfig(): ClusterConfig {
  return { $type: "envoy.extensions.clusters.mcp_multicluster.v3.ClusterConfig" };
}

export const ClusterConfig: MessageFns<ClusterConfig, "envoy.extensions.clusters.mcp_multicluster.v3.ClusterConfig"> = {
  $type: "envoy.extensions.clusters.mcp_multicluster.v3.ClusterConfig" as const,

  encode(message: ClusterConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.servers !== undefined && message.servers.length !== 0) {
      for (const v of message.servers) {
        ClusterConfig_McpBackend.encode(v!, writer.uint32(10).fork()).join();
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClusterConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClusterConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          if (message.servers === undefined) {
            message.servers = [];
          }
          const el = ClusterConfig_McpBackend.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.servers!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClusterConfig {
    return {
      $type: ClusterConfig.$type,
      servers: globalThis.Array.isArray(object?.servers)
        ? object.servers.map((e: any) => ClusterConfig_McpBackend.fromJSON(e))
        : undefined,
    };
  },

  toJSON(message: ClusterConfig): unknown {
    const obj: any = {};
    if (message.servers?.length) {
      obj.servers = message.servers.map((e) => ClusterConfig_McpBackend.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ClusterConfig>, I>>(base?: I): ClusterConfig {
    return ClusterConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ClusterConfig>, I>>(object: I): ClusterConfig {
    const message = createBaseClusterConfig();
    message.servers = object.servers?.map((e) => ClusterConfig_McpBackend.fromPartial(e)) || undefined;
    return message;
  },
};

messageTypeRegistry.set(ClusterConfig.$type, ClusterConfig);

function createBaseClusterConfig_McpCluster(): ClusterConfig_McpCluster {
  return { $type: "envoy.extensions.clusters.mcp_multicluster.v3.ClusterConfig.McpCluster" };
}

export const ClusterConfig_McpCluster: MessageFns<
  ClusterConfig_McpCluster,
  "envoy.extensions.clusters.mcp_multicluster.v3.ClusterConfig.McpCluster"
> = {
  $type: "envoy.extensions.clusters.mcp_multicluster.v3.ClusterConfig.McpCluster" as const,

  encode(message: ClusterConfig_McpCluster, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cluster !== undefined && message.cluster !== "") {
      writer.uint32(10).string(message.cluster);
    }
    if (message.path !== undefined && message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    if (message.timeout !== undefined) {
      Duration.encode(message.timeout, writer.uint32(26).fork()).join();
    }
    if (message.host_rewrite_literal !== undefined && message.host_rewrite_literal !== "") {
      writer.uint32(34).string(message.host_rewrite_literal);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClusterConfig_McpCluster {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClusterConfig_McpCluster();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.cluster = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.timeout = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.host_rewrite_literal = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClusterConfig_McpCluster {
    return {
      $type: ClusterConfig_McpCluster.$type,
      cluster: isSet(object.cluster) ? globalThis.String(object.cluster) : undefined,
      path: isSet(object.path) ? globalThis.String(object.path) : undefined,
      timeout: isSet(object.timeout) ? Duration.fromJSON(object.timeout) : undefined,
      host_rewrite_literal: isSet(object.host_rewrite_literal)
        ? globalThis.String(object.host_rewrite_literal)
        : undefined,
    };
  },

  toJSON(message: ClusterConfig_McpCluster): unknown {
    const obj: any = {};
    if (message.cluster !== undefined) {
      obj.cluster = message.cluster;
    }
    if (message.path !== undefined) {
      obj.path = message.path;
    }
    if (message.timeout !== undefined) {
      obj.timeout = Duration.toJSON(message.timeout);
    }
    if (message.host_rewrite_literal !== undefined) {
      obj.host_rewrite_literal = message.host_rewrite_literal;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ClusterConfig_McpCluster>, I>>(base?: I): ClusterConfig_McpCluster {
    return ClusterConfig_McpCluster.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ClusterConfig_McpCluster>, I>>(object: I): ClusterConfig_McpCluster {
    const message = createBaseClusterConfig_McpCluster();
    message.cluster = object.cluster ?? undefined;
    message.path = object.path ?? undefined;
    message.timeout = (object.timeout !== undefined && object.timeout !== null)
      ? Duration.fromPartial(object.timeout)
      : undefined;
    message.host_rewrite_literal = object.host_rewrite_literal ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(ClusterConfig_McpCluster.$type, ClusterConfig_McpCluster);

function createBaseClusterConfig_McpBackend(): ClusterConfig_McpBackend {
  return { $type: "envoy.extensions.clusters.mcp_multicluster.v3.ClusterConfig.McpBackend" };
}

export const ClusterConfig_McpBackend: MessageFns<
  ClusterConfig_McpBackend,
  "envoy.extensions.clusters.mcp_multicluster.v3.ClusterConfig.McpBackend"
> = {
  $type: "envoy.extensions.clusters.mcp_multicluster.v3.ClusterConfig.McpBackend" as const,

  encode(message: ClusterConfig_McpBackend, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.mcp_cluster !== undefined) {
      ClusterConfig_McpCluster.encode(message.mcp_cluster, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClusterConfig_McpBackend {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClusterConfig_McpBackend();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.mcp_cluster = ClusterConfig_McpCluster.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClusterConfig_McpBackend {
    return {
      $type: ClusterConfig_McpBackend.$type,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      mcp_cluster: isSet(object.mcp_cluster) ? ClusterConfig_McpCluster.fromJSON(object.mcp_cluster) : undefined,
    };
  },

  toJSON(message: ClusterConfig_McpBackend): unknown {
    const obj: any = {};
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.mcp_cluster !== undefined) {
      obj.mcp_cluster = ClusterConfig_McpCluster.toJSON(message.mcp_cluster);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ClusterConfig_McpBackend>, I>>(base?: I): ClusterConfig_McpBackend {
    return ClusterConfig_McpBackend.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ClusterConfig_McpBackend>, I>>(object: I): ClusterConfig_McpBackend {
    const message = createBaseClusterConfig_McpBackend();
    message.name = object.name ?? undefined;
    message.mcp_cluster = (object.mcp_cluster !== undefined && object.mcp_cluster !== null)
      ? ClusterConfig_McpCluster.fromPartial(object.mcp_cluster)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(ClusterConfig_McpBackend.$type, ClusterConfig_McpBackend);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
