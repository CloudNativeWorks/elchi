// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/matching/common_inputs/transport_socket/v3/transport_socket_inputs.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.matching.common_inputs.transport_socket.v3";

/**
 * Specifies that matching should be performed by the endpoint metadata.
 * This input extracts metadata from the selected endpoint for transport socket selection.
 * The metadata is extracted using a filter and path specification similar to
 * :ref:`DynamicMetadataInput <envoy_v3_api_msg_extensions.matching.common_inputs.network.v3.DynamicMetadataInput>`.
 *
 * Example: Extract a metadata value for transport socket matching.
 *
 * .. code-block:: yaml
 *
 *   typed_config:
 *     "@type": type.googleapis.com/envoy.extensions.matching.common_inputs.transport_socket.v3.EndpointMetadataInput
 *     filter: "envoy.transport_socket_match"
 *     path:
 *       - key: "socket_type"
 *
 * This configuration extracts the value at path ``["envoy.transport_socket_match"]["socket_type"]``
 * from the endpoint metadata for use in transport socket selection.
 *
 * [#extension: envoy.matching.inputs.endpoint_metadata]
 */
export interface EndpointMetadataInput {
  $type: "envoy.extensions.matching.common_inputs.transport_socket.v3.EndpointMetadataInput";
  /**
   * The filter name to retrieve the Struct from the endpoint metadata.
   * If not specified, defaults to ``envoy.lb`` which is commonly used for load balancing metadata.
   */
  filter?:
    | string
    | undefined;
  /** The path to retrieve the Value from the Struct. */
  path?: EndpointMetadataInput_PathSegment[] | undefined;
}

/**
 * Specifies the segment in a path to retrieve value from Metadata.
 * Note: Currently it's not supported to retrieve a value from a list in Metadata. This means that
 * if the segment key refers to a list, it has to be the last segment in a path.
 */
export interface EndpointMetadataInput_PathSegment {
  $type: "envoy.extensions.matching.common_inputs.transport_socket.v3.EndpointMetadataInput.PathSegment";
  segment?:
    | //
    /** If specified, use the key to retrieve the value in a Struct. */
    { $case: "key"; key: string }
    | undefined;
}

/**
 * Specifies that matching should be performed by the locality metadata.
 * This input extracts metadata from the endpoint's locality for transport socket selection.
 * The metadata is extracted using a filter and path specification similar to
 * :ref:`DynamicMetadataInput <envoy_v3_api_msg_extensions.matching.common_inputs.network.v3.DynamicMetadataInput>`.
 *
 * Example: Extract a metadata value from locality for transport socket matching.
 *
 * .. code-block:: yaml
 *
 *   typed_config:
 *     "@type": type.googleapis.com/envoy.extensions.matching.common_inputs.transport_socket.v3.LocalityMetadataInput
 *     filter: "envoy.transport_socket_match"
 *     path:
 *       - key: "region"
 *
 * This configuration extracts the value at path ``["envoy.transport_socket_match"]["region"]``
 * from the locality metadata for use in transport socket selection.
 *
 * [#extension: envoy.matching.inputs.locality_metadata]
 */
export interface LocalityMetadataInput {
  $type: "envoy.extensions.matching.common_inputs.transport_socket.v3.LocalityMetadataInput";
  /**
   * The filter name to retrieve the Struct from the locality metadata.
   * If not specified, defaults to ``envoy.lb`` which is commonly used for load balancing metadata.
   */
  filter?:
    | string
    | undefined;
  /** The path to retrieve the Value from the Struct. */
  path?: LocalityMetadataInput_PathSegment[] | undefined;
}

/**
 * Specifies the segment in a path to retrieve value from Metadata.
 * Note: Currently it's not supported to retrieve a value from a list in Metadata. This means that
 * if the segment key refers to a list, it has to be the last segment in a path.
 */
export interface LocalityMetadataInput_PathSegment {
  $type: "envoy.extensions.matching.common_inputs.transport_socket.v3.LocalityMetadataInput.PathSegment";
  segment?:
    | //
    /** If specified, use the key to retrieve the value in a Struct. */
    { $case: "key"; key: string }
    | undefined;
}

/**
 * Specifies that matching should be performed by filter state.
 * This input extracts a value from filter state that was explicitly shared from the
 * downstream connection to the upstream connection via ``TransportSocketOptions``.
 * This enables flexible downstream-connection-based transport socket selection,
 * such as matching on network namespace or any custom filter state data.
 *
 * Example: Match on network namespace stored in filter state.
 *
 * .. code-block:: yaml
 *
 *   typed_config:
 *     "@type": type.googleapis.com/envoy.extensions.matching.common_inputs.transport_socket.v3.FilterStateInput
 *     key: "envoy.network.namespace"
 *
 * [#extension: envoy.matching.inputs.transport_socket_filter_state]
 */
export interface FilterStateInput {
  $type: "envoy.extensions.matching.common_inputs.transport_socket.v3.FilterStateInput";
  /**
   * The key of the filter state object to retrieve.
   * The object must implement serializeAsString() to be used for matching.
   */
  key?: string | undefined;
}

/**
 * Configuration for the transport socket name action.
 * This action sets the name of the transport socket to use when the matcher matches.
 * [#extension: envoy.matching.action.transport_socket.name]
 */
export interface TransportSocketNameAction {
  $type: "envoy.extensions.matching.common_inputs.transport_socket.v3.TransportSocketNameAction";
  /**
   * The name of the transport socket to use.
   * This name must reference a named transport socket in the cluster's transport_socket_matches.
   */
  name?: string | undefined;
}

function createBaseEndpointMetadataInput(): EndpointMetadataInput {
  return { $type: "envoy.extensions.matching.common_inputs.transport_socket.v3.EndpointMetadataInput" };
}

export const EndpointMetadataInput: MessageFns<
  EndpointMetadataInput,
  "envoy.extensions.matching.common_inputs.transport_socket.v3.EndpointMetadataInput"
> = {
  $type: "envoy.extensions.matching.common_inputs.transport_socket.v3.EndpointMetadataInput" as const,

  encode(message: EndpointMetadataInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.filter !== undefined && message.filter !== "") {
      writer.uint32(10).string(message.filter);
    }
    if (message.path !== undefined && message.path.length !== 0) {
      for (const v of message.path) {
        EndpointMetadataInput_PathSegment.encode(v!, writer.uint32(18).fork()).join();
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EndpointMetadataInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEndpointMetadataInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.filter = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          if (message.path === undefined) {
            message.path = [];
          }
          const el = EndpointMetadataInput_PathSegment.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.path!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EndpointMetadataInput {
    return {
      $type: EndpointMetadataInput.$type,
      filter: isSet(object.filter) ? globalThis.String(object.filter) : undefined,
      path: globalThis.Array.isArray(object?.path)
        ? object.path.map((e: any) => EndpointMetadataInput_PathSegment.fromJSON(e))
        : undefined,
    };
  },

  toJSON(message: EndpointMetadataInput): unknown {
    const obj: any = {};
    if (message.filter !== undefined) {
      obj.filter = message.filter;
    }
    if (message.path?.length) {
      obj.path = message.path.map((e) => EndpointMetadataInput_PathSegment.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EndpointMetadataInput>, I>>(base?: I): EndpointMetadataInput {
    return EndpointMetadataInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EndpointMetadataInput>, I>>(object: I): EndpointMetadataInput {
    const message = createBaseEndpointMetadataInput();
    message.filter = object.filter ?? undefined;
    message.path = object.path?.map((e) => EndpointMetadataInput_PathSegment.fromPartial(e)) || undefined;
    return message;
  },
};

messageTypeRegistry.set(EndpointMetadataInput.$type, EndpointMetadataInput);

function createBaseEndpointMetadataInput_PathSegment(): EndpointMetadataInput_PathSegment {
  return {
    $type: "envoy.extensions.matching.common_inputs.transport_socket.v3.EndpointMetadataInput.PathSegment",
    segment: undefined,
  };
}

export const EndpointMetadataInput_PathSegment: MessageFns<
  EndpointMetadataInput_PathSegment,
  "envoy.extensions.matching.common_inputs.transport_socket.v3.EndpointMetadataInput.PathSegment"
> = {
  $type: "envoy.extensions.matching.common_inputs.transport_socket.v3.EndpointMetadataInput.PathSegment" as const,

  encode(message: EndpointMetadataInput_PathSegment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.segment?.$case) {
      case "key":
        writer.uint32(10).string(message.segment.key);
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EndpointMetadataInput_PathSegment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEndpointMetadataInput_PathSegment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.segment = { $case: "key", key: reader.string() };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EndpointMetadataInput_PathSegment {
    return {
      $type: EndpointMetadataInput_PathSegment.$type,
      segment: isSet(object.key) ? { $case: "key", key: globalThis.String(object.key) } : undefined,
    };
  },

  toJSON(message: EndpointMetadataInput_PathSegment): unknown {
    const obj: any = {};
    if (message.segment?.$case === "key") {
      obj.key = message.segment.key;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EndpointMetadataInput_PathSegment>, I>>(
    base?: I,
  ): EndpointMetadataInput_PathSegment {
    return EndpointMetadataInput_PathSegment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EndpointMetadataInput_PathSegment>, I>>(
    object: I,
  ): EndpointMetadataInput_PathSegment {
    const message = createBaseEndpointMetadataInput_PathSegment();
    if (object.segment?.$case === "key" && object.segment?.key !== undefined && object.segment?.key !== null) {
      message.segment = { $case: "key", key: object.segment.key };
    }
    return message;
  },
};

messageTypeRegistry.set(EndpointMetadataInput_PathSegment.$type, EndpointMetadataInput_PathSegment);

function createBaseLocalityMetadataInput(): LocalityMetadataInput {
  return { $type: "envoy.extensions.matching.common_inputs.transport_socket.v3.LocalityMetadataInput" };
}

export const LocalityMetadataInput: MessageFns<
  LocalityMetadataInput,
  "envoy.extensions.matching.common_inputs.transport_socket.v3.LocalityMetadataInput"
> = {
  $type: "envoy.extensions.matching.common_inputs.transport_socket.v3.LocalityMetadataInput" as const,

  encode(message: LocalityMetadataInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.filter !== undefined && message.filter !== "") {
      writer.uint32(10).string(message.filter);
    }
    if (message.path !== undefined && message.path.length !== 0) {
      for (const v of message.path) {
        LocalityMetadataInput_PathSegment.encode(v!, writer.uint32(18).fork()).join();
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LocalityMetadataInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLocalityMetadataInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.filter = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          if (message.path === undefined) {
            message.path = [];
          }
          const el = LocalityMetadataInput_PathSegment.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.path!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LocalityMetadataInput {
    return {
      $type: LocalityMetadataInput.$type,
      filter: isSet(object.filter) ? globalThis.String(object.filter) : undefined,
      path: globalThis.Array.isArray(object?.path)
        ? object.path.map((e: any) => LocalityMetadataInput_PathSegment.fromJSON(e))
        : undefined,
    };
  },

  toJSON(message: LocalityMetadataInput): unknown {
    const obj: any = {};
    if (message.filter !== undefined) {
      obj.filter = message.filter;
    }
    if (message.path?.length) {
      obj.path = message.path.map((e) => LocalityMetadataInput_PathSegment.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LocalityMetadataInput>, I>>(base?: I): LocalityMetadataInput {
    return LocalityMetadataInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LocalityMetadataInput>, I>>(object: I): LocalityMetadataInput {
    const message = createBaseLocalityMetadataInput();
    message.filter = object.filter ?? undefined;
    message.path = object.path?.map((e) => LocalityMetadataInput_PathSegment.fromPartial(e)) || undefined;
    return message;
  },
};

messageTypeRegistry.set(LocalityMetadataInput.$type, LocalityMetadataInput);

function createBaseLocalityMetadataInput_PathSegment(): LocalityMetadataInput_PathSegment {
  return {
    $type: "envoy.extensions.matching.common_inputs.transport_socket.v3.LocalityMetadataInput.PathSegment",
    segment: undefined,
  };
}

export const LocalityMetadataInput_PathSegment: MessageFns<
  LocalityMetadataInput_PathSegment,
  "envoy.extensions.matching.common_inputs.transport_socket.v3.LocalityMetadataInput.PathSegment"
> = {
  $type: "envoy.extensions.matching.common_inputs.transport_socket.v3.LocalityMetadataInput.PathSegment" as const,

  encode(message: LocalityMetadataInput_PathSegment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.segment?.$case) {
      case "key":
        writer.uint32(10).string(message.segment.key);
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LocalityMetadataInput_PathSegment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLocalityMetadataInput_PathSegment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.segment = { $case: "key", key: reader.string() };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LocalityMetadataInput_PathSegment {
    return {
      $type: LocalityMetadataInput_PathSegment.$type,
      segment: isSet(object.key) ? { $case: "key", key: globalThis.String(object.key) } : undefined,
    };
  },

  toJSON(message: LocalityMetadataInput_PathSegment): unknown {
    const obj: any = {};
    if (message.segment?.$case === "key") {
      obj.key = message.segment.key;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LocalityMetadataInput_PathSegment>, I>>(
    base?: I,
  ): LocalityMetadataInput_PathSegment {
    return LocalityMetadataInput_PathSegment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LocalityMetadataInput_PathSegment>, I>>(
    object: I,
  ): LocalityMetadataInput_PathSegment {
    const message = createBaseLocalityMetadataInput_PathSegment();
    if (object.segment?.$case === "key" && object.segment?.key !== undefined && object.segment?.key !== null) {
      message.segment = { $case: "key", key: object.segment.key };
    }
    return message;
  },
};

messageTypeRegistry.set(LocalityMetadataInput_PathSegment.$type, LocalityMetadataInput_PathSegment);

function createBaseFilterStateInput(): FilterStateInput {
  return { $type: "envoy.extensions.matching.common_inputs.transport_socket.v3.FilterStateInput" };
}

export const FilterStateInput: MessageFns<
  FilterStateInput,
  "envoy.extensions.matching.common_inputs.transport_socket.v3.FilterStateInput"
> = {
  $type: "envoy.extensions.matching.common_inputs.transport_socket.v3.FilterStateInput" as const,

  encode(message: FilterStateInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== undefined && message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FilterStateInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFilterStateInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FilterStateInput {
    return { $type: FilterStateInput.$type, key: isSet(object.key) ? globalThis.String(object.key) : undefined };
  },

  toJSON(message: FilterStateInput): unknown {
    const obj: any = {};
    if (message.key !== undefined) {
      obj.key = message.key;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FilterStateInput>, I>>(base?: I): FilterStateInput {
    return FilterStateInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FilterStateInput>, I>>(object: I): FilterStateInput {
    const message = createBaseFilterStateInput();
    message.key = object.key ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(FilterStateInput.$type, FilterStateInput);

function createBaseTransportSocketNameAction(): TransportSocketNameAction {
  return { $type: "envoy.extensions.matching.common_inputs.transport_socket.v3.TransportSocketNameAction" };
}

export const TransportSocketNameAction: MessageFns<
  TransportSocketNameAction,
  "envoy.extensions.matching.common_inputs.transport_socket.v3.TransportSocketNameAction"
> = {
  $type: "envoy.extensions.matching.common_inputs.transport_socket.v3.TransportSocketNameAction" as const,

  encode(message: TransportSocketNameAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TransportSocketNameAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTransportSocketNameAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TransportSocketNameAction {
    return {
      $type: TransportSocketNameAction.$type,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
    };
  },

  toJSON(message: TransportSocketNameAction): unknown {
    const obj: any = {};
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TransportSocketNameAction>, I>>(base?: I): TransportSocketNameAction {
    return TransportSocketNameAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TransportSocketNameAction>, I>>(object: I): TransportSocketNameAction {
    const message = createBaseTransportSocketNameAction();
    message.name = object.name ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(TransportSocketNameAction.$type, TransportSocketNameAction);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
