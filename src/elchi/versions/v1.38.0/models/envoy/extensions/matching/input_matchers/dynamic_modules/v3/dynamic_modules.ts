// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/matching/input_matchers/dynamic_modules/v3/dynamic_modules.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Any } from "../../../../../../google/protobuf/any";
import { messageTypeRegistry } from "../../../../../../typeRegistry";
import { DynamicModuleConfig } from "../../../../dynamic_modules/v3/dynamic_modules";

export const protobufPackage = "envoy.extensions.matching.input_matchers.dynamic_modules.v3";

/**
 * Configuration for the Dynamic Modules Input Matcher. This matcher allows loading shared object
 * files via ``dlopen`` to implement custom matching logic in dynamic modules (e.g. Rust, Go).
 *
 * A module can implement arbitrary matching logic by examining request headers and other HTTP
 * attributes during the match evaluation. This is useful for scenarios that require complex
 * matching beyond what built-in matchers provide, such as JWT/OAuth token analysis, custom
 * routing decisions, or integration with external data sources.
 */
export interface DynamicModuleMatcher {
  $type: "envoy.extensions.matching.input_matchers.dynamic_modules.v3.DynamicModuleMatcher";
  /** Specifies the shared-object level configuration. This field is required. */
  dynamic_module_config?:
    | DynamicModuleConfig
    | undefined;
  /**
   * The name for this matcher configuration. If not specified, defaults to an empty string.
   *
   * This can be used to distinguish between different matcher implementations inside a dynamic
   * module. For example, a module can have completely different matcher implementations (e.g.,
   * OAuth token matcher, geo-IP matcher). When Envoy receives this configuration, it passes
   * the ``matcher_name`` to the dynamic module's matcher config init function together with the
   * ``matcher_config``. That way a module can decide which in-module matcher implementation to
   * use based on the name at load time.
   */
  matcher_name?:
    | string
    | undefined;
  /**
   * The configuration for the matcher chosen by ``matcher_name``. If not specified, an empty
   * configuration is passed to the module.
   *
   * This is passed to the module's matcher initialization function. Together with the
   * ``matcher_name``, the module can decide which in-module matcher implementation to use and
   * fine-tune the behavior of the matcher.
   *
   * ``google.protobuf.Struct`` is serialized as JSON before passing it to the module.
   * ``google.protobuf.BytesValue`` and ``google.protobuf.StringValue`` are passed directly
   * without the wrapper.
   */
  matcher_config?: Any | undefined;
}

function createBaseDynamicModuleMatcher(): DynamicModuleMatcher {
  return { $type: "envoy.extensions.matching.input_matchers.dynamic_modules.v3.DynamicModuleMatcher" };
}

export const DynamicModuleMatcher: MessageFns<
  DynamicModuleMatcher,
  "envoy.extensions.matching.input_matchers.dynamic_modules.v3.DynamicModuleMatcher"
> = {
  $type: "envoy.extensions.matching.input_matchers.dynamic_modules.v3.DynamicModuleMatcher" as const,

  encode(message: DynamicModuleMatcher, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dynamic_module_config !== undefined) {
      DynamicModuleConfig.encode(message.dynamic_module_config, writer.uint32(10).fork()).join();
    }
    if (message.matcher_name !== undefined && message.matcher_name !== "") {
      writer.uint32(18).string(message.matcher_name);
    }
    if (message.matcher_config !== undefined) {
      Any.encode(message.matcher_config, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DynamicModuleMatcher {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDynamicModuleMatcher();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dynamic_module_config = DynamicModuleConfig.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.matcher_name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.matcher_config = Any.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DynamicModuleMatcher {
    return {
      $type: DynamicModuleMatcher.$type,
      dynamic_module_config: isSet(object.dynamic_module_config)
        ? DynamicModuleConfig.fromJSON(object.dynamic_module_config)
        : undefined,
      matcher_name: isSet(object.matcher_name) ? globalThis.String(object.matcher_name) : undefined,
      matcher_config: isSet(object.matcher_config) ? Any.fromJSON(object.matcher_config) : undefined,
    };
  },

  toJSON(message: DynamicModuleMatcher): unknown {
    const obj: any = {};
    if (message.dynamic_module_config !== undefined) {
      obj.dynamic_module_config = DynamicModuleConfig.toJSON(message.dynamic_module_config);
    }
    if (message.matcher_name !== undefined) {
      obj.matcher_name = message.matcher_name;
    }
    if (message.matcher_config !== undefined) {
      obj.matcher_config = Any.toJSON(message.matcher_config);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DynamicModuleMatcher>, I>>(base?: I): DynamicModuleMatcher {
    return DynamicModuleMatcher.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DynamicModuleMatcher>, I>>(object: I): DynamicModuleMatcher {
    const message = createBaseDynamicModuleMatcher();
    message.dynamic_module_config =
      (object.dynamic_module_config !== undefined && object.dynamic_module_config !== null)
        ? DynamicModuleConfig.fromPartial(object.dynamic_module_config)
        : undefined;
    message.matcher_name = object.matcher_name ?? undefined;
    message.matcher_config = (object.matcher_config !== undefined && object.matcher_config !== null)
      ? Any.fromPartial(object.matcher_config)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(DynamicModuleMatcher.$type, DynamicModuleMatcher);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
