// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/formatter/file_content/v3/file_content.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.formatter.file_content.v3";

/**
 * FileContent formatter extension implements the ``%FILE_CONTENT(/path/to/file)%`` command operator
 * that reads the contents of the specified file. File-based data is automatically re-read when the
 * file is modified on disk.
 *
 * Optionally, a directory to watch for changes can be specified with a
 * colon followed by the directory to watch, eg ``%FILE_CONTENT(/path/to/file:/path/to/watch)%``.
 * See :ref:`watched_directory <envoy_v3_api_field_config.core.v3.DataSource.watched_directory>` for
 * detailed semantics.
 */
export interface FileContent {
  $type: "envoy.extensions.formatter.file_content.v3.FileContent";
}

function createBaseFileContent(): FileContent {
  return { $type: "envoy.extensions.formatter.file_content.v3.FileContent" };
}

export const FileContent: MessageFns<FileContent, "envoy.extensions.formatter.file_content.v3.FileContent"> = {
  $type: "envoy.extensions.formatter.file_content.v3.FileContent" as const,

  encode(_: FileContent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FileContent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFileContent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): FileContent {
    return { $type: FileContent.$type };
  },

  toJSON(_: FileContent): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<FileContent>, I>>(base?: I): FileContent {
    return FileContent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FileContent>, I>>(_: I): FileContent {
    const message = createBaseFileContent();
    return message;
  },
};

messageTypeRegistry.set(FileContent.$type, FileContent);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
