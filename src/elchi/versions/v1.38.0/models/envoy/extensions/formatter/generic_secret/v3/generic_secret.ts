// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/formatter/generic_secret/v3/generic_secret.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../typeRegistry";
import { SdsSecretConfig } from "../../../transport_sockets/tls/v3/secret";

export const protobufPackage = "envoy.extensions.formatter.generic_secret.v3";

/**
 * GenericSecret formatter extension implements the ``%SECRET(name)%`` command operator that
 * resolves the value of a named generic secret obtained via SDS or static bootstrap configuration.
 *
 * The secret must be a :ref:`GenericSecret <envoy_v3_api_msg_extensions.transport_sockets.tls.v3.GenericSecret>`
 * with the ``secret`` field set.
 *
 * Example configuration adding an authorization header with a secret obtained via SDS:
 *
 * .. code-block:: yaml
 *
 *   http_uri:
 *     uri: https://api.example.com/v1/data
 *     cluster: api_backend
 *     timeout: 5s
 *   request_headers_to_add:
 *   - header:
 *       key: "authorization"
 *       value: "Bearer %SECRET(my-api-token)%"
 *   formatters:
 *   - name: envoy.formatter.generic_secret
 *     typed_config:
 *       "@type": type.googleapis.com/envoy.extensions.formatter.generic_secret.v3.GenericSecret
 *       secret_configs:
 *         my-api-token:
 *           name: bearer-token
 *           sds_config:
 *             ads: {}
 */
export interface GenericSecret {
  $type: "envoy.extensions.formatter.generic_secret.v3.GenericSecret";
  /**
   * Map from formatter lookup name to SDS secret configuration. The map key is the name used
   * in the ``%SECRET(name)%`` command operator.
   */
  secret_configs?: Map<string, SdsSecretConfig> | undefined;
}

export interface GenericSecret_SecretConfigsEntry {
  $type: "envoy.extensions.formatter.generic_secret.v3.GenericSecret.SecretConfigsEntry";
  key: string;
  value?: SdsSecretConfig | undefined;
}

function createBaseGenericSecret(): GenericSecret {
  return { $type: "envoy.extensions.formatter.generic_secret.v3.GenericSecret" };
}

export const GenericSecret: MessageFns<GenericSecret, "envoy.extensions.formatter.generic_secret.v3.GenericSecret"> = {
  $type: "envoy.extensions.formatter.generic_secret.v3.GenericSecret" as const,

  encode(message: GenericSecret, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    (message.secret_configs || new Map()).forEach((value, key) => {
      GenericSecret_SecretConfigsEntry.encode({
        $type: "envoy.extensions.formatter.generic_secret.v3.GenericSecret.SecretConfigsEntry",
        key: key as any,
        value,
      }, writer.uint32(10).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenericSecret {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenericSecret();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = GenericSecret_SecretConfigsEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            if (message.secret_configs === undefined) {
              message.secret_configs = new Map();
            }
            message.secret_configs!.set(entry1.key, entry1.value);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenericSecret {
    return {
      $type: GenericSecret.$type,
      secret_configs: isObject(object.secret_configs)
        ? Object.entries(object.secret_configs).reduce<Map<string, SdsSecretConfig>>((acc, [key, value]) => {
          acc.set(key, SdsSecretConfig.fromJSON(value));
          return acc;
        }, new Map())
        : undefined,
    };
  },

  toJSON(message: GenericSecret): unknown {
    const obj: any = {};
    if (message.secret_configs?.size) {
      obj.secret_configs = {};
      message.secret_configs.forEach((v, k) => {
        obj.secret_configs[k] = SdsSecretConfig.toJSON(v);
      });
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenericSecret>, I>>(base?: I): GenericSecret {
    return GenericSecret.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenericSecret>, I>>(object: I): GenericSecret {
    const message = createBaseGenericSecret();
    message.secret_configs = (object.secret_configs === undefined || object.secret_configs === null)
      ? undefined
      : (() => {
        const m = new Map();
        (object.secret_configs as Map<string, SdsSecretConfig> ?? new Map()).forEach((value, key) => {
          if (value !== undefined) {
            m.set(key, SdsSecretConfig.fromPartial(value));
          }
        });
        return m;
      })();
    return message;
  },
};

messageTypeRegistry.set(GenericSecret.$type, GenericSecret);

function createBaseGenericSecret_SecretConfigsEntry(): GenericSecret_SecretConfigsEntry {
  return { $type: "envoy.extensions.formatter.generic_secret.v3.GenericSecret.SecretConfigsEntry", key: "" };
}

export const GenericSecret_SecretConfigsEntry: MessageFns<
  GenericSecret_SecretConfigsEntry,
  "envoy.extensions.formatter.generic_secret.v3.GenericSecret.SecretConfigsEntry"
> = {
  $type: "envoy.extensions.formatter.generic_secret.v3.GenericSecret.SecretConfigsEntry" as const,

  encode(message: GenericSecret_SecretConfigsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      SdsSecretConfig.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenericSecret_SecretConfigsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenericSecret_SecretConfigsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = SdsSecretConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenericSecret_SecretConfigsEntry {
    return {
      $type: GenericSecret_SecretConfigsEntry.$type,
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? SdsSecretConfig.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: GenericSecret_SecretConfigsEntry): unknown {
    const obj: any = {};
    if (message.key !== undefined) {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = SdsSecretConfig.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenericSecret_SecretConfigsEntry>, I>>(
    base?: I,
  ): GenericSecret_SecretConfigsEntry {
    return GenericSecret_SecretConfigsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenericSecret_SecretConfigsEntry>, I>>(
    object: I,
  ): GenericSecret_SecretConfigsEntry {
    const message = createBaseGenericSecret_SecretConfigsEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? SdsSecretConfig.fromPartial(object.value)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(GenericSecret_SecretConfigsEntry.$type, GenericSecret_SecretConfigsEntry);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
