// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/filters/http/a2a/v3/a2a.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { UInt32Value } from "../../../../../../google/protobuf/wrappers";
import { messageTypeRegistry } from "../../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.filters.http.a2a.v3";

/**
 * This filter will inspect and get attributes from A2A traffic.
 * [#next-free-field: 6]
 */
export interface A2a {
  $type: "envoy.extensions.filters.http.a2a.v3.A2a";
  /** Configures how the filter handles non-A2A traffic. */
  traffic_mode?:
    | A2a_TrafficMode
    | undefined;
  /**
   * Maximum size of the request body to buffer for JSON-RPC validation.
   * If the request body exceeds this size, the request is rejected with ``413
   * Payload Too Large``. This limit applies to both ``REJECT`` and
   * ``PASS_THROUGH`` modes to prevent unbounded buffering.
   *
   * It defaults to 8KB (8192 bytes) and the maximum allowed value is 10MB
   * (10485760 bytes).
   *
   * Setting it to 0 would disable the limit. It is not recommended to do so in
   * production.
   */
  max_request_body_size?:
    | number
    | undefined;
  /** Customized configuration for A2A parser. */
  parser_config?:
    | ParserConfig
    | undefined;
  /**
   * Where to store parsed A2A message attributes.
   * Controls whether attributes are written to dynamic metadata, filter state, or both.
   * Default is no storage.
   */
  storage_mode?: A2a_StorageMode | undefined;
}

/** Traffic handling mode for non-A2A traffic. */
export enum A2a_TrafficMode {
  /** PASS_THROUGH - Proxies the HTTP request and response without A2A spec check. */
  PASS_THROUGH = "PASS_THROUGH",
  /** REJECT - Reject requests that are not following A2A spec. */
  REJECT = "REJECT",
}

export function a2a_TrafficModeFromJSON(object: any): A2a_TrafficMode {
  switch (object) {
    case 0:
    case "PASS_THROUGH":
      return A2a_TrafficMode.PASS_THROUGH;
    case 1:
    case "REJECT":
      return A2a_TrafficMode.REJECT;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum A2a_TrafficMode");
  }
}

export function a2a_TrafficModeToJSON(object: A2a_TrafficMode): string {
  switch (object) {
    case A2a_TrafficMode.PASS_THROUGH:
      return "PASS_THROUGH";
    case A2a_TrafficMode.REJECT:
      return "REJECT";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum A2a_TrafficMode");
  }
}

export function a2a_TrafficModeToNumber(object: A2a_TrafficMode): number {
  switch (object) {
    case A2a_TrafficMode.PASS_THROUGH:
      return 0;
    case A2a_TrafficMode.REJECT:
      return 1;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum A2a_TrafficMode");
  }
}

/** Where to store parsed A2A message attributes. */
export enum A2a_StorageMode {
  /** NONE - Default behavior: no storage. */
  NONE = "NONE",
  /** DYNAMIC_METADATA - Store message attributes in dynamic metadata only. */
  DYNAMIC_METADATA = "DYNAMIC_METADATA",
  /** FILTER_STATE - Store message attributes in filter state only. */
  FILTER_STATE = "FILTER_STATE",
  /** DYNAMIC_METADATA_AND_FILTER_STATE - Store message attributes in both dynamic metadata and filter state. */
  DYNAMIC_METADATA_AND_FILTER_STATE = "DYNAMIC_METADATA_AND_FILTER_STATE",
}

export function a2a_StorageModeFromJSON(object: any): A2a_StorageMode {
  switch (object) {
    case 0:
    case "NONE":
      return A2a_StorageMode.NONE;
    case 1:
    case "DYNAMIC_METADATA":
      return A2a_StorageMode.DYNAMIC_METADATA;
    case 2:
    case "FILTER_STATE":
      return A2a_StorageMode.FILTER_STATE;
    case 3:
    case "DYNAMIC_METADATA_AND_FILTER_STATE":
      return A2a_StorageMode.DYNAMIC_METADATA_AND_FILTER_STATE;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum A2a_StorageMode");
  }
}

export function a2a_StorageModeToJSON(object: A2a_StorageMode): string {
  switch (object) {
    case A2a_StorageMode.NONE:
      return "NONE";
    case A2a_StorageMode.DYNAMIC_METADATA:
      return "DYNAMIC_METADATA";
    case A2a_StorageMode.FILTER_STATE:
      return "FILTER_STATE";
    case A2a_StorageMode.DYNAMIC_METADATA_AND_FILTER_STATE:
      return "DYNAMIC_METADATA_AND_FILTER_STATE";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum A2a_StorageMode");
  }
}

export function a2a_StorageModeToNumber(object: A2a_StorageMode): number {
  switch (object) {
    case A2a_StorageMode.NONE:
      return 0;
    case A2a_StorageMode.DYNAMIC_METADATA:
      return 1;
    case A2a_StorageMode.FILTER_STATE:
      return 2;
    case A2a_StorageMode.DYNAMIC_METADATA_AND_FILTER_STATE:
      return 3;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum A2a_StorageMode");
  }
}

export interface MethodParsingConfig {
  $type: "envoy.extensions.filters.http.a2a.v3.MethodParsingConfig";
  /**
   * The group/category name to assign to this method (e.g., "tasks", "message").
   * If provided, this overrides any built-in classification for the method.
   * This will be emitted to dynamic metadata under the key specified by ``group_metadata_key``.
   */
  group?:
    | string
    | undefined;
  /** List of attributes to extract for this method, specified by their JSON paths (e.g., "params.name"). */
  paths?: string[] | undefined;
}

export interface ParserConfig {
  $type: "envoy.extensions.filters.http.a2a.v3.ParserConfig";
  /**
   * Method-specific overrides for grouping and attribute extraction.
   * If a method is not specified in method_configs, or if 'group' is empty in its config,
   * its group will be determined by built-in classification based on method prefix
   * (e.g., "message" for "message/send") and default extraction rules will be applied for that method.
   */
  method_configs?:
    | Map<string, MethodParsingConfig>
    | undefined;
  /**
   * Attributes that should always be extracted regardless of the method.
   * Specified by their JSON paths (e.g., "params.id").
   */
  always_extract_attributes?:
    | string[]
    | undefined;
  /**
   * The dynamic metadata key under which the method's group name will be stored
   * (e.g., "a2a_group"). If this key is empty, group information will not be
   * added to dynamic metadata.
   */
  group_metadata_key?: string | undefined;
}

export interface ParserConfig_MethodConfigsEntry {
  $type: "envoy.extensions.filters.http.a2a.v3.ParserConfig.MethodConfigsEntry";
  key: string;
  value?: MethodParsingConfig | undefined;
}

function createBaseA2a(): A2a {
  return { $type: "envoy.extensions.filters.http.a2a.v3.A2a" };
}

export const A2a: MessageFns<A2a, "envoy.extensions.filters.http.a2a.v3.A2a"> = {
  $type: "envoy.extensions.filters.http.a2a.v3.A2a" as const,

  encode(message: A2a, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.traffic_mode !== undefined && message.traffic_mode !== A2a_TrafficMode.PASS_THROUGH) {
      writer.uint32(8).int32(a2a_TrafficModeToNumber(message.traffic_mode));
    }
    if (message.max_request_body_size !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.max_request_body_size! },
        writer.uint32(26).fork(),
      ).join();
    }
    if (message.parser_config !== undefined) {
      ParserConfig.encode(message.parser_config, writer.uint32(34).fork()).join();
    }
    if (message.storage_mode !== undefined && message.storage_mode !== A2a_StorageMode.NONE) {
      writer.uint32(40).int32(a2a_StorageModeToNumber(message.storage_mode));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): A2a {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseA2a();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.traffic_mode = a2a_TrafficModeFromJSON(reader.int32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.max_request_body_size = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.parser_config = ParserConfig.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.storage_mode = a2a_StorageModeFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): A2a {
    return {
      $type: A2a.$type,
      traffic_mode: isSet(object.traffic_mode) ? a2a_TrafficModeFromJSON(object.traffic_mode) : undefined,
      max_request_body_size: isSet(object.max_request_body_size) ? Number(object.max_request_body_size) : undefined,
      parser_config: isSet(object.parser_config) ? ParserConfig.fromJSON(object.parser_config) : undefined,
      storage_mode: isSet(object.storage_mode) ? a2a_StorageModeFromJSON(object.storage_mode) : undefined,
    };
  },

  toJSON(message: A2a): unknown {
    const obj: any = {};
    if (message.traffic_mode !== undefined) {
      obj.traffic_mode = a2a_TrafficModeToJSON(message.traffic_mode);
    }
    if (message.max_request_body_size !== undefined) {
      obj.max_request_body_size = message.max_request_body_size;
    }
    if (message.parser_config !== undefined) {
      obj.parser_config = ParserConfig.toJSON(message.parser_config);
    }
    if (message.storage_mode !== undefined) {
      obj.storage_mode = a2a_StorageModeToJSON(message.storage_mode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<A2a>, I>>(base?: I): A2a {
    return A2a.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<A2a>, I>>(object: I): A2a {
    const message = createBaseA2a();
    message.traffic_mode = object.traffic_mode ?? undefined;
    message.max_request_body_size = object.max_request_body_size ?? undefined;
    message.parser_config = (object.parser_config !== undefined && object.parser_config !== null)
      ? ParserConfig.fromPartial(object.parser_config)
      : undefined;
    message.storage_mode = object.storage_mode ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(A2a.$type, A2a);

function createBaseMethodParsingConfig(): MethodParsingConfig {
  return { $type: "envoy.extensions.filters.http.a2a.v3.MethodParsingConfig" };
}

export const MethodParsingConfig: MessageFns<
  MethodParsingConfig,
  "envoy.extensions.filters.http.a2a.v3.MethodParsingConfig"
> = {
  $type: "envoy.extensions.filters.http.a2a.v3.MethodParsingConfig" as const,

  encode(message: MethodParsingConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.group !== undefined && message.group !== "") {
      writer.uint32(10).string(message.group);
    }
    if (message.paths !== undefined && message.paths.length !== 0) {
      for (const v of message.paths) {
        writer.uint32(18).string(v!);
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MethodParsingConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMethodParsingConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.group = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          if (message.paths === undefined) {
            message.paths = [];
          }
          const el = reader.string();
          if (el !== undefined) {
            message.paths!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MethodParsingConfig {
    return {
      $type: MethodParsingConfig.$type,
      group: isSet(object.group) ? globalThis.String(object.group) : undefined,
      paths: globalThis.Array.isArray(object?.paths) ? object.paths.map((e: any) => globalThis.String(e)) : undefined,
    };
  },

  toJSON(message: MethodParsingConfig): unknown {
    const obj: any = {};
    if (message.group !== undefined) {
      obj.group = message.group;
    }
    if (message.paths?.length) {
      obj.paths = message.paths;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MethodParsingConfig>, I>>(base?: I): MethodParsingConfig {
    return MethodParsingConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MethodParsingConfig>, I>>(object: I): MethodParsingConfig {
    const message = createBaseMethodParsingConfig();
    message.group = object.group ?? undefined;
    message.paths = object.paths?.map((e) => e) || undefined;
    return message;
  },
};

messageTypeRegistry.set(MethodParsingConfig.$type, MethodParsingConfig);

function createBaseParserConfig(): ParserConfig {
  return { $type: "envoy.extensions.filters.http.a2a.v3.ParserConfig" };
}

export const ParserConfig: MessageFns<ParserConfig, "envoy.extensions.filters.http.a2a.v3.ParserConfig"> = {
  $type: "envoy.extensions.filters.http.a2a.v3.ParserConfig" as const,

  encode(message: ParserConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    (message.method_configs || new Map()).forEach((value, key) => {
      ParserConfig_MethodConfigsEntry.encode({
        $type: "envoy.extensions.filters.http.a2a.v3.ParserConfig.MethodConfigsEntry",
        key: key as any,
        value,
      }, writer.uint32(10).fork()).join();
    });
    if (message.always_extract_attributes !== undefined && message.always_extract_attributes.length !== 0) {
      for (const v of message.always_extract_attributes) {
        writer.uint32(18).string(v!);
      }
    }
    if (message.group_metadata_key !== undefined && message.group_metadata_key !== "") {
      writer.uint32(26).string(message.group_metadata_key);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ParserConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParserConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = ParserConfig_MethodConfigsEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            if (message.method_configs === undefined) {
              message.method_configs = new Map();
            }
            message.method_configs!.set(entry1.key, entry1.value);
          }
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          if (message.always_extract_attributes === undefined) {
            message.always_extract_attributes = [];
          }
          const el = reader.string();
          if (el !== undefined) {
            message.always_extract_attributes!.push(el);
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.group_metadata_key = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParserConfig {
    return {
      $type: ParserConfig.$type,
      method_configs: isObject(object.method_configs)
        ? Object.entries(object.method_configs).reduce<Map<string, MethodParsingConfig>>((acc, [key, value]) => {
          acc.set(key, MethodParsingConfig.fromJSON(value));
          return acc;
        }, new Map())
        : undefined,
      always_extract_attributes: globalThis.Array.isArray(object?.always_extract_attributes)
        ? object.always_extract_attributes.map((e: any) => globalThis.String(e))
        : undefined,
      group_metadata_key: isSet(object.group_metadata_key) ? globalThis.String(object.group_metadata_key) : undefined,
    };
  },

  toJSON(message: ParserConfig): unknown {
    const obj: any = {};
    if (message.method_configs?.size) {
      obj.method_configs = {};
      message.method_configs.forEach((v, k) => {
        obj.method_configs[k] = MethodParsingConfig.toJSON(v);
      });
    }
    if (message.always_extract_attributes?.length) {
      obj.always_extract_attributes = message.always_extract_attributes;
    }
    if (message.group_metadata_key !== undefined) {
      obj.group_metadata_key = message.group_metadata_key;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ParserConfig>, I>>(base?: I): ParserConfig {
    return ParserConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ParserConfig>, I>>(object: I): ParserConfig {
    const message = createBaseParserConfig();
    message.method_configs = (object.method_configs === undefined || object.method_configs === null)
      ? undefined
      : (() => {
        const m = new Map();
        (object.method_configs as Map<string, MethodParsingConfig> ?? new Map()).forEach((value, key) => {
          if (value !== undefined) {
            m.set(key, MethodParsingConfig.fromPartial(value));
          }
        });
        return m;
      })();
    message.always_extract_attributes = object.always_extract_attributes?.map((e) => e) || undefined;
    message.group_metadata_key = object.group_metadata_key ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(ParserConfig.$type, ParserConfig);

function createBaseParserConfig_MethodConfigsEntry(): ParserConfig_MethodConfigsEntry {
  return { $type: "envoy.extensions.filters.http.a2a.v3.ParserConfig.MethodConfigsEntry", key: "" };
}

export const ParserConfig_MethodConfigsEntry: MessageFns<
  ParserConfig_MethodConfigsEntry,
  "envoy.extensions.filters.http.a2a.v3.ParserConfig.MethodConfigsEntry"
> = {
  $type: "envoy.extensions.filters.http.a2a.v3.ParserConfig.MethodConfigsEntry" as const,

  encode(message: ParserConfig_MethodConfigsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      MethodParsingConfig.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ParserConfig_MethodConfigsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParserConfig_MethodConfigsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = MethodParsingConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParserConfig_MethodConfigsEntry {
    return {
      $type: ParserConfig_MethodConfigsEntry.$type,
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? MethodParsingConfig.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: ParserConfig_MethodConfigsEntry): unknown {
    const obj: any = {};
    if (message.key !== undefined) {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = MethodParsingConfig.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ParserConfig_MethodConfigsEntry>, I>>(base?: I): ParserConfig_MethodConfigsEntry {
    return ParserConfig_MethodConfigsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ParserConfig_MethodConfigsEntry>, I>>(
    object: I,
  ): ParserConfig_MethodConfigsEntry {
    const message = createBaseParserConfig_MethodConfigsEntry();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? MethodParsingConfig.fromPartial(object.value)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(ParserConfig_MethodConfigsEntry.$type, ParserConfig_MethodConfigsEntry);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
