// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/filters/listener/dynamic_modules/v3/dynamic_modules.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Any } from "../../../../../../google/protobuf/any";
import { messageTypeRegistry } from "../../../../../../typeRegistry";
import { DynamicModuleConfig } from "../../../../dynamic_modules/v3/dynamic_modules";

export const protobufPackage = "envoy.extensions.filters.listener.dynamic_modules.v3";

/**
 * Configuration of the listener filter for dynamic modules. This filter allows loading shared object
 * files that can be loaded via dlopen by the listener filter.
 *
 * A module can be loaded by multiple listener filters, hence the program can be structured in a way
 * that the module is loaded only once and shared across multiple filters providing multiple
 * functionalities.
 *
 * Unlike network filters which operate on established TCP connections, listener filters
 * work with raw accepted sockets BEFORE a Connection object is created. The filter can:
 *
 * * Inspect initial bytes to detect protocols (TLS, HTTP, PostgreSQL, etc.).
 * * Set socket properties (SNI, ALPN, transport protocol, fingerprints).
 * * Modify connection addresses (original destination restoration).
 * * Set dynamic metadata and filter state for downstream filters.
 * * Rate limit incoming connections.
 */
export interface DynamicModuleListenerFilter {
  $type: "envoy.extensions.filters.listener.dynamic_modules.v3.DynamicModuleListenerFilter";
  /** Specifies the shared-object level configuration. */
  dynamic_module_config?:
    | DynamicModuleConfig
    | undefined;
  /**
   * The name for this filter configuration. This can be used to distinguish between different
   * filter implementations inside a dynamic module. For example, a module can have completely
   * different filter implementations (TLS inspector, rate limiter, proxy protocol parser).
   * When Envoy receives this configuration, it passes the ``filter_name`` to the dynamic module's
   * listener filter config init function together with the ``filter_config``. That way a module
   * can decide which in-module filter implementation to use based on the name at load time.
   */
  filter_name?:
    | string
    | undefined;
  /**
   * The configuration for the filter chosen by ``filter_name``. This is passed to the module's
   * listener filter initialization function. Together with the ``filter_name``, the module can
   * decide which in-module filter implementation to use and fine-tune the behavior of the filter.
   *
   * For example, if a module has two filter implementations, one for TLS inspection and one for
   * rate limiting, ``filter_name`` is used to choose either TLS or rate limiting. The ``filter_config``
   * can be used to configure the TLS inspection options or the rate limiting parameters.
   *
   * ``google.protobuf.Struct`` is serialized as JSON before passing it to the module.
   * ``google.protobuf.BytesValue`` and ``google.protobuf.StringValue`` are passed directly
   * without the wrapper.
   *
   * .. code-block:: yaml
   *
   *  # Passing a string value
   *  filter_config:
   *    "@type": "type.googleapis.com/google.protobuf.StringValue"
   *    value: hello
   *
   *  # Passing raw bytes
   *  filter_config:
   *    "@type": "type.googleapis.com/google.protobuf.BytesValue"
   *    value: aGVsbG8=  # echo -n "hello" | base64
   */
  filter_config?: Any | undefined;
}

function createBaseDynamicModuleListenerFilter(): DynamicModuleListenerFilter {
  return { $type: "envoy.extensions.filters.listener.dynamic_modules.v3.DynamicModuleListenerFilter" };
}

export const DynamicModuleListenerFilter: MessageFns<
  DynamicModuleListenerFilter,
  "envoy.extensions.filters.listener.dynamic_modules.v3.DynamicModuleListenerFilter"
> = {
  $type: "envoy.extensions.filters.listener.dynamic_modules.v3.DynamicModuleListenerFilter" as const,

  encode(message: DynamicModuleListenerFilter, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dynamic_module_config !== undefined) {
      DynamicModuleConfig.encode(message.dynamic_module_config, writer.uint32(10).fork()).join();
    }
    if (message.filter_name !== undefined && message.filter_name !== "") {
      writer.uint32(18).string(message.filter_name);
    }
    if (message.filter_config !== undefined) {
      Any.encode(message.filter_config, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DynamicModuleListenerFilter {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDynamicModuleListenerFilter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dynamic_module_config = DynamicModuleConfig.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.filter_name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.filter_config = Any.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DynamicModuleListenerFilter {
    return {
      $type: DynamicModuleListenerFilter.$type,
      dynamic_module_config: isSet(object.dynamic_module_config)
        ? DynamicModuleConfig.fromJSON(object.dynamic_module_config)
        : undefined,
      filter_name: isSet(object.filter_name) ? globalThis.String(object.filter_name) : undefined,
      filter_config: isSet(object.filter_config) ? Any.fromJSON(object.filter_config) : undefined,
    };
  },

  toJSON(message: DynamicModuleListenerFilter): unknown {
    const obj: any = {};
    if (message.dynamic_module_config !== undefined) {
      obj.dynamic_module_config = DynamicModuleConfig.toJSON(message.dynamic_module_config);
    }
    if (message.filter_name !== undefined) {
      obj.filter_name = message.filter_name;
    }
    if (message.filter_config !== undefined) {
      obj.filter_config = Any.toJSON(message.filter_config);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DynamicModuleListenerFilter>, I>>(base?: I): DynamicModuleListenerFilter {
    return DynamicModuleListenerFilter.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DynamicModuleListenerFilter>, I>>(object: I): DynamicModuleListenerFilter {
    const message = createBaseDynamicModuleListenerFilter();
    message.dynamic_module_config =
      (object.dynamic_module_config !== undefined && object.dynamic_module_config !== null)
        ? DynamicModuleConfig.fromPartial(object.dynamic_module_config)
        : undefined;
    message.filter_name = object.filter_name ?? undefined;
    message.filter_config = (object.filter_config !== undefined && object.filter_config !== null)
      ? Any.fromPartial(object.filter_config)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(DynamicModuleListenerFilter.$type, DynamicModuleListenerFilter);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
