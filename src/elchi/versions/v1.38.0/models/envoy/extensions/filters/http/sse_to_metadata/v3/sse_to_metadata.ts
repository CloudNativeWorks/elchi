// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/filters/http/sse_to_metadata/v3/sse_to_metadata.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { UInt32Value } from "../../../../../../google/protobuf/wrappers";
import { messageTypeRegistry } from "../../../../../../typeRegistry";
import { TypedExtensionConfig } from "../../../../../config/core/v3/extension";

export const protobufPackage = "envoy.extensions.filters.http.sse_to_metadata.v3";

export interface SseToMetadata {
  $type: "envoy.extensions.filters.http.sse_to_metadata.v3.SseToMetadata";
  /** Rules for processing SSE response streams. */
  response_rules?: SseToMetadata_ProcessingRules | undefined;
}

/**
 * Rules for processing SSE streams and extracting metadata.
 *
 * The filter parses the SSE protocol (events delimited by blank lines), then delegates
 * to a content parser to parse event content and extract metadata. The content parser
 * determines which values to extract and how to write them to metadata.
 */
export interface SseToMetadata_ProcessingRules {
  $type: "envoy.extensions.filters.http.sse_to_metadata.v3.SseToMetadata.ProcessingRules";
  /**
   * Content parser configuration for parsing event content and extracting metadata.
   *
   * The content parser specifies:
   * - How to parse the event data (e.g., JSON, XML, plaintext)
   * - Which values to extract from the parsed content (e.g., JSON paths like usage.total_tokens)
   * - How to map extracted values to metadata (namespace, key, type conversions)
   * - When to write metadata (on_present, on_missing, on_error actions)
   * [#extension-category: envoy.content_parsers]
   */
  content_parser?:
    | TypedExtensionConfig
    | undefined;
  /**
   * Maximum size in bytes for a single SSE event before it's considered invalid
   * and discarded. This protects against unbounded memory growth from malicious
   * or malformed streams that never send event delimiters (blank lines).
   *
   * Default is 8192 bytes (8KB), which is sufficient for most legitimate events.
   * Set to 0 to disable the limit (not recommended for production).
   * Maximum allowed value is 10485760 bytes (10MB).
   */
  max_event_size?: number | undefined;
}

function createBaseSseToMetadata(): SseToMetadata {
  return { $type: "envoy.extensions.filters.http.sse_to_metadata.v3.SseToMetadata" };
}

export const SseToMetadata: MessageFns<
  SseToMetadata,
  "envoy.extensions.filters.http.sse_to_metadata.v3.SseToMetadata"
> = {
  $type: "envoy.extensions.filters.http.sse_to_metadata.v3.SseToMetadata" as const,

  encode(message: SseToMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.response_rules !== undefined) {
      SseToMetadata_ProcessingRules.encode(message.response_rules, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SseToMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSseToMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.response_rules = SseToMetadata_ProcessingRules.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SseToMetadata {
    return {
      $type: SseToMetadata.$type,
      response_rules: isSet(object.response_rules)
        ? SseToMetadata_ProcessingRules.fromJSON(object.response_rules)
        : undefined,
    };
  },

  toJSON(message: SseToMetadata): unknown {
    const obj: any = {};
    if (message.response_rules !== undefined) {
      obj.response_rules = SseToMetadata_ProcessingRules.toJSON(message.response_rules);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SseToMetadata>, I>>(base?: I): SseToMetadata {
    return SseToMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SseToMetadata>, I>>(object: I): SseToMetadata {
    const message = createBaseSseToMetadata();
    message.response_rules = (object.response_rules !== undefined && object.response_rules !== null)
      ? SseToMetadata_ProcessingRules.fromPartial(object.response_rules)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(SseToMetadata.$type, SseToMetadata);

function createBaseSseToMetadata_ProcessingRules(): SseToMetadata_ProcessingRules {
  return { $type: "envoy.extensions.filters.http.sse_to_metadata.v3.SseToMetadata.ProcessingRules" };
}

export const SseToMetadata_ProcessingRules: MessageFns<
  SseToMetadata_ProcessingRules,
  "envoy.extensions.filters.http.sse_to_metadata.v3.SseToMetadata.ProcessingRules"
> = {
  $type: "envoy.extensions.filters.http.sse_to_metadata.v3.SseToMetadata.ProcessingRules" as const,

  encode(message: SseToMetadata_ProcessingRules, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.content_parser !== undefined) {
      TypedExtensionConfig.encode(message.content_parser, writer.uint32(10).fork()).join();
    }
    if (message.max_event_size !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.max_event_size! },
        writer.uint32(18).fork(),
      ).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SseToMetadata_ProcessingRules {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSseToMetadata_ProcessingRules();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.content_parser = TypedExtensionConfig.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.max_event_size = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SseToMetadata_ProcessingRules {
    return {
      $type: SseToMetadata_ProcessingRules.$type,
      content_parser: isSet(object.content_parser) ? TypedExtensionConfig.fromJSON(object.content_parser) : undefined,
      max_event_size: isSet(object.max_event_size) ? Number(object.max_event_size) : undefined,
    };
  },

  toJSON(message: SseToMetadata_ProcessingRules): unknown {
    const obj: any = {};
    if (message.content_parser !== undefined) {
      obj.content_parser = TypedExtensionConfig.toJSON(message.content_parser);
    }
    if (message.max_event_size !== undefined) {
      obj.max_event_size = message.max_event_size;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SseToMetadata_ProcessingRules>, I>>(base?: I): SseToMetadata_ProcessingRules {
    return SseToMetadata_ProcessingRules.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SseToMetadata_ProcessingRules>, I>>(
    object: I,
  ): SseToMetadata_ProcessingRules {
    const message = createBaseSseToMetadata_ProcessingRules();
    message.content_parser = (object.content_parser !== undefined && object.content_parser !== null)
      ? TypedExtensionConfig.fromPartial(object.content_parser)
      : undefined;
    message.max_event_size = object.max_event_size ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(SseToMetadata_ProcessingRules.$type, SseToMetadata_ProcessingRules);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
