// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/filters/http/mcp_json_rest_bridge/v3/mcp_json_rest_bridge.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { StringValue } from "../../../../../../google/protobuf/wrappers";
import { messageTypeRegistry } from "../../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.filters.http.mcp_json_rest_bridge.v3";

/**
 * Configuration for the MCP MCP JSON REST Bridge.
 *
 * This extension translates Model Context Protocol (MCP) JSON-RPC requests into standard JSON-REST
 * HTTP requests. This enables existing REST backends to function as MCP servers without native MCP
 * support.
 *
 * Main functionalities:
 *
 * 1. Transcoding: Converts JSON-RPC request payload to HTTP REST request, and maps JSON response
 *    back to JSON-RPC.
 * 2. Session negotiation: Handles MCP connection prerequisites.
 *
 * The core logic transforms "tools/call" request into HTTP request following the ``HttpRule``
 * specification.
 *
 * Example 1: GET request with path and query parameters
 *
 * .. code-block:: text
 *
 *   tools: {
 *     name: "getResource"
 *     http_rule: {
 *       get: "/v1/projects/{project_id}/resources/{resource_id}"
 *       // body is omitted for GET
 *     }
 *   }
 *   If tools/call params are:
 *   { "name": "getResource", "arguments": {"project_id": "foo", "resource_id": "res-789", "view": "FULL"} }
 *   Translation:
 *   - Method: GET
 *   - URL: /v1/projects/foo/resources/res-789?view=FULL
 *     (Arguments not matching path templates become query parameters.)
 *
 * Example 2: POST request with wildcard body
 *
 * .. code-block:: text
 *
 *   tools: {
 *     name: "createResource"
 *     http_rule: {
 *       post: "/v1/projects/{project_id}/resources"
 *       body: "*"
 *     }
 *   }
 *   If tools/call params are:
 *   { "name": "createResource", "arguments": {"project_id": "foo", "resource_id": "res-456", "payload": { "data": "some value" }} }
 *   Translation:
 *   - Method: POST
 *   - URL: /v1/projects/foo/resources
 *   - Body: {"resource_id": "res-456", "payload": { "data": "some value" }}
 *     (Arguments not used in the path form the body, as per body: "*".)
 *
 * Example 3: PUT request with a specific field as body
 *
 * .. code-block:: text
 *
 *   tools: {
 *     name: "updateResource"
 *     http_rule: {
 *       put: "/v1/projects/{project_id}"
 *       body: "payload"
 *     }
 *   }
 *   If tools/call params are:
 *   { "name": "updateResource", "arguments": {"project_id": "foo", "resource_id": "res-456", "payload": { "data": "updated value" }} }
 *   Translation:
 *   - Method: PUT
 *   - URL: /v1/projects/foo?resource_id=res-456
 *   - Body: {"data": "updated value"}
 *     (Only the "payload" field from arguments is used as the body. Other arguments not in the
 *     path, like 'resource_id', become query parameters.)
 */
export interface McpJsonRestBridge {
  $type: "envoy.extensions.filters.http.mcp_json_rest_bridge.v3.McpJsonRestBridge";
  /** General server information. */
  server_info?:
    | ServerInfo
    | undefined;
  /** Configuration for the MCP tools. */
  tool_config?: ServerToolConfig | undefined;
}

/** Configuration for the server metadata. */
export interface ServerInfo {
  $type: "envoy.extensions.filters.http.mcp_json_rest_bridge.v3.ServerInfo";
  /**
   * [#not-implemented-hide:]
   * [#comment:TODO(guoyilin42): Implement supported_protocol_versions]
   * Lists the MCP protocol versions supported by this MCP endpoint.
   *
   * - If provided: The extension enforces version negotiation according to the MCP specification:
   *   https://modelcontextprotocol.io/specification/2025-11-25/basic/lifecycle#version-negotiation
   * - If not provided: The extension accepts any version sent by the client during negotiation and
   *   skips validation of the mcp-protocol-version header on subsequent requests.
   *
   * Example values: ["2025-11-25", "2025-06-18"]
   */
  supported_protocol_versions?:
    | string[]
    | undefined;
  /**
   * [#not-implemented-hide:]
   * [#comment:TODO(guoyilin42): Implement description]
   * Optional description of the server.
   */
  description?:
    | string
    | undefined;
  /**
   * The fallback protocol version to use if the client does not provide the ``mcp-protocol-version`` header.
   *
   * - If provided: The extension uses this version as the fallback protocol version.
   * - If not provided: The extension uses the fallback protocol version defined in the latest MCP
   *   specification. For example, the current latest 2025-11-25 specification designates "2025-03-26"
   *   as the fallback protocol version.
   *   See https://modelcontextprotocol.io/specification/2025-11-25/basic/transports#protocol-version-header
   */
  fallback_protocol_version?: string | undefined;
}

/** Configuration for the MCP tool capability of the server. */
export interface ServerToolConfig {
  $type: "envoy.extensions.filters.http.mcp_json_rest_bridge.v3.ServerToolConfig";
  /** List of MCP tools configurations. */
  tools?:
    | ToolConfig[]
    | undefined;
  /**
   * [#not-implemented-hide:]
   * [#comment:TODO(guoyilin42): Implement list_changed]
   * Whether this server supports notifications for changes to the tool list.
   */
  list_changed?:
    | boolean
    | undefined;
  /**
   * Optional configuration to transcode the tools/list requests to a standard HTTP request.
   *
   * Note: tools/list should be mapped to a GET request with an empty body.
   *
   * - If provided: The extension transcodes the request and forwards it down the filter chain.
   *   The response (whether from an upstream backend, a configured ``direct_response``, or another
   *   extension) MUST be a JSON body strictly matching the MCP ``ListToolsResult`` schema.
   *   Ref: https://modelcontextprotocol.io/specification/2025-11-25/schema#listtoolsresult
   * - If not provided: The ``tools/list`` request is passed through. This allows subsequent
   *   extension or the backend itself to handle the tools/list request if they support it.
   */
  tool_list_http_rule?: HttpRule | undefined;
}

/** Configuration for a specific MCP tool. */
export interface ToolConfig {
  $type: "envoy.extensions.filters.http.mcp_json_rest_bridge.v3.ToolConfig";
  /** Name of the tool. */
  name?:
    | string
    | undefined;
  /** The HTTP configuration rules that apply to the normal backend. */
  http_rule?: HttpRule | undefined;
}

/**
 * Defines the schema of the JSON-RPC to REST mapping. It specifies how the "arguments"
 * in a tools/call request are mapped to the URL path, query parameters, and HTTP request body.
 *
 * Mapping Rules:
 *
 * 1. Path: Fields defined in the path template (e.g., ``/v1/resources/{id}``) are extracted from
 *    arguments and placed in the URL.
 * 2. Body: Determined by the ``body`` field.
 *    - If "*": All arguments not used in the path become the HTTP JSON body.
 *    - If specify a field: Only that specific argument becomes the HTTP JSON body.
 *    - If empty: No body is sent.
 * 3. Query: Any leaf arguments not mapped to Path or Body are added as URL query parameters.
 * [#next-free-field: 7]
 */
export interface HttpRule {
  $type: "envoy.extensions.filters.http.mcp_json_rest_bridge.v3.HttpRule";
  /**
   * Determines the HTTP method and the URL path template.
   *
   * Path templating uses curly braces ``{}`` to mark a section of the URL path as replaceable.
   * Each template variable MUST correspond to a field in the JSON-RPC "arguments".
   * Use dot-notation to access fields within nested objects (e.g., "user.id" maps the value of the
   * "id" field inside "user").
   *
   * To support backward compatibility with future methods, these are defined as individual fields
   * rather than a "oneof". If multiple fields are present, the one with the highest field number
   * highest priority) is the effective method.
   *
   * Maps to HTTP GET.
   */
  get?:
    | string
    | undefined;
  /** Maps to HTTP PUT. */
  put?:
    | string
    | undefined;
  /** Maps to HTTP POST. */
  post?:
    | string
    | undefined;
  /** Maps to HTTP DELETE. */
  delete?:
    | string
    | undefined;
  /** Maps to HTTP PATCH. */
  patch?:
    | string
    | undefined;
  /**
   * The name of the request field whose value is mapped to the HTTP request body.
   *
   * - If "*": All fields not bound by the path template are mapped to the request body.
   * - If specify a field: This specific field is mapped to the body. Uses dot-notation for nested
   *   fields (e.g., "user.data" maps the value of the "data" field inside "user").
   * - If omitted: There is no HTTP request body; fields not in the path become query parameters.
   */
  body?: string | undefined;
}

function createBaseMcpJsonRestBridge(): McpJsonRestBridge {
  return { $type: "envoy.extensions.filters.http.mcp_json_rest_bridge.v3.McpJsonRestBridge" };
}

export const McpJsonRestBridge: MessageFns<
  McpJsonRestBridge,
  "envoy.extensions.filters.http.mcp_json_rest_bridge.v3.McpJsonRestBridge"
> = {
  $type: "envoy.extensions.filters.http.mcp_json_rest_bridge.v3.McpJsonRestBridge" as const,

  encode(message: McpJsonRestBridge, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.server_info !== undefined) {
      ServerInfo.encode(message.server_info, writer.uint32(10).fork()).join();
    }
    if (message.tool_config !== undefined) {
      ServerToolConfig.encode(message.tool_config, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): McpJsonRestBridge {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMcpJsonRestBridge();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.server_info = ServerInfo.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tool_config = ServerToolConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): McpJsonRestBridge {
    return {
      $type: McpJsonRestBridge.$type,
      server_info: isSet(object.server_info) ? ServerInfo.fromJSON(object.server_info) : undefined,
      tool_config: isSet(object.tool_config) ? ServerToolConfig.fromJSON(object.tool_config) : undefined,
    };
  },

  toJSON(message: McpJsonRestBridge): unknown {
    const obj: any = {};
    if (message.server_info !== undefined) {
      obj.server_info = ServerInfo.toJSON(message.server_info);
    }
    if (message.tool_config !== undefined) {
      obj.tool_config = ServerToolConfig.toJSON(message.tool_config);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<McpJsonRestBridge>, I>>(base?: I): McpJsonRestBridge {
    return McpJsonRestBridge.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<McpJsonRestBridge>, I>>(object: I): McpJsonRestBridge {
    const message = createBaseMcpJsonRestBridge();
    message.server_info = (object.server_info !== undefined && object.server_info !== null)
      ? ServerInfo.fromPartial(object.server_info)
      : undefined;
    message.tool_config = (object.tool_config !== undefined && object.tool_config !== null)
      ? ServerToolConfig.fromPartial(object.tool_config)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(McpJsonRestBridge.$type, McpJsonRestBridge);

function createBaseServerInfo(): ServerInfo {
  return { $type: "envoy.extensions.filters.http.mcp_json_rest_bridge.v3.ServerInfo" };
}

export const ServerInfo: MessageFns<ServerInfo, "envoy.extensions.filters.http.mcp_json_rest_bridge.v3.ServerInfo"> = {
  $type: "envoy.extensions.filters.http.mcp_json_rest_bridge.v3.ServerInfo" as const,

  encode(message: ServerInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.supported_protocol_versions !== undefined && message.supported_protocol_versions.length !== 0) {
      for (const v of message.supported_protocol_versions) {
        writer.uint32(10).string(v!);
      }
    }
    if (message.description !== undefined && message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.fallback_protocol_version !== undefined) {
      StringValue.encode(
        { $type: "google.protobuf.StringValue", value: message.fallback_protocol_version! },
        writer.uint32(26).fork(),
      ).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          if (message.supported_protocol_versions === undefined) {
            message.supported_protocol_versions = [];
          }
          const el = reader.string();
          if (el !== undefined) {
            message.supported_protocol_versions!.push(el);
          }
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fallback_protocol_version = StringValue.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerInfo {
    return {
      $type: ServerInfo.$type,
      supported_protocol_versions: globalThis.Array.isArray(object?.supported_protocol_versions)
        ? object.supported_protocol_versions.map((e: any) => globalThis.String(e))
        : undefined,
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
      fallback_protocol_version: isSet(object.fallback_protocol_version)
        ? String(object.fallback_protocol_version)
        : undefined,
    };
  },

  toJSON(message: ServerInfo): unknown {
    const obj: any = {};
    if (message.supported_protocol_versions?.length) {
      obj.supported_protocol_versions = message.supported_protocol_versions;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.fallback_protocol_version !== undefined) {
      obj.fallback_protocol_version = message.fallback_protocol_version;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerInfo>, I>>(base?: I): ServerInfo {
    return ServerInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerInfo>, I>>(object: I): ServerInfo {
    const message = createBaseServerInfo();
    message.supported_protocol_versions = object.supported_protocol_versions?.map((e) => e) || undefined;
    message.description = object.description ?? undefined;
    message.fallback_protocol_version = object.fallback_protocol_version ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(ServerInfo.$type, ServerInfo);

function createBaseServerToolConfig(): ServerToolConfig {
  return { $type: "envoy.extensions.filters.http.mcp_json_rest_bridge.v3.ServerToolConfig" };
}

export const ServerToolConfig: MessageFns<
  ServerToolConfig,
  "envoy.extensions.filters.http.mcp_json_rest_bridge.v3.ServerToolConfig"
> = {
  $type: "envoy.extensions.filters.http.mcp_json_rest_bridge.v3.ServerToolConfig" as const,

  encode(message: ServerToolConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tools !== undefined && message.tools.length !== 0) {
      for (const v of message.tools) {
        ToolConfig.encode(v!, writer.uint32(10).fork()).join();
      }
    }
    if (message.list_changed !== undefined && message.list_changed !== false) {
      writer.uint32(16).bool(message.list_changed);
    }
    if (message.tool_list_http_rule !== undefined) {
      HttpRule.encode(message.tool_list_http_rule, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerToolConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerToolConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          if (message.tools === undefined) {
            message.tools = [];
          }
          const el = ToolConfig.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.tools!.push(el);
          }
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.list_changed = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tool_list_http_rule = HttpRule.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerToolConfig {
    return {
      $type: ServerToolConfig.$type,
      tools: globalThis.Array.isArray(object?.tools) ? object.tools.map((e: any) => ToolConfig.fromJSON(e)) : undefined,
      list_changed: isSet(object.list_changed) ? globalThis.Boolean(object.list_changed) : undefined,
      tool_list_http_rule: isSet(object.tool_list_http_rule)
        ? HttpRule.fromJSON(object.tool_list_http_rule)
        : undefined,
    };
  },

  toJSON(message: ServerToolConfig): unknown {
    const obj: any = {};
    if (message.tools?.length) {
      obj.tools = message.tools.map((e) => ToolConfig.toJSON(e));
    }
    if (message.list_changed !== undefined) {
      obj.list_changed = message.list_changed;
    }
    if (message.tool_list_http_rule !== undefined) {
      obj.tool_list_http_rule = HttpRule.toJSON(message.tool_list_http_rule);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerToolConfig>, I>>(base?: I): ServerToolConfig {
    return ServerToolConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerToolConfig>, I>>(object: I): ServerToolConfig {
    const message = createBaseServerToolConfig();
    message.tools = object.tools?.map((e) => ToolConfig.fromPartial(e)) || undefined;
    message.list_changed = object.list_changed ?? undefined;
    message.tool_list_http_rule = (object.tool_list_http_rule !== undefined && object.tool_list_http_rule !== null)
      ? HttpRule.fromPartial(object.tool_list_http_rule)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(ServerToolConfig.$type, ServerToolConfig);

function createBaseToolConfig(): ToolConfig {
  return { $type: "envoy.extensions.filters.http.mcp_json_rest_bridge.v3.ToolConfig" };
}

export const ToolConfig: MessageFns<ToolConfig, "envoy.extensions.filters.http.mcp_json_rest_bridge.v3.ToolConfig"> = {
  $type: "envoy.extensions.filters.http.mcp_json_rest_bridge.v3.ToolConfig" as const,

  encode(message: ToolConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.http_rule !== undefined) {
      HttpRule.encode(message.http_rule, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.http_rule = HttpRule.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolConfig {
    return {
      $type: ToolConfig.$type,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      http_rule: isSet(object.http_rule) ? HttpRule.fromJSON(object.http_rule) : undefined,
    };
  },

  toJSON(message: ToolConfig): unknown {
    const obj: any = {};
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.http_rule !== undefined) {
      obj.http_rule = HttpRule.toJSON(message.http_rule);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolConfig>, I>>(base?: I): ToolConfig {
    return ToolConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolConfig>, I>>(object: I): ToolConfig {
    const message = createBaseToolConfig();
    message.name = object.name ?? undefined;
    message.http_rule = (object.http_rule !== undefined && object.http_rule !== null)
      ? HttpRule.fromPartial(object.http_rule)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(ToolConfig.$type, ToolConfig);

function createBaseHttpRule(): HttpRule {
  return { $type: "envoy.extensions.filters.http.mcp_json_rest_bridge.v3.HttpRule" };
}

export const HttpRule: MessageFns<HttpRule, "envoy.extensions.filters.http.mcp_json_rest_bridge.v3.HttpRule"> = {
  $type: "envoy.extensions.filters.http.mcp_json_rest_bridge.v3.HttpRule" as const,

  encode(message: HttpRule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.get !== undefined && message.get !== "") {
      writer.uint32(10).string(message.get);
    }
    if (message.put !== undefined && message.put !== "") {
      writer.uint32(18).string(message.put);
    }
    if (message.post !== undefined && message.post !== "") {
      writer.uint32(26).string(message.post);
    }
    if (message.delete !== undefined && message.delete !== "") {
      writer.uint32(34).string(message.delete);
    }
    if (message.patch !== undefined && message.patch !== "") {
      writer.uint32(42).string(message.patch);
    }
    if (message.body !== undefined && message.body !== "") {
      writer.uint32(50).string(message.body);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HttpRule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttpRule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.get = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.put = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.post = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.delete = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.patch = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.body = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HttpRule {
    return {
      $type: HttpRule.$type,
      get: isSet(object.get) ? globalThis.String(object.get) : undefined,
      put: isSet(object.put) ? globalThis.String(object.put) : undefined,
      post: isSet(object.post) ? globalThis.String(object.post) : undefined,
      delete: isSet(object.delete) ? globalThis.String(object.delete) : undefined,
      patch: isSet(object.patch) ? globalThis.String(object.patch) : undefined,
      body: isSet(object.body) ? globalThis.String(object.body) : undefined,
    };
  },

  toJSON(message: HttpRule): unknown {
    const obj: any = {};
    if (message.get !== undefined) {
      obj.get = message.get;
    }
    if (message.put !== undefined) {
      obj.put = message.put;
    }
    if (message.post !== undefined) {
      obj.post = message.post;
    }
    if (message.delete !== undefined) {
      obj.delete = message.delete;
    }
    if (message.patch !== undefined) {
      obj.patch = message.patch;
    }
    if (message.body !== undefined) {
      obj.body = message.body;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HttpRule>, I>>(base?: I): HttpRule {
    return HttpRule.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HttpRule>, I>>(object: I): HttpRule {
    const message = createBaseHttpRule();
    message.get = object.get ?? undefined;
    message.put = object.put ?? undefined;
    message.post = object.post ?? undefined;
    message.delete = object.delete ?? undefined;
    message.patch = object.patch ?? undefined;
    message.body = object.body ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(HttpRule.$type, HttpRule);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
