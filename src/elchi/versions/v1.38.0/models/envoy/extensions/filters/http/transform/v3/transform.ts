// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/filters/http/transform/v3/transform.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../../typeRegistry";
import { HeaderMutation } from "../../../../../config/common/mutation_rules/v3/mutation_rules";
import { SubstitutionFormatString } from "../../../../../config/core/v3/substitution_format_string";

export const protobufPackage = "envoy.extensions.filters.http.transform.v3";

/**
 * Configuration for the transform filter. The filter may buffer the request/response until the
 * entire body is received, and then mutate the headers and body according to the contents
 * of the request/response. The request and response transformations are independent and could
 * be configured separately.
 * Only JSON body transformation is supported for now.
 */
export interface TransformConfig {
  $type: "envoy.extensions.filters.http.transform.v3.TransformConfig";
  /**
   * Configuration for transforming request.
   *
   * .. note::
   *
   *   If set then the entire request headers and body will always be buffered on a JSON request
   *   even if only headers are transformed.
   */
  request_transformation?:
    | Transformation
    | undefined;
  /**
   * Configuration for transforming response.
   *
   * .. note::
   *
   *   If set then the entire response headers and body will always be buffered on a JSON response
   *   even if only headers are transformed.
   */
  response_transformation?:
    | Transformation
    | undefined;
  /**
   * If true and the request headers are transformed, Envoy will re-evaluate the target
   * cluster in the same route. Please ensure the cluster specifier in the route supports
   * dynamic evaluation or this flag will have no effect, e.g.
   * :ref:`matcher cluster specifier
   * <envoy_v3_api_msg_extensions.router.cluster_specifiers.matcher.v3.MatcherClusterSpecifier>`.
   *
   * Only one of ``clear_cluster_cache`` and ``clear_route_cache`` can be true.
   */
  clear_cluster_cache?:
    | boolean
    | undefined;
  /**
   * If true and the request headers are transformed, Envoy will clear the route cache for
   * the current request and force re-evaluation of the route. This has performance penalty and
   * should only be used when the route match criteria depends on the transformed headers.
   *
   * Only one of ``clear_cluster_cache`` and ``clear_route_cache`` can be true.
   */
  clear_route_cache?: boolean | undefined;
}

export interface Transformation {
  $type: "envoy.extensions.filters.http.transform.v3.Transformation";
  /**
   * The header mutations to perform.
   * The :ref:`substitution format specifier <config_access_log_format>` could be applied here.
   * In addition to the commonly used format specifiers, this filter introduces additional format specifiers:
   *
   * * ``%REQUEST_BODY(KEY*)%``: the request body. And ``Key`` KEY is an optional
   *   lookup key in the namespace with the option of specifying nested keys separated by ':'.
   * * ``%RESPONSE_BODY(KEY*)%``: the response body. And ``Key`` KEY is an optional
   *   lookup key in the namespace with the option of specifying nested keys separated by ':'.
   */
  headers_mutations?:
    | HeaderMutation[]
    | undefined;
  /** The body transformation configuration. If not set, no body transformation will be performed. */
  body_transformation?: BodyTransformation | undefined;
}

export interface BodyTransformation {
  $type: "envoy.extensions.filters.http.transform.v3.BodyTransformation";
  /**
   * Body transformation configuration. The substitution format string is used as the template
   * to generate the transformed new body content.
   * The :ref:`substitution format specifier <config_access_log_format>` could be applied here.
   * And except the commonly used format specifiers, the additional format specifiers
   * ``%REQUEST_BODY(KEY*)%`` and ``%RESPONSE_BODY(KEY*)%`` could also be used here.
   */
  body_format?:
    | SubstitutionFormatString
    | undefined;
  /**
   * The action to perform for new body content and original body content.
   * For example, if ``MERGE`` is used, then the new body content generated from the ``body_format``
   * will be merged into the original body content.
   *
   * Default is ``MERGE``.
   */
  action?: BodyTransformation_TransformAction | undefined;
}

export enum BodyTransformation_TransformAction {
  /** MERGE - Merge the transformed body with the original body. This is the default action. */
  MERGE = "MERGE",
  /** REPLACE - Replace the original body with the transformed body. */
  REPLACE = "REPLACE",
}

export function bodyTransformation_TransformActionFromJSON(object: any): BodyTransformation_TransformAction {
  switch (object) {
    case 0:
    case "MERGE":
      return BodyTransformation_TransformAction.MERGE;
    case 1:
    case "REPLACE":
      return BodyTransformation_TransformAction.REPLACE;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum BodyTransformation_TransformAction");
  }
}

export function bodyTransformation_TransformActionToJSON(object: BodyTransformation_TransformAction): string {
  switch (object) {
    case BodyTransformation_TransformAction.MERGE:
      return "MERGE";
    case BodyTransformation_TransformAction.REPLACE:
      return "REPLACE";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum BodyTransformation_TransformAction");
  }
}

export function bodyTransformation_TransformActionToNumber(object: BodyTransformation_TransformAction): number {
  switch (object) {
    case BodyTransformation_TransformAction.MERGE:
      return 0;
    case BodyTransformation_TransformAction.REPLACE:
      return 1;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum BodyTransformation_TransformAction");
  }
}

function createBaseTransformConfig(): TransformConfig {
  return { $type: "envoy.extensions.filters.http.transform.v3.TransformConfig" };
}

export const TransformConfig: MessageFns<
  TransformConfig,
  "envoy.extensions.filters.http.transform.v3.TransformConfig"
> = {
  $type: "envoy.extensions.filters.http.transform.v3.TransformConfig" as const,

  encode(message: TransformConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.request_transformation !== undefined) {
      Transformation.encode(message.request_transformation, writer.uint32(10).fork()).join();
    }
    if (message.response_transformation !== undefined) {
      Transformation.encode(message.response_transformation, writer.uint32(18).fork()).join();
    }
    if (message.clear_cluster_cache !== undefined && message.clear_cluster_cache !== false) {
      writer.uint32(24).bool(message.clear_cluster_cache);
    }
    if (message.clear_route_cache !== undefined && message.clear_route_cache !== false) {
      writer.uint32(32).bool(message.clear_route_cache);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TransformConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTransformConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.request_transformation = Transformation.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.response_transformation = Transformation.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.clear_cluster_cache = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.clear_route_cache = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TransformConfig {
    return {
      $type: TransformConfig.$type,
      request_transformation: isSet(object.request_transformation)
        ? Transformation.fromJSON(object.request_transformation)
        : undefined,
      response_transformation: isSet(object.response_transformation)
        ? Transformation.fromJSON(object.response_transformation)
        : undefined,
      clear_cluster_cache: isSet(object.clear_cluster_cache)
        ? globalThis.Boolean(object.clear_cluster_cache)
        : undefined,
      clear_route_cache: isSet(object.clear_route_cache) ? globalThis.Boolean(object.clear_route_cache) : undefined,
    };
  },

  toJSON(message: TransformConfig): unknown {
    const obj: any = {};
    if (message.request_transformation !== undefined) {
      obj.request_transformation = Transformation.toJSON(message.request_transformation);
    }
    if (message.response_transformation !== undefined) {
      obj.response_transformation = Transformation.toJSON(message.response_transformation);
    }
    if (message.clear_cluster_cache !== undefined) {
      obj.clear_cluster_cache = message.clear_cluster_cache;
    }
    if (message.clear_route_cache !== undefined) {
      obj.clear_route_cache = message.clear_route_cache;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TransformConfig>, I>>(base?: I): TransformConfig {
    return TransformConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TransformConfig>, I>>(object: I): TransformConfig {
    const message = createBaseTransformConfig();
    message.request_transformation =
      (object.request_transformation !== undefined && object.request_transformation !== null)
        ? Transformation.fromPartial(object.request_transformation)
        : undefined;
    message.response_transformation =
      (object.response_transformation !== undefined && object.response_transformation !== null)
        ? Transformation.fromPartial(object.response_transformation)
        : undefined;
    message.clear_cluster_cache = object.clear_cluster_cache ?? undefined;
    message.clear_route_cache = object.clear_route_cache ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(TransformConfig.$type, TransformConfig);

function createBaseTransformation(): Transformation {
  return { $type: "envoy.extensions.filters.http.transform.v3.Transformation" };
}

export const Transformation: MessageFns<Transformation, "envoy.extensions.filters.http.transform.v3.Transformation"> = {
  $type: "envoy.extensions.filters.http.transform.v3.Transformation" as const,

  encode(message: Transformation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.headers_mutations !== undefined && message.headers_mutations.length !== 0) {
      for (const v of message.headers_mutations) {
        HeaderMutation.encode(v!, writer.uint32(10).fork()).join();
      }
    }
    if (message.body_transformation !== undefined) {
      BodyTransformation.encode(message.body_transformation, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Transformation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTransformation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          if (message.headers_mutations === undefined) {
            message.headers_mutations = [];
          }
          const el = HeaderMutation.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.headers_mutations!.push(el);
          }
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.body_transformation = BodyTransformation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Transformation {
    return {
      $type: Transformation.$type,
      headers_mutations: globalThis.Array.isArray(object?.headers_mutations)
        ? object.headers_mutations.map((e: any) => HeaderMutation.fromJSON(e))
        : undefined,
      body_transformation: isSet(object.body_transformation)
        ? BodyTransformation.fromJSON(object.body_transformation)
        : undefined,
    };
  },

  toJSON(message: Transformation): unknown {
    const obj: any = {};
    if (message.headers_mutations?.length) {
      obj.headers_mutations = message.headers_mutations.map((e) => HeaderMutation.toJSON(e));
    }
    if (message.body_transformation !== undefined) {
      obj.body_transformation = BodyTransformation.toJSON(message.body_transformation);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Transformation>, I>>(base?: I): Transformation {
    return Transformation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Transformation>, I>>(object: I): Transformation {
    const message = createBaseTransformation();
    message.headers_mutations = object.headers_mutations?.map((e) => HeaderMutation.fromPartial(e)) || undefined;
    message.body_transformation = (object.body_transformation !== undefined && object.body_transformation !== null)
      ? BodyTransformation.fromPartial(object.body_transformation)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(Transformation.$type, Transformation);

function createBaseBodyTransformation(): BodyTransformation {
  return { $type: "envoy.extensions.filters.http.transform.v3.BodyTransformation" };
}

export const BodyTransformation: MessageFns<
  BodyTransformation,
  "envoy.extensions.filters.http.transform.v3.BodyTransformation"
> = {
  $type: "envoy.extensions.filters.http.transform.v3.BodyTransformation" as const,

  encode(message: BodyTransformation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.body_format !== undefined) {
      SubstitutionFormatString.encode(message.body_format, writer.uint32(10).fork()).join();
    }
    if (message.action !== undefined && message.action !== BodyTransformation_TransformAction.MERGE) {
      writer.uint32(16).int32(bodyTransformation_TransformActionToNumber(message.action));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BodyTransformation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBodyTransformation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.body_format = SubstitutionFormatString.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.action = bodyTransformation_TransformActionFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BodyTransformation {
    return {
      $type: BodyTransformation.$type,
      body_format: isSet(object.body_format) ? SubstitutionFormatString.fromJSON(object.body_format) : undefined,
      action: isSet(object.action) ? bodyTransformation_TransformActionFromJSON(object.action) : undefined,
    };
  },

  toJSON(message: BodyTransformation): unknown {
    const obj: any = {};
    if (message.body_format !== undefined) {
      obj.body_format = SubstitutionFormatString.toJSON(message.body_format);
    }
    if (message.action !== undefined) {
      obj.action = bodyTransformation_TransformActionToJSON(message.action);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BodyTransformation>, I>>(base?: I): BodyTransformation {
    return BodyTransformation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BodyTransformation>, I>>(object: I): BodyTransformation {
    const message = createBaseBodyTransformation();
    message.body_format = (object.body_format !== undefined && object.body_format !== null)
      ? SubstitutionFormatString.fromPartial(object.body_format)
      : undefined;
    message.action = object.action ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(BodyTransformation.$type, BodyTransformation);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
