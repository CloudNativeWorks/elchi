// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/filters/network/geoip/v3/geoip.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../../typeRegistry";
import { TypedExtensionConfig } from "../../../../../config/core/v3/extension";

export const protobufPackage = "envoy.extensions.filters.network.geoip.v3";

/**
 * The network geolocation filter performs IP geolocation lookups on incoming connections
 * and stores the results in the connection's filter state under the well-known key
 * ``envoy.geoip``. The stored data is a ``GeoipInfo`` object that supports
 * serialization for access logging and field-level access.
 *
 * See :ref:`well known filter state <well_known_filter_state>` for details on accessing
 * the geolocation data.
 */
export interface Geoip {
  $type: "envoy.extensions.filters.network.geoip.v3.Geoip";
  /**
   * The prefix to use when emitting statistics. This is useful when there are multiple
   * listeners configured with geoip filters, allowing stats to be grouped per listener.
   * For example, with ``stat_prefix: "listener_1."``, stats would be emitted as
   * ``listener_1.geoip.total``.
   */
  stat_prefix?:
    | string
    | undefined;
  /**
   * Geoip driver specific configuration which depends on the driver being instantiated.
   * [#extension-category: envoy.geoip_providers]
   */
  provider?:
    | TypedExtensionConfig
    | undefined;
  /**
   * Configuration for dynamically extracting the client IP address used for geolocation lookups.
   *
   * This field accepts the same :ref:`format specifiers <config_access_log_format>` as used for
   * :ref:`HTTP access logging <config_access_log>` to extract the client IP.
   * The formatted result must be a valid IPv4 or IPv6 address string. For example:
   *
   * * ``%FILTER_STATE(my.custom.client.ip:PLAIN)%`` - Read from filter state populated by a preceding filter.
   * * ``%DYNAMIC_METADATA(namespace:key)%`` - Read from dynamic metadata.
   * * ``%REQ(X-Forwarded-For)%`` - Extract from request header (if applicable in context).
   *
   * If not specified, defaults to the downstream connection's remote address.
   * If specified but the result is empty, ``-``, or not a valid IP address, the filter
   * falls back to the downstream connection's remote address.
   *
   * Example reading from filter state:
   *
   * .. code-block:: yaml
   *
   *   client_ip: "%FILTER_STATE(my.custom.client.ip:PLAIN)%"
   */
  client_ip?: string | undefined;
}

function createBaseGeoip(): Geoip {
  return { $type: "envoy.extensions.filters.network.geoip.v3.Geoip" };
}

export const Geoip: MessageFns<Geoip, "envoy.extensions.filters.network.geoip.v3.Geoip"> = {
  $type: "envoy.extensions.filters.network.geoip.v3.Geoip" as const,

  encode(message: Geoip, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stat_prefix !== undefined && message.stat_prefix !== "") {
      writer.uint32(10).string(message.stat_prefix);
    }
    if (message.provider !== undefined) {
      TypedExtensionConfig.encode(message.provider, writer.uint32(18).fork()).join();
    }
    if (message.client_ip !== undefined && message.client_ip !== "") {
      writer.uint32(26).string(message.client_ip);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Geoip {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGeoip();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stat_prefix = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.provider = TypedExtensionConfig.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.client_ip = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Geoip {
    return {
      $type: Geoip.$type,
      stat_prefix: isSet(object.stat_prefix) ? globalThis.String(object.stat_prefix) : undefined,
      provider: isSet(object.provider) ? TypedExtensionConfig.fromJSON(object.provider) : undefined,
      client_ip: isSet(object.client_ip) ? globalThis.String(object.client_ip) : undefined,
    };
  },

  toJSON(message: Geoip): unknown {
    const obj: any = {};
    if (message.stat_prefix !== undefined) {
      obj.stat_prefix = message.stat_prefix;
    }
    if (message.provider !== undefined) {
      obj.provider = TypedExtensionConfig.toJSON(message.provider);
    }
    if (message.client_ip !== undefined) {
      obj.client_ip = message.client_ip;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Geoip>, I>>(base?: I): Geoip {
    return Geoip.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Geoip>, I>>(object: I): Geoip {
    const message = createBaseGeoip();
    message.stat_prefix = object.stat_prefix ?? undefined;
    message.provider = (object.provider !== undefined && object.provider !== null)
      ? TypedExtensionConfig.fromPartial(object.provider)
      : undefined;
    message.client_ip = object.client_ip ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(Geoip.$type, Geoip);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
