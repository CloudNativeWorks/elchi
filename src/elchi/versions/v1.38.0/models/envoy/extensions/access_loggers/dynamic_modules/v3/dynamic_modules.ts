// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/access_loggers/dynamic_modules/v3/dynamic_modules.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Any } from "../../../../../google/protobuf/any";
import { messageTypeRegistry } from "../../../../../typeRegistry";
import { DynamicModuleConfig } from "../../../dynamic_modules/v3/dynamic_modules";

export const protobufPackage = "envoy.extensions.access_loggers.dynamic_modules.v3";

/**
 * Configuration for the Dynamic Modules Access Logger. This logger allows loading shared object
 * files via ``dlopen`` to implement custom access logging behavior.
 *
 * A module can be loaded by multiple access loggers; the module is loaded only once and shared
 * across multiple logger instances.
 *
 * The access logger receives completed request information including request/response headers,
 * stream info (timing, upstream info, response codes), and the log context type.
 */
export interface DynamicModuleAccessLog {
  $type: "envoy.extensions.access_loggers.dynamic_modules.v3.DynamicModuleAccessLog";
  /** Specifies the shared-object level configuration. This field is required. */
  dynamic_module_config?:
    | DynamicModuleConfig
    | undefined;
  /**
   * The name for this logger configuration. If not specified, defaults to an empty string.
   *
   * This can be used to distinguish between different logger implementations inside a dynamic
   * module. For example, a module can have completely different logger implementations (e.g.,
   * file logger, gRPC logger, metrics logger). When Envoy receives this configuration, it passes
   * the ``logger_name`` to the dynamic module's access logger config init function together with
   * the ``logger_config``. That way a module can decide which in-module logger implementation to
   * use based on the name at load time.
   */
  logger_name?:
    | string
    | undefined;
  /**
   * The configuration for the logger chosen by ``logger_name``. If not specified, an empty
   * configuration is passed to the module.
   *
   * This is passed to the module's access logger initialization function. Together with the
   * ``logger_name``, the module can decide which in-module logger implementation to use and
   * fine-tune the behavior of the logger.
   *
   * For example, if a module has two logger implementations, one for file output and one for
   * sending to an external service, ``logger_name`` is used to choose either file or external.
   * The ``logger_config`` can be used to configure file paths, service endpoints, batching
   * parameters, format strings, etc.
   *
   * ``google.protobuf.Struct`` is serialized as JSON before passing it to the module.
   * ``google.protobuf.BytesValue`` and ``google.protobuf.StringValue`` are passed directly
   * without the wrapper.
   *
   * .. code-block:: yaml
   *
   *  # Passing a JSON struct configuration
   *  logger_config:
   *    "@type": "type.googleapis.com/google.protobuf.Struct"
   *    value:
   *      output_path: "/var/log/envoy/access.log"
   *      format: "json"
   *      buffer_size: 1000
   *
   *  # Passing a simple string configuration
   *  logger_config:
   *    "@type": "type.googleapis.com/google.protobuf.StringValue"
   *    value: "/var/log/envoy/access.log"
   */
  logger_config?: Any | undefined;
}

function createBaseDynamicModuleAccessLog(): DynamicModuleAccessLog {
  return { $type: "envoy.extensions.access_loggers.dynamic_modules.v3.DynamicModuleAccessLog" };
}

export const DynamicModuleAccessLog: MessageFns<
  DynamicModuleAccessLog,
  "envoy.extensions.access_loggers.dynamic_modules.v3.DynamicModuleAccessLog"
> = {
  $type: "envoy.extensions.access_loggers.dynamic_modules.v3.DynamicModuleAccessLog" as const,

  encode(message: DynamicModuleAccessLog, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dynamic_module_config !== undefined) {
      DynamicModuleConfig.encode(message.dynamic_module_config, writer.uint32(10).fork()).join();
    }
    if (message.logger_name !== undefined && message.logger_name !== "") {
      writer.uint32(18).string(message.logger_name);
    }
    if (message.logger_config !== undefined) {
      Any.encode(message.logger_config, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DynamicModuleAccessLog {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDynamicModuleAccessLog();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dynamic_module_config = DynamicModuleConfig.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.logger_name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.logger_config = Any.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DynamicModuleAccessLog {
    return {
      $type: DynamicModuleAccessLog.$type,
      dynamic_module_config: isSet(object.dynamic_module_config)
        ? DynamicModuleConfig.fromJSON(object.dynamic_module_config)
        : undefined,
      logger_name: isSet(object.logger_name) ? globalThis.String(object.logger_name) : undefined,
      logger_config: isSet(object.logger_config) ? Any.fromJSON(object.logger_config) : undefined,
    };
  },

  toJSON(message: DynamicModuleAccessLog): unknown {
    const obj: any = {};
    if (message.dynamic_module_config !== undefined) {
      obj.dynamic_module_config = DynamicModuleConfig.toJSON(message.dynamic_module_config);
    }
    if (message.logger_name !== undefined) {
      obj.logger_name = message.logger_name;
    }
    if (message.logger_config !== undefined) {
      obj.logger_config = Any.toJSON(message.logger_config);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DynamicModuleAccessLog>, I>>(base?: I): DynamicModuleAccessLog {
    return DynamicModuleAccessLog.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DynamicModuleAccessLog>, I>>(object: I): DynamicModuleAccessLog {
    const message = createBaseDynamicModuleAccessLog();
    message.dynamic_module_config =
      (object.dynamic_module_config !== undefined && object.dynamic_module_config !== null)
        ? DynamicModuleConfig.fromPartial(object.dynamic_module_config)
        : undefined;
    message.logger_name = object.logger_name ?? undefined;
    message.logger_config = (object.logger_config !== undefined && object.logger_config !== null)
      ? Any.fromPartial(object.logger_config)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(DynamicModuleAccessLog.$type, DynamicModuleAccessLog);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
