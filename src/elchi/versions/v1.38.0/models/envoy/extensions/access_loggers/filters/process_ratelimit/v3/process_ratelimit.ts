// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/access_loggers/filters/process_ratelimit/v3/process_ratelimit.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../../typeRegistry";
import { ConfigSource } from "../../../../../config/core/v3/config_source";

export const protobufPackage = "envoy.extensions.access_loggers.filters.process_ratelimit.v3";

/** Filters for rate limiting the access log emission using global token buckets per process and shared across all listeners. */
export interface ProcessRateLimitFilter {
  $type: "envoy.extensions.access_loggers.filters.process_ratelimit.v3.ProcessRateLimitFilter";
  /** The dynamic config for the token bucket. */
  dynamic_config?: DynamicTokenBucket | undefined;
}

export interface DynamicTokenBucket {
  $type: "envoy.extensions.access_loggers.filters.process_ratelimit.v3.DynamicTokenBucket";
  /** the key used to find the token bucket in the singleton map. */
  resource_name?:
    | string
    | undefined;
  /**
   * The configuration source for the :ref:`token_bucket <envoy_v3_api_msg_type.v3.TokenBucket>`.
   * It should stay the same through the process lifetime.
   */
  config_source?: ConfigSource | undefined;
}

function createBaseProcessRateLimitFilter(): ProcessRateLimitFilter {
  return { $type: "envoy.extensions.access_loggers.filters.process_ratelimit.v3.ProcessRateLimitFilter" };
}

export const ProcessRateLimitFilter: MessageFns<
  ProcessRateLimitFilter,
  "envoy.extensions.access_loggers.filters.process_ratelimit.v3.ProcessRateLimitFilter"
> = {
  $type: "envoy.extensions.access_loggers.filters.process_ratelimit.v3.ProcessRateLimitFilter" as const,

  encode(message: ProcessRateLimitFilter, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dynamic_config !== undefined) {
      DynamicTokenBucket.encode(message.dynamic_config, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProcessRateLimitFilter {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProcessRateLimitFilter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dynamic_config = DynamicTokenBucket.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProcessRateLimitFilter {
    return {
      $type: ProcessRateLimitFilter.$type,
      dynamic_config: isSet(object.dynamic_config) ? DynamicTokenBucket.fromJSON(object.dynamic_config) : undefined,
    };
  },

  toJSON(message: ProcessRateLimitFilter): unknown {
    const obj: any = {};
    if (message.dynamic_config !== undefined) {
      obj.dynamic_config = DynamicTokenBucket.toJSON(message.dynamic_config);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProcessRateLimitFilter>, I>>(base?: I): ProcessRateLimitFilter {
    return ProcessRateLimitFilter.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProcessRateLimitFilter>, I>>(object: I): ProcessRateLimitFilter {
    const message = createBaseProcessRateLimitFilter();
    message.dynamic_config = (object.dynamic_config !== undefined && object.dynamic_config !== null)
      ? DynamicTokenBucket.fromPartial(object.dynamic_config)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(ProcessRateLimitFilter.$type, ProcessRateLimitFilter);

function createBaseDynamicTokenBucket(): DynamicTokenBucket {
  return { $type: "envoy.extensions.access_loggers.filters.process_ratelimit.v3.DynamicTokenBucket" };
}

export const DynamicTokenBucket: MessageFns<
  DynamicTokenBucket,
  "envoy.extensions.access_loggers.filters.process_ratelimit.v3.DynamicTokenBucket"
> = {
  $type: "envoy.extensions.access_loggers.filters.process_ratelimit.v3.DynamicTokenBucket" as const,

  encode(message: DynamicTokenBucket, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.resource_name !== undefined && message.resource_name !== "") {
      writer.uint32(10).string(message.resource_name);
    }
    if (message.config_source !== undefined) {
      ConfigSource.encode(message.config_source, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DynamicTokenBucket {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDynamicTokenBucket();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.resource_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.config_source = ConfigSource.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DynamicTokenBucket {
    return {
      $type: DynamicTokenBucket.$type,
      resource_name: isSet(object.resource_name) ? globalThis.String(object.resource_name) : undefined,
      config_source: isSet(object.config_source) ? ConfigSource.fromJSON(object.config_source) : undefined,
    };
  },

  toJSON(message: DynamicTokenBucket): unknown {
    const obj: any = {};
    if (message.resource_name !== undefined) {
      obj.resource_name = message.resource_name;
    }
    if (message.config_source !== undefined) {
      obj.config_source = ConfigSource.toJSON(message.config_source);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DynamicTokenBucket>, I>>(base?: I): DynamicTokenBucket {
    return DynamicTokenBucket.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DynamicTokenBucket>, I>>(object: I): DynamicTokenBucket {
    const message = createBaseDynamicTokenBucket();
    message.resource_name = object.resource_name ?? undefined;
    message.config_source = (object.config_source !== undefined && object.config_source !== null)
      ? ConfigSource.fromPartial(object.config_source)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(DynamicTokenBucket.$type, DynamicTokenBucket);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
