// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/config/trace/v3/datadog.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../../../../google/protobuf/duration";
import { messageTypeRegistry } from "../../../../typeRegistry";

export const protobufPackage = "envoy.config.trace.v3";

/** Configuration for the Remote Configuration feature. */
export interface DatadogRemoteConfig {
  $type: "envoy.config.trace.v3.DatadogRemoteConfig";
  /**
   * Frequency at which new configuration updates are queried.
   * If no value is provided, the default value is delegated to the Datadog tracing library.
   */
  polling_interval?: Duration | undefined;
}

/**
 * Configuration for the Datadog tracer.
 * [#extension: envoy.tracers.datadog]
 */
export interface DatadogConfig {
  $type: "envoy.config.trace.v3.DatadogConfig";
  /** The cluster to use for submitting traces to the Datadog agent. */
  collector_cluster?:
    | string
    | undefined;
  /** The name used for the service when traces are generated by envoy. */
  service_name?:
    | string
    | undefined;
  /**
   * Optional hostname to use when sending spans to the collector_cluster. Useful for collectors
   * that require a specific hostname. Defaults to :ref:`collector_cluster <envoy_v3_api_field_config.trace.v3.DatadogConfig.collector_cluster>` above.
   */
  collector_hostname?:
    | string
    | undefined;
  /**
   * Enables and configures remote configuration.
   * Remote Configuration allows to configure the tracer from Datadog's user interface.
   * This feature can drastically increase the number of connections to the Datadog Agent.
   * Each tracer regularly polls for configuration updates, and the number of tracers is the product
   * of the number of listeners and worker threads.
   */
  remote_config?: DatadogRemoteConfig | undefined;
}

function createBaseDatadogRemoteConfig(): DatadogRemoteConfig {
  return { $type: "envoy.config.trace.v3.DatadogRemoteConfig" };
}

export const DatadogRemoteConfig: MessageFns<DatadogRemoteConfig, "envoy.config.trace.v3.DatadogRemoteConfig"> = {
  $type: "envoy.config.trace.v3.DatadogRemoteConfig" as const,

  encode(message: DatadogRemoteConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.polling_interval !== undefined) {
      Duration.encode(message.polling_interval, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DatadogRemoteConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDatadogRemoteConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.polling_interval = Duration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DatadogRemoteConfig {
    return {
      $type: DatadogRemoteConfig.$type,
      polling_interval: isSet(object.polling_interval) ? Duration.fromJSON(object.polling_interval) : undefined,
    };
  },

  toJSON(message: DatadogRemoteConfig): unknown {
    const obj: any = {};
    if (message.polling_interval !== undefined) {
      obj.polling_interval = Duration.toJSON(message.polling_interval);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DatadogRemoteConfig>, I>>(base?: I): DatadogRemoteConfig {
    return DatadogRemoteConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DatadogRemoteConfig>, I>>(object: I): DatadogRemoteConfig {
    const message = createBaseDatadogRemoteConfig();
    message.polling_interval = (object.polling_interval !== undefined && object.polling_interval !== null)
      ? Duration.fromPartial(object.polling_interval)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(DatadogRemoteConfig.$type, DatadogRemoteConfig);

function createBaseDatadogConfig(): DatadogConfig {
  return { $type: "envoy.config.trace.v3.DatadogConfig" };
}

export const DatadogConfig: MessageFns<DatadogConfig, "envoy.config.trace.v3.DatadogConfig"> = {
  $type: "envoy.config.trace.v3.DatadogConfig" as const,

  encode(message: DatadogConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.collector_cluster !== undefined && message.collector_cluster !== "") {
      writer.uint32(10).string(message.collector_cluster);
    }
    if (message.service_name !== undefined && message.service_name !== "") {
      writer.uint32(18).string(message.service_name);
    }
    if (message.collector_hostname !== undefined && message.collector_hostname !== "") {
      writer.uint32(26).string(message.collector_hostname);
    }
    if (message.remote_config !== undefined) {
      DatadogRemoteConfig.encode(message.remote_config, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DatadogConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDatadogConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.collector_cluster = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.service_name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.collector_hostname = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.remote_config = DatadogRemoteConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DatadogConfig {
    return {
      $type: DatadogConfig.$type,
      collector_cluster: isSet(object.collector_cluster) ? globalThis.String(object.collector_cluster) : undefined,
      service_name: isSet(object.service_name) ? globalThis.String(object.service_name) : undefined,
      collector_hostname: isSet(object.collector_hostname) ? globalThis.String(object.collector_hostname) : undefined,
      remote_config: isSet(object.remote_config) ? DatadogRemoteConfig.fromJSON(object.remote_config) : undefined,
    };
  },

  toJSON(message: DatadogConfig): unknown {
    const obj: any = {};
    if (message.collector_cluster !== undefined) {
      obj.collector_cluster = message.collector_cluster;
    }
    if (message.service_name !== undefined) {
      obj.service_name = message.service_name;
    }
    if (message.collector_hostname !== undefined) {
      obj.collector_hostname = message.collector_hostname;
    }
    if (message.remote_config !== undefined) {
      obj.remote_config = DatadogRemoteConfig.toJSON(message.remote_config);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DatadogConfig>, I>>(base?: I): DatadogConfig {
    return DatadogConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DatadogConfig>, I>>(object: I): DatadogConfig {
    const message = createBaseDatadogConfig();
    message.collector_cluster = object.collector_cluster ?? undefined;
    message.service_name = object.service_name ?? undefined;
    message.collector_hostname = object.collector_hostname ?? undefined;
    message.remote_config = (object.remote_config !== undefined && object.remote_config !== null)
      ? DatadogRemoteConfig.fromPartial(object.remote_config)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(DatadogConfig.$type, DatadogConfig);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
