// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/config/resource_monitor/injected_resource/v2alpha/injected_resource.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../typeRegistry";

export const protobufPackage = "envoy.config.resource_monitor.injected_resource.v2alpha";

/**
 * The injected resource monitor allows injecting a synthetic resource pressure into Envoy
 * via a text file, which must contain a floating-point number in the range [0..1] representing
 * the resource pressure and be updated atomically by a symbolic link swap.
 * This is intended primarily for integration tests to force Envoy into an overloaded state.
 */
export interface InjectedResourceConfig {
  $type: "envoy.config.resource_monitor.injected_resource.v2alpha.InjectedResourceConfig";
  filename?: string | undefined;
}

function createBaseInjectedResourceConfig(): InjectedResourceConfig {
  return { $type: "envoy.config.resource_monitor.injected_resource.v2alpha.InjectedResourceConfig" };
}

export const InjectedResourceConfig: MessageFns<
  InjectedResourceConfig,
  "envoy.config.resource_monitor.injected_resource.v2alpha.InjectedResourceConfig"
> = {
  $type: "envoy.config.resource_monitor.injected_resource.v2alpha.InjectedResourceConfig" as const,

  encode(message: InjectedResourceConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.filename !== undefined && message.filename !== "") {
      writer.uint32(10).string(message.filename);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InjectedResourceConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInjectedResourceConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.filename = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InjectedResourceConfig {
    return {
      $type: InjectedResourceConfig.$type,
      filename: isSet(object.filename) ? globalThis.String(object.filename) : undefined,
    };
  },

  toJSON(message: InjectedResourceConfig): unknown {
    const obj: any = {};
    if (message.filename !== undefined) {
      obj.filename = message.filename;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InjectedResourceConfig>, I>>(base?: I): InjectedResourceConfig {
    return InjectedResourceConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InjectedResourceConfig>, I>>(object: I): InjectedResourceConfig {
    const message = createBaseInjectedResourceConfig();
    message.filename = object.filename ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(InjectedResourceConfig.$type, InjectedResourceConfig);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
