// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/config/ratelimit/v3/rls.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../typeRegistry";
import { ApiVersion, apiVersionFromJSON, apiVersionToJSON, apiVersionToNumber } from "../../core/v3/config_source";
import { GrpcService } from "../../core/v3/grpc_service";

export const protobufPackage = "envoy.config.ratelimit.v3";

/** Rate limit :ref:`configuration overview <config_rate_limit_service>`. */
export interface RateLimitServiceConfig {
  $type: "envoy.config.ratelimit.v3.RateLimitServiceConfig";
  /**
   * Specifies the gRPC service that hosts the rate limit service. The client
   * will connect to this cluster when it needs to make rate limit service
   * requests.
   */
  grpc_service?:
    | GrpcService
    | undefined;
  /**
   * API version for rate limit transport protocol. This describes the rate limit gRPC endpoint and
   * version of messages used on the wire.
   */
  transport_api_version?: ApiVersion | undefined;
}

function createBaseRateLimitServiceConfig(): RateLimitServiceConfig {
  return { $type: "envoy.config.ratelimit.v3.RateLimitServiceConfig" };
}

export const RateLimitServiceConfig: MessageFns<
  RateLimitServiceConfig,
  "envoy.config.ratelimit.v3.RateLimitServiceConfig"
> = {
  $type: "envoy.config.ratelimit.v3.RateLimitServiceConfig" as const,

  encode(message: RateLimitServiceConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.grpc_service !== undefined) {
      GrpcService.encode(message.grpc_service, writer.uint32(18).fork()).join();
    }
    if (message.transport_api_version !== undefined && message.transport_api_version !== ApiVersion.AUTO) {
      writer.uint32(32).int32(apiVersionToNumber(message.transport_api_version));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RateLimitServiceConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRateLimitServiceConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.grpc_service = GrpcService.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.transport_api_version = apiVersionFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RateLimitServiceConfig {
    return {
      $type: RateLimitServiceConfig.$type,
      grpc_service: isSet(object.grpc_service) ? GrpcService.fromJSON(object.grpc_service) : undefined,
      transport_api_version: isSet(object.transport_api_version)
        ? apiVersionFromJSON(object.transport_api_version)
        : undefined,
    };
  },

  toJSON(message: RateLimitServiceConfig): unknown {
    const obj: any = {};
    if (message.grpc_service !== undefined) {
      obj.grpc_service = GrpcService.toJSON(message.grpc_service);
    }
    if (message.transport_api_version !== undefined) {
      obj.transport_api_version = apiVersionToJSON(message.transport_api_version);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RateLimitServiceConfig>, I>>(base?: I): RateLimitServiceConfig {
    return RateLimitServiceConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RateLimitServiceConfig>, I>>(object: I): RateLimitServiceConfig {
    const message = createBaseRateLimitServiceConfig();
    message.grpc_service = (object.grpc_service !== undefined && object.grpc_service !== null)
      ? GrpcService.fromPartial(object.grpc_service)
      : undefined;
    message.transport_api_version = object.transport_api_version ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(RateLimitServiceConfig.$type, RateLimitServiceConfig);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
