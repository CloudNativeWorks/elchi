// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/service/endpoint/v3/leds.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Observable } from "rxjs";
import { map } from "rxjs/operators";
import { messageTypeRegistry } from "../../../../typeRegistry";
import { DeltaDiscoveryRequest, DeltaDiscoveryResponse } from "../../discovery/v3/discovery";

export const protobufPackage = "envoy.service.endpoint.v3";

/**
 * [#not-implemented-hide:] Not configuration. Workaround c++ protobuf issue with importing
 * services: https://github.com/google/protobuf/issues/4221 and protoxform to upgrade the file.
 */
export interface LedsDummy {
  $type: "envoy.service.endpoint.v3.LedsDummy";
}

function createBaseLedsDummy(): LedsDummy {
  return { $type: "envoy.service.endpoint.v3.LedsDummy" };
}

export const LedsDummy: MessageFns<LedsDummy, "envoy.service.endpoint.v3.LedsDummy"> = {
  $type: "envoy.service.endpoint.v3.LedsDummy" as const,

  encode(_: LedsDummy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LedsDummy {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLedsDummy();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): LedsDummy {
    return { $type: LedsDummy.$type };
  },

  toJSON(_: LedsDummy): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<LedsDummy>, I>>(base?: I): LedsDummy {
    return LedsDummy.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LedsDummy>, I>>(_: I): LedsDummy {
    const message = createBaseLedsDummy();
    return message;
  },
};

messageTypeRegistry.set(LedsDummy.$type, LedsDummy);

export interface LocalityEndpointDiscoveryService {
  /**
   * The resource_names_subscribe resource_names_unsubscribe fields in DeltaDiscoveryRequest
   * specify a list of glob collections to subscribe to updates for.
   */
  DeltaLocalityEndpoints(request: Observable<DeltaDiscoveryRequest>): Observable<DeltaDiscoveryResponse>;
}

export const LocalityEndpointDiscoveryServiceServiceName = "envoy.service.endpoint.v3.LocalityEndpointDiscoveryService";
export class LocalityEndpointDiscoveryServiceClientImpl implements LocalityEndpointDiscoveryService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || LocalityEndpointDiscoveryServiceServiceName;
    this.rpc = rpc;
    this.DeltaLocalityEndpoints = this.DeltaLocalityEndpoints.bind(this);
  }
  DeltaLocalityEndpoints(request: Observable<DeltaDiscoveryRequest>): Observable<DeltaDiscoveryResponse> {
    const data = request.pipe(map((request) => DeltaDiscoveryRequest.encode(request).finish()));
    const result = this.rpc.bidirectionalStreamingRequest(this.service, "DeltaLocalityEndpoints", data);
    return result.pipe(map((data) => DeltaDiscoveryResponse.decode(new BinaryReader(data))));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
  clientStreamingRequest(service: string, method: string, data: Observable<Uint8Array>): Promise<Uint8Array>;
  serverStreamingRequest(service: string, method: string, data: Uint8Array): Observable<Uint8Array>;
  bidirectionalStreamingRequest(service: string, method: string, data: Observable<Uint8Array>): Observable<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
