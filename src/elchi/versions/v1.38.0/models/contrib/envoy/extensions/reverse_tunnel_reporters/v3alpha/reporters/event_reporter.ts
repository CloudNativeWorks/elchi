// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: contrib/envoy/extensions/reverse_tunnel_reporters/v3alpha/reporters/event_reporter.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Any } from "../../../../../../google/protobuf/any";
import { messageTypeRegistry } from "../../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.reverse_tunnel_reporters.v3alpha.reporters";

export interface ReverseConnectionReporterClient {
  $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.reporters.ReverseConnectionReporterClient";
  /** Name to use to pick out the client should match the one reported by the factory. */
  name?:
    | string
    | undefined;
  /** Typed config for the client */
  typed_config?: Any | undefined;
}

/** Configuration for the connection event reporter. */
export interface EventReporterConfig {
  $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.reporters.EventReporterConfig";
  /**
   * Stat prefix for this reporter's metrics.
   * Metrics will be emitted as ``{stat_prefix}.events_pushed``, etc.
   */
  stat_prefix?:
    | string
    | undefined;
  /** List of clients to report to. */
  clients?: ReverseConnectionReporterClient[] | undefined;
}

function createBaseReverseConnectionReporterClient(): ReverseConnectionReporterClient {
  return { $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.reporters.ReverseConnectionReporterClient" };
}

export const ReverseConnectionReporterClient: MessageFns<
  ReverseConnectionReporterClient,
  "envoy.extensions.reverse_tunnel_reporters.v3alpha.reporters.ReverseConnectionReporterClient"
> = {
  $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.reporters.ReverseConnectionReporterClient" as const,

  encode(message: ReverseConnectionReporterClient, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.typed_config !== undefined) {
      Any.encode(message.typed_config, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReverseConnectionReporterClient {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReverseConnectionReporterClient();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.typed_config = Any.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReverseConnectionReporterClient {
    return {
      $type: ReverseConnectionReporterClient.$type,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      typed_config: isSet(object.typed_config) ? Any.fromJSON(object.typed_config) : undefined,
    };
  },

  toJSON(message: ReverseConnectionReporterClient): unknown {
    const obj: any = {};
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.typed_config !== undefined) {
      obj.typed_config = Any.toJSON(message.typed_config);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReverseConnectionReporterClient>, I>>(base?: I): ReverseConnectionReporterClient {
    return ReverseConnectionReporterClient.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReverseConnectionReporterClient>, I>>(
    object: I,
  ): ReverseConnectionReporterClient {
    const message = createBaseReverseConnectionReporterClient();
    message.name = object.name ?? undefined;
    message.typed_config = (object.typed_config !== undefined && object.typed_config !== null)
      ? Any.fromPartial(object.typed_config)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(ReverseConnectionReporterClient.$type, ReverseConnectionReporterClient);

function createBaseEventReporterConfig(): EventReporterConfig {
  return { $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.reporters.EventReporterConfig" };
}

export const EventReporterConfig: MessageFns<
  EventReporterConfig,
  "envoy.extensions.reverse_tunnel_reporters.v3alpha.reporters.EventReporterConfig"
> = {
  $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.reporters.EventReporterConfig" as const,

  encode(message: EventReporterConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stat_prefix !== undefined && message.stat_prefix !== "") {
      writer.uint32(10).string(message.stat_prefix);
    }
    if (message.clients !== undefined && message.clients.length !== 0) {
      for (const v of message.clients) {
        ReverseConnectionReporterClient.encode(v!, writer.uint32(18).fork()).join();
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventReporterConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventReporterConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stat_prefix = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          if (message.clients === undefined) {
            message.clients = [];
          }
          const el = ReverseConnectionReporterClient.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.clients!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventReporterConfig {
    return {
      $type: EventReporterConfig.$type,
      stat_prefix: isSet(object.stat_prefix) ? globalThis.String(object.stat_prefix) : undefined,
      clients: globalThis.Array.isArray(object?.clients)
        ? object.clients.map((e: any) => ReverseConnectionReporterClient.fromJSON(e))
        : undefined,
    };
  },

  toJSON(message: EventReporterConfig): unknown {
    const obj: any = {};
    if (message.stat_prefix !== undefined) {
      obj.stat_prefix = message.stat_prefix;
    }
    if (message.clients?.length) {
      obj.clients = message.clients.map((e) => ReverseConnectionReporterClient.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventReporterConfig>, I>>(base?: I): EventReporterConfig {
    return EventReporterConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventReporterConfig>, I>>(object: I): EventReporterConfig {
    const message = createBaseEventReporterConfig();
    message.stat_prefix = object.stat_prefix ?? undefined;
    message.clients = object.clients?.map((e) => ReverseConnectionReporterClient.fromPartial(e)) || undefined;
    return message;
  },
};

messageTypeRegistry.set(EventReporterConfig.$type, EventReporterConfig);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
