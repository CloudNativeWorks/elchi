// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: contrib/envoy/extensions/reverse_tunnel_reporters/v3alpha/clients/grpc_client/stream_reverse_tunnels.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Observable } from "rxjs";
import { map } from "rxjs/operators";
import { Node } from "../../../../../../../envoy/config/core/v3/base";
import { Duration } from "../../../../../../../google/protobuf/duration";
import { Timestamp } from "../../../../../../../google/protobuf/timestamp";
import { Status } from "../../../../../../../google/rpc/status";
import { messageTypeRegistry } from "../../../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client";

/**
 * Request message sent by Envoy to report reverse tunnel state changes.
 * [#next-free-field: 6]
 */
export interface StreamReverseTunnelsRequest {
  $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.StreamReverseTunnelsRequest";
  /**
   * Node identifier for the reporting Envoy instance.
   * This identifies which Envoy instance is sending the report.
   */
  node?:
    | Node
    | undefined;
  /**
   * List of reverse tunnels that were established since the last report.
   * Each tunnel represents a new connection from a downstream Envoy.
   */
  added_tunnels?:
    | ReverseTunnel[]
    | undefined;
  /**
   * List of tunnel names that were disconnected since the last report.
   * Only the tunnel name is needed for removal notifications.
   */
  removed_tunnel_names?:
    | string[]
    | undefined;
  /**
   * Indicates whether this report contains all active tunnels (true) or
   * only changes since the last report (false). Usually invoked only on server disconnects.
   */
  full_push?:
    | boolean
    | undefined;
  /**
   * Unique nonce for this request to enable proper ACK/NACK handling.
   * Must be non-negative and should increment for each request.
   * This can also be modified to be used for checksum and tracking in the future.
   */
  nonce?: number | undefined;
}

/** Response message sent by the management server to control reporting behavior. */
export interface StreamReverseTunnelsResponse {
  $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.StreamReverseTunnelsResponse";
  /**
   * Node identifier acknowledging which Envoy instance this response is for.
   * Should match the node from the corresponding request.
   */
  node_id?:
    | string
    | undefined;
  /**
   * Interval at which Envoy should send tunnel state reports.
   * This is used to change the reporting_interval -> no need to repeat the same value.
   */
  report_interval?:
    | Duration
    | undefined;
  /**
   * Nonce from the request being acknowledged or rejected.
   * Must match the nonce from the corresponding request.
   */
  request_nonce?:
    | number
    | undefined;
  /**
   * Error details if the previous request failed processing.
   * If populated, indicates the request was rejected (NACK).
   * If empty, indicates successful processing (ACK).
   * NACK will terminate the connection -> useful for logging rather than just some disconnect.
   * So basically -> NACK then terminate.
   */
  error_detail?: Status | undefined;
}

/** Represents a single reverse tunnel connection with its metadata. */
export interface ReverseTunnel {
  $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.ReverseTunnel";
  /**
   * Unique name to identify this tunnel connection.
   * Must be unique within the reporting Envoy instance.
   * This is also used for the reporting the disconnection with the associated tunnel initiator.
   */
  name?:
    | string
    | undefined;
  /**
   * Identity information of the tunnel initiator (downstream Envoy).
   * Contains ``node_id``, ``cluster_id``, and ``tenant_id`` for proper identification.
   */
  identity?:
    | TunnelInitiatorIdentity
    | undefined;
  /**
   * Timestamp when this tunnel connection was created.
   * Used for ordering events and debugging connection timing issues.
   */
  created_at?: Date | undefined;
}

export interface TunnelInitiatorIdentity {
  $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.TunnelInitiatorIdentity";
  /** Required: Tenant identifier of the initiating Envoy instance. */
  tenant_id?:
    | string
    | undefined;
  /** Required: Cluster identifier of the initiating Envoy instance. */
  cluster_id?:
    | string
    | undefined;
  /** Required: Node identifier of the initiating Envoy instance. */
  node_id?: string | undefined;
}

function createBaseStreamReverseTunnelsRequest(): StreamReverseTunnelsRequest {
  return { $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.StreamReverseTunnelsRequest" };
}

export const StreamReverseTunnelsRequest: MessageFns<
  StreamReverseTunnelsRequest,
  "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.StreamReverseTunnelsRequest"
> = {
  $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.StreamReverseTunnelsRequest" as const,

  encode(message: StreamReverseTunnelsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.node !== undefined) {
      Node.encode(message.node, writer.uint32(10).fork()).join();
    }
    if (message.added_tunnels !== undefined && message.added_tunnels.length !== 0) {
      for (const v of message.added_tunnels) {
        ReverseTunnel.encode(v!, writer.uint32(18).fork()).join();
      }
    }
    if (message.removed_tunnel_names !== undefined && message.removed_tunnel_names.length !== 0) {
      for (const v of message.removed_tunnel_names) {
        writer.uint32(26).string(v!);
      }
    }
    if (message.full_push !== undefined && message.full_push !== false) {
      writer.uint32(32).bool(message.full_push);
    }
    if (message.nonce !== undefined && message.nonce !== 0) {
      writer.uint32(40).int64(message.nonce);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamReverseTunnelsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamReverseTunnelsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.node = Node.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          if (message.added_tunnels === undefined) {
            message.added_tunnels = [];
          }
          const el = ReverseTunnel.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.added_tunnels!.push(el);
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          if (message.removed_tunnel_names === undefined) {
            message.removed_tunnel_names = [];
          }
          const el = reader.string();
          if (el !== undefined) {
            message.removed_tunnel_names!.push(el);
          }
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.full_push = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.nonce = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamReverseTunnelsRequest {
    return {
      $type: StreamReverseTunnelsRequest.$type,
      node: isSet(object.node) ? Node.fromJSON(object.node) : undefined,
      added_tunnels: globalThis.Array.isArray(object?.added_tunnels)
        ? object.added_tunnels.map((e: any) => ReverseTunnel.fromJSON(e))
        : undefined,
      removed_tunnel_names: globalThis.Array.isArray(object?.removed_tunnel_names)
        ? object.removed_tunnel_names.map((e: any) => globalThis.String(e))
        : undefined,
      full_push: isSet(object.full_push) ? globalThis.Boolean(object.full_push) : undefined,
      nonce: isSet(object.nonce) ? globalThis.Number(object.nonce) : undefined,
    };
  },

  toJSON(message: StreamReverseTunnelsRequest): unknown {
    const obj: any = {};
    if (message.node !== undefined) {
      obj.node = Node.toJSON(message.node);
    }
    if (message.added_tunnels?.length) {
      obj.added_tunnels = message.added_tunnels.map((e) => ReverseTunnel.toJSON(e));
    }
    if (message.removed_tunnel_names?.length) {
      obj.removed_tunnel_names = message.removed_tunnel_names;
    }
    if (message.full_push !== undefined) {
      obj.full_push = message.full_push;
    }
    if (message.nonce !== undefined) {
      obj.nonce = Math.round(message.nonce);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamReverseTunnelsRequest>, I>>(base?: I): StreamReverseTunnelsRequest {
    return StreamReverseTunnelsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamReverseTunnelsRequest>, I>>(object: I): StreamReverseTunnelsRequest {
    const message = createBaseStreamReverseTunnelsRequest();
    message.node = (object.node !== undefined && object.node !== null) ? Node.fromPartial(object.node) : undefined;
    message.added_tunnels = object.added_tunnels?.map((e) => ReverseTunnel.fromPartial(e)) || undefined;
    message.removed_tunnel_names = object.removed_tunnel_names?.map((e) => e) || undefined;
    message.full_push = object.full_push ?? undefined;
    message.nonce = object.nonce ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(StreamReverseTunnelsRequest.$type, StreamReverseTunnelsRequest);

function createBaseStreamReverseTunnelsResponse(): StreamReverseTunnelsResponse {
  return {
    $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.StreamReverseTunnelsResponse",
  };
}

export const StreamReverseTunnelsResponse: MessageFns<
  StreamReverseTunnelsResponse,
  "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.StreamReverseTunnelsResponse"
> = {
  $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.StreamReverseTunnelsResponse" as const,

  encode(message: StreamReverseTunnelsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.node_id !== undefined && message.node_id !== "") {
      writer.uint32(10).string(message.node_id);
    }
    if (message.report_interval !== undefined) {
      Duration.encode(message.report_interval, writer.uint32(18).fork()).join();
    }
    if (message.request_nonce !== undefined && message.request_nonce !== 0) {
      writer.uint32(24).int64(message.request_nonce);
    }
    if (message.error_detail !== undefined) {
      Status.encode(message.error_detail, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamReverseTunnelsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamReverseTunnelsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.node_id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.report_interval = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.request_nonce = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.error_detail = Status.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamReverseTunnelsResponse {
    return {
      $type: StreamReverseTunnelsResponse.$type,
      node_id: isSet(object.node_id) ? globalThis.String(object.node_id) : undefined,
      report_interval: isSet(object.report_interval) ? Duration.fromJSON(object.report_interval) : undefined,
      request_nonce: isSet(object.request_nonce) ? globalThis.Number(object.request_nonce) : undefined,
      error_detail: isSet(object.error_detail) ? Status.fromJSON(object.error_detail) : undefined,
    };
  },

  toJSON(message: StreamReverseTunnelsResponse): unknown {
    const obj: any = {};
    if (message.node_id !== undefined) {
      obj.node_id = message.node_id;
    }
    if (message.report_interval !== undefined) {
      obj.report_interval = Duration.toJSON(message.report_interval);
    }
    if (message.request_nonce !== undefined) {
      obj.request_nonce = Math.round(message.request_nonce);
    }
    if (message.error_detail !== undefined) {
      obj.error_detail = Status.toJSON(message.error_detail);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamReverseTunnelsResponse>, I>>(base?: I): StreamReverseTunnelsResponse {
    return StreamReverseTunnelsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamReverseTunnelsResponse>, I>>(object: I): StreamReverseTunnelsResponse {
    const message = createBaseStreamReverseTunnelsResponse();
    message.node_id = object.node_id ?? undefined;
    message.report_interval = (object.report_interval !== undefined && object.report_interval !== null)
      ? Duration.fromPartial(object.report_interval)
      : undefined;
    message.request_nonce = object.request_nonce ?? undefined;
    message.error_detail = (object.error_detail !== undefined && object.error_detail !== null)
      ? Status.fromPartial(object.error_detail)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(StreamReverseTunnelsResponse.$type, StreamReverseTunnelsResponse);

function createBaseReverseTunnel(): ReverseTunnel {
  return { $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.ReverseTunnel" };
}

export const ReverseTunnel: MessageFns<
  ReverseTunnel,
  "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.ReverseTunnel"
> = {
  $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.ReverseTunnel" as const,

  encode(message: ReverseTunnel, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.identity !== undefined) {
      TunnelInitiatorIdentity.encode(message.identity, writer.uint32(18).fork()).join();
    }
    if (message.created_at !== undefined) {
      Timestamp.encode(toTimestamp(message.created_at), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReverseTunnel {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReverseTunnel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.identity = TunnelInitiatorIdentity.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.created_at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReverseTunnel {
    return {
      $type: ReverseTunnel.$type,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      identity: isSet(object.identity) ? TunnelInitiatorIdentity.fromJSON(object.identity) : undefined,
      created_at: isSet(object.created_at) ? fromJsonTimestamp(object.created_at) : undefined,
    };
  },

  toJSON(message: ReverseTunnel): unknown {
    const obj: any = {};
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.identity !== undefined) {
      obj.identity = TunnelInitiatorIdentity.toJSON(message.identity);
    }
    if (message.created_at !== undefined) {
      obj.created_at = message.created_at.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReverseTunnel>, I>>(base?: I): ReverseTunnel {
    return ReverseTunnel.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReverseTunnel>, I>>(object: I): ReverseTunnel {
    const message = createBaseReverseTunnel();
    message.name = object.name ?? undefined;
    message.identity = (object.identity !== undefined && object.identity !== null)
      ? TunnelInitiatorIdentity.fromPartial(object.identity)
      : undefined;
    message.created_at = object.created_at ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(ReverseTunnel.$type, ReverseTunnel);

function createBaseTunnelInitiatorIdentity(): TunnelInitiatorIdentity {
  return { $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.TunnelInitiatorIdentity" };
}

export const TunnelInitiatorIdentity: MessageFns<
  TunnelInitiatorIdentity,
  "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.TunnelInitiatorIdentity"
> = {
  $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.TunnelInitiatorIdentity" as const,

  encode(message: TunnelInitiatorIdentity, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tenant_id !== undefined && message.tenant_id !== "") {
      writer.uint32(10).string(message.tenant_id);
    }
    if (message.cluster_id !== undefined && message.cluster_id !== "") {
      writer.uint32(18).string(message.cluster_id);
    }
    if (message.node_id !== undefined && message.node_id !== "") {
      writer.uint32(26).string(message.node_id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TunnelInitiatorIdentity {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTunnelInitiatorIdentity();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tenant_id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cluster_id = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.node_id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TunnelInitiatorIdentity {
    return {
      $type: TunnelInitiatorIdentity.$type,
      tenant_id: isSet(object.tenant_id) ? globalThis.String(object.tenant_id) : undefined,
      cluster_id: isSet(object.cluster_id) ? globalThis.String(object.cluster_id) : undefined,
      node_id: isSet(object.node_id) ? globalThis.String(object.node_id) : undefined,
    };
  },

  toJSON(message: TunnelInitiatorIdentity): unknown {
    const obj: any = {};
    if (message.tenant_id !== undefined) {
      obj.tenant_id = message.tenant_id;
    }
    if (message.cluster_id !== undefined) {
      obj.cluster_id = message.cluster_id;
    }
    if (message.node_id !== undefined) {
      obj.node_id = message.node_id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TunnelInitiatorIdentity>, I>>(base?: I): TunnelInitiatorIdentity {
    return TunnelInitiatorIdentity.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TunnelInitiatorIdentity>, I>>(object: I): TunnelInitiatorIdentity {
    const message = createBaseTunnelInitiatorIdentity();
    message.tenant_id = object.tenant_id ?? undefined;
    message.cluster_id = object.cluster_id ?? undefined;
    message.node_id = object.node_id ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(TunnelInitiatorIdentity.$type, TunnelInitiatorIdentity);

/**
 * ReverseTunnelReportingService allows Envoy instances to report reverse tunnel
 * connection state changes to a management server for monitoring and coordination.
 */
export interface ReverseTunnelReportingService {
  /**
   * Bidirectional stream for reporting reverse tunnel connection state changes.
   * The management server can control reporting intervals and acknowledge received reports.
   */
  StreamReverseTunnels(request: Observable<StreamReverseTunnelsRequest>): Observable<StreamReverseTunnelsResponse>;
}

export const ReverseTunnelReportingServiceServiceName =
  "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.ReverseTunnelReportingService";
export class ReverseTunnelReportingServiceClientImpl implements ReverseTunnelReportingService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || ReverseTunnelReportingServiceServiceName;
    this.rpc = rpc;
    this.StreamReverseTunnels = this.StreamReverseTunnels.bind(this);
  }
  StreamReverseTunnels(request: Observable<StreamReverseTunnelsRequest>): Observable<StreamReverseTunnelsResponse> {
    const data = request.pipe(map((request) => StreamReverseTunnelsRequest.encode(request).finish()));
    const result = this.rpc.bidirectionalStreamingRequest(this.service, "StreamReverseTunnels", data);
    return result.pipe(map((data) => StreamReverseTunnelsResponse.decode(new BinaryReader(data))));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
  clientStreamingRequest(service: string, method: string, data: Observable<Uint8Array>): Promise<Uint8Array>;
  serverStreamingRequest(service: string, method: string, data: Uint8Array): Observable<Uint8Array>;
  bidirectionalStreamingRequest(service: string, method: string, data: Observable<Uint8Array>): Observable<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { $type: "google.protobuf.Timestamp", seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
