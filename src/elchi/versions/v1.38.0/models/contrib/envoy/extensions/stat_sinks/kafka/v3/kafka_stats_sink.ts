// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: contrib/envoy/extensions/stat_sinks/kafka/v3/kafka_stats_sink.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { BoolValue, UInt32Value } from "../../../../../../google/protobuf/wrappers";
import { messageTypeRegistry } from "../../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.stat_sinks.kafka.v3";

/** Serialization format for metrics produced to Kafka. */
export enum SerializationFormat {
  /** JSON - JSON serialization. Metrics are encoded as human-readable JSON objects. */
  JSON = "JSON",
  /**
   * PROTOBUF - Protocol Buffers serialization. Each Kafka message value is a binary-serialized
   * ``envoy.service.metrics.v3.StreamMetricsMessage`` containing
   * ``io.prometheus.client.MetricFamily`` entries -- the same wire format used by
   * the gRPC ``envoy.stat_sinks.metrics_service`` sink.
   */
  PROTOBUF = "PROTOBUF",
}

export function serializationFormatFromJSON(object: any): SerializationFormat {
  switch (object) {
    case 0:
    case "JSON":
      return SerializationFormat.JSON;
    case 1:
    case "PROTOBUF":
      return SerializationFormat.PROTOBUF;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum SerializationFormat");
  }
}

export function serializationFormatToJSON(object: SerializationFormat): string {
  switch (object) {
    case SerializationFormat.JSON:
      return "JSON";
    case SerializationFormat.PROTOBUF:
      return "PROTOBUF";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum SerializationFormat");
  }
}

export function serializationFormatToNumber(object: SerializationFormat): number {
  switch (object) {
    case SerializationFormat.JSON:
      return 0;
    case SerializationFormat.PROTOBUF:
      return 1;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum SerializationFormat");
  }
}

/**
 * Configuration for the Kafka stats sink. Metrics are serialized and
 * produced to a Kafka topic using librdkafka.
 * [#next-free-field: 9]
 */
export interface KafkaStatsSinkConfig {
  $type: "envoy.extensions.stat_sinks.kafka.v3.KafkaStatsSinkConfig";
  /**
   * Comma-separated list of Kafka broker addresses in host:port format.
   * At least one broker must be specified.
   */
  broker_list?:
    | string
    | undefined;
  /** Kafka topic to produce metrics to. */
  topic?:
    | string
    | undefined;
  /**
   * Number of metrics to batch into a single Kafka message. If 0 or unset,
   * all metrics from a single flush are sent in one message. Setting a batch
   * size helps control message sizes when there are many metrics.
   */
  batch_size?:
    | number
    | undefined;
  /**
   * If true, counters are reported as the delta since last flush rather than
   * the absolute cumulative value. Defaults to false.
   */
  report_counters_as_deltas?:
    | boolean
    | undefined;
  /**
   * If true, tag-extracted metric names are used and tags are emitted as
   * separate labels/JSON fields. If false, the full metric name (including tag
   * values) is used. Defaults to true.
   */
  emit_tags_as_labels?:
    | boolean
    | undefined;
  /**
   * Additional librdkafka producer configuration properties as key-value pairs.
   * These are passed directly to librdkafka and can be used to configure
   * compression (``compression.type``), authentication (``security.protocol``,
   * ``sasl.mechanism``, etc.), batching (``batch.num.messages``), and more.
   * See https://github.com/confluentinc/librdkafka/blob/master/CONFIGURATION.md
   */
  producer_config?:
    | Map<string, string>
    | undefined;
  /**
   * Maximum time in milliseconds to buffer messages before forcing a produce.
   * Maps to librdkafka's ``linger.ms``. If not set, defaults to 500ms.
   */
  buffer_flush_timeout_ms?:
    | number
    | undefined;
  /**
   * Serialization format for metric messages produced to Kafka.
   * Defaults to JSON.
   */
  format?: SerializationFormat | undefined;
}

export interface KafkaStatsSinkConfig_ProducerConfigEntry {
  $type: "envoy.extensions.stat_sinks.kafka.v3.KafkaStatsSinkConfig.ProducerConfigEntry";
  key: string;
  value: string;
}

function createBaseKafkaStatsSinkConfig(): KafkaStatsSinkConfig {
  return { $type: "envoy.extensions.stat_sinks.kafka.v3.KafkaStatsSinkConfig" };
}

export const KafkaStatsSinkConfig: MessageFns<
  KafkaStatsSinkConfig,
  "envoy.extensions.stat_sinks.kafka.v3.KafkaStatsSinkConfig"
> = {
  $type: "envoy.extensions.stat_sinks.kafka.v3.KafkaStatsSinkConfig" as const,

  encode(message: KafkaStatsSinkConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.broker_list !== undefined && message.broker_list !== "") {
      writer.uint32(10).string(message.broker_list);
    }
    if (message.topic !== undefined && message.topic !== "") {
      writer.uint32(18).string(message.topic);
    }
    if (message.batch_size !== undefined && message.batch_size !== 0) {
      writer.uint32(24).uint32(message.batch_size);
    }
    if (message.report_counters_as_deltas !== undefined) {
      BoolValue.encode(
        { $type: "google.protobuf.BoolValue", value: message.report_counters_as_deltas! },
        writer.uint32(34).fork(),
      ).join();
    }
    if (message.emit_tags_as_labels !== undefined) {
      BoolValue.encode(
        { $type: "google.protobuf.BoolValue", value: message.emit_tags_as_labels! },
        writer.uint32(42).fork(),
      ).join();
    }
    (message.producer_config || new Map()).forEach((value, key) => {
      KafkaStatsSinkConfig_ProducerConfigEntry.encode({
        $type: "envoy.extensions.stat_sinks.kafka.v3.KafkaStatsSinkConfig.ProducerConfigEntry",
        key: key as any,
        value,
      }, writer.uint32(50).fork()).join();
    });
    if (message.buffer_flush_timeout_ms !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.buffer_flush_timeout_ms! },
        writer.uint32(58).fork(),
      ).join();
    }
    if (message.format !== undefined && message.format !== SerializationFormat.JSON) {
      writer.uint32(64).int32(serializationFormatToNumber(message.format));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KafkaStatsSinkConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKafkaStatsSinkConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.broker_list = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.topic = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.batch_size = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.report_counters_as_deltas = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.emit_tags_as_labels = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          const entry6 = KafkaStatsSinkConfig_ProducerConfigEntry.decode(reader, reader.uint32());
          if (entry6.value !== undefined) {
            if (message.producer_config === undefined) {
              message.producer_config = new Map();
            }
            message.producer_config!.set(entry6.key, entry6.value);
          }
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.buffer_flush_timeout_ms = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.format = serializationFormatFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KafkaStatsSinkConfig {
    return {
      $type: KafkaStatsSinkConfig.$type,
      broker_list: isSet(object.broker_list) ? globalThis.String(object.broker_list) : undefined,
      topic: isSet(object.topic) ? globalThis.String(object.topic) : undefined,
      batch_size: isSet(object.batch_size) ? globalThis.Number(object.batch_size) : undefined,
      report_counters_as_deltas: isSet(object.report_counters_as_deltas)
        ? Boolean(object.report_counters_as_deltas)
        : undefined,
      emit_tags_as_labels: isSet(object.emit_tags_as_labels) ? Boolean(object.emit_tags_as_labels) : undefined,
      producer_config: isObject(object.producer_config)
        ? Object.entries(object.producer_config).reduce<Map<string, string>>((acc, [key, value]) => {
          acc.set(key, String(value));
          return acc;
        }, new Map())
        : undefined,
      buffer_flush_timeout_ms: isSet(object.buffer_flush_timeout_ms)
        ? Number(object.buffer_flush_timeout_ms)
        : undefined,
      format: isSet(object.format) ? serializationFormatFromJSON(object.format) : undefined,
    };
  },

  toJSON(message: KafkaStatsSinkConfig): unknown {
    const obj: any = {};
    if (message.broker_list !== undefined) {
      obj.broker_list = message.broker_list;
    }
    if (message.topic !== undefined) {
      obj.topic = message.topic;
    }
    if (message.batch_size !== undefined) {
      obj.batch_size = Math.round(message.batch_size);
    }
    if (message.report_counters_as_deltas !== undefined) {
      obj.report_counters_as_deltas = message.report_counters_as_deltas;
    }
    if (message.emit_tags_as_labels !== undefined) {
      obj.emit_tags_as_labels = message.emit_tags_as_labels;
    }
    if (message.producer_config?.size) {
      obj.producer_config = {};
      message.producer_config.forEach((v, k) => {
        obj.producer_config[k] = v;
      });
    }
    if (message.buffer_flush_timeout_ms !== undefined) {
      obj.buffer_flush_timeout_ms = message.buffer_flush_timeout_ms;
    }
    if (message.format !== undefined) {
      obj.format = serializationFormatToJSON(message.format);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KafkaStatsSinkConfig>, I>>(base?: I): KafkaStatsSinkConfig {
    return KafkaStatsSinkConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KafkaStatsSinkConfig>, I>>(object: I): KafkaStatsSinkConfig {
    const message = createBaseKafkaStatsSinkConfig();
    message.broker_list = object.broker_list ?? undefined;
    message.topic = object.topic ?? undefined;
    message.batch_size = object.batch_size ?? undefined;
    message.report_counters_as_deltas = object.report_counters_as_deltas ?? undefined;
    message.emit_tags_as_labels = object.emit_tags_as_labels ?? undefined;
    message.producer_config = (object.producer_config === undefined || object.producer_config === null)
      ? undefined
      : (() => {
        const m = new Map();
        (object.producer_config as Map<string, string> ?? new Map()).forEach((value, key) => {
          if (value !== undefined) {
            m.set(key, globalThis.String(value));
          }
        });
        return m;
      })();
    message.buffer_flush_timeout_ms = object.buffer_flush_timeout_ms ?? undefined;
    message.format = object.format ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(KafkaStatsSinkConfig.$type, KafkaStatsSinkConfig);

function createBaseKafkaStatsSinkConfig_ProducerConfigEntry(): KafkaStatsSinkConfig_ProducerConfigEntry {
  return { $type: "envoy.extensions.stat_sinks.kafka.v3.KafkaStatsSinkConfig.ProducerConfigEntry", key: "", value: "" };
}

export const KafkaStatsSinkConfig_ProducerConfigEntry: MessageFns<
  KafkaStatsSinkConfig_ProducerConfigEntry,
  "envoy.extensions.stat_sinks.kafka.v3.KafkaStatsSinkConfig.ProducerConfigEntry"
> = {
  $type: "envoy.extensions.stat_sinks.kafka.v3.KafkaStatsSinkConfig.ProducerConfigEntry" as const,

  encode(message: KafkaStatsSinkConfig_ProducerConfigEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KafkaStatsSinkConfig_ProducerConfigEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKafkaStatsSinkConfig_ProducerConfigEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KafkaStatsSinkConfig_ProducerConfigEntry {
    return {
      $type: KafkaStatsSinkConfig_ProducerConfigEntry.$type,
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: KafkaStatsSinkConfig_ProducerConfigEntry): unknown {
    const obj: any = {};
    if (message.key !== undefined) {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KafkaStatsSinkConfig_ProducerConfigEntry>, I>>(
    base?: I,
  ): KafkaStatsSinkConfig_ProducerConfigEntry {
    return KafkaStatsSinkConfig_ProducerConfigEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KafkaStatsSinkConfig_ProducerConfigEntry>, I>>(
    object: I,
  ): KafkaStatsSinkConfig_ProducerConfigEntry {
    const message = createBaseKafkaStatsSinkConfig_ProducerConfigEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

messageTypeRegistry.set(KafkaStatsSinkConfig_ProducerConfigEntry.$type, KafkaStatsSinkConfig_ProducerConfigEntry);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
