// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: contrib/envoy/extensions/tap_sinks/udp_sink/v3alpha/udp_sink.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { SocketAddress } from "../../../../../../envoy/config/core/v3/address";
import { messageTypeRegistry } from "../../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.tap_sinks.udp_sink.v3alpha";

/** Udp sink configuration. */
export interface UdpSink {
  $type: "envoy.extensions.tap_sinks.udp_sink.v3alpha.UdpSink";
  /** Configure UDP Address. */
  udp_address?: SocketAddress | undefined;
}

function createBaseUdpSink(): UdpSink {
  return { $type: "envoy.extensions.tap_sinks.udp_sink.v3alpha.UdpSink" };
}

export const UdpSink: MessageFns<UdpSink, "envoy.extensions.tap_sinks.udp_sink.v3alpha.UdpSink"> = {
  $type: "envoy.extensions.tap_sinks.udp_sink.v3alpha.UdpSink" as const,

  encode(message: UdpSink, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.udp_address !== undefined) {
      SocketAddress.encode(message.udp_address, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UdpSink {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUdpSink();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.udp_address = SocketAddress.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UdpSink {
    return {
      $type: UdpSink.$type,
      udp_address: isSet(object.udp_address) ? SocketAddress.fromJSON(object.udp_address) : undefined,
    };
  },

  toJSON(message: UdpSink): unknown {
    const obj: any = {};
    if (message.udp_address !== undefined) {
      obj.udp_address = SocketAddress.toJSON(message.udp_address);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UdpSink>, I>>(base?: I): UdpSink {
    return UdpSink.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UdpSink>, I>>(object: I): UdpSink {
    const message = createBaseUdpSink();
    message.udp_address = (object.udp_address !== undefined && object.udp_address !== null)
      ? SocketAddress.fromPartial(object.udp_address)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(UdpSink.$type, UdpSink);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
