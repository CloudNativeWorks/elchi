// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: contrib/envoy/extensions/regex_engines/hyperscan/v3alpha/hyperscan.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.regex_engines.hyperscan.v3alpha";

/**
 * `Hyperscan <https://github.com/intel/hyperscan>`_ regex engine. The engine uses hybrid automata
 * techniques to allow simultaneous matching of large numbers of regular expressions across streams
 * of data.
 *
 * The engine follows PCRE pattern syntax, and the regex string must adhere to the documented
 * `pattern support <https://intel.github.io/hyperscan/dev-reference/compilation.html#pattern-support>`_.
 * The syntax is not compatible with the default RE2 regex engine. Depending on configured
 * expressions, swapping regex engine may cause match rules to no longer be valid.
 */
export interface Hyperscan {
  $type: "envoy.extensions.regex_engines.hyperscan.v3alpha.Hyperscan";
}

function createBaseHyperscan(): Hyperscan {
  return { $type: "envoy.extensions.regex_engines.hyperscan.v3alpha.Hyperscan" };
}

export const Hyperscan: MessageFns<Hyperscan, "envoy.extensions.regex_engines.hyperscan.v3alpha.Hyperscan"> = {
  $type: "envoy.extensions.regex_engines.hyperscan.v3alpha.Hyperscan" as const,

  encode(_: Hyperscan, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Hyperscan {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHyperscan();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): Hyperscan {
    return { $type: Hyperscan.$type };
  },

  toJSON(_: Hyperscan): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<Hyperscan>, I>>(base?: I): Hyperscan {
    return Hyperscan.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Hyperscan>, I>>(_: I): Hyperscan {
    const message = createBaseHyperscan();
    return message;
  },
};

messageTypeRegistry.set(Hyperscan.$type, Hyperscan);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
