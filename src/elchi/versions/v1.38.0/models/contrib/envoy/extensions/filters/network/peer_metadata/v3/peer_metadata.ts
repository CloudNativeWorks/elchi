// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: contrib/envoy/extensions/filters/network/peer_metadata/v3/peer_metadata.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.filters.network.peer_metadata.v3";

/**
 * Configuration for the downstream network filter installed in the
 * connect_originate or inner_connect_originate filter chains. It extracts
 * peer metadata from TCP Proxy filter state (baggage header from HBONE CONNECT
 * response) and injects it as a data preamble to be consumed by the upstream
 * filter.
 */
export interface Config {
  $type: "envoy.extensions.filters.network.peer_metadata.v3.Config";
  /**
   * Filter state key under which the baggage value encoding the proxy workload
   * is stored. The upstream filter that populates the baggage header in the
   * HBONE request should use the same key.
   */
  baggage_key?: string | undefined;
}

/**
 * Configuration for the upstream network filter installed in service clusters
 * that communicate over HBONE. It consumes and removes the metadata preamble
 * injected by the downstream filter and populates filter state for use by
 * Istio telemetry filters.
 */
export interface UpstreamConfig {
  $type: "envoy.extensions.filters.network.peer_metadata.v3.UpstreamConfig";
}

function createBaseConfig(): Config {
  return { $type: "envoy.extensions.filters.network.peer_metadata.v3.Config" };
}

export const Config: MessageFns<Config, "envoy.extensions.filters.network.peer_metadata.v3.Config"> = {
  $type: "envoy.extensions.filters.network.peer_metadata.v3.Config" as const,

  encode(message: Config, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.baggage_key !== undefined && message.baggage_key !== "") {
      writer.uint32(10).string(message.baggage_key);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Config {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.baggage_key = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Config {
    return {
      $type: Config.$type,
      baggage_key: isSet(object.baggage_key) ? globalThis.String(object.baggage_key) : undefined,
    };
  },

  toJSON(message: Config): unknown {
    const obj: any = {};
    if (message.baggage_key !== undefined) {
      obj.baggage_key = message.baggage_key;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Config>, I>>(base?: I): Config {
    return Config.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Config>, I>>(object: I): Config {
    const message = createBaseConfig();
    message.baggage_key = object.baggage_key ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(Config.$type, Config);

function createBaseUpstreamConfig(): UpstreamConfig {
  return { $type: "envoy.extensions.filters.network.peer_metadata.v3.UpstreamConfig" };
}

export const UpstreamConfig: MessageFns<
  UpstreamConfig,
  "envoy.extensions.filters.network.peer_metadata.v3.UpstreamConfig"
> = {
  $type: "envoy.extensions.filters.network.peer_metadata.v3.UpstreamConfig" as const,

  encode(_: UpstreamConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpstreamConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpstreamConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): UpstreamConfig {
    return { $type: UpstreamConfig.$type };
  },

  toJSON(_: UpstreamConfig): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<UpstreamConfig>, I>>(base?: I): UpstreamConfig {
    return UpstreamConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpstreamConfig>, I>>(_: I): UpstreamConfig {
    const message = createBaseUpstreamConfig();
    return message;
  },
};

messageTypeRegistry.set(UpstreamConfig.$type, UpstreamConfig);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
