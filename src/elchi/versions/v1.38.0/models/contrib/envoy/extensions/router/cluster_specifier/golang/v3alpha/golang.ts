// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: contrib/envoy/extensions/router/cluster_specifier/golang/v3alpha/golang.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Any } from "../../../../../../../google/protobuf/any";
import { messageTypeRegistry } from "../../../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.router.cluster_specifier.golang.v3alpha";

/** [#extension-category: envoy.router.cluster_specifier_plugin] */
export interface Config {
  $type: "envoy.extensions.router.cluster_specifier.golang.v3alpha.Config";
  /** Globally unique ID for a dynamic library file. */
  library_id?:
    | string
    | undefined;
  /**
   * Path to a dynamic library implementing the
   * :repo:`ClusterSpecifier API <contrib/golang/router/cluster_specifier/source/go/pkg/api.ClusterSpecifier>`
   * interface.
   * [#comment:TODO(wangfakang): Support for downloading libraries from remote repositories.]
   */
  library_path?:
    | string
    | undefined;
  /**
   * Default cluster.
   *
   * It will be used when the specifier interface return empty string or panic.
   */
  default_cluster?:
    | string
    | undefined;
  /**
   * Configuration for the Go cluster specifier plugin.
   *
   * .. note::
   *     This configuration is only parsed in the go cluster specifier, and is therefore not validated
   *     by Envoy.
   *
   *     See the :repo:`StreamFilter API <contrib/golang/router/cluster_specifier/source/go/pkg/cluster_specifier/config.go>`
   *     for more information about how the plugin's configuration data can be accessed.
   */
  config?: Any | undefined;
}

function createBaseConfig(): Config {
  return { $type: "envoy.extensions.router.cluster_specifier.golang.v3alpha.Config" };
}

export const Config: MessageFns<Config, "envoy.extensions.router.cluster_specifier.golang.v3alpha.Config"> = {
  $type: "envoy.extensions.router.cluster_specifier.golang.v3alpha.Config" as const,

  encode(message: Config, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.library_id !== undefined && message.library_id !== "") {
      writer.uint32(10).string(message.library_id);
    }
    if (message.library_path !== undefined && message.library_path !== "") {
      writer.uint32(18).string(message.library_path);
    }
    if (message.default_cluster !== undefined && message.default_cluster !== "") {
      writer.uint32(26).string(message.default_cluster);
    }
    if (message.config !== undefined) {
      Any.encode(message.config, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Config {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.library_id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.library_path = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.default_cluster = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.config = Any.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Config {
    return {
      $type: Config.$type,
      library_id: isSet(object.library_id) ? globalThis.String(object.library_id) : undefined,
      library_path: isSet(object.library_path) ? globalThis.String(object.library_path) : undefined,
      default_cluster: isSet(object.default_cluster) ? globalThis.String(object.default_cluster) : undefined,
      config: isSet(object.config) ? Any.fromJSON(object.config) : undefined,
    };
  },

  toJSON(message: Config): unknown {
    const obj: any = {};
    if (message.library_id !== undefined) {
      obj.library_id = message.library_id;
    }
    if (message.library_path !== undefined) {
      obj.library_path = message.library_path;
    }
    if (message.default_cluster !== undefined) {
      obj.default_cluster = message.default_cluster;
    }
    if (message.config !== undefined) {
      obj.config = Any.toJSON(message.config);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Config>, I>>(base?: I): Config {
    return Config.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Config>, I>>(object: I): Config {
    const message = createBaseConfig();
    message.library_id = object.library_id ?? undefined;
    message.library_path = object.library_path ?? undefined;
    message.default_cluster = object.default_cluster ?? undefined;
    message.config = (object.config !== undefined && object.config !== null)
      ? Any.fromPartial(object.config)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(Config.$type, Config);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
