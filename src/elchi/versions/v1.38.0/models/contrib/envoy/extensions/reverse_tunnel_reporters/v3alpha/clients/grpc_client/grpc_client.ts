// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: contrib/envoy/extensions/reverse_tunnel_reporters/v3alpha/clients/grpc_client/grpc_client.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../../../../../../../google/protobuf/duration";
import { messageTypeRegistry } from "../../../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client";

/**
 * Configuration for gRPC push-based connection event client.
 * Actively pushes connection events to a cluster using grpc using some internal timing.
 * [#next-free-field: 7]
 */
export interface GrpcClientConfig {
  $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.GrpcClientConfig";
  /** Stat prefix for this client's metrics. */
  stat_prefix?:
    | string
    | undefined;
  /**
   * Name of the cluster to send gRPC requests to.
   * It must be present in the config otherwise the setup will throw error in the onServerInitialized.
   */
  cluster?:
    | string
    | undefined;
  /**
   * Default interval between sending batched connection events.
   * Default is 5s.
   */
  default_send_interval?:
    | Duration
    | undefined;
  /**
   * Interval between connection retry attempts to the gRPC service.
   * Connect timeouts are provided at the cluster level and will be handled by the http/2 client.
   * How much time to wait after a failed connect before retrying. Default is 5s.
   */
  connect_retry_interval?:
    | Duration
    | undefined;
  /**
   * Maximum number of retry attempts for failed gRPC sends.
   * Basically the cluster will have default_send_interval * max_retries time to respond.
   * Default is 5. After this we will disconnect and try to connect again.
   */
  max_retries?:
    | number
    | undefined;
  /**
   * Maximum events to buffer at any given time
   * Default is 1,000,000.
   */
  max_buffer_count?: number | undefined;
}

function createBaseGrpcClientConfig(): GrpcClientConfig {
  return { $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.GrpcClientConfig" };
}

export const GrpcClientConfig: MessageFns<
  GrpcClientConfig,
  "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.GrpcClientConfig"
> = {
  $type: "envoy.extensions.reverse_tunnel_reporters.v3alpha.clients.grpc_client.GrpcClientConfig" as const,

  encode(message: GrpcClientConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stat_prefix !== undefined && message.stat_prefix !== "") {
      writer.uint32(10).string(message.stat_prefix);
    }
    if (message.cluster !== undefined && message.cluster !== "") {
      writer.uint32(18).string(message.cluster);
    }
    if (message.default_send_interval !== undefined) {
      Duration.encode(message.default_send_interval, writer.uint32(26).fork()).join();
    }
    if (message.connect_retry_interval !== undefined) {
      Duration.encode(message.connect_retry_interval, writer.uint32(34).fork()).join();
    }
    if (message.max_retries !== undefined && message.max_retries !== 0) {
      writer.uint32(40).uint32(message.max_retries);
    }
    if (message.max_buffer_count !== undefined && message.max_buffer_count !== 0) {
      writer.uint32(48).uint32(message.max_buffer_count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcClientConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcClientConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stat_prefix = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cluster = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.default_send_interval = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.connect_retry_interval = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.max_retries = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.max_buffer_count = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcClientConfig {
    return {
      $type: GrpcClientConfig.$type,
      stat_prefix: isSet(object.stat_prefix) ? globalThis.String(object.stat_prefix) : undefined,
      cluster: isSet(object.cluster) ? globalThis.String(object.cluster) : undefined,
      default_send_interval: isSet(object.default_send_interval)
        ? Duration.fromJSON(object.default_send_interval)
        : undefined,
      connect_retry_interval: isSet(object.connect_retry_interval)
        ? Duration.fromJSON(object.connect_retry_interval)
        : undefined,
      max_retries: isSet(object.max_retries) ? globalThis.Number(object.max_retries) : undefined,
      max_buffer_count: isSet(object.max_buffer_count) ? globalThis.Number(object.max_buffer_count) : undefined,
    };
  },

  toJSON(message: GrpcClientConfig): unknown {
    const obj: any = {};
    if (message.stat_prefix !== undefined) {
      obj.stat_prefix = message.stat_prefix;
    }
    if (message.cluster !== undefined) {
      obj.cluster = message.cluster;
    }
    if (message.default_send_interval !== undefined) {
      obj.default_send_interval = Duration.toJSON(message.default_send_interval);
    }
    if (message.connect_retry_interval !== undefined) {
      obj.connect_retry_interval = Duration.toJSON(message.connect_retry_interval);
    }
    if (message.max_retries !== undefined) {
      obj.max_retries = Math.round(message.max_retries);
    }
    if (message.max_buffer_count !== undefined) {
      obj.max_buffer_count = Math.round(message.max_buffer_count);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcClientConfig>, I>>(base?: I): GrpcClientConfig {
    return GrpcClientConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcClientConfig>, I>>(object: I): GrpcClientConfig {
    const message = createBaseGrpcClientConfig();
    message.stat_prefix = object.stat_prefix ?? undefined;
    message.cluster = object.cluster ?? undefined;
    message.default_send_interval =
      (object.default_send_interval !== undefined && object.default_send_interval !== null)
        ? Duration.fromPartial(object.default_send_interval)
        : undefined;
    message.connect_retry_interval =
      (object.connect_retry_interval !== undefined && object.connect_retry_interval !== null)
        ? Duration.fromPartial(object.connect_retry_interval)
        : undefined;
    message.max_retries = object.max_retries ?? undefined;
    message.max_buffer_count = object.max_buffer_count ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(GrpcClientConfig.$type, GrpcClientConfig);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
