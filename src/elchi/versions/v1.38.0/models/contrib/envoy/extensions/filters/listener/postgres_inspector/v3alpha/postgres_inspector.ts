// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: contrib/envoy/extensions/filters/listener/postgres_inspector/v3alpha/postgres_inspector.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../../../../../../../google/protobuf/duration";
import { BoolValue, UInt32Value } from "../../../../../../../google/protobuf/wrappers";
import { messageTypeRegistry } from "../../../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.filters.listener.postgres_inspector.v3alpha";

export interface PostgresInspector {
  $type: "envoy.extensions.filters.listener.postgres_inspector.v3alpha.PostgresInspector";
  /**
   * Enable extraction of connection metadata (user, database, application name) from
   * the startup message. This metadata is made available for access logging and stats.
   *
   * Defaults to ``true``.
   */
  enable_metadata_extraction?:
    | boolean
    | undefined;
  /**
   * The maximum size of the startup message that the postgres inspector will accept.
   * Messages larger than this will be rejected. If not specified, defaults to 10KB.
   *
   * PostgreSQL defines MAX_STARTUP_PACKET_LENGTH as 10KB.
   * Valid range is 256 bytes to 10KB.
   */
  max_startup_message_size?:
    | number
    | undefined;
  /**
   * Timeout for the inspector to receive and process the startup message.
   * The timeout starts when the connection is accepted by the listener.
   * If the timeout is reached before the startup message is fully received and processed,
   * the connection will be closed.
   *
   * If not specified, defaults to 10 seconds. Minimum is 1 second.
   */
  startup_timeout?: Duration | undefined;
}

/**
 * StartupMetadata stores connection attributes extracted from the PostgreSQL startup message.
 * This is attached as typed dynamic metadata under the key ``envoy.postgres_inspector``.
 */
export interface StartupMetadata {
  $type: "envoy.extensions.filters.listener.postgres_inspector.v3alpha.StartupMetadata";
  /** The username supplied in the startup message. */
  user?:
    | string
    | undefined;
  /** The database name supplied in the startup message. If not provided, it may default to the user name. */
  database?:
    | string
    | undefined;
  /** The application name supplied in the startup message. */
  application_name?: string | undefined;
}

function createBasePostgresInspector(): PostgresInspector {
  return { $type: "envoy.extensions.filters.listener.postgres_inspector.v3alpha.PostgresInspector" };
}

export const PostgresInspector: MessageFns<
  PostgresInspector,
  "envoy.extensions.filters.listener.postgres_inspector.v3alpha.PostgresInspector"
> = {
  $type: "envoy.extensions.filters.listener.postgres_inspector.v3alpha.PostgresInspector" as const,

  encode(message: PostgresInspector, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enable_metadata_extraction !== undefined) {
      BoolValue.encode(
        { $type: "google.protobuf.BoolValue", value: message.enable_metadata_extraction! },
        writer.uint32(10).fork(),
      ).join();
    }
    if (message.max_startup_message_size !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.max_startup_message_size! },
        writer.uint32(18).fork(),
      ).join();
    }
    if (message.startup_timeout !== undefined) {
      Duration.encode(message.startup_timeout, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PostgresInspector {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePostgresInspector();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.enable_metadata_extraction = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.max_startup_message_size = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.startup_timeout = Duration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PostgresInspector {
    return {
      $type: PostgresInspector.$type,
      enable_metadata_extraction: isSet(object.enable_metadata_extraction)
        ? Boolean(object.enable_metadata_extraction)
        : undefined,
      max_startup_message_size: isSet(object.max_startup_message_size)
        ? Number(object.max_startup_message_size)
        : undefined,
      startup_timeout: isSet(object.startup_timeout) ? Duration.fromJSON(object.startup_timeout) : undefined,
    };
  },

  toJSON(message: PostgresInspector): unknown {
    const obj: any = {};
    if (message.enable_metadata_extraction !== undefined) {
      obj.enable_metadata_extraction = message.enable_metadata_extraction;
    }
    if (message.max_startup_message_size !== undefined) {
      obj.max_startup_message_size = message.max_startup_message_size;
    }
    if (message.startup_timeout !== undefined) {
      obj.startup_timeout = Duration.toJSON(message.startup_timeout);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PostgresInspector>, I>>(base?: I): PostgresInspector {
    return PostgresInspector.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PostgresInspector>, I>>(object: I): PostgresInspector {
    const message = createBasePostgresInspector();
    message.enable_metadata_extraction = object.enable_metadata_extraction ?? undefined;
    message.max_startup_message_size = object.max_startup_message_size ?? undefined;
    message.startup_timeout = (object.startup_timeout !== undefined && object.startup_timeout !== null)
      ? Duration.fromPartial(object.startup_timeout)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(PostgresInspector.$type, PostgresInspector);

function createBaseStartupMetadata(): StartupMetadata {
  return { $type: "envoy.extensions.filters.listener.postgres_inspector.v3alpha.StartupMetadata" };
}

export const StartupMetadata: MessageFns<
  StartupMetadata,
  "envoy.extensions.filters.listener.postgres_inspector.v3alpha.StartupMetadata"
> = {
  $type: "envoy.extensions.filters.listener.postgres_inspector.v3alpha.StartupMetadata" as const,

  encode(message: StartupMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.user !== undefined && message.user !== "") {
      writer.uint32(10).string(message.user);
    }
    if (message.database !== undefined && message.database !== "") {
      writer.uint32(18).string(message.database);
    }
    if (message.application_name !== undefined && message.application_name !== "") {
      writer.uint32(26).string(message.application_name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartupMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartupMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.user = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.database = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.application_name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartupMetadata {
    return {
      $type: StartupMetadata.$type,
      user: isSet(object.user) ? globalThis.String(object.user) : undefined,
      database: isSet(object.database) ? globalThis.String(object.database) : undefined,
      application_name: isSet(object.application_name) ? globalThis.String(object.application_name) : undefined,
    };
  },

  toJSON(message: StartupMetadata): unknown {
    const obj: any = {};
    if (message.user !== undefined) {
      obj.user = message.user;
    }
    if (message.database !== undefined) {
      obj.database = message.database;
    }
    if (message.application_name !== undefined) {
      obj.application_name = message.application_name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StartupMetadata>, I>>(base?: I): StartupMetadata {
    return StartupMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartupMetadata>, I>>(object: I): StartupMetadata {
    const message = createBaseStartupMetadata();
    message.user = object.user ?? undefined;
    message.database = object.database ?? undefined;
    message.application_name = object.application_name ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(StartupMetadata.$type, StartupMetadata);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
