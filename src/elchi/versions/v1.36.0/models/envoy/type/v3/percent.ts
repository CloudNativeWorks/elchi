// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/type/v3/percent.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../typeRegistry";

export const protobufPackage = "envoy.type.v3";

/** Identifies a percentage, in the range [0.0, 100.0]. */
export interface Percent {
  $type: "envoy.type.v3.Percent";
  value?: number | undefined;
}

/**
 * A fractional percentage is used in cases in which for performance reasons performing floating
 * point to integer conversions during randomness calculations is undesirable. The message includes
 * both a numerator and denominator that together determine the final fractional value.
 *
 * * **Example**: 1/100 = 1%.
 * * **Example**: 3/10000 = 0.03%.
 */
export interface FractionalPercent {
  $type: "envoy.type.v3.FractionalPercent";
  /** Specifies the numerator. Defaults to 0. */
  numerator?:
    | number
    | undefined;
  /**
   * Specifies the denominator. If the denominator specified is less than the numerator, the final
   * fractional percentage is capped at 1 (100%).
   */
  denominator?: FractionalPercent_DenominatorType | undefined;
}

/** Fraction percentages support several fixed denominator values. */
export enum FractionalPercent_DenominatorType {
  /**
   * HUNDRED - 100.
   *
   * **Example**: 1/100 = 1%.
   */
  HUNDRED = "HUNDRED",
  /**
   * TEN_THOUSAND - 10,000.
   *
   * **Example**: 1/10000 = 0.01%.
   */
  TEN_THOUSAND = "TEN_THOUSAND",
  /**
   * MILLION - 1,000,000.
   *
   * **Example**: 1/1000000 = 0.0001%.
   */
  MILLION = "MILLION",
}

export function fractionalPercent_DenominatorTypeFromJSON(object: any): FractionalPercent_DenominatorType {
  switch (object) {
    case 0:
    case "HUNDRED":
      return FractionalPercent_DenominatorType.HUNDRED;
    case 1:
    case "TEN_THOUSAND":
      return FractionalPercent_DenominatorType.TEN_THOUSAND;
    case 2:
    case "MILLION":
      return FractionalPercent_DenominatorType.MILLION;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum FractionalPercent_DenominatorType");
  }
}

export function fractionalPercent_DenominatorTypeToJSON(object: FractionalPercent_DenominatorType): string {
  switch (object) {
    case FractionalPercent_DenominatorType.HUNDRED:
      return "HUNDRED";
    case FractionalPercent_DenominatorType.TEN_THOUSAND:
      return "TEN_THOUSAND";
    case FractionalPercent_DenominatorType.MILLION:
      return "MILLION";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum FractionalPercent_DenominatorType");
  }
}

export function fractionalPercent_DenominatorTypeToNumber(object: FractionalPercent_DenominatorType): number {
  switch (object) {
    case FractionalPercent_DenominatorType.HUNDRED:
      return 0;
    case FractionalPercent_DenominatorType.TEN_THOUSAND:
      return 1;
    case FractionalPercent_DenominatorType.MILLION:
      return 2;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum FractionalPercent_DenominatorType");
  }
}

function createBasePercent(): Percent {
  return { $type: "envoy.type.v3.Percent" };
}

export const Percent: MessageFns<Percent, "envoy.type.v3.Percent"> = {
  $type: "envoy.type.v3.Percent" as const,

  encode(message: Percent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== undefined && message.value !== 0) {
      writer.uint32(9).double(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Percent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePercent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.value = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Percent {
    return { $type: Percent.$type, value: isSet(object.value) ? globalThis.Number(object.value) : undefined };
  },

  toJSON(message: Percent): unknown {
    const obj: any = {};
    if (message.value !== undefined) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Percent>, I>>(base?: I): Percent {
    return Percent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Percent>, I>>(object: I): Percent {
    const message = createBasePercent();
    message.value = object.value ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(Percent.$type, Percent);

function createBaseFractionalPercent(): FractionalPercent {
  return { $type: "envoy.type.v3.FractionalPercent" };
}

export const FractionalPercent: MessageFns<FractionalPercent, "envoy.type.v3.FractionalPercent"> = {
  $type: "envoy.type.v3.FractionalPercent" as const,

  encode(message: FractionalPercent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.numerator !== undefined && message.numerator !== 0) {
      writer.uint32(8).uint32(message.numerator);
    }
    if (message.denominator !== undefined && message.denominator !== FractionalPercent_DenominatorType.HUNDRED) {
      writer.uint32(16).int32(fractionalPercent_DenominatorTypeToNumber(message.denominator));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FractionalPercent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFractionalPercent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.numerator = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.denominator = fractionalPercent_DenominatorTypeFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FractionalPercent {
    return {
      $type: FractionalPercent.$type,
      numerator: isSet(object.numerator) ? globalThis.Number(object.numerator) : undefined,
      denominator: isSet(object.denominator)
        ? fractionalPercent_DenominatorTypeFromJSON(object.denominator)
        : undefined,
    };
  },

  toJSON(message: FractionalPercent): unknown {
    const obj: any = {};
    if (message.numerator !== undefined) {
      obj.numerator = Math.round(message.numerator);
    }
    if (message.denominator !== undefined) {
      obj.denominator = fractionalPercent_DenominatorTypeToJSON(message.denominator);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FractionalPercent>, I>>(base?: I): FractionalPercent {
    return FractionalPercent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FractionalPercent>, I>>(object: I): FractionalPercent {
    const message = createBaseFractionalPercent();
    message.numerator = object.numerator ?? undefined;
    message.denominator = object.denominator ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(FractionalPercent.$type, FractionalPercent);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
