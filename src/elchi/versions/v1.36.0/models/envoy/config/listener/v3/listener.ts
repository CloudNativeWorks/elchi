// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/config/listener/v3/listener.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../../../../google/protobuf/duration";
import { BoolValue, UInt32Value } from "../../../../google/protobuf/wrappers";
import { messageTypeRegistry } from "../../../../typeRegistry";
import { CollectionEntry } from "../../../../xds/core/v3/collection_entry";
import { Matcher } from "../../../../xds/type/matcher/v3/matcher";
import { AccessLog } from "../../accesslog/v3/accesslog";
import { Address } from "../../core/v3/address";
import {
  Metadata,
  TrafficDirection,
  trafficDirectionFromJSON,
  trafficDirectionToJSON,
  trafficDirectionToNumber,
} from "../../core/v3/base";
import { ConfigSource } from "../../core/v3/config_source";
import { TypedExtensionConfig } from "../../core/v3/extension";
import { SocketOption, SocketOptionsOverride } from "../../core/v3/socket_option";
import { ApiListener } from "./api_listener";
import { FilterChain, ListenerFilter } from "./listener_components";
import { UdpListenerConfig } from "./udp_listener_config";

export const protobufPackage = "envoy.config.listener.v3";

/** The additional address the listener is listening on. */
export interface AdditionalAddress {
  $type: "envoy.config.listener.v3.AdditionalAddress";
  address?:
    | Address
    | undefined;
  /**
   * Additional socket options that may not be present in Envoy source code or
   * precompiled binaries. If specified, this will override the
   * :ref:`socket_options <envoy_v3_api_field_config.listener.v3.Listener.socket_options>`
   * in the listener. If specified with no
   * :ref:`socket_options <envoy_v3_api_field_config.core.v3.SocketOptionsOverride.socket_options>`
   * or an empty list of :ref:`socket_options <envoy_v3_api_field_config.core.v3.SocketOptionsOverride.socket_options>`,
   * it means no socket option will apply.
   */
  socket_options?: SocketOptionsOverride | undefined;
}

/**
 * Listener list collections. Entries are ``Listener`` resources or references.
 * [#not-implemented-hide:]
 */
export interface ListenerCollection {
  $type: "envoy.config.listener.v3.ListenerCollection";
  entries?: CollectionEntry[] | undefined;
}

/** [#next-free-field: 37] */
export interface Listener {
  $type: "envoy.config.listener.v3.Listener";
  /**
   * The unique name by which this listener is known. If no name is provided,
   * Envoy will allocate an internal UUID for the listener. If the listener is to be dynamically
   * updated or removed via :ref:`LDS <config_listeners_lds>` a unique name must be provided.
   */
  name?:
    | string
    | undefined;
  /**
   * The address that the listener should listen on. In general, the address must be unique, though
   * that is governed by the bind rules of the OS. E.g., multiple listeners can listen on port 0 on
   * Linux as the actual port will be allocated by the OS.
   * Required unless ``api_listener`` or ``listener_specifier`` is populated.
   */
  address?:
    | Address
    | undefined;
  /**
   * The additional addresses the listener should listen on. The addresses must be unique across all
   * listeners. Multiple addresses with port 0 can be supplied. When using multiple addresses in a single listener,
   * all addresses use the same protocol, and multiple internal addresses are not supported.
   */
  additional_addresses?:
    | AdditionalAddress[]
    | undefined;
  /**
   * Optional prefix to use on listener stats. If empty, the stats will be rooted at
   * ``listener.<address as string>.``. If non-empty, stats will be rooted at
   * ``listener.<stat_prefix>.``.
   */
  stat_prefix?:
    | string
    | undefined;
  /**
   * A list of filter chains to consider for this listener. The
   * :ref:`FilterChain <envoy_v3_api_msg_config.listener.v3.FilterChain>` with the most specific
   * :ref:`FilterChainMatch <envoy_v3_api_msg_config.listener.v3.FilterChainMatch>` criteria is used on a
   * connection.
   *
   * Example using SNI for filter chain selection can be found in the
   * :ref:`FAQ entry <faq_how_to_setup_sni>`.
   */
  filter_chains?:
    | FilterChain[]
    | undefined;
  /**
   * Discover filter chains configurations by external service. Dynamic discovery of filter chains is allowed
   * while having statically configured filter chains, however, a filter chain name must be unique within a
   * listener. If a discovered filter chain matches a name of an existing filter chain, it is discarded.
   * [#not-implemented-hide:]
   */
  fcds_config?:
    | Listener_FcdsConfig
    | undefined;
  /**
   * :ref:`Matcher API <arch_overview_matching_listener>` resolving the filter chain name from the
   * network properties. This matcher is used as a replacement for the filter chain match condition
   * :ref:`filter_chain_match
   * <envoy_v3_api_field_config.listener.v3.FilterChain.filter_chain_match>`. If specified, all
   * :ref:`filter_chains <envoy_v3_api_field_config.listener.v3.Listener.filter_chains>` must have a
   * non-empty and unique :ref:`name <envoy_v3_api_field_config.listener.v3.FilterChain.name>` field
   * and not specify :ref:`filter_chain_match
   * <envoy_v3_api_field_config.listener.v3.FilterChain.filter_chain_match>` field.
   *
   * .. note::
   *
   *  Once matched, each connection is permanently bound to its filter chain.
   *  If the matcher changes but the filter chain remains the same, the
   *  connections bound to the filter chain are not drained. If, however, the
   *  filter chain is removed or structurally modified, then the drain for its
   *  connections is initiated.
   */
  filter_chain_matcher?:
    | Matcher
    | undefined;
  /**
   * If a connection is redirected using ``iptables``, the port on which the proxy
   * receives it might be different from the original destination address. When this flag is set to
   * true, the listener hands off redirected connections to the listener associated with the
   * original destination address. If there is no listener associated with the original destination
   * address, the connection is handled by the listener that receives it. Defaults to false.
   */
  use_original_dst?:
    | boolean
    | undefined;
  /**
   * The default filter chain if none of the filter chain matches. If no default filter chain is supplied,
   * the connection will be closed. The filter chain match is ignored in this field.
   */
  default_filter_chain?:
    | FilterChain
    | undefined;
  /**
   * Soft limit on size of the listener’s new connection read and write buffers.
   * If unspecified, an implementation defined default is applied (1MiB).
   */
  per_connection_buffer_limit_bytes?:
    | number
    | undefined;
  /** Listener metadata. */
  metadata?:
    | Metadata
    | undefined;
  /**
   * [#not-implemented-hide:]
   *
   * @deprecated
   */
  deprecated_v1?:
    | Listener_DeprecatedV1
    | undefined;
  /** The type of draining to perform at a listener-wide level. */
  drain_type?:
    | Listener_DrainType
    | undefined;
  /**
   * Listener filters have the opportunity to manipulate and augment the connection metadata that
   * is used in connection filter chain matching, for example. These filters are run before any in
   * :ref:`filter_chains <envoy_v3_api_field_config.listener.v3.Listener.filter_chains>`. Order matters as the
   * filters are processed sequentially right after a socket has been accepted by the listener, and
   * before a connection is created.
   * UDP Listener filters can be specified when the protocol in the listener socket address in
   * :ref:`protocol <envoy_v3_api_field_config.core.v3.SocketAddress.protocol>` is :ref:`UDP
   * <envoy_v3_api_enum_value_config.core.v3.SocketAddress.Protocol.UDP>` and no
   * :ref:`quic_options <envoy_v3_api_field_config.listener.v3.UdpListenerConfig.quic_options>` is specified in :ref:`udp_listener_config <envoy_v3_api_field_config.listener.v3.Listener.udp_listener_config>`.
   * QUIC listener filters can be specified when :ref:`quic_options
   * <envoy_v3_api_field_config.listener.v3.UdpListenerConfig.quic_options>` is
   * specified in :ref:`udp_listener_config <envoy_v3_api_field_config.listener.v3.Listener.udp_listener_config>`.
   * They are processed sequentially right before connection creation. And like TCP Listener filters, they can be used to manipulate the connection metadata and socket. But the difference is that they can't be used to pause connection creation.
   */
  listener_filters?:
    | ListenerFilter[]
    | undefined;
  /**
   * The timeout to wait for all listener filters to complete operation. If the timeout is reached,
   * the accepted socket is closed without a connection being created unless
   * ``continue_on_listener_filters_timeout`` is set to true. Specify 0 to disable the
   * timeout. If not specified, a default timeout of 15s is used.
   */
  listener_filters_timeout?:
    | Duration
    | undefined;
  /**
   * Whether a connection should be created when listener filters timeout. Default is false.
   *
   * .. attention::
   *
   *   Some listener filters, such as :ref:`Proxy Protocol filter
   *   <config_listener_filters_proxy_protocol>`, should not be used with this option. It will cause
   *   unexpected behavior when a connection is created.
   */
  continue_on_listener_filters_timeout?:
    | boolean
    | undefined;
  /**
   * Whether the listener should be set as a transparent socket.
   * When this flag is set to true, connections can be redirected to the listener using an
   * ``iptables`` ``TPROXY`` target, in which case the original source and destination addresses and
   * ports are preserved on accepted connections. This flag should be used in combination with
   * :ref:`an original_dst <config_listener_filters_original_dst>` :ref:`listener filter
   * <envoy_v3_api_field_config.listener.v3.Listener.listener_filters>` to mark the connections' local addresses as
   * "restored." This can be used to hand off each redirected connection to another listener
   * associated with the connection's destination address. Direct connections to the socket without
   * using ``TPROXY`` cannot be distinguished from connections redirected using ``TPROXY`` and are
   * therefore treated as if they were redirected.
   * When this flag is set to false, the listener's socket is explicitly reset as non-transparent.
   * Setting this flag requires Envoy to run with the ``CAP_NET_ADMIN`` capability.
   * When this flag is not set (default), the socket is not modified, i.e. the transparent option
   * is neither set nor reset.
   */
  transparent?:
    | boolean
    | undefined;
  /**
   * Whether the listener should set the ``IP_FREEBIND`` socket option. When this
   * flag is set to true, listeners can be bound to an IP address that is not
   * configured on the system running Envoy. When this flag is set to false, the
   * option ``IP_FREEBIND`` is disabled on the socket. When this flag is not set
   * (default), the socket is not modified, i.e. the option is neither enabled
   * nor disabled.
   */
  freebind?:
    | boolean
    | undefined;
  /**
   * Additional socket options that may not be present in Envoy source code or
   * precompiled binaries.
   * It is not allowed to update the socket options for any existing address if
   * :ref:`enable_reuse_port <envoy_v3_api_field_config.listener.v3.Listener.enable_reuse_port>`
   * is ``false`` to avoid the conflict when creating new sockets for the listener.
   */
  socket_options?:
    | SocketOption[]
    | undefined;
  /**
   * Whether the listener should accept TCP Fast Open (TFO) connections.
   * When this flag is set to a value greater than 0, the option TCP_FASTOPEN is enabled on
   * the socket, with a queue length of the specified size
   * (see `details in RFC7413 <https://tools.ietf.org/html/rfc7413#section-5.1>`_).
   * When this flag is set to 0, the option TCP_FASTOPEN is disabled on the socket.
   * When this flag is not set (default), the socket is not modified,
   * i.e. the option is neither enabled nor disabled.
   *
   * On Linux, the net.ipv4.tcp_fastopen kernel parameter must include flag 0x2 to enable
   * TCP_FASTOPEN.
   * See `ip-sysctl.txt <https://www.kernel.org/doc/Documentation/networking/ip-sysctl.txt>`_.
   *
   * On macOS, only values of 0, 1, and unset are valid; other values may result in an error.
   * To set the queue length on macOS, set the net.inet.tcp.fastopen_backlog kernel parameter.
   */
  tcp_fast_open_queue_length?:
    | number
    | undefined;
  /**
   * Specifies the intended direction of the traffic relative to the local Envoy.
   * This property is required on Windows for listeners using the original destination filter,
   * see :ref:`Original Destination <config_listener_filters_original_dst>`.
   */
  traffic_direction?:
    | TrafficDirection
    | undefined;
  /**
   * If the protocol in the listener socket address in :ref:`protocol
   * <envoy_v3_api_field_config.core.v3.SocketAddress.protocol>` is :ref:`UDP
   * <envoy_v3_api_enum_value_config.core.v3.SocketAddress.Protocol.UDP>`, this field specifies UDP
   * listener specific configuration.
   */
  udp_listener_config?:
    | UdpListenerConfig
    | undefined;
  /**
   * Used to represent an API listener, which is used in non-proxy clients. The type of API
   * exposed to the non-proxy application depends on the type of API listener.
   * When this field is set, no other field except for :ref:`name<envoy_v3_api_field_config.listener.v3.Listener.name>`
   * should be set.
   *
   * .. note::
   *
   *  Currently only one ApiListener can be installed; and it can only be done via bootstrap config,
   *  not LDS.
   *
   * [#next-major-version: In the v3 API, instead of this messy approach where the socket
   * listener fields are directly in the top-level Listener message and the API listener types
   * are in the ApiListener message, the socket listener messages should be in their own message,
   * and the top-level Listener should essentially be a oneof that selects between the
   * socket listener and the various types of API listener. That way, a given Listener message
   * can structurally only contain the fields of the relevant type.]
   */
  api_listener?:
    | ApiListener
    | undefined;
  /**
   * The listener's connection balancer configuration, currently only applicable to TCP listeners.
   * If no configuration is specified, Envoy will not attempt to balance active connections between
   * worker threads.
   *
   * In the scenario that the listener X redirects all the connections to the listeners Y1 and Y2
   * by setting :ref:`use_original_dst <envoy_v3_api_field_config.listener.v3.Listener.use_original_dst>` in X
   * and :ref:`bind_to_port <envoy_v3_api_field_config.listener.v3.Listener.bind_to_port>` to false in Y1 and Y2,
   * it is recommended to disable the balance config in listener X to avoid the cost of balancing, and
   * enable the balance config in Y1 and Y2 to balance the connections among the workers.
   */
  connection_balance_config?:
    | Listener_ConnectionBalanceConfig
    | undefined;
  /**
   * Deprecated. Use ``enable_reuse_port`` instead.
   *
   * @deprecated
   */
  reuse_port?:
    | boolean
    | undefined;
  /**
   * When this flag is set to true, listeners set the ``SO_REUSEPORT`` socket option and
   * create one socket for each worker thread. This makes inbound connections
   * distribute among worker threads roughly evenly in cases where there are a high number
   * of connections. When this flag is set to false, all worker threads share one socket. This field
   * defaults to true. The change of field will be rejected during an listener update when the
   * runtime flag ``envoy.reloadable_features.enable_update_listener_socket_options`` is enabled.
   * Otherwise, the update of this field will be ignored quietly.
   *
   * .. attention::
   *
   *   Although this field defaults to true, it has different behavior on different platforms. See
   *   the following text for more information.
   *
   * * On Linux, reuse_port is respected for both TCP and UDP listeners. It also works correctly
   *   with hot restart.
   * * On macOS, reuse_port for TCP does not do what it does on Linux. Instead of load balancing,
   *   the last socket wins and receives all connections/packets. For TCP, reuse_port is force
   *   disabled and the user is warned. For UDP, it is enabled, but only one worker will receive
   *   packets. For QUIC/H3, SW routing will send packets to other workers. For "raw" UDP, only
   *   a single worker will currently receive packets.
   * * On Windows, reuse_port for TCP has undefined behavior. It is force disabled and the user
   *   is warned similar to macOS. It is left enabled for UDP with undefined behavior currently.
   */
  enable_reuse_port?:
    | boolean
    | undefined;
  /**
   * Configuration for :ref:`access logs <arch_overview_access_logs>`
   * emitted by this listener.
   */
  access_log?:
    | AccessLog[]
    | undefined;
  /**
   * The maximum length a tcp listener's pending connections queue can grow to. If no value is
   * provided net.core.somaxconn will be used on Linux and 128 otherwise.
   */
  tcp_backlog_size?:
    | number
    | undefined;
  /**
   * The maximum number of connections to accept from the kernel per socket
   * event. Envoy may decide to close these connections after accepting them
   * from the kernel e.g. due to load shedding, or other policies.
   * If there are more than max_connections_to_accept_per_socket_event
   * connections pending accept, connections over this threshold will be
   * accepted in later event loop iterations.
   * If no value is provided Envoy will accept all connections pending accept
   * from the kernel.
   *
   * .. note::
   *
   *  It is recommended to lower this value for better overload management and reduced per-event cost.
   *  Setting it to 1 is a viable option with no noticeable impact on performance.
   */
  max_connections_to_accept_per_socket_event?:
    | number
    | undefined;
  /**
   * Whether the listener should bind to the port. A listener that doesn't
   * bind can only receive connections redirected from other listeners that set
   * :ref:`use_original_dst <envoy_v3_api_field_config.listener.v3.Listener.use_original_dst>`
   * to true. Default is true.
   */
  bind_to_port?:
    | boolean
    | undefined;
  /** The exclusive listener type and the corresponding config. */
  listener_specifier?:
    | //
    /**
     * Used to represent an internal listener which does not listen on OSI L4 address but can be used by the
     * :ref:`envoy cluster <envoy_v3_api_msg_config.cluster.v3.Cluster>` to create a user space connection to.
     * The internal listener acts as a TCP listener. It supports listener filters and network filter chains.
     * Upstream clusters refer to the internal listeners by their :ref:`name
     * <envoy_v3_api_field_config.listener.v3.Listener.name>`. :ref:`Address
     * <envoy_v3_api_field_config.listener.v3.Listener.address>` must not be set on the internal listeners.
     *
     * There are some limitations that are derived from the implementation. The known limitations include:
     *
     * * :ref:`ConnectionBalanceConfig <envoy_v3_api_msg_config.listener.v3.Listener.ConnectionBalanceConfig>` is not
     *   allowed because both the cluster connection and the listener connection must be owned by the same dispatcher.
     * * :ref:`tcp_backlog_size <envoy_v3_api_field_config.listener.v3.Listener.tcp_backlog_size>`
     * * :ref:`freebind <envoy_v3_api_field_config.listener.v3.Listener.freebind>`
     * * :ref:`transparent <envoy_v3_api_field_config.listener.v3.Listener.transparent>`
     */
    { $case: "internal_listener"; internal_listener: Listener_InternalListenerConfig }
    | undefined;
  /**
   * Enable MPTCP (multi-path TCP) on this listener. Clients will be allowed to establish
   * MPTCP connections. Non-MPTCP clients will fall back to regular TCP.
   */
  enable_mptcp?:
    | boolean
    | undefined;
  /**
   * Whether the listener should limit connections based upon the value of
   * :ref:`global_downstream_max_connections <config_overload_manager_limiting_connections>`.
   */
  ignore_global_conn_limit?:
    | boolean
    | undefined;
  /** Whether the listener bypasses configured overload manager actions. */
  bypass_overload_manager?: boolean | undefined;
}

export enum Listener_DrainType {
  /**
   * DEFAULT - Drain in response to calling /healthcheck/fail admin endpoint (along with the health check
   * filter), listener removal/modification, and hot restart.
   */
  DEFAULT = "DEFAULT",
  /**
   * MODIFY_ONLY - Drain in response to listener removal/modification and hot restart. This setting does not
   * include /healthcheck/fail. This setting may be desirable if Envoy is hosting both ingress
   * and egress listeners.
   */
  MODIFY_ONLY = "MODIFY_ONLY",
}

export function listener_DrainTypeFromJSON(object: any): Listener_DrainType {
  switch (object) {
    case 0:
    case "DEFAULT":
      return Listener_DrainType.DEFAULT;
    case 1:
    case "MODIFY_ONLY":
      return Listener_DrainType.MODIFY_ONLY;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Listener_DrainType");
  }
}

export function listener_DrainTypeToJSON(object: Listener_DrainType): string {
  switch (object) {
    case Listener_DrainType.DEFAULT:
      return "DEFAULT";
    case Listener_DrainType.MODIFY_ONLY:
      return "MODIFY_ONLY";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Listener_DrainType");
  }
}

export function listener_DrainTypeToNumber(object: Listener_DrainType): number {
  switch (object) {
    case Listener_DrainType.DEFAULT:
      return 0;
    case Listener_DrainType.MODIFY_ONLY:
      return 1;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Listener_DrainType");
  }
}

/** [#not-implemented-hide:] */
export interface Listener_DeprecatedV1 {
  $type: "envoy.config.listener.v3.Listener.DeprecatedV1";
  /**
   * Whether the listener should bind to the port. A listener that doesn't
   * bind can only receive connections redirected from other listeners that
   * set use_original_dst parameter to true. Default is true.
   *
   * This is deprecated. Use :ref:`Listener.bind_to_port
   * <envoy_v3_api_field_config.listener.v3.Listener.bind_to_port>`
   */
  bind_to_port?: boolean | undefined;
}

/** Configuration for listener connection balancing. */
export interface Listener_ConnectionBalanceConfig {
  $type: "envoy.config.listener.v3.Listener.ConnectionBalanceConfig";
  balance_type?:
    | //
    /** If specified, the listener will use the exact connection balancer. */
    { $case: "exact_balance"; exact_balance: Listener_ConnectionBalanceConfig_ExactBalance }
    | //
    /**
     * The listener will use the connection balancer according to ``type_url``. If ``type_url`` is invalid,
     * Envoy will not attempt to balance active connections between worker threads.
     * [#extension-category: envoy.network.connection_balance]
     */
    { $case: "extend_balance"; extend_balance: TypedExtensionConfig }
    | undefined;
}

/**
 * A connection balancer implementation that does exact balancing. This means that a lock is
 * held during balancing so that connection counts are nearly exactly balanced between worker
 * threads. This is "nearly" exact in the sense that a connection might close in parallel thus
 * making the counts incorrect, but this should be rectified on the next accept. This balancer
 * sacrifices accept throughput for accuracy and should be used when there are a small number of
 * connections that rarely cycle (e.g., service mesh gRPC egress).
 */
export interface Listener_ConnectionBalanceConfig_ExactBalance {
  $type: "envoy.config.listener.v3.Listener.ConnectionBalanceConfig.ExactBalance";
}

/** Configuration for envoy internal listener. All the future internal listener features should be added here. */
export interface Listener_InternalListenerConfig {
  $type: "envoy.config.listener.v3.Listener.InternalListenerConfig";
}

/**
 * Configuration for filter chains discovery.
 * [#not-implemented-hide:]
 */
export interface Listener_FcdsConfig {
  $type: "envoy.config.listener.v3.Listener.FcdsConfig";
  /**
   * Optional name to present to the filter chain discovery service. This may be an arbitrary name with arbitrary
   * length. If a name is not provided, the listener's name is used. Refer to :ref:`filter_chains <envoy_v3_api_field_config.listener.v3.Listener.name>`.
   * for details on how listener name is determined if unspecified. In addition, this may be a xdstp:// URL.
   */
  name?:
    | string
    | undefined;
  /**
   * Configuration for the source of FCDS updates for this listener.
   * .. note::
   *   This discovery service only supports ``AGGREGATED_GRPC`` API type.
   */
  config_source?: ConfigSource | undefined;
}

/**
 * A placeholder proto so that users can explicitly configure the standard
 * Listener Manager via the bootstrap's :ref:`listener_manager <envoy_v3_api_field_config.bootstrap.v3.Bootstrap.listener_manager>`.
 * [#not-implemented-hide:]
 */
export interface ListenerManager {
  $type: "envoy.config.listener.v3.ListenerManager";
}

/**
 * A placeholder proto so that users can explicitly configure the standard
 * Validation Listener Manager via the bootstrap's :ref:`listener_manager <envoy_v3_api_field_config.bootstrap.v3.Bootstrap.listener_manager>`.
 * [#not-implemented-hide:]
 */
export interface ValidationListenerManager {
  $type: "envoy.config.listener.v3.ValidationListenerManager";
}

/**
 * A placeholder proto so that users can explicitly configure the API
 * Listener Manager via the bootstrap's :ref:`listener_manager <envoy_v3_api_field_config.bootstrap.v3.Bootstrap.listener_manager>`.
 * [#not-implemented-hide:]
 */
export interface ApiListenerManager {
  $type: "envoy.config.listener.v3.ApiListenerManager";
}

function createBaseAdditionalAddress(): AdditionalAddress {
  return { $type: "envoy.config.listener.v3.AdditionalAddress" };
}

export const AdditionalAddress: MessageFns<AdditionalAddress, "envoy.config.listener.v3.AdditionalAddress"> = {
  $type: "envoy.config.listener.v3.AdditionalAddress" as const,

  encode(message: AdditionalAddress, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== undefined) {
      Address.encode(message.address, writer.uint32(10).fork()).join();
    }
    if (message.socket_options !== undefined) {
      SocketOptionsOverride.encode(message.socket_options, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AdditionalAddress {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAdditionalAddress();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = Address.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.socket_options = SocketOptionsOverride.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AdditionalAddress {
    return {
      $type: AdditionalAddress.$type,
      address: isSet(object.address) ? Address.fromJSON(object.address) : undefined,
      socket_options: isSet(object.socket_options) ? SocketOptionsOverride.fromJSON(object.socket_options) : undefined,
    };
  },

  toJSON(message: AdditionalAddress): unknown {
    const obj: any = {};
    if (message.address !== undefined) {
      obj.address = Address.toJSON(message.address);
    }
    if (message.socket_options !== undefined) {
      obj.socket_options = SocketOptionsOverride.toJSON(message.socket_options);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AdditionalAddress>, I>>(base?: I): AdditionalAddress {
    return AdditionalAddress.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AdditionalAddress>, I>>(object: I): AdditionalAddress {
    const message = createBaseAdditionalAddress();
    message.address = (object.address !== undefined && object.address !== null)
      ? Address.fromPartial(object.address)
      : undefined;
    message.socket_options = (object.socket_options !== undefined && object.socket_options !== null)
      ? SocketOptionsOverride.fromPartial(object.socket_options)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(AdditionalAddress.$type, AdditionalAddress);

function createBaseListenerCollection(): ListenerCollection {
  return { $type: "envoy.config.listener.v3.ListenerCollection" };
}

export const ListenerCollection: MessageFns<ListenerCollection, "envoy.config.listener.v3.ListenerCollection"> = {
  $type: "envoy.config.listener.v3.ListenerCollection" as const,

  encode(message: ListenerCollection, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.entries !== undefined && message.entries.length !== 0) {
      for (const v of message.entries) {
        CollectionEntry.encode(v!, writer.uint32(10).fork()).join();
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListenerCollection {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListenerCollection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          if (message.entries === undefined) {
            message.entries = [];
          }
          const el = CollectionEntry.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.entries!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListenerCollection {
    return {
      $type: ListenerCollection.$type,
      entries: globalThis.Array.isArray(object?.entries)
        ? object.entries.map((e: any) => CollectionEntry.fromJSON(e))
        : undefined,
    };
  },

  toJSON(message: ListenerCollection): unknown {
    const obj: any = {};
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => CollectionEntry.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListenerCollection>, I>>(base?: I): ListenerCollection {
    return ListenerCollection.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListenerCollection>, I>>(object: I): ListenerCollection {
    const message = createBaseListenerCollection();
    message.entries = object.entries?.map((e) => CollectionEntry.fromPartial(e)) || undefined;
    return message;
  },
};

messageTypeRegistry.set(ListenerCollection.$type, ListenerCollection);

function createBaseListener(): Listener {
  return { $type: "envoy.config.listener.v3.Listener", listener_specifier: undefined };
}

export const Listener: MessageFns<Listener, "envoy.config.listener.v3.Listener"> = {
  $type: "envoy.config.listener.v3.Listener" as const,

  encode(message: Listener, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.address !== undefined) {
      Address.encode(message.address, writer.uint32(18).fork()).join();
    }
    if (message.additional_addresses !== undefined && message.additional_addresses.length !== 0) {
      for (const v of message.additional_addresses) {
        AdditionalAddress.encode(v!, writer.uint32(266).fork()).join();
      }
    }
    if (message.stat_prefix !== undefined && message.stat_prefix !== "") {
      writer.uint32(226).string(message.stat_prefix);
    }
    if (message.filter_chains !== undefined && message.filter_chains.length !== 0) {
      for (const v of message.filter_chains) {
        FilterChain.encode(v!, writer.uint32(26).fork()).join();
      }
    }
    if (message.fcds_config !== undefined) {
      Listener_FcdsConfig.encode(message.fcds_config, writer.uint32(290).fork()).join();
    }
    if (message.filter_chain_matcher !== undefined) {
      Matcher.encode(message.filter_chain_matcher, writer.uint32(258).fork()).join();
    }
    if (message.use_original_dst !== undefined) {
      BoolValue.encode(
        { $type: "google.protobuf.BoolValue", value: message.use_original_dst! },
        writer.uint32(34).fork(),
      ).join();
    }
    if (message.default_filter_chain !== undefined) {
      FilterChain.encode(message.default_filter_chain, writer.uint32(202).fork()).join();
    }
    if (message.per_connection_buffer_limit_bytes !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.per_connection_buffer_limit_bytes! },
        writer.uint32(42).fork(),
      ).join();
    }
    if (message.metadata !== undefined) {
      Metadata.encode(message.metadata, writer.uint32(50).fork()).join();
    }
    if (message.deprecated_v1 !== undefined) {
      Listener_DeprecatedV1.encode(message.deprecated_v1, writer.uint32(58).fork()).join();
    }
    if (message.drain_type !== undefined && message.drain_type !== Listener_DrainType.DEFAULT) {
      writer.uint32(64).int32(listener_DrainTypeToNumber(message.drain_type));
    }
    if (message.listener_filters !== undefined && message.listener_filters.length !== 0) {
      for (const v of message.listener_filters) {
        ListenerFilter.encode(v!, writer.uint32(74).fork()).join();
      }
    }
    if (message.listener_filters_timeout !== undefined) {
      Duration.encode(message.listener_filters_timeout, writer.uint32(122).fork()).join();
    }
    if (
      message.continue_on_listener_filters_timeout !== undefined &&
      message.continue_on_listener_filters_timeout !== false
    ) {
      writer.uint32(136).bool(message.continue_on_listener_filters_timeout);
    }
    if (message.transparent !== undefined) {
      BoolValue.encode({ $type: "google.protobuf.BoolValue", value: message.transparent! }, writer.uint32(82).fork())
        .join();
    }
    if (message.freebind !== undefined) {
      BoolValue.encode({ $type: "google.protobuf.BoolValue", value: message.freebind! }, writer.uint32(90).fork())
        .join();
    }
    if (message.socket_options !== undefined && message.socket_options.length !== 0) {
      for (const v of message.socket_options) {
        SocketOption.encode(v!, writer.uint32(106).fork()).join();
      }
    }
    if (message.tcp_fast_open_queue_length !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.tcp_fast_open_queue_length! },
        writer.uint32(98).fork(),
      ).join();
    }
    if (message.traffic_direction !== undefined && message.traffic_direction !== TrafficDirection.UNSPECIFIED) {
      writer.uint32(128).int32(trafficDirectionToNumber(message.traffic_direction));
    }
    if (message.udp_listener_config !== undefined) {
      UdpListenerConfig.encode(message.udp_listener_config, writer.uint32(146).fork()).join();
    }
    if (message.api_listener !== undefined) {
      ApiListener.encode(message.api_listener, writer.uint32(154).fork()).join();
    }
    if (message.connection_balance_config !== undefined) {
      Listener_ConnectionBalanceConfig.encode(message.connection_balance_config, writer.uint32(162).fork()).join();
    }
    if (message.reuse_port !== undefined && message.reuse_port !== false) {
      writer.uint32(168).bool(message.reuse_port);
    }
    if (message.enable_reuse_port !== undefined) {
      BoolValue.encode(
        { $type: "google.protobuf.BoolValue", value: message.enable_reuse_port! },
        writer.uint32(234).fork(),
      ).join();
    }
    if (message.access_log !== undefined && message.access_log.length !== 0) {
      for (const v of message.access_log) {
        AccessLog.encode(v!, writer.uint32(178).fork()).join();
      }
    }
    if (message.tcp_backlog_size !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.tcp_backlog_size! },
        writer.uint32(194).fork(),
      ).join();
    }
    if (message.max_connections_to_accept_per_socket_event !== undefined) {
      UInt32Value.encode({
        $type: "google.protobuf.UInt32Value",
        value: message.max_connections_to_accept_per_socket_event!,
      }, writer.uint32(274).fork()).join();
    }
    if (message.bind_to_port !== undefined) {
      BoolValue.encode({ $type: "google.protobuf.BoolValue", value: message.bind_to_port! }, writer.uint32(210).fork())
        .join();
    }
    switch (message.listener_specifier?.$case) {
      case "internal_listener":
        Listener_InternalListenerConfig.encode(message.listener_specifier.internal_listener, writer.uint32(218).fork())
          .join();
        break;
    }
    if (message.enable_mptcp !== undefined && message.enable_mptcp !== false) {
      writer.uint32(240).bool(message.enable_mptcp);
    }
    if (message.ignore_global_conn_limit !== undefined && message.ignore_global_conn_limit !== false) {
      writer.uint32(248).bool(message.ignore_global_conn_limit);
    }
    if (message.bypass_overload_manager !== undefined && message.bypass_overload_manager !== false) {
      writer.uint32(280).bool(message.bypass_overload_manager);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Listener {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListener();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.address = Address.decode(reader, reader.uint32());
          continue;
        }
        case 33: {
          if (tag !== 266) {
            break;
          }

          if (message.additional_addresses === undefined) {
            message.additional_addresses = [];
          }
          const el = AdditionalAddress.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.additional_addresses!.push(el);
          }
          continue;
        }
        case 28: {
          if (tag !== 226) {
            break;
          }

          message.stat_prefix = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          if (message.filter_chains === undefined) {
            message.filter_chains = [];
          }
          const el = FilterChain.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.filter_chains!.push(el);
          }
          continue;
        }
        case 36: {
          if (tag !== 290) {
            break;
          }

          message.fcds_config = Listener_FcdsConfig.decode(reader, reader.uint32());
          continue;
        }
        case 32: {
          if (tag !== 258) {
            break;
          }

          message.filter_chain_matcher = Matcher.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.use_original_dst = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 25: {
          if (tag !== 202) {
            break;
          }

          message.default_filter_chain = FilterChain.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.per_connection_buffer_limit_bytes = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.metadata = Metadata.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.deprecated_v1 = Listener_DeprecatedV1.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.drain_type = listener_DrainTypeFromJSON(reader.int32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          if (message.listener_filters === undefined) {
            message.listener_filters = [];
          }
          const el = ListenerFilter.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.listener_filters!.push(el);
          }
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.listener_filters_timeout = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 17: {
          if (tag !== 136) {
            break;
          }

          message.continue_on_listener_filters_timeout = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.transparent = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.freebind = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          if (message.socket_options === undefined) {
            message.socket_options = [];
          }
          const el = SocketOption.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.socket_options!.push(el);
          }
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.tcp_fast_open_queue_length = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 16: {
          if (tag !== 128) {
            break;
          }

          message.traffic_direction = trafficDirectionFromJSON(reader.int32());
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.udp_listener_config = UdpListenerConfig.decode(reader, reader.uint32());
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.api_listener = ApiListener.decode(reader, reader.uint32());
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          message.connection_balance_config = Listener_ConnectionBalanceConfig.decode(reader, reader.uint32());
          continue;
        }
        case 21: {
          if (tag !== 168) {
            break;
          }

          message.reuse_port = reader.bool();
          continue;
        }
        case 29: {
          if (tag !== 234) {
            break;
          }

          message.enable_reuse_port = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 22: {
          if (tag !== 178) {
            break;
          }

          if (message.access_log === undefined) {
            message.access_log = [];
          }
          const el = AccessLog.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.access_log!.push(el);
          }
          continue;
        }
        case 24: {
          if (tag !== 194) {
            break;
          }

          message.tcp_backlog_size = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 34: {
          if (tag !== 274) {
            break;
          }

          message.max_connections_to_accept_per_socket_event = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 26: {
          if (tag !== 210) {
            break;
          }

          message.bind_to_port = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 27: {
          if (tag !== 218) {
            break;
          }

          message.listener_specifier = {
            $case: "internal_listener",
            internal_listener: Listener_InternalListenerConfig.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 30: {
          if (tag !== 240) {
            break;
          }

          message.enable_mptcp = reader.bool();
          continue;
        }
        case 31: {
          if (tag !== 248) {
            break;
          }

          message.ignore_global_conn_limit = reader.bool();
          continue;
        }
        case 35: {
          if (tag !== 280) {
            break;
          }

          message.bypass_overload_manager = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Listener {
    return {
      $type: Listener.$type,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      address: isSet(object.address) ? Address.fromJSON(object.address) : undefined,
      additional_addresses: globalThis.Array.isArray(object?.additional_addresses)
        ? object.additional_addresses.map((e: any) => AdditionalAddress.fromJSON(e))
        : undefined,
      stat_prefix: isSet(object.stat_prefix) ? globalThis.String(object.stat_prefix) : undefined,
      filter_chains: globalThis.Array.isArray(object?.filter_chains)
        ? object.filter_chains.map((e: any) => FilterChain.fromJSON(e))
        : undefined,
      fcds_config: isSet(object.fcds_config) ? Listener_FcdsConfig.fromJSON(object.fcds_config) : undefined,
      filter_chain_matcher: isSet(object.filter_chain_matcher)
        ? Matcher.fromJSON(object.filter_chain_matcher)
        : undefined,
      use_original_dst: isSet(object.use_original_dst) ? Boolean(object.use_original_dst) : undefined,
      default_filter_chain: isSet(object.default_filter_chain)
        ? FilterChain.fromJSON(object.default_filter_chain)
        : undefined,
      per_connection_buffer_limit_bytes: isSet(object.per_connection_buffer_limit_bytes)
        ? Number(object.per_connection_buffer_limit_bytes)
        : undefined,
      metadata: isSet(object.metadata) ? Metadata.fromJSON(object.metadata) : undefined,
      deprecated_v1: isSet(object.deprecated_v1) ? Listener_DeprecatedV1.fromJSON(object.deprecated_v1) : undefined,
      drain_type: isSet(object.drain_type) ? listener_DrainTypeFromJSON(object.drain_type) : undefined,
      listener_filters: globalThis.Array.isArray(object?.listener_filters)
        ? object.listener_filters.map((e: any) => ListenerFilter.fromJSON(e))
        : undefined,
      listener_filters_timeout: isSet(object.listener_filters_timeout)
        ? Duration.fromJSON(object.listener_filters_timeout)
        : undefined,
      continue_on_listener_filters_timeout: isSet(object.continue_on_listener_filters_timeout)
        ? globalThis.Boolean(object.continue_on_listener_filters_timeout)
        : undefined,
      transparent: isSet(object.transparent) ? Boolean(object.transparent) : undefined,
      freebind: isSet(object.freebind) ? Boolean(object.freebind) : undefined,
      socket_options: globalThis.Array.isArray(object?.socket_options)
        ? object.socket_options.map((e: any) => SocketOption.fromJSON(e))
        : undefined,
      tcp_fast_open_queue_length: isSet(object.tcp_fast_open_queue_length)
        ? Number(object.tcp_fast_open_queue_length)
        : undefined,
      traffic_direction: isSet(object.traffic_direction)
        ? trafficDirectionFromJSON(object.traffic_direction)
        : undefined,
      udp_listener_config: isSet(object.udp_listener_config)
        ? UdpListenerConfig.fromJSON(object.udp_listener_config)
        : undefined,
      api_listener: isSet(object.api_listener) ? ApiListener.fromJSON(object.api_listener) : undefined,
      connection_balance_config: isSet(object.connection_balance_config)
        ? Listener_ConnectionBalanceConfig.fromJSON(object.connection_balance_config)
        : undefined,
      reuse_port: isSet(object.reuse_port) ? globalThis.Boolean(object.reuse_port) : undefined,
      enable_reuse_port: isSet(object.enable_reuse_port) ? Boolean(object.enable_reuse_port) : undefined,
      access_log: globalThis.Array.isArray(object?.access_log)
        ? object.access_log.map((e: any) => AccessLog.fromJSON(e))
        : undefined,
      tcp_backlog_size: isSet(object.tcp_backlog_size) ? Number(object.tcp_backlog_size) : undefined,
      max_connections_to_accept_per_socket_event: isSet(object.max_connections_to_accept_per_socket_event)
        ? Number(object.max_connections_to_accept_per_socket_event)
        : undefined,
      bind_to_port: isSet(object.bind_to_port) ? Boolean(object.bind_to_port) : undefined,
      listener_specifier: isSet(object.internal_listener)
        ? {
          $case: "internal_listener",
          internal_listener: Listener_InternalListenerConfig.fromJSON(object.internal_listener),
        }
        : undefined,
      enable_mptcp: isSet(object.enable_mptcp) ? globalThis.Boolean(object.enable_mptcp) : undefined,
      ignore_global_conn_limit: isSet(object.ignore_global_conn_limit)
        ? globalThis.Boolean(object.ignore_global_conn_limit)
        : undefined,
      bypass_overload_manager: isSet(object.bypass_overload_manager)
        ? globalThis.Boolean(object.bypass_overload_manager)
        : undefined,
    };
  },

  toJSON(message: Listener): unknown {
    const obj: any = {};
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.address !== undefined) {
      obj.address = Address.toJSON(message.address);
    }
    if (message.additional_addresses?.length) {
      obj.additional_addresses = message.additional_addresses.map((e) => AdditionalAddress.toJSON(e));
    }
    if (message.stat_prefix !== undefined) {
      obj.stat_prefix = message.stat_prefix;
    }
    if (message.filter_chains?.length) {
      obj.filter_chains = message.filter_chains.map((e) => FilterChain.toJSON(e));
    }
    if (message.fcds_config !== undefined) {
      obj.fcds_config = Listener_FcdsConfig.toJSON(message.fcds_config);
    }
    if (message.filter_chain_matcher !== undefined) {
      obj.filter_chain_matcher = Matcher.toJSON(message.filter_chain_matcher);
    }
    if (message.use_original_dst !== undefined) {
      obj.use_original_dst = message.use_original_dst;
    }
    if (message.default_filter_chain !== undefined) {
      obj.default_filter_chain = FilterChain.toJSON(message.default_filter_chain);
    }
    if (message.per_connection_buffer_limit_bytes !== undefined) {
      obj.per_connection_buffer_limit_bytes = message.per_connection_buffer_limit_bytes;
    }
    if (message.metadata !== undefined) {
      obj.metadata = Metadata.toJSON(message.metadata);
    }
    if (message.deprecated_v1 !== undefined) {
      obj.deprecated_v1 = Listener_DeprecatedV1.toJSON(message.deprecated_v1);
    }
    if (message.drain_type !== undefined) {
      obj.drain_type = listener_DrainTypeToJSON(message.drain_type);
    }
    if (message.listener_filters?.length) {
      obj.listener_filters = message.listener_filters.map((e) => ListenerFilter.toJSON(e));
    }
    if (message.listener_filters_timeout !== undefined) {
      obj.listener_filters_timeout = Duration.toJSON(message.listener_filters_timeout);
    }
    if (message.continue_on_listener_filters_timeout !== undefined) {
      obj.continue_on_listener_filters_timeout = message.continue_on_listener_filters_timeout;
    }
    if (message.transparent !== undefined) {
      obj.transparent = message.transparent;
    }
    if (message.freebind !== undefined) {
      obj.freebind = message.freebind;
    }
    if (message.socket_options?.length) {
      obj.socket_options = message.socket_options.map((e) => SocketOption.toJSON(e));
    }
    if (message.tcp_fast_open_queue_length !== undefined) {
      obj.tcp_fast_open_queue_length = message.tcp_fast_open_queue_length;
    }
    if (message.traffic_direction !== undefined) {
      obj.traffic_direction = trafficDirectionToJSON(message.traffic_direction);
    }
    if (message.udp_listener_config !== undefined) {
      obj.udp_listener_config = UdpListenerConfig.toJSON(message.udp_listener_config);
    }
    if (message.api_listener !== undefined) {
      obj.api_listener = ApiListener.toJSON(message.api_listener);
    }
    if (message.connection_balance_config !== undefined) {
      obj.connection_balance_config = Listener_ConnectionBalanceConfig.toJSON(message.connection_balance_config);
    }
    if (message.reuse_port !== undefined) {
      obj.reuse_port = message.reuse_port;
    }
    if (message.enable_reuse_port !== undefined) {
      obj.enable_reuse_port = message.enable_reuse_port;
    }
    if (message.access_log?.length) {
      obj.access_log = message.access_log.map((e) => AccessLog.toJSON(e));
    }
    if (message.tcp_backlog_size !== undefined) {
      obj.tcp_backlog_size = message.tcp_backlog_size;
    }
    if (message.max_connections_to_accept_per_socket_event !== undefined) {
      obj.max_connections_to_accept_per_socket_event = message.max_connections_to_accept_per_socket_event;
    }
    if (message.bind_to_port !== undefined) {
      obj.bind_to_port = message.bind_to_port;
    }
    if (message.listener_specifier?.$case === "internal_listener") {
      obj.internal_listener = Listener_InternalListenerConfig.toJSON(message.listener_specifier.internal_listener);
    }
    if (message.enable_mptcp !== undefined) {
      obj.enable_mptcp = message.enable_mptcp;
    }
    if (message.ignore_global_conn_limit !== undefined) {
      obj.ignore_global_conn_limit = message.ignore_global_conn_limit;
    }
    if (message.bypass_overload_manager !== undefined) {
      obj.bypass_overload_manager = message.bypass_overload_manager;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Listener>, I>>(base?: I): Listener {
    return Listener.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Listener>, I>>(object: I): Listener {
    const message = createBaseListener();
    message.name = object.name ?? undefined;
    message.address = (object.address !== undefined && object.address !== null)
      ? Address.fromPartial(object.address)
      : undefined;
    message.additional_addresses = object.additional_addresses?.map((e) => AdditionalAddress.fromPartial(e)) ||
      undefined;
    message.stat_prefix = object.stat_prefix ?? undefined;
    message.filter_chains = object.filter_chains?.map((e) => FilterChain.fromPartial(e)) || undefined;
    message.fcds_config = (object.fcds_config !== undefined && object.fcds_config !== null)
      ? Listener_FcdsConfig.fromPartial(object.fcds_config)
      : undefined;
    message.filter_chain_matcher = (object.filter_chain_matcher !== undefined && object.filter_chain_matcher !== null)
      ? Matcher.fromPartial(object.filter_chain_matcher)
      : undefined;
    message.use_original_dst = object.use_original_dst ?? undefined;
    message.default_filter_chain = (object.default_filter_chain !== undefined && object.default_filter_chain !== null)
      ? FilterChain.fromPartial(object.default_filter_chain)
      : undefined;
    message.per_connection_buffer_limit_bytes = object.per_connection_buffer_limit_bytes ?? undefined;
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? Metadata.fromPartial(object.metadata)
      : undefined;
    message.deprecated_v1 = (object.deprecated_v1 !== undefined && object.deprecated_v1 !== null)
      ? Listener_DeprecatedV1.fromPartial(object.deprecated_v1)
      : undefined;
    message.drain_type = object.drain_type ?? undefined;
    message.listener_filters = object.listener_filters?.map((e) => ListenerFilter.fromPartial(e)) || undefined;
    message.listener_filters_timeout =
      (object.listener_filters_timeout !== undefined && object.listener_filters_timeout !== null)
        ? Duration.fromPartial(object.listener_filters_timeout)
        : undefined;
    message.continue_on_listener_filters_timeout = object.continue_on_listener_filters_timeout ?? undefined;
    message.transparent = object.transparent ?? undefined;
    message.freebind = object.freebind ?? undefined;
    message.socket_options = object.socket_options?.map((e) => SocketOption.fromPartial(e)) || undefined;
    message.tcp_fast_open_queue_length = object.tcp_fast_open_queue_length ?? undefined;
    message.traffic_direction = object.traffic_direction ?? undefined;
    message.udp_listener_config = (object.udp_listener_config !== undefined && object.udp_listener_config !== null)
      ? UdpListenerConfig.fromPartial(object.udp_listener_config)
      : undefined;
    message.api_listener = (object.api_listener !== undefined && object.api_listener !== null)
      ? ApiListener.fromPartial(object.api_listener)
      : undefined;
    message.connection_balance_config =
      (object.connection_balance_config !== undefined && object.connection_balance_config !== null)
        ? Listener_ConnectionBalanceConfig.fromPartial(object.connection_balance_config)
        : undefined;
    message.reuse_port = object.reuse_port ?? undefined;
    message.enable_reuse_port = object.enable_reuse_port ?? undefined;
    message.access_log = object.access_log?.map((e) => AccessLog.fromPartial(e)) || undefined;
    message.tcp_backlog_size = object.tcp_backlog_size ?? undefined;
    message.max_connections_to_accept_per_socket_event = object.max_connections_to_accept_per_socket_event ?? undefined;
    message.bind_to_port = object.bind_to_port ?? undefined;
    if (
      object.listener_specifier?.$case === "internal_listener" &&
      object.listener_specifier?.internal_listener !== undefined &&
      object.listener_specifier?.internal_listener !== null
    ) {
      message.listener_specifier = {
        $case: "internal_listener",
        internal_listener: Listener_InternalListenerConfig.fromPartial(object.listener_specifier.internal_listener),
      };
    }
    message.enable_mptcp = object.enable_mptcp ?? undefined;
    message.ignore_global_conn_limit = object.ignore_global_conn_limit ?? undefined;
    message.bypass_overload_manager = object.bypass_overload_manager ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(Listener.$type, Listener);

function createBaseListener_DeprecatedV1(): Listener_DeprecatedV1 {
  return { $type: "envoy.config.listener.v3.Listener.DeprecatedV1" };
}

export const Listener_DeprecatedV1: MessageFns<
  Listener_DeprecatedV1,
  "envoy.config.listener.v3.Listener.DeprecatedV1"
> = {
  $type: "envoy.config.listener.v3.Listener.DeprecatedV1" as const,

  encode(message: Listener_DeprecatedV1, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.bind_to_port !== undefined) {
      BoolValue.encode({ $type: "google.protobuf.BoolValue", value: message.bind_to_port! }, writer.uint32(10).fork())
        .join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Listener_DeprecatedV1 {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListener_DeprecatedV1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.bind_to_port = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Listener_DeprecatedV1 {
    return {
      $type: Listener_DeprecatedV1.$type,
      bind_to_port: isSet(object.bind_to_port) ? Boolean(object.bind_to_port) : undefined,
    };
  },

  toJSON(message: Listener_DeprecatedV1): unknown {
    const obj: any = {};
    if (message.bind_to_port !== undefined) {
      obj.bind_to_port = message.bind_to_port;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Listener_DeprecatedV1>, I>>(base?: I): Listener_DeprecatedV1 {
    return Listener_DeprecatedV1.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Listener_DeprecatedV1>, I>>(object: I): Listener_DeprecatedV1 {
    const message = createBaseListener_DeprecatedV1();
    message.bind_to_port = object.bind_to_port ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(Listener_DeprecatedV1.$type, Listener_DeprecatedV1);

function createBaseListener_ConnectionBalanceConfig(): Listener_ConnectionBalanceConfig {
  return { $type: "envoy.config.listener.v3.Listener.ConnectionBalanceConfig", balance_type: undefined };
}

export const Listener_ConnectionBalanceConfig: MessageFns<
  Listener_ConnectionBalanceConfig,
  "envoy.config.listener.v3.Listener.ConnectionBalanceConfig"
> = {
  $type: "envoy.config.listener.v3.Listener.ConnectionBalanceConfig" as const,

  encode(message: Listener_ConnectionBalanceConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.balance_type?.$case) {
      case "exact_balance":
        Listener_ConnectionBalanceConfig_ExactBalance.encode(
          message.balance_type.exact_balance,
          writer.uint32(10).fork(),
        ).join();
        break;
      case "extend_balance":
        TypedExtensionConfig.encode(message.balance_type.extend_balance, writer.uint32(18).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Listener_ConnectionBalanceConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListener_ConnectionBalanceConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.balance_type = {
            $case: "exact_balance",
            exact_balance: Listener_ConnectionBalanceConfig_ExactBalance.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.balance_type = {
            $case: "extend_balance",
            extend_balance: TypedExtensionConfig.decode(reader, reader.uint32()),
          };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Listener_ConnectionBalanceConfig {
    return {
      $type: Listener_ConnectionBalanceConfig.$type,
      balance_type: isSet(object.exact_balance)
        ? {
          $case: "exact_balance",
          exact_balance: Listener_ConnectionBalanceConfig_ExactBalance.fromJSON(object.exact_balance),
        }
        : isSet(object.extend_balance)
        ? { $case: "extend_balance", extend_balance: TypedExtensionConfig.fromJSON(object.extend_balance) }
        : undefined,
    };
  },

  toJSON(message: Listener_ConnectionBalanceConfig): unknown {
    const obj: any = {};
    if (message.balance_type?.$case === "exact_balance") {
      obj.exact_balance = Listener_ConnectionBalanceConfig_ExactBalance.toJSON(message.balance_type.exact_balance);
    }
    if (message.balance_type?.$case === "extend_balance") {
      obj.extend_balance = TypedExtensionConfig.toJSON(message.balance_type.extend_balance);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Listener_ConnectionBalanceConfig>, I>>(
    base?: I,
  ): Listener_ConnectionBalanceConfig {
    return Listener_ConnectionBalanceConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Listener_ConnectionBalanceConfig>, I>>(
    object: I,
  ): Listener_ConnectionBalanceConfig {
    const message = createBaseListener_ConnectionBalanceConfig();
    if (
      object.balance_type?.$case === "exact_balance" &&
      object.balance_type?.exact_balance !== undefined &&
      object.balance_type?.exact_balance !== null
    ) {
      message.balance_type = {
        $case: "exact_balance",
        exact_balance: Listener_ConnectionBalanceConfig_ExactBalance.fromPartial(object.balance_type.exact_balance),
      };
    }
    if (
      object.balance_type?.$case === "extend_balance" &&
      object.balance_type?.extend_balance !== undefined &&
      object.balance_type?.extend_balance !== null
    ) {
      message.balance_type = {
        $case: "extend_balance",
        extend_balance: TypedExtensionConfig.fromPartial(object.balance_type.extend_balance),
      };
    }
    return message;
  },
};

messageTypeRegistry.set(Listener_ConnectionBalanceConfig.$type, Listener_ConnectionBalanceConfig);

function createBaseListener_ConnectionBalanceConfig_ExactBalance(): Listener_ConnectionBalanceConfig_ExactBalance {
  return { $type: "envoy.config.listener.v3.Listener.ConnectionBalanceConfig.ExactBalance" };
}

export const Listener_ConnectionBalanceConfig_ExactBalance: MessageFns<
  Listener_ConnectionBalanceConfig_ExactBalance,
  "envoy.config.listener.v3.Listener.ConnectionBalanceConfig.ExactBalance"
> = {
  $type: "envoy.config.listener.v3.Listener.ConnectionBalanceConfig.ExactBalance" as const,

  encode(_: Listener_ConnectionBalanceConfig_ExactBalance, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Listener_ConnectionBalanceConfig_ExactBalance {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListener_ConnectionBalanceConfig_ExactBalance();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): Listener_ConnectionBalanceConfig_ExactBalance {
    return { $type: Listener_ConnectionBalanceConfig_ExactBalance.$type };
  },

  toJSON(_: Listener_ConnectionBalanceConfig_ExactBalance): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<Listener_ConnectionBalanceConfig_ExactBalance>, I>>(
    base?: I,
  ): Listener_ConnectionBalanceConfig_ExactBalance {
    return Listener_ConnectionBalanceConfig_ExactBalance.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Listener_ConnectionBalanceConfig_ExactBalance>, I>>(
    _: I,
  ): Listener_ConnectionBalanceConfig_ExactBalance {
    const message = createBaseListener_ConnectionBalanceConfig_ExactBalance();
    return message;
  },
};

messageTypeRegistry.set(
  Listener_ConnectionBalanceConfig_ExactBalance.$type,
  Listener_ConnectionBalanceConfig_ExactBalance,
);

function createBaseListener_InternalListenerConfig(): Listener_InternalListenerConfig {
  return { $type: "envoy.config.listener.v3.Listener.InternalListenerConfig" };
}

export const Listener_InternalListenerConfig: MessageFns<
  Listener_InternalListenerConfig,
  "envoy.config.listener.v3.Listener.InternalListenerConfig"
> = {
  $type: "envoy.config.listener.v3.Listener.InternalListenerConfig" as const,

  encode(_: Listener_InternalListenerConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Listener_InternalListenerConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListener_InternalListenerConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): Listener_InternalListenerConfig {
    return { $type: Listener_InternalListenerConfig.$type };
  },

  toJSON(_: Listener_InternalListenerConfig): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<Listener_InternalListenerConfig>, I>>(base?: I): Listener_InternalListenerConfig {
    return Listener_InternalListenerConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Listener_InternalListenerConfig>, I>>(_: I): Listener_InternalListenerConfig {
    const message = createBaseListener_InternalListenerConfig();
    return message;
  },
};

messageTypeRegistry.set(Listener_InternalListenerConfig.$type, Listener_InternalListenerConfig);

function createBaseListener_FcdsConfig(): Listener_FcdsConfig {
  return { $type: "envoy.config.listener.v3.Listener.FcdsConfig" };
}

export const Listener_FcdsConfig: MessageFns<Listener_FcdsConfig, "envoy.config.listener.v3.Listener.FcdsConfig"> = {
  $type: "envoy.config.listener.v3.Listener.FcdsConfig" as const,

  encode(message: Listener_FcdsConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.config_source !== undefined) {
      ConfigSource.encode(message.config_source, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Listener_FcdsConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListener_FcdsConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.config_source = ConfigSource.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Listener_FcdsConfig {
    return {
      $type: Listener_FcdsConfig.$type,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      config_source: isSet(object.config_source) ? ConfigSource.fromJSON(object.config_source) : undefined,
    };
  },

  toJSON(message: Listener_FcdsConfig): unknown {
    const obj: any = {};
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.config_source !== undefined) {
      obj.config_source = ConfigSource.toJSON(message.config_source);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Listener_FcdsConfig>, I>>(base?: I): Listener_FcdsConfig {
    return Listener_FcdsConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Listener_FcdsConfig>, I>>(object: I): Listener_FcdsConfig {
    const message = createBaseListener_FcdsConfig();
    message.name = object.name ?? undefined;
    message.config_source = (object.config_source !== undefined && object.config_source !== null)
      ? ConfigSource.fromPartial(object.config_source)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(Listener_FcdsConfig.$type, Listener_FcdsConfig);

function createBaseListenerManager(): ListenerManager {
  return { $type: "envoy.config.listener.v3.ListenerManager" };
}

export const ListenerManager: MessageFns<ListenerManager, "envoy.config.listener.v3.ListenerManager"> = {
  $type: "envoy.config.listener.v3.ListenerManager" as const,

  encode(_: ListenerManager, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListenerManager {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListenerManager();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListenerManager {
    return { $type: ListenerManager.$type };
  },

  toJSON(_: ListenerManager): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ListenerManager>, I>>(base?: I): ListenerManager {
    return ListenerManager.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListenerManager>, I>>(_: I): ListenerManager {
    const message = createBaseListenerManager();
    return message;
  },
};

messageTypeRegistry.set(ListenerManager.$type, ListenerManager);

function createBaseValidationListenerManager(): ValidationListenerManager {
  return { $type: "envoy.config.listener.v3.ValidationListenerManager" };
}

export const ValidationListenerManager: MessageFns<
  ValidationListenerManager,
  "envoy.config.listener.v3.ValidationListenerManager"
> = {
  $type: "envoy.config.listener.v3.ValidationListenerManager" as const,

  encode(_: ValidationListenerManager, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValidationListenerManager {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseValidationListenerManager();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ValidationListenerManager {
    return { $type: ValidationListenerManager.$type };
  },

  toJSON(_: ValidationListenerManager): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ValidationListenerManager>, I>>(base?: I): ValidationListenerManager {
    return ValidationListenerManager.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ValidationListenerManager>, I>>(_: I): ValidationListenerManager {
    const message = createBaseValidationListenerManager();
    return message;
  },
};

messageTypeRegistry.set(ValidationListenerManager.$type, ValidationListenerManager);

function createBaseApiListenerManager(): ApiListenerManager {
  return { $type: "envoy.config.listener.v3.ApiListenerManager" };
}

export const ApiListenerManager: MessageFns<ApiListenerManager, "envoy.config.listener.v3.ApiListenerManager"> = {
  $type: "envoy.config.listener.v3.ApiListenerManager" as const,

  encode(_: ApiListenerManager, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApiListenerManager {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApiListenerManager();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ApiListenerManager {
    return { $type: ApiListenerManager.$type };
  },

  toJSON(_: ApiListenerManager): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ApiListenerManager>, I>>(base?: I): ApiListenerManager {
    return ApiListenerManager.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ApiListenerManager>, I>>(_: I): ApiListenerManager {
    const message = createBaseApiListenerManager();
    return message;
  },
};

messageTypeRegistry.set(ApiListenerManager.$type, ApiListenerManager);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
