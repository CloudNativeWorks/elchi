// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/health_checkers/redis/v3/redis.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../typeRegistry";
import { AwsIam } from "../../../filters/network/redis_proxy/v3/redis_proxy";

export const protobufPackage = "envoy.extensions.health_checkers.redis.v3";

export interface Redis {
  $type: "envoy.extensions.health_checkers.redis.v3.Redis";
  /**
   * If set, optionally perform ``EXISTS <key>`` instead of ``PING``. A return value
   * from Redis of 0 (does not exist) is considered a passing healthcheck. A return value other
   * than 0 is considered a failure. This allows the user to mark a Redis instance for maintenance
   * by setting the specified key to any value and waiting for traffic to drain.
   */
  key?:
    | string
    | undefined;
  /** Use AWS IAM for health checker authentication */
  aws_iam?: AwsIam | undefined;
}

function createBaseRedis(): Redis {
  return { $type: "envoy.extensions.health_checkers.redis.v3.Redis" };
}

export const Redis: MessageFns<Redis, "envoy.extensions.health_checkers.redis.v3.Redis"> = {
  $type: "envoy.extensions.health_checkers.redis.v3.Redis" as const,

  encode(message: Redis, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== undefined && message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.aws_iam !== undefined) {
      AwsIam.encode(message.aws_iam, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Redis {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRedis();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.aws_iam = AwsIam.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Redis {
    return {
      $type: Redis.$type,
      key: isSet(object.key) ? globalThis.String(object.key) : undefined,
      aws_iam: isSet(object.aws_iam) ? AwsIam.fromJSON(object.aws_iam) : undefined,
    };
  },

  toJSON(message: Redis): unknown {
    const obj: any = {};
    if (message.key !== undefined) {
      obj.key = message.key;
    }
    if (message.aws_iam !== undefined) {
      obj.aws_iam = AwsIam.toJSON(message.aws_iam);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Redis>, I>>(base?: I): Redis {
    return Redis.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Redis>, I>>(object: I): Redis {
    const message = createBaseRedis();
    message.key = object.key ?? undefined;
    message.aws_iam = (object.aws_iam !== undefined && object.aws_iam !== null)
      ? AwsIam.fromPartial(object.aws_iam)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(Redis.$type, Redis);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
