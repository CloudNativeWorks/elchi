// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/grpc_service/call_credentials/service_account_jwt_access/v3/service_account_jwt_access_credentials.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../../../../../../google/protobuf/duration";
import { messageTypeRegistry } from "../../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.grpc_service.call_credentials.service_account_jwt_access.v3";

/** [#not-implemented-hide:] */
export interface ServiceAccountJwtAccessCredentials {
  $type:
    "envoy.extensions.grpc_service.call_credentials.service_account_jwt_access.v3.ServiceAccountJwtAccessCredentials";
  /** JSON key. */
  json_key?:
    | string
    | undefined;
  /** Token lifetime. */
  token_lifetime?: Duration | undefined;
}

function createBaseServiceAccountJwtAccessCredentials(): ServiceAccountJwtAccessCredentials {
  return {
    $type:
      "envoy.extensions.grpc_service.call_credentials.service_account_jwt_access.v3.ServiceAccountJwtAccessCredentials",
  };
}

export const ServiceAccountJwtAccessCredentials: MessageFns<
  ServiceAccountJwtAccessCredentials,
  "envoy.extensions.grpc_service.call_credentials.service_account_jwt_access.v3.ServiceAccountJwtAccessCredentials"
> = {
  $type:
    "envoy.extensions.grpc_service.call_credentials.service_account_jwt_access.v3.ServiceAccountJwtAccessCredentials" as const,

  encode(message: ServiceAccountJwtAccessCredentials, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.json_key !== undefined && message.json_key !== "") {
      writer.uint32(10).string(message.json_key);
    }
    if (message.token_lifetime !== undefined) {
      Duration.encode(message.token_lifetime, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServiceAccountJwtAccessCredentials {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServiceAccountJwtAccessCredentials();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.json_key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.token_lifetime = Duration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServiceAccountJwtAccessCredentials {
    return {
      $type: ServiceAccountJwtAccessCredentials.$type,
      json_key: isSet(object.json_key) ? globalThis.String(object.json_key) : undefined,
      token_lifetime: isSet(object.token_lifetime) ? Duration.fromJSON(object.token_lifetime) : undefined,
    };
  },

  toJSON(message: ServiceAccountJwtAccessCredentials): unknown {
    const obj: any = {};
    if (message.json_key !== undefined) {
      obj.json_key = message.json_key;
    }
    if (message.token_lifetime !== undefined) {
      obj.token_lifetime = Duration.toJSON(message.token_lifetime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServiceAccountJwtAccessCredentials>, I>>(
    base?: I,
  ): ServiceAccountJwtAccessCredentials {
    return ServiceAccountJwtAccessCredentials.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServiceAccountJwtAccessCredentials>, I>>(
    object: I,
  ): ServiceAccountJwtAccessCredentials {
    const message = createBaseServiceAccountJwtAccessCredentials();
    message.json_key = object.json_key ?? undefined;
    message.token_lifetime = (object.token_lifetime !== undefined && object.token_lifetime !== null)
      ? Duration.fromPartial(object.token_lifetime)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(ServiceAccountJwtAccessCredentials.$type, ServiceAccountJwtAccessCredentials);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
