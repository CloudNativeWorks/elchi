// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/filters/common/matcher/action/v3/skip_action.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.filters.common.matcher.action.v3";

/**
 * Configuration for the SkipFilter match action. When matching results in this action, the
 * associated filter will be ignored for all filter callbacks (e.g. ``encodeHeaders``, ``encodeData``,
 * etc. for HTTP filters) after the matcher arrives at the match, including the callback that
 * caused the match result. For example, when used with a HTTP filter and the match result was
 * resolved after receiving the HTTP response headers, the HTTP filter will *not* receive the
 * response header callback.
 *
 * As a result, if this match action is resolved before the first filter callback (e.g. HTTP request
 * headers), the filter will be completely skipped.
 */
export interface SkipFilter {
  $type: "envoy.extensions.filters.common.matcher.action.v3.SkipFilter";
}

function createBaseSkipFilter(): SkipFilter {
  return { $type: "envoy.extensions.filters.common.matcher.action.v3.SkipFilter" };
}

export const SkipFilter: MessageFns<SkipFilter, "envoy.extensions.filters.common.matcher.action.v3.SkipFilter"> = {
  $type: "envoy.extensions.filters.common.matcher.action.v3.SkipFilter" as const,

  encode(_: SkipFilter, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SkipFilter {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSkipFilter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): SkipFilter {
    return { $type: SkipFilter.$type };
  },

  toJSON(_: SkipFilter): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<SkipFilter>, I>>(base?: I): SkipFilter {
    return SkipFilter.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SkipFilter>, I>>(_: I): SkipFilter {
    const message = createBaseSkipFilter();
    return message;
  },
};

messageTypeRegistry.set(SkipFilter.$type, SkipFilter);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
