// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/config/common/key_value/v3/config.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../typeRegistry";
import { TypedExtensionConfig } from "../../../core/v3/extension";

export const protobufPackage = "envoy.config.common.key_value.v3";

/** This shared configuration for Envoy key value stores. */
export interface KeyValueStoreConfig {
  $type: "envoy.config.common.key_value.v3.KeyValueStoreConfig";
  /** [#extension-category: envoy.common.key_value] */
  config?: TypedExtensionConfig | undefined;
}

function createBaseKeyValueStoreConfig(): KeyValueStoreConfig {
  return { $type: "envoy.config.common.key_value.v3.KeyValueStoreConfig" };
}

export const KeyValueStoreConfig: MessageFns<
  KeyValueStoreConfig,
  "envoy.config.common.key_value.v3.KeyValueStoreConfig"
> = {
  $type: "envoy.config.common.key_value.v3.KeyValueStoreConfig" as const,

  encode(message: KeyValueStoreConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.config !== undefined) {
      TypedExtensionConfig.encode(message.config, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeyValueStoreConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeyValueStoreConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.config = TypedExtensionConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KeyValueStoreConfig {
    return {
      $type: KeyValueStoreConfig.$type,
      config: isSet(object.config) ? TypedExtensionConfig.fromJSON(object.config) : undefined,
    };
  },

  toJSON(message: KeyValueStoreConfig): unknown {
    const obj: any = {};
    if (message.config !== undefined) {
      obj.config = TypedExtensionConfig.toJSON(message.config);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KeyValueStoreConfig>, I>>(base?: I): KeyValueStoreConfig {
    return KeyValueStoreConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KeyValueStoreConfig>, I>>(object: I): KeyValueStoreConfig {
    const message = createBaseKeyValueStoreConfig();
    message.config = (object.config !== undefined && object.config !== null)
      ? TypedExtensionConfig.fromPartial(object.config)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(KeyValueStoreConfig.$type, KeyValueStoreConfig);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
