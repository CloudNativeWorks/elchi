// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: contrib/envoy/extensions/compression/qatzstd/compressor/v3alpha/qatzstd.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { UInt32Value } from "../../../../../../../google/protobuf/wrappers";
import { messageTypeRegistry } from "../../../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.compression.qatzstd.compressor.v3alpha";

/** [#next-free-field: 8] */
export interface Qatzstd {
  $type: "envoy.extensions.compression.qatzstd.compressor.v3alpha.Qatzstd";
  /**
   * Set compression parameters according to pre-defined compression level table.
   * Note that exact compression parameters are dynamically determined,
   * depending on both compression level and source content size (when known).
   * Value 0 means default, and default level is 3.
   *
   * Setting a level does not automatically set all other compression parameters
   * to default. Setting this will however eventually dynamically impact the compression
   * parameters which have not been manually set. The manually set
   * ones will 'stick'.
   */
  compression_level?:
    | number
    | undefined;
  /** A 32-bits checksum of content is written at end of frame. If not set, defaults to false. */
  enable_checksum?:
    | boolean
    | undefined;
  /**
   * The higher the value of selected strategy, the more complex it is,
   * resulting in stronger and slower compression.
   *
   * Special: value 0 means "use default strategy".
   */
  strategy?:
    | Qatzstd_Strategy
    | undefined;
  /** Value for compressor's next output buffer. If not set, defaults to 4096. */
  chunk_size?:
    | number
    | undefined;
  /**
   * Enable QAT to accelerate Zstd compression or not. If not set, defaults to false.
   *
   * This is useful in the case that users want to enable QAT for a period of time and disable QAT for another period of time,
   * they don't have to change the config too much or prepare for another config that has software zstd compressor and just changing the value of this filed.
   */
  enable_qat_zstd?:
    | boolean
    | undefined;
  /**
   * Fallback to software for Qatzstd when input size is less than this value.
   * Valid only ``enable_qat_zstd`` is ``true``. 0 means no fallback at all. If not set, defaults to 4000.
   */
  qat_zstd_fallback_threshold?: number | undefined;
}

/** Reference to http://facebook.github.io/zstd/zstd_manual.html */
export enum Qatzstd_Strategy {
  DEFAULT = "DEFAULT",
  FAST = "FAST",
  DFAST = "DFAST",
  GREEDY = "GREEDY",
  LAZY = "LAZY",
  LAZY2 = "LAZY2",
  BTLAZY2 = "BTLAZY2",
  BTOPT = "BTOPT",
  BTULTRA = "BTULTRA",
  BTULTRA2 = "BTULTRA2",
}

export function qatzstd_StrategyFromJSON(object: any): Qatzstd_Strategy {
  switch (object) {
    case 0:
    case "DEFAULT":
      return Qatzstd_Strategy.DEFAULT;
    case 1:
    case "FAST":
      return Qatzstd_Strategy.FAST;
    case 2:
    case "DFAST":
      return Qatzstd_Strategy.DFAST;
    case 3:
    case "GREEDY":
      return Qatzstd_Strategy.GREEDY;
    case 4:
    case "LAZY":
      return Qatzstd_Strategy.LAZY;
    case 5:
    case "LAZY2":
      return Qatzstd_Strategy.LAZY2;
    case 6:
    case "BTLAZY2":
      return Qatzstd_Strategy.BTLAZY2;
    case 7:
    case "BTOPT":
      return Qatzstd_Strategy.BTOPT;
    case 8:
    case "BTULTRA":
      return Qatzstd_Strategy.BTULTRA;
    case 9:
    case "BTULTRA2":
      return Qatzstd_Strategy.BTULTRA2;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Qatzstd_Strategy");
  }
}

export function qatzstd_StrategyToJSON(object: Qatzstd_Strategy): string {
  switch (object) {
    case Qatzstd_Strategy.DEFAULT:
      return "DEFAULT";
    case Qatzstd_Strategy.FAST:
      return "FAST";
    case Qatzstd_Strategy.DFAST:
      return "DFAST";
    case Qatzstd_Strategy.GREEDY:
      return "GREEDY";
    case Qatzstd_Strategy.LAZY:
      return "LAZY";
    case Qatzstd_Strategy.LAZY2:
      return "LAZY2";
    case Qatzstd_Strategy.BTLAZY2:
      return "BTLAZY2";
    case Qatzstd_Strategy.BTOPT:
      return "BTOPT";
    case Qatzstd_Strategy.BTULTRA:
      return "BTULTRA";
    case Qatzstd_Strategy.BTULTRA2:
      return "BTULTRA2";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Qatzstd_Strategy");
  }
}

export function qatzstd_StrategyToNumber(object: Qatzstd_Strategy): number {
  switch (object) {
    case Qatzstd_Strategy.DEFAULT:
      return 0;
    case Qatzstd_Strategy.FAST:
      return 1;
    case Qatzstd_Strategy.DFAST:
      return 2;
    case Qatzstd_Strategy.GREEDY:
      return 3;
    case Qatzstd_Strategy.LAZY:
      return 4;
    case Qatzstd_Strategy.LAZY2:
      return 5;
    case Qatzstd_Strategy.BTLAZY2:
      return 6;
    case Qatzstd_Strategy.BTOPT:
      return 7;
    case Qatzstd_Strategy.BTULTRA:
      return 8;
    case Qatzstd_Strategy.BTULTRA2:
      return 9;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum Qatzstd_Strategy");
  }
}

function createBaseQatzstd(): Qatzstd {
  return { $type: "envoy.extensions.compression.qatzstd.compressor.v3alpha.Qatzstd" };
}

export const Qatzstd: MessageFns<Qatzstd, "envoy.extensions.compression.qatzstd.compressor.v3alpha.Qatzstd"> = {
  $type: "envoy.extensions.compression.qatzstd.compressor.v3alpha.Qatzstd" as const,

  encode(message: Qatzstd, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.compression_level !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.compression_level! },
        writer.uint32(10).fork(),
      ).join();
    }
    if (message.enable_checksum !== undefined && message.enable_checksum !== false) {
      writer.uint32(16).bool(message.enable_checksum);
    }
    if (message.strategy !== undefined && message.strategy !== Qatzstd_Strategy.DEFAULT) {
      writer.uint32(24).int32(qatzstd_StrategyToNumber(message.strategy));
    }
    if (message.chunk_size !== undefined) {
      UInt32Value.encode({ $type: "google.protobuf.UInt32Value", value: message.chunk_size! }, writer.uint32(42).fork())
        .join();
    }
    if (message.enable_qat_zstd !== undefined && message.enable_qat_zstd !== false) {
      writer.uint32(48).bool(message.enable_qat_zstd);
    }
    if (message.qat_zstd_fallback_threshold !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.qat_zstd_fallback_threshold! },
        writer.uint32(58).fork(),
      ).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Qatzstd {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQatzstd();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.compression_level = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.enable_checksum = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.strategy = qatzstd_StrategyFromJSON(reader.int32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.chunk_size = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.enable_qat_zstd = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.qat_zstd_fallback_threshold = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Qatzstd {
    return {
      $type: Qatzstd.$type,
      compression_level: isSet(object.compression_level) ? Number(object.compression_level) : undefined,
      enable_checksum: isSet(object.enable_checksum) ? globalThis.Boolean(object.enable_checksum) : undefined,
      strategy: isSet(object.strategy) ? qatzstd_StrategyFromJSON(object.strategy) : undefined,
      chunk_size: isSet(object.chunk_size) ? Number(object.chunk_size) : undefined,
      enable_qat_zstd: isSet(object.enable_qat_zstd) ? globalThis.Boolean(object.enable_qat_zstd) : undefined,
      qat_zstd_fallback_threshold: isSet(object.qat_zstd_fallback_threshold)
        ? Number(object.qat_zstd_fallback_threshold)
        : undefined,
    };
  },

  toJSON(message: Qatzstd): unknown {
    const obj: any = {};
    if (message.compression_level !== undefined) {
      obj.compression_level = message.compression_level;
    }
    if (message.enable_checksum !== undefined) {
      obj.enable_checksum = message.enable_checksum;
    }
    if (message.strategy !== undefined) {
      obj.strategy = qatzstd_StrategyToJSON(message.strategy);
    }
    if (message.chunk_size !== undefined) {
      obj.chunk_size = message.chunk_size;
    }
    if (message.enable_qat_zstd !== undefined) {
      obj.enable_qat_zstd = message.enable_qat_zstd;
    }
    if (message.qat_zstd_fallback_threshold !== undefined) {
      obj.qat_zstd_fallback_threshold = message.qat_zstd_fallback_threshold;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Qatzstd>, I>>(base?: I): Qatzstd {
    return Qatzstd.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Qatzstd>, I>>(object: I): Qatzstd {
    const message = createBaseQatzstd();
    message.compression_level = object.compression_level ?? undefined;
    message.enable_checksum = object.enable_checksum ?? undefined;
    message.strategy = object.strategy ?? undefined;
    message.chunk_size = object.chunk_size ?? undefined;
    message.enable_qat_zstd = object.enable_qat_zstd ?? undefined;
    message.qat_zstd_fallback_threshold = object.qat_zstd_fallback_threshold ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(Qatzstd.$type, Qatzstd);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
