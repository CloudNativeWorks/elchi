// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/admin/v3/tap.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../typeRegistry";
import { TapConfig } from "../../config/tap/v3/common";

export const protobufPackage = "envoy.admin.v3";

/** The /tap admin request body that is used to configure an active tap session. */
export interface TapRequest {
  $type: "envoy.admin.v3.TapRequest";
  /**
   * The opaque configuration ID used to match the configuration to a loaded extension.
   * A tap extension configures a similar opaque ID that is used to match.
   */
  config_id?:
    | string
    | undefined;
  /** The tap configuration to load. */
  tap_config?: TapConfig | undefined;
}

function createBaseTapRequest(): TapRequest {
  return { $type: "envoy.admin.v3.TapRequest" };
}

export const TapRequest: MessageFns<TapRequest, "envoy.admin.v3.TapRequest"> = {
  $type: "envoy.admin.v3.TapRequest" as const,

  encode(message: TapRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.config_id !== undefined && message.config_id !== "") {
      writer.uint32(10).string(message.config_id);
    }
    if (message.tap_config !== undefined) {
      TapConfig.encode(message.tap_config, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TapRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTapRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.config_id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tap_config = TapConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TapRequest {
    return {
      $type: TapRequest.$type,
      config_id: isSet(object.config_id) ? globalThis.String(object.config_id) : undefined,
      tap_config: isSet(object.tap_config) ? TapConfig.fromJSON(object.tap_config) : undefined,
    };
  },

  toJSON(message: TapRequest): unknown {
    const obj: any = {};
    if (message.config_id !== undefined) {
      obj.config_id = message.config_id;
    }
    if (message.tap_config !== undefined) {
      obj.tap_config = TapConfig.toJSON(message.tap_config);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TapRequest>, I>>(base?: I): TapRequest {
    return TapRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TapRequest>, I>>(object: I): TapRequest {
    const message = createBaseTapRequest();
    message.config_id = object.config_id ?? undefined;
    message.tap_config = (object.tap_config !== undefined && object.tap_config !== null)
      ? TapConfig.fromPartial(object.tap_config)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(TapRequest.$type, TapRequest);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
