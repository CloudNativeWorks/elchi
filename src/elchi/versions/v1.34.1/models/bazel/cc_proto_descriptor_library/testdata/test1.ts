// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: bazel/cc_proto_descriptor_library/testdata/test1.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../typeRegistry";

export const protobufPackage = "testdata.dynamic_descriptors";

export interface FooCopy {
  $type: "testdata.dynamic_descriptors.FooCopy";
  bar?: string | undefined;
}

function createBaseFooCopy(): FooCopy {
  return { $type: "testdata.dynamic_descriptors.FooCopy" };
}

export const FooCopy: MessageFns<FooCopy, "testdata.dynamic_descriptors.FooCopy"> = {
  $type: "testdata.dynamic_descriptors.FooCopy" as const,

  encode(message: FooCopy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.bar !== undefined && message.bar !== "") {
      writer.uint32(10).string(message.bar);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FooCopy {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFooCopy();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.bar = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FooCopy {
    return { $type: FooCopy.$type, bar: isSet(object.bar) ? globalThis.String(object.bar) : undefined };
  },

  toJSON(message: FooCopy): unknown {
    const obj: any = {};
    if (message.bar !== undefined) {
      obj.bar = message.bar;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FooCopy>, I>>(base?: I): FooCopy {
    return FooCopy.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FooCopy>, I>>(object: I): FooCopy {
    const message = createBaseFooCopy();
    message.bar = object.bar ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(FooCopy.$type, FooCopy);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
