// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/service/auth/v2/external_auth.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Status } from "../../../../google/rpc/status";
import { messageTypeRegistry } from "../../../../typeRegistry";
import { HeaderValueOption } from "../../../api/v2/core/base";
import { HttpStatus } from "../../../type/http_status";
import { AttributeContext } from "./attribute_context";

export const protobufPackage = "envoy.service.auth.v2";

export interface CheckRequest {
  $type: "envoy.service.auth.v2.CheckRequest";
  /** The request attributes. */
  attributes?: AttributeContext | undefined;
}

/** HTTP attributes for a denied response. */
export interface DeniedHttpResponse {
  $type: "envoy.service.auth.v2.DeniedHttpResponse";
  /**
   * This field allows the authorization service to send a HTTP response status
   * code to the downstream client other than 403 (Forbidden).
   */
  status?:
    | HttpStatus
    | undefined;
  /**
   * This field allows the authorization service to send HTTP response headers
   * to the downstream client. Note that the `append` field in `HeaderValueOption` defaults to
   * false when used in this message.
   */
  headers?:
    | HeaderValueOption[]
    | undefined;
  /**
   * This field allows the authorization service to send a response body data
   * to the downstream client.
   */
  body?: string | undefined;
}

/** HTTP attributes for an ok response. */
export interface OkHttpResponse {
  $type: "envoy.service.auth.v2.OkHttpResponse";
  /**
   * HTTP entity headers in addition to the original request headers. This allows the authorization
   * service to append, to add or to override headers from the original request before
   * dispatching it to the upstream. Note that the `append` field in `HeaderValueOption` defaults to
   * false when used in this message. By setting the `append` field to `true`,
   * the filter will append the correspondent header value to the matched request header.
   * By leaving `append` as false, the filter will either add a new header, or override an existing
   * one if there is a match.
   */
  headers?: HeaderValueOption[] | undefined;
}

/** Intended for gRPC and Network Authorization servers `only`. */
export interface CheckResponse {
  $type: "envoy.service.auth.v2.CheckResponse";
  /** Status `OK` allows the request. Any other status indicates the request should be denied. */
  status?:
    | Status
    | undefined;
  /**
   * An message that contains HTTP response attributes. This message is
   * used when the authorization service needs to send custom responses to the
   * downstream client or, to modify/add request headers being dispatched to the upstream.
   */
  http_response?:
    | //
    /** Supplies http attributes for a denied response. */
    { $case: "denied_response"; denied_response: DeniedHttpResponse }
    | //
    /** Supplies http attributes for an ok response. */
    { $case: "ok_response"; ok_response: OkHttpResponse }
    | undefined;
}

function createBaseCheckRequest(): CheckRequest {
  return { $type: "envoy.service.auth.v2.CheckRequest" };
}

export const CheckRequest: MessageFns<CheckRequest, "envoy.service.auth.v2.CheckRequest"> = {
  $type: "envoy.service.auth.v2.CheckRequest" as const,

  encode(message: CheckRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.attributes !== undefined) {
      AttributeContext.encode(message.attributes, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CheckRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCheckRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.attributes = AttributeContext.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CheckRequest {
    return {
      $type: CheckRequest.$type,
      attributes: isSet(object.attributes) ? AttributeContext.fromJSON(object.attributes) : undefined,
    };
  },

  toJSON(message: CheckRequest): unknown {
    const obj: any = {};
    if (message.attributes !== undefined) {
      obj.attributes = AttributeContext.toJSON(message.attributes);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CheckRequest>, I>>(base?: I): CheckRequest {
    return CheckRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CheckRequest>, I>>(object: I): CheckRequest {
    const message = createBaseCheckRequest();
    message.attributes = (object.attributes !== undefined && object.attributes !== null)
      ? AttributeContext.fromPartial(object.attributes)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(CheckRequest.$type, CheckRequest);

function createBaseDeniedHttpResponse(): DeniedHttpResponse {
  return { $type: "envoy.service.auth.v2.DeniedHttpResponse" };
}

export const DeniedHttpResponse: MessageFns<DeniedHttpResponse, "envoy.service.auth.v2.DeniedHttpResponse"> = {
  $type: "envoy.service.auth.v2.DeniedHttpResponse" as const,

  encode(message: DeniedHttpResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== undefined) {
      HttpStatus.encode(message.status, writer.uint32(10).fork()).join();
    }
    if (message.headers !== undefined && message.headers.length !== 0) {
      for (const v of message.headers) {
        HeaderValueOption.encode(v!, writer.uint32(18).fork()).join();
      }
    }
    if (message.body !== undefined && message.body !== "") {
      writer.uint32(26).string(message.body);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeniedHttpResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeniedHttpResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = HttpStatus.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          if (message.headers === undefined) {
            message.headers = [];
          }
          const el = HeaderValueOption.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.headers!.push(el);
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.body = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeniedHttpResponse {
    return {
      $type: DeniedHttpResponse.$type,
      status: isSet(object.status) ? HttpStatus.fromJSON(object.status) : undefined,
      headers: globalThis.Array.isArray(object?.headers)
        ? object.headers.map((e: any) => HeaderValueOption.fromJSON(e))
        : undefined,
      body: isSet(object.body) ? globalThis.String(object.body) : undefined,
    };
  },

  toJSON(message: DeniedHttpResponse): unknown {
    const obj: any = {};
    if (message.status !== undefined) {
      obj.status = HttpStatus.toJSON(message.status);
    }
    if (message.headers?.length) {
      obj.headers = message.headers.map((e) => HeaderValueOption.toJSON(e));
    }
    if (message.body !== undefined) {
      obj.body = message.body;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeniedHttpResponse>, I>>(base?: I): DeniedHttpResponse {
    return DeniedHttpResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeniedHttpResponse>, I>>(object: I): DeniedHttpResponse {
    const message = createBaseDeniedHttpResponse();
    message.status = (object.status !== undefined && object.status !== null)
      ? HttpStatus.fromPartial(object.status)
      : undefined;
    message.headers = object.headers?.map((e) => HeaderValueOption.fromPartial(e)) || undefined;
    message.body = object.body ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(DeniedHttpResponse.$type, DeniedHttpResponse);

function createBaseOkHttpResponse(): OkHttpResponse {
  return { $type: "envoy.service.auth.v2.OkHttpResponse" };
}

export const OkHttpResponse: MessageFns<OkHttpResponse, "envoy.service.auth.v2.OkHttpResponse"> = {
  $type: "envoy.service.auth.v2.OkHttpResponse" as const,

  encode(message: OkHttpResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.headers !== undefined && message.headers.length !== 0) {
      for (const v of message.headers) {
        HeaderValueOption.encode(v!, writer.uint32(18).fork()).join();
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OkHttpResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOkHttpResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }

          if (message.headers === undefined) {
            message.headers = [];
          }
          const el = HeaderValueOption.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.headers!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OkHttpResponse {
    return {
      $type: OkHttpResponse.$type,
      headers: globalThis.Array.isArray(object?.headers)
        ? object.headers.map((e: any) => HeaderValueOption.fromJSON(e))
        : undefined,
    };
  },

  toJSON(message: OkHttpResponse): unknown {
    const obj: any = {};
    if (message.headers?.length) {
      obj.headers = message.headers.map((e) => HeaderValueOption.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OkHttpResponse>, I>>(base?: I): OkHttpResponse {
    return OkHttpResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OkHttpResponse>, I>>(object: I): OkHttpResponse {
    const message = createBaseOkHttpResponse();
    message.headers = object.headers?.map((e) => HeaderValueOption.fromPartial(e)) || undefined;
    return message;
  },
};

messageTypeRegistry.set(OkHttpResponse.$type, OkHttpResponse);

function createBaseCheckResponse(): CheckResponse {
  return { $type: "envoy.service.auth.v2.CheckResponse", http_response: undefined };
}

export const CheckResponse: MessageFns<CheckResponse, "envoy.service.auth.v2.CheckResponse"> = {
  $type: "envoy.service.auth.v2.CheckResponse" as const,

  encode(message: CheckResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== undefined) {
      Status.encode(message.status, writer.uint32(10).fork()).join();
    }
    switch (message.http_response?.$case) {
      case "denied_response":
        DeniedHttpResponse.encode(message.http_response.denied_response, writer.uint32(18).fork()).join();
        break;
      case "ok_response":
        OkHttpResponse.encode(message.http_response.ok_response, writer.uint32(26).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CheckResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCheckResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = Status.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.http_response = {
            $case: "denied_response",
            denied_response: DeniedHttpResponse.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.http_response = { $case: "ok_response", ok_response: OkHttpResponse.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CheckResponse {
    return {
      $type: CheckResponse.$type,
      status: isSet(object.status) ? Status.fromJSON(object.status) : undefined,
      http_response: isSet(object.denied_response)
        ? { $case: "denied_response", denied_response: DeniedHttpResponse.fromJSON(object.denied_response) }
        : isSet(object.ok_response)
        ? { $case: "ok_response", ok_response: OkHttpResponse.fromJSON(object.ok_response) }
        : undefined,
    };
  },

  toJSON(message: CheckResponse): unknown {
    const obj: any = {};
    if (message.status !== undefined) {
      obj.status = Status.toJSON(message.status);
    }
    if (message.http_response?.$case === "denied_response") {
      obj.denied_response = DeniedHttpResponse.toJSON(message.http_response.denied_response);
    }
    if (message.http_response?.$case === "ok_response") {
      obj.ok_response = OkHttpResponse.toJSON(message.http_response.ok_response);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CheckResponse>, I>>(base?: I): CheckResponse {
    return CheckResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CheckResponse>, I>>(object: I): CheckResponse {
    const message = createBaseCheckResponse();
    message.status = (object.status !== undefined && object.status !== null)
      ? Status.fromPartial(object.status)
      : undefined;
    if (
      object.http_response?.$case === "denied_response" &&
      object.http_response?.denied_response !== undefined &&
      object.http_response?.denied_response !== null
    ) {
      message.http_response = {
        $case: "denied_response",
        denied_response: DeniedHttpResponse.fromPartial(object.http_response.denied_response),
      };
    }
    if (
      object.http_response?.$case === "ok_response" &&
      object.http_response?.ok_response !== undefined &&
      object.http_response?.ok_response !== null
    ) {
      message.http_response = {
        $case: "ok_response",
        ok_response: OkHttpResponse.fromPartial(object.http_response.ok_response),
      };
    }
    return message;
  },
};

messageTypeRegistry.set(CheckResponse.$type, CheckResponse);

/**
 * A generic interface for performing authorization check on incoming
 * requests to a networked service.
 */
export interface Authorization {
  /**
   * Performs authorization check based on the attributes associated with the
   * incoming request, and returns status `OK` or not `OK`.
   */
  Check(request: CheckRequest): Promise<CheckResponse>;
}

export const AuthorizationServiceName = "envoy.service.auth.v2.Authorization";
export class AuthorizationClientImpl implements Authorization {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || AuthorizationServiceName;
    this.rpc = rpc;
    this.Check = this.Check.bind(this);
  }
  Check(request: CheckRequest): Promise<CheckResponse> {
    const data = CheckRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Check", data);
    return promise.then((data) => CheckResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
