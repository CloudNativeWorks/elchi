// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/common/dynamic_forward_proxy/v3/dns_cache.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../../../../../google/protobuf/duration";
import { UInt32Value } from "../../../../../google/protobuf/wrappers";
import { messageTypeRegistry } from "../../../../../typeRegistry";
import {
  Cluster_DnsLookupFamily,
  cluster_DnsLookupFamilyFromJSON,
  cluster_DnsLookupFamilyToJSON,
  cluster_DnsLookupFamilyToNumber,
  Cluster_RefreshRate,
} from "../../../../config/cluster/v3/cluster";
import { KeyValueStoreConfig } from "../../../../config/common/key_value/v3/config";
import { SocketAddress } from "../../../../config/core/v3/address";
import { TypedExtensionConfig } from "../../../../config/core/v3/extension";
import { DnsResolutionConfig } from "../../../../config/core/v3/resolver";

export const protobufPackage = "envoy.extensions.common.dynamic_forward_proxy.v3";

/** Configuration of circuit breakers for resolver. */
export interface DnsCacheCircuitBreakers {
  $type: "envoy.extensions.common.dynamic_forward_proxy.v3.DnsCacheCircuitBreakers";
  /**
   * The maximum number of pending requests that Envoy will allow to the
   * resolver. If not specified, the default is 1024.
   */
  max_pending_requests?: number | undefined;
}

/**
 * Configuration for the dynamic forward proxy DNS cache. See the :ref:`architecture overview
 * <arch_overview_http_dynamic_forward_proxy>` for more information.
 * [#next-free-field: 16]
 */
export interface DnsCacheConfig {
  $type: "envoy.extensions.common.dynamic_forward_proxy.v3.DnsCacheConfig";
  /**
   * The name of the cache. Multiple named caches allow independent dynamic forward proxy
   * configurations to operate within a single Envoy process using different configurations. All
   * configurations with the same name *must* otherwise have the same settings when referenced
   * from different configuration components. Configuration will fail to load if this is not
   * the case.
   */
  name?:
    | string
    | undefined;
  /**
   * The DNS lookup family to use during resolution.
   *
   * [#comment:TODO(mattklein123): Figure out how to support IPv4/IPv6 "happy eyeballs" mode. The
   * way this might work is a new lookup family which returns both IPv4 and IPv6 addresses, and
   * then configures a host to have a primary and fall back address. With this, we could very
   * likely build a "happy eyeballs" connection pool which would race the primary / fall back
   * address and return the one that wins. This same method could potentially also be used for
   * QUIC to TCP fall back.]
   */
  dns_lookup_family?:
    | Cluster_DnsLookupFamily
    | undefined;
  /**
   * The DNS refresh rate for unresolved DNS hosts. If not specified defaults to 60s.
   *
   * The refresh rate is rounded to the closest millisecond, and must be at least 1ms.
   *
   * Once a host has been resolved, the refresh rate will be the DNS TTL, capped
   * at a minimum of ``dns_min_refresh_rate``.
   */
  dns_refresh_rate?:
    | Duration
    | undefined;
  /**
   * The minimum rate that DNS resolution will occur. Per ``dns_refresh_rate``, once a host is
   * resolved, the DNS TTL will be used, with a minimum set by ``dns_min_refresh_rate``.
   * ``dns_min_refresh_rate`` defaults to 5s and must also be >= 1s.
   */
  dns_min_refresh_rate?:
    | Duration
    | undefined;
  /**
   * The TTL for hosts that are unused. Hosts that have not been used in the configured time
   * interval will be purged. If not specified defaults to 5m.
   *
   * .. note:
   *
   *   The TTL is only checked at the time of DNS refresh, as specified by ``dns_refresh_rate``. This
   *   means that if the configured TTL is shorter than the refresh rate the host may not be removed
   *   immediately.
   *
   *  .. note:
   *
   *   The TTL has no relation to DNS TTL and is only used to control Envoy's resource usage.
   */
  host_ttl?:
    | Duration
    | undefined;
  /**
   * The maximum number of hosts that the cache will hold. If not specified defaults to 1024.
   *
   * .. note:
   *
   *   The implementation is approximate and enforced independently on each worker thread, thus
   *   it is possible for the maximum hosts in the cache to go slightly above the configured
   *   value depending on timing. This is similar to how other circuit breakers work.
   */
  max_hosts?:
    | number
    | undefined;
  /**
   * Disable the DNS refresh on failure. If this field is set to true, it will ignore the
   * :ref:`typed_dns_resolver_config <envoy_v3_api_field_extensions.common.dynamic_forward_proxy.v3.DnsCacheConfig.dns_failure_refresh_rate>`.
   * If not specified, it defaults to false. By enabling this feature, the failed hosts will now be treated as a cache miss,
   * allowing the failed hosts to be resolved on demand.
   */
  disable_dns_refresh_on_failure?:
    | boolean
    | undefined;
  /**
   * If the DNS failure refresh rate is specified,
   * this is used as the cache's DNS refresh rate when DNS requests are failing. If this setting is
   * not specified, the failure refresh rate defaults to the dns_refresh_rate.
   */
  dns_failure_refresh_rate?:
    | Cluster_RefreshRate
    | undefined;
  /**
   * The config of circuit breakers for resolver. It provides a configurable threshold.
   * Envoy will use dns cache circuit breakers with default settings even if this value is not set.
   */
  dns_cache_circuit_breaker?:
    | DnsCacheCircuitBreakers
    | undefined;
  /**
   * Always use TCP queries instead of UDP queries for DNS lookups.
   * This field is deprecated in favor of ``dns_resolution_config``
   * which aggregates all of the DNS resolver configuration in a single message.
   *
   * @deprecated
   */
  use_tcp_for_dns_lookups?:
    | boolean
    | undefined;
  /**
   * DNS resolution configuration which includes the underlying dns resolver addresses and options.
   * This field is deprecated in favor of
   * :ref:`typed_dns_resolver_config <envoy_v3_api_field_extensions.common.dynamic_forward_proxy.v3.DnsCacheConfig.typed_dns_resolver_config>`.
   *
   * @deprecated
   */
  dns_resolution_config?:
    | DnsResolutionConfig
    | undefined;
  /**
   * DNS resolver type configuration extension. This extension can be used to configure c-ares, apple,
   * or any other DNS resolver types and the related parameters.
   * For example, an object of
   * :ref:`CaresDnsResolverConfig <envoy_v3_api_msg_extensions.network.dns_resolver.cares.v3.CaresDnsResolverConfig>`
   * can be packed into this ``typed_dns_resolver_config``. This configuration replaces the
   * :ref:`dns_resolution_config <envoy_v3_api_field_extensions.common.dynamic_forward_proxy.v3.DnsCacheConfig.dns_resolution_config>`
   * configuration.
   * During the transition period when both ``dns_resolution_config`` and ``typed_dns_resolver_config`` exists,
   * when ``typed_dns_resolver_config`` is in place, Envoy will use it and ignore ``dns_resolution_config``.
   * When ``typed_dns_resolver_config`` is missing, the default behavior is in place.
   * [#extension-category: envoy.network.dns_resolver]
   */
  typed_dns_resolver_config?:
    | TypedExtensionConfig
    | undefined;
  /**
   * Hostnames that should be preresolved into the cache upon creation. This might provide a
   * performance improvement, in the form of cache hits, for hostnames that are going to be
   * resolved during steady state and are known at config load time.
   */
  preresolve_hostnames?:
    | SocketAddress[]
    | undefined;
  /**
   * The timeout used for DNS queries. This timeout is independent of any timeout and retry policy
   * used by the underlying DNS implementation (e.g., c-areas and Apple DNS) which are opaque.
   * Setting this timeout will ensure that queries succeed or fail within the specified time frame
   * and are then retried using the standard refresh rates. Setting it to 0 will disable the Envoy DNS
   * query timeout and use the underlying DNS implementation timeout. Defaults to 5s if not set.
   */
  dns_query_timeout?:
    | Duration
    | undefined;
  /** Configuration to flush the DNS cache to long term storage. */
  key_value_config?: KeyValueStoreConfig | undefined;
}

function createBaseDnsCacheCircuitBreakers(): DnsCacheCircuitBreakers {
  return { $type: "envoy.extensions.common.dynamic_forward_proxy.v3.DnsCacheCircuitBreakers" };
}

export const DnsCacheCircuitBreakers: MessageFns<
  DnsCacheCircuitBreakers,
  "envoy.extensions.common.dynamic_forward_proxy.v3.DnsCacheCircuitBreakers"
> = {
  $type: "envoy.extensions.common.dynamic_forward_proxy.v3.DnsCacheCircuitBreakers" as const,

  encode(message: DnsCacheCircuitBreakers, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.max_pending_requests !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.max_pending_requests! },
        writer.uint32(10).fork(),
      ).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DnsCacheCircuitBreakers {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDnsCacheCircuitBreakers();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.max_pending_requests = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DnsCacheCircuitBreakers {
    return {
      $type: DnsCacheCircuitBreakers.$type,
      max_pending_requests: isSet(object.max_pending_requests) ? Number(object.max_pending_requests) : undefined,
    };
  },

  toJSON(message: DnsCacheCircuitBreakers): unknown {
    const obj: any = {};
    if (message.max_pending_requests !== undefined) {
      obj.max_pending_requests = message.max_pending_requests;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DnsCacheCircuitBreakers>, I>>(base?: I): DnsCacheCircuitBreakers {
    return DnsCacheCircuitBreakers.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DnsCacheCircuitBreakers>, I>>(object: I): DnsCacheCircuitBreakers {
    const message = createBaseDnsCacheCircuitBreakers();
    message.max_pending_requests = object.max_pending_requests ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(DnsCacheCircuitBreakers.$type, DnsCacheCircuitBreakers);

function createBaseDnsCacheConfig(): DnsCacheConfig {
  return { $type: "envoy.extensions.common.dynamic_forward_proxy.v3.DnsCacheConfig" };
}

export const DnsCacheConfig: MessageFns<
  DnsCacheConfig,
  "envoy.extensions.common.dynamic_forward_proxy.v3.DnsCacheConfig"
> = {
  $type: "envoy.extensions.common.dynamic_forward_proxy.v3.DnsCacheConfig" as const,

  encode(message: DnsCacheConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.dns_lookup_family !== undefined && message.dns_lookup_family !== Cluster_DnsLookupFamily.AUTO) {
      writer.uint32(16).int32(cluster_DnsLookupFamilyToNumber(message.dns_lookup_family));
    }
    if (message.dns_refresh_rate !== undefined) {
      Duration.encode(message.dns_refresh_rate, writer.uint32(26).fork()).join();
    }
    if (message.dns_min_refresh_rate !== undefined) {
      Duration.encode(message.dns_min_refresh_rate, writer.uint32(114).fork()).join();
    }
    if (message.host_ttl !== undefined) {
      Duration.encode(message.host_ttl, writer.uint32(34).fork()).join();
    }
    if (message.max_hosts !== undefined) {
      UInt32Value.encode({ $type: "google.protobuf.UInt32Value", value: message.max_hosts! }, writer.uint32(42).fork())
        .join();
    }
    if (message.disable_dns_refresh_on_failure !== undefined && message.disable_dns_refresh_on_failure !== false) {
      writer.uint32(120).bool(message.disable_dns_refresh_on_failure);
    }
    if (message.dns_failure_refresh_rate !== undefined) {
      Cluster_RefreshRate.encode(message.dns_failure_refresh_rate, writer.uint32(50).fork()).join();
    }
    if (message.dns_cache_circuit_breaker !== undefined) {
      DnsCacheCircuitBreakers.encode(message.dns_cache_circuit_breaker, writer.uint32(58).fork()).join();
    }
    if (message.use_tcp_for_dns_lookups !== undefined && message.use_tcp_for_dns_lookups !== false) {
      writer.uint32(64).bool(message.use_tcp_for_dns_lookups);
    }
    if (message.dns_resolution_config !== undefined) {
      DnsResolutionConfig.encode(message.dns_resolution_config, writer.uint32(74).fork()).join();
    }
    if (message.typed_dns_resolver_config !== undefined) {
      TypedExtensionConfig.encode(message.typed_dns_resolver_config, writer.uint32(98).fork()).join();
    }
    if (message.preresolve_hostnames !== undefined && message.preresolve_hostnames.length !== 0) {
      for (const v of message.preresolve_hostnames) {
        SocketAddress.encode(v!, writer.uint32(82).fork()).join();
      }
    }
    if (message.dns_query_timeout !== undefined) {
      Duration.encode(message.dns_query_timeout, writer.uint32(90).fork()).join();
    }
    if (message.key_value_config !== undefined) {
      KeyValueStoreConfig.encode(message.key_value_config, writer.uint32(106).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DnsCacheConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDnsCacheConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.dns_lookup_family = cluster_DnsLookupFamilyFromJSON(reader.int32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.dns_refresh_rate = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.dns_min_refresh_rate = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.host_ttl = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.max_hosts = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.disable_dns_refresh_on_failure = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.dns_failure_refresh_rate = Cluster_RefreshRate.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.dns_cache_circuit_breaker = DnsCacheCircuitBreakers.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.use_tcp_for_dns_lookups = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.dns_resolution_config = DnsResolutionConfig.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.typed_dns_resolver_config = TypedExtensionConfig.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          if (message.preresolve_hostnames === undefined) {
            message.preresolve_hostnames = [];
          }
          const el = SocketAddress.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.preresolve_hostnames!.push(el);
          }
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.dns_query_timeout = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.key_value_config = KeyValueStoreConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DnsCacheConfig {
    return {
      $type: DnsCacheConfig.$type,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      dns_lookup_family: isSet(object.dns_lookup_family)
        ? cluster_DnsLookupFamilyFromJSON(object.dns_lookup_family)
        : undefined,
      dns_refresh_rate: isSet(object.dns_refresh_rate) ? Duration.fromJSON(object.dns_refresh_rate) : undefined,
      dns_min_refresh_rate: isSet(object.dns_min_refresh_rate)
        ? Duration.fromJSON(object.dns_min_refresh_rate)
        : undefined,
      host_ttl: isSet(object.host_ttl) ? Duration.fromJSON(object.host_ttl) : undefined,
      max_hosts: isSet(object.max_hosts) ? Number(object.max_hosts) : undefined,
      disable_dns_refresh_on_failure: isSet(object.disable_dns_refresh_on_failure)
        ? globalThis.Boolean(object.disable_dns_refresh_on_failure)
        : undefined,
      dns_failure_refresh_rate: isSet(object.dns_failure_refresh_rate)
        ? Cluster_RefreshRate.fromJSON(object.dns_failure_refresh_rate)
        : undefined,
      dns_cache_circuit_breaker: isSet(object.dns_cache_circuit_breaker)
        ? DnsCacheCircuitBreakers.fromJSON(object.dns_cache_circuit_breaker)
        : undefined,
      use_tcp_for_dns_lookups: isSet(object.use_tcp_for_dns_lookups)
        ? globalThis.Boolean(object.use_tcp_for_dns_lookups)
        : undefined,
      dns_resolution_config: isSet(object.dns_resolution_config)
        ? DnsResolutionConfig.fromJSON(object.dns_resolution_config)
        : undefined,
      typed_dns_resolver_config: isSet(object.typed_dns_resolver_config)
        ? TypedExtensionConfig.fromJSON(object.typed_dns_resolver_config)
        : undefined,
      preresolve_hostnames: globalThis.Array.isArray(object?.preresolve_hostnames)
        ? object.preresolve_hostnames.map((e: any) => SocketAddress.fromJSON(e))
        : undefined,
      dns_query_timeout: isSet(object.dns_query_timeout) ? Duration.fromJSON(object.dns_query_timeout) : undefined,
      key_value_config: isSet(object.key_value_config)
        ? KeyValueStoreConfig.fromJSON(object.key_value_config)
        : undefined,
    };
  },

  toJSON(message: DnsCacheConfig): unknown {
    const obj: any = {};
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.dns_lookup_family !== undefined) {
      obj.dns_lookup_family = cluster_DnsLookupFamilyToJSON(message.dns_lookup_family);
    }
    if (message.dns_refresh_rate !== undefined) {
      obj.dns_refresh_rate = Duration.toJSON(message.dns_refresh_rate);
    }
    if (message.dns_min_refresh_rate !== undefined) {
      obj.dns_min_refresh_rate = Duration.toJSON(message.dns_min_refresh_rate);
    }
    if (message.host_ttl !== undefined) {
      obj.host_ttl = Duration.toJSON(message.host_ttl);
    }
    if (message.max_hosts !== undefined) {
      obj.max_hosts = message.max_hosts;
    }
    if (message.disable_dns_refresh_on_failure !== undefined) {
      obj.disable_dns_refresh_on_failure = message.disable_dns_refresh_on_failure;
    }
    if (message.dns_failure_refresh_rate !== undefined) {
      obj.dns_failure_refresh_rate = Cluster_RefreshRate.toJSON(message.dns_failure_refresh_rate);
    }
    if (message.dns_cache_circuit_breaker !== undefined) {
      obj.dns_cache_circuit_breaker = DnsCacheCircuitBreakers.toJSON(message.dns_cache_circuit_breaker);
    }
    if (message.use_tcp_for_dns_lookups !== undefined) {
      obj.use_tcp_for_dns_lookups = message.use_tcp_for_dns_lookups;
    }
    if (message.dns_resolution_config !== undefined) {
      obj.dns_resolution_config = DnsResolutionConfig.toJSON(message.dns_resolution_config);
    }
    if (message.typed_dns_resolver_config !== undefined) {
      obj.typed_dns_resolver_config = TypedExtensionConfig.toJSON(message.typed_dns_resolver_config);
    }
    if (message.preresolve_hostnames?.length) {
      obj.preresolve_hostnames = message.preresolve_hostnames.map((e) => SocketAddress.toJSON(e));
    }
    if (message.dns_query_timeout !== undefined) {
      obj.dns_query_timeout = Duration.toJSON(message.dns_query_timeout);
    }
    if (message.key_value_config !== undefined) {
      obj.key_value_config = KeyValueStoreConfig.toJSON(message.key_value_config);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DnsCacheConfig>, I>>(base?: I): DnsCacheConfig {
    return DnsCacheConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DnsCacheConfig>, I>>(object: I): DnsCacheConfig {
    const message = createBaseDnsCacheConfig();
    message.name = object.name ?? undefined;
    message.dns_lookup_family = object.dns_lookup_family ?? undefined;
    message.dns_refresh_rate = (object.dns_refresh_rate !== undefined && object.dns_refresh_rate !== null)
      ? Duration.fromPartial(object.dns_refresh_rate)
      : undefined;
    message.dns_min_refresh_rate = (object.dns_min_refresh_rate !== undefined && object.dns_min_refresh_rate !== null)
      ? Duration.fromPartial(object.dns_min_refresh_rate)
      : undefined;
    message.host_ttl = (object.host_ttl !== undefined && object.host_ttl !== null)
      ? Duration.fromPartial(object.host_ttl)
      : undefined;
    message.max_hosts = object.max_hosts ?? undefined;
    message.disable_dns_refresh_on_failure = object.disable_dns_refresh_on_failure ?? undefined;
    message.dns_failure_refresh_rate =
      (object.dns_failure_refresh_rate !== undefined && object.dns_failure_refresh_rate !== null)
        ? Cluster_RefreshRate.fromPartial(object.dns_failure_refresh_rate)
        : undefined;
    message.dns_cache_circuit_breaker =
      (object.dns_cache_circuit_breaker !== undefined && object.dns_cache_circuit_breaker !== null)
        ? DnsCacheCircuitBreakers.fromPartial(object.dns_cache_circuit_breaker)
        : undefined;
    message.use_tcp_for_dns_lookups = object.use_tcp_for_dns_lookups ?? undefined;
    message.dns_resolution_config =
      (object.dns_resolution_config !== undefined && object.dns_resolution_config !== null)
        ? DnsResolutionConfig.fromPartial(object.dns_resolution_config)
        : undefined;
    message.typed_dns_resolver_config =
      (object.typed_dns_resolver_config !== undefined && object.typed_dns_resolver_config !== null)
        ? TypedExtensionConfig.fromPartial(object.typed_dns_resolver_config)
        : undefined;
    message.preresolve_hostnames = object.preresolve_hostnames?.map((e) => SocketAddress.fromPartial(e)) || undefined;
    message.dns_query_timeout = (object.dns_query_timeout !== undefined && object.dns_query_timeout !== null)
      ? Duration.fromPartial(object.dns_query_timeout)
      : undefined;
    message.key_value_config = (object.key_value_config !== undefined && object.key_value_config !== null)
      ? KeyValueStoreConfig.fromPartial(object.key_value_config)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(DnsCacheConfig.$type, DnsCacheConfig);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
