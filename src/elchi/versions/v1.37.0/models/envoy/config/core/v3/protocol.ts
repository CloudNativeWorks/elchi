// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/config/core/v3/protocol.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../../../../google/protobuf/duration";
import { BoolValue, UInt32Value, UInt64Value } from "../../../../google/protobuf/wrappers";
import { messageTypeRegistry } from "../../../../typeRegistry";
import { StringMatcher } from "../../../type/matcher/v3/string";
import { Percent } from "../../../type/v3/percent";
import { TypedExtensionConfig } from "./extension";

export const protobufPackage = "envoy.config.core.v3";

/** [#not-implemented-hide:] */
export interface TcpProtocolOptions {
  $type: "envoy.config.core.v3.TcpProtocolOptions";
}

/**
 * Config for keepalive probes in a QUIC connection.
 *
 * .. note::
 *
 *   QUIC keep-alive probing packets work differently from HTTP/2 keep-alive PINGs in a sense that the probing packet
 *   itself doesn't timeout waiting for a probing response. QUIC has a shorter idle timeout than TCP, so it doesn't rely on such probing to discover dead connections. If the peer fails to respond, the connection will idle timeout eventually. Thus, they are configured differently from :ref:`connection_keepalive <envoy_v3_api_field_config.core.v3.Http2ProtocolOptions.connection_keepalive>`.
 */
export interface QuicKeepAliveSettings {
  $type: "envoy.config.core.v3.QuicKeepAliveSettings";
  /**
   * The max interval for a connection to send keep-alive probing packets (with ``PING`` or ``PATH_RESPONSE``). The value should be smaller than :ref:`connection idle_timeout <envoy_v3_api_field_config.listener.v3.QuicProtocolOptions.idle_timeout>` to prevent idle timeout while not less than ``1s`` to avoid throttling the connection or flooding the peer with probes.
   *
   * If :ref:`initial_interval <envoy_v3_api_field_config.core.v3.QuicKeepAliveSettings.initial_interval>` is absent or zero, a client connection will use this value to start probing.
   *
   * If zero, disable keepalive probing.
   * If absent, use the QUICHE default interval to probe.
   */
  max_interval?:
    | Duration
    | undefined;
  /**
   * The interval to send the first few keep-alive probing packets to prevent connection from hitting the idle timeout. Subsequent probes will be sent, each one with an interval exponentially longer than previous one, till it reaches :ref:`max_interval <envoy_v3_api_field_config.core.v3.QuicKeepAliveSettings.max_interval>`. And the probes afterwards will always use :ref:`max_interval <envoy_v3_api_field_config.core.v3.QuicKeepAliveSettings.max_interval>`.
   *
   * The value should be smaller than :ref:`connection idle_timeout <envoy_v3_api_field_config.listener.v3.QuicProtocolOptions.idle_timeout>` to prevent idle timeout and smaller than max_interval to take effect.
   *
   * If absent, disable keepalive probing for a server connection. For a client connection, if :ref:`max_interval <envoy_v3_api_field_config.core.v3.QuicKeepAliveSettings.max_interval>` is zero, do not keepalive, otherwise use max_interval or QUICHE default to probe all the time.
   */
  initial_interval?: Duration | undefined;
}

/**
 * QUIC protocol options which apply to both downstream and upstream connections.
 * [#next-free-field: 12]
 */
export interface QuicProtocolOptions {
  $type: "envoy.config.core.v3.QuicProtocolOptions";
  /**
   * Maximum number of streams that the client can negotiate per connection. ``100``
   * if not specified.
   */
  max_concurrent_streams?:
    | number
    | undefined;
  /**
   * `Initial stream-level flow-control receive window
   * <https://tools.ietf.org/html/draft-ietf-quic-transport-34#section-4.1>`_ size. Valid values range from
   * ``1`` to ``16777216`` (``2^24``, maximum supported by QUICHE) and defaults to ``16777216`` (``16 * 1024 * 1024``).
   *
   * .. note::
   *
   *   ``16384`` (``2^14``) is the minimum window size supported in Google QUIC. If configured smaller than it, we will use
   *   ``16384`` instead. QUICHE IETF QUIC implementation supports ``1`` byte window. We only support increasing the default
   *   window size now, so it's also the minimum.
   *
   * This field also acts as a soft limit on the number of bytes Envoy will buffer per-stream in the
   * QUIC stream send and receive buffers. Once the buffer reaches this pointer, watermark callbacks will fire to
   * stop the flow of data to the stream buffers.
   */
  initial_stream_window_size?:
    | number
    | undefined;
  /**
   * Similar to ``initial_stream_window_size``, but for connection-level
   * flow-control. Valid values range from ``1`` to ``25165824`` (``24MB``, maximum supported by QUICHE) and defaults
   * to ``25165824`` (``24 * 1024 * 1024``).
   *
   * .. note::
   *
   *   ``16384`` (``2^14``) is the minimum window size supported in Google QUIC. We only support increasing the default
   *   window size now, so it's also the minimum.
   */
  initial_connection_window_size?:
    | number
    | undefined;
  /**
   * The number of timeouts that can occur before port migration is triggered for QUIC clients.
   * This defaults to ``4``. If set to ``0``, port migration will not occur on path degrading.
   * Timeout here refers to QUIC internal path degrading timeout mechanism, such as ``PTO``.
   * This has no effect on server sessions.
   */
  num_timeouts_to_trigger_port_migration?:
    | number
    | undefined;
  /**
   * Probes the peer at the configured interval to solicit traffic, i.e. ``ACK`` or ``PATH_RESPONSE``, from the peer to push back connection idle timeout.
   * If absent, use the default keepalive behavior of which a client connection sends ``PING``s every ``15s``, and a server connection doesn't do anything.
   */
  connection_keepalive?:
    | QuicKeepAliveSettings
    | undefined;
  /**
   * A comma-separated list of strings representing QUIC connection options defined in
   * `QUICHE <https://github.com/google/quiche/blob/main/quiche/quic/core/crypto/crypto_protocol.h>`_ and to be sent by upstream connections.
   */
  connection_options?:
    | string
    | undefined;
  /**
   * A comma-separated list of strings representing QUIC client connection options defined in
   * `QUICHE <https://github.com/google/quiche/blob/main/quiche/quic/core/crypto/crypto_protocol.h>`_ and to be sent by upstream connections.
   */
  client_connection_options?:
    | string
    | undefined;
  /**
   * The duration that a QUIC connection stays idle before it closes itself. If this field is not present, QUICHE
   * default ``600s`` will be applied.
   * For internal corporate network, a long timeout is often fine.
   * But for client facing network, ``30s`` is usually a good choice.
   * Do not add an upper bound here. A long idle timeout is useful for maintaining warm connections at non-front-line proxy for low QPS services.
   */
  idle_network_timeout?:
    | Duration
    | undefined;
  /**
   * Maximum packet length for QUIC connections. It refers to the largest size of a QUIC packet that can be transmitted over the connection.
   * If not specified, one of the `default values in QUICHE <https://github.com/google/quiche/blob/main/quiche/quic/core/quic_constants.h>`_ is used.
   */
  max_packet_length?:
    | number
    | undefined;
  /**
   * A customized UDP socket and a QUIC packet writer using the socket for
   * client connections. i.e. Mobile uses its own implementation to interact
   * with platform socket APIs.
   * If not present, the default platform-independent socket and writer will be used.
   * [#extension-category: envoy.quic.client_packet_writer]
   */
  client_packet_writer?:
    | TypedExtensionConfig
    | undefined;
  /**
   * Enable QUIC `connection migration
   * <https://datatracker.ietf.org/doc/html/rfc9000#name-connection-migration>`
   * to a different network interface when the current network is degrading or
   * has become bad.
   * In order to use a different network interface other than the platform's default one,
   * a customized :ref:`client_packet_writer <envoy_v3_api_field_config.core.v3.QuicProtocolOptions.client_packet_writer>` needs to be configured to
   * create UDP sockets on non-default networks.
   * Only takes effect when runtime key ``envoy.reloadable_features.use_migration_in_quiche`` is true.
   * If absent, the feature will be disabled.
   * [#not-implemented-hide:]
   */
  connection_migration?: QuicProtocolOptions_ConnectionMigrationSettings | undefined;
}

/**
 * Config for QUIC connection migration across network interfaces, i.e. cellular to WIFI, upon
 * network change events from the platform, i.e. the current network gets
 * disconnected, or upon the QUIC detecting a bad connection. After migration, the
 * connection may be on a different network other than the default network
 * picked by the platform. Both iOS and Android will use a default network to interact with the internet, usually prefer unmetered network (WIFI)
 * over metered ones (cellular). And users can specify which network to be used as the default. A connection on non-default network is only allowed to
 * serve new requests for a certain period of time before being drained, and
 * meanwhile, QUIC will try to migrate to the default network if possible.
 */
export interface QuicProtocolOptions_ConnectionMigrationSettings {
  $type: "envoy.config.core.v3.QuicProtocolOptions.ConnectionMigrationSettings";
  /**
   * Config whether and how to migrate idle connections.
   * If absent, idle connections will not be migrated but be closed upon
   * migration signals.
   */
  migrate_idle_connections?:
    | QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings
    | undefined;
  /**
   * After migrating to a non-default network interface, the connection will
   * only be allowed to stay on that network for up to this period of time before
   * being drained unless it migrates to the default network or that network
   * gets picked as the default by the device by then.
   * Default to 128s.
   */
  max_time_on_non_default_network?: Duration | undefined;
}

/** Config for options to migrate idle connections which aren't serving any requests. */
export interface QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings {
  $type: "envoy.config.core.v3.QuicProtocolOptions.ConnectionMigrationSettings.MigrateIdleConnectionSettings";
  /**
   * If idle connections are allowed to be migrated, only migrate the connection
   * if it hasn't been idle for longer than this idle period. Otherwise, the
   * connection will be closed instead.
   * Default to 30s.
   */
  max_idle_time_before_migration?: Duration | undefined;
}

export interface UpstreamHttpProtocolOptions {
  $type: "envoy.config.core.v3.UpstreamHttpProtocolOptions";
  /**
   * Set transport socket `SNI <https://en.wikipedia.org/wiki/Server_Name_Indication>`_ for new
   * upstream connections based on the downstream HTTP host/authority header or any other arbitrary
   * header when :ref:`override_auto_sni_header <envoy_v3_api_field_config.core.v3.UpstreamHttpProtocolOptions.override_auto_sni_header>`
   * is set, as seen by the :ref:`router filter <config_http_filters_router>`.
   * Does nothing if a filter before the http router filter sets the corresponding metadata.
   *
   * See :ref:`SNI configuration <start_quick_start_securing_sni_client>` for details on how this
   * interacts with other validation options.
   */
  auto_sni?:
    | boolean
    | undefined;
  /**
   * Automatic validate upstream presented certificate for new upstream connections based on the
   * downstream HTTP host/authority header or any other arbitrary header when :ref:`override_auto_sni_header <envoy_v3_api_field_config.core.v3.UpstreamHttpProtocolOptions.override_auto_sni_header>`
   * is set, as seen by the :ref:`router filter <config_http_filters_router>`.
   * This field is intended to be set with ``auto_sni`` field.
   * Does nothing if a filter before the http router filter sets the corresponding metadata.
   *
   * See :ref:`validation configuration <start_quick_start_securing_validation>` for how this interacts with
   * other validation options.
   */
  auto_san_validation?:
    | boolean
    | undefined;
  /**
   * An optional alternative to the host/authority header to be used for setting the SNI value.
   * It should be a valid downstream HTTP header, as seen by the
   * :ref:`router filter <config_http_filters_router>`.
   * If unset, host/authority header will be used for populating the SNI. If the specified header
   * is not found or the value is empty, host/authority header will be used instead.
   * This field is intended to be set with ``auto_sni`` and/or ``auto_san_validation`` fields.
   * If none of these fields are set then setting this would be a no-op.
   * Does nothing if a filter before the http router filter sets the corresponding metadata.
   */
  override_auto_sni_header?: string | undefined;
}

/**
 * Configures the alternate protocols cache which tracks alternate protocols that can be used to
 * make an HTTP connection to an origin server. See https://tools.ietf.org/html/rfc7838 for
 * HTTP Alternative Services and https://datatracker.ietf.org/doc/html/draft-ietf-dnsop-svcb-https-04
 * for the "HTTPS" DNS resource record.
 * [#next-free-field: 6]
 */
export interface AlternateProtocolsCacheOptions {
  $type: "envoy.config.core.v3.AlternateProtocolsCacheOptions";
  /**
   * The name of the cache. Multiple named caches allow independent alternate protocols cache
   * configurations to operate within a single Envoy process using different configurations. All
   * alternate protocols cache options with the same name *must* be equal in all fields when
   * referenced from different configuration components. Configuration will fail to load if this is
   * not the case.
   */
  name?:
    | string
    | undefined;
  /**
   * The maximum number of entries that the cache will hold. If not specified defaults to ``1024``.
   *
   * .. note::
   *
   *   The implementation is approximate and enforced independently on each worker thread, thus
   *   it is possible for the maximum entries in the cache to go slightly above the configured
   *   value depending on timing. This is similar to how other circuit breakers work.
   */
  max_entries?:
    | number
    | undefined;
  /**
   * Allows configuring a persistent
   * :ref:`key value store <envoy_v3_api_msg_config.common.key_value.v3.KeyValueStoreConfig>` to flush
   * alternate protocols entries to disk.
   * This function is currently only supported if concurrency is 1
   * Cached entries will take precedence over pre-populated entries below.
   */
  key_value_store_config?:
    | TypedExtensionConfig
    | undefined;
  /** Allows pre-populating the cache with entries, as described above. */
  prepopulated_entries?:
    | AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry[]
    | undefined;
  /**
   * Optional list of hostnames suffixes for which Alt-Svc entries can be shared. For example, if
   * this list contained the value ``.c.example.com``, then an Alt-Svc entry for ``foo.c.example.com``
   * could be shared with ``bar.c.example.com`` but would not be shared with ``baz.example.com``. On
   * the other hand, if the list contained the value ``.example.com`` then all three hosts could share
   * Alt-Svc entries. Each entry must start with ``.``. If a hostname matches multiple suffixes, the
   * first listed suffix will be used.
   *
   * Since lookup in this list is O(n), it is recommended that the number of suffixes be limited.
   * [#not-implemented-hide:]
   */
  canonical_suffixes?: string[] | undefined;
}

/**
 * Allows pre-populating the cache with HTTP/3 alternate protocols entries with a 7 day lifetime.
 * This will cause Envoy to attempt HTTP/3 to those upstreams, even if the upstreams have not
 * advertised HTTP/3 support. These entries will be overwritten by alt-svc
 * response headers or cached values.
 * As with regular cached entries, if the origin response would result in clearing an existing
 * alternate protocol cache entry, pre-populated entries will also be cleared.
 * Adding a cache entry with hostname=foo.com port=123 is the equivalent of getting
 * response headers
 * alt-svc: h3=:"123"; ma=86400" in a response to a request to foo.com:123
 */
export interface AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry {
  $type: "envoy.config.core.v3.AlternateProtocolsCacheOptions.AlternateProtocolsCacheEntry";
  /** The host name for the alternate protocol entry. */
  hostname?:
    | string
    | undefined;
  /** The port for the alternate protocol entry. */
  port?: number | undefined;
}

/** [#next-free-field: 8] */
export interface HttpProtocolOptions {
  $type: "envoy.config.core.v3.HttpProtocolOptions";
  /**
   * The idle timeout for connections. The idle timeout is defined as the
   * period in which there are no active requests. When the
   * idle timeout is reached the connection will be closed. If the connection is an HTTP/2
   * downstream connection a drain sequence will occur prior to closing the connection, see
   * :ref:`drain_timeout
   * <envoy_v3_api_field_extensions.filters.network.http_connection_manager.v3.HttpConnectionManager.drain_timeout>`.
   *
   * .. note::
   *
   *   Request based timeouts mean that HTTP/2 PINGs will not keep the connection alive.
   *
   * If not specified, this defaults to ``1 hour``. To disable idle timeouts explicitly set this to ``0``.
   *
   * .. warning::
   *   Disabling this timeout has a highly likelihood of yielding connection leaks due to lost TCP
   *   FIN packets, etc.
   *
   * If the :ref:`overload action <config_overload_manager_overload_actions>` "envoy.overload_actions.reduce_timeouts"
   * is configured, this timeout is scaled for downstream connections according to the value for
   * :ref:`HTTP_DOWNSTREAM_CONNECTION_IDLE <envoy_v3_api_enum_value_config.overload.v3.ScaleTimersOverloadActionConfig.TimerType.HTTP_DOWNSTREAM_CONNECTION_IDLE>`.
   */
  idle_timeout?:
    | Duration
    | undefined;
  /**
   * The maximum duration of a connection. The duration is defined as a period since a connection
   * was established. If not set, there is no max duration. When max_connection_duration is reached,
   * the drain sequence will kick-in. The connection will be closed after the drain timeout period
   * if there are no active streams. See :ref:`drain_timeout
   * <envoy_v3_api_field_extensions.filters.network.http_connection_manager.v3.HttpConnectionManager.drain_timeout>`.
   */
  max_connection_duration?:
    | Duration
    | undefined;
  /**
   * The maximum number of headers (request headers if configured on HttpConnectionManager,
   * response headers when configured on a cluster).
   * If unconfigured, the default maximum number of headers allowed is ``100``.
   * The default value for requests can be overridden by setting runtime key ``envoy.reloadable_features.max_request_headers_count``.
   * The default value for responses can be overridden by setting runtime key ``envoy.reloadable_features.max_response_headers_count``.
   * Downstream requests that exceed this limit will receive a ``HTTP 431`` response for HTTP/1.x and cause a stream
   * reset for HTTP/2.
   * Upstream responses that exceed this limit will result in a ``HTTP 502`` response.
   */
  max_headers_count?:
    | number
    | undefined;
  /**
   * The maximum size of response headers.
   * If unconfigured, the default is ``60 KiB``, except for HTTP/1 response headers which have a default
   * of ``80 KiB``.
   * The default value can be overridden by setting runtime key ``envoy.reloadable_features.max_response_headers_size_kb``.
   * Responses that exceed this limit will result in a ``HTTP 503`` response.
   * In Envoy, this setting is only valid when configured on an upstream cluster, not on the
   * :ref:`HTTP Connection Manager
   * <envoy_v3_api_field_extensions.filters.network.http_connection_manager.v3.HttpConnectionManager.common_http_protocol_options>`.
   *
   * .. note::
   *
   *   Currently some protocol codecs impose limits on the maximum size of a single header.
   *
   *   * HTTP/2 (when using ``nghttp2``) limits a single header to around ``100kb``.
   *   * HTTP/3 limits a single header to around ``1024kb``.
   */
  max_response_headers_kb?:
    | number
    | undefined;
  /**
   * Total duration to keep alive an HTTP request/response stream. If the time limit is reached the stream will be
   * reset independent of any other timeouts. If not specified, this value is not set.
   */
  max_stream_duration?:
    | Duration
    | undefined;
  /**
   * Action to take when a client request with a header name containing underscore characters is received.
   * If this setting is not specified, the value defaults to ``ALLOW``.
   *
   * .. note::
   *
   *   Upstream responses are not affected by this setting.
   *
   * .. note::
   *
   *   This only affects client headers. It does not affect headers added by Envoy filters and does not have any
   *   impact if added to cluster config.
   */
  headers_with_underscores_action?:
    | HttpProtocolOptions_HeadersWithUnderscoresAction
    | undefined;
  /**
   * Optional maximum requests for both upstream and downstream connections.
   * If not specified, there is no limit.
   * Setting this parameter to ``1`` will effectively disable keep alive.
   * For HTTP/2 and HTTP/3, due to concurrent stream processing, the limit is approximate.
   */
  max_requests_per_connection?: number | undefined;
}

/**
 * Action to take when Envoy receives client request with header names containing underscore
 * characters.
 * Underscore character is allowed in header names by the RFC-7230 and this behavior is implemented
 * as a security measure due to systems that treat '_' and '-' as interchangeable. Envoy by default allows client request headers with underscore
 * characters.
 */
export enum HttpProtocolOptions_HeadersWithUnderscoresAction {
  /** ALLOW - Allow headers with underscores. This is the default behavior. */
  ALLOW = "ALLOW",
  /**
   * REJECT_REQUEST - Reject client request. HTTP/1 requests are rejected with ``HTTP 400`` status. HTTP/2 requests
   * end with the stream reset. The ``httpN.requests_rejected_with_underscores_in_headers`` counter
   * is incremented for each rejected request.
   */
  REJECT_REQUEST = "REJECT_REQUEST",
  /**
   * DROP_HEADER - Drop the client header with name containing underscores. The header is dropped before the filter chain is
   * invoked and as such filters will not see dropped headers. The
   * ``httpN.dropped_headers_with_underscores`` is incremented for each dropped header.
   */
  DROP_HEADER = "DROP_HEADER",
}

export function httpProtocolOptions_HeadersWithUnderscoresActionFromJSON(
  object: any,
): HttpProtocolOptions_HeadersWithUnderscoresAction {
  switch (object) {
    case 0:
    case "ALLOW":
      return HttpProtocolOptions_HeadersWithUnderscoresAction.ALLOW;
    case 1:
    case "REJECT_REQUEST":
      return HttpProtocolOptions_HeadersWithUnderscoresAction.REJECT_REQUEST;
    case 2:
    case "DROP_HEADER":
      return HttpProtocolOptions_HeadersWithUnderscoresAction.DROP_HEADER;
    default:
      throw new globalThis.Error(
        "Unrecognized enum value " + object + " for enum HttpProtocolOptions_HeadersWithUnderscoresAction",
      );
  }
}

export function httpProtocolOptions_HeadersWithUnderscoresActionToJSON(
  object: HttpProtocolOptions_HeadersWithUnderscoresAction,
): string {
  switch (object) {
    case HttpProtocolOptions_HeadersWithUnderscoresAction.ALLOW:
      return "ALLOW";
    case HttpProtocolOptions_HeadersWithUnderscoresAction.REJECT_REQUEST:
      return "REJECT_REQUEST";
    case HttpProtocolOptions_HeadersWithUnderscoresAction.DROP_HEADER:
      return "DROP_HEADER";
    default:
      throw new globalThis.Error(
        "Unrecognized enum value " + object + " for enum HttpProtocolOptions_HeadersWithUnderscoresAction",
      );
  }
}

export function httpProtocolOptions_HeadersWithUnderscoresActionToNumber(
  object: HttpProtocolOptions_HeadersWithUnderscoresAction,
): number {
  switch (object) {
    case HttpProtocolOptions_HeadersWithUnderscoresAction.ALLOW:
      return 0;
    case HttpProtocolOptions_HeadersWithUnderscoresAction.REJECT_REQUEST:
      return 1;
    case HttpProtocolOptions_HeadersWithUnderscoresAction.DROP_HEADER:
      return 2;
    default:
      throw new globalThis.Error(
        "Unrecognized enum value " + object + " for enum HttpProtocolOptions_HeadersWithUnderscoresAction",
      );
  }
}

/** [#next-free-field: 12] */
export interface Http1ProtocolOptions {
  $type: "envoy.config.core.v3.Http1ProtocolOptions";
  /**
   * Handle HTTP requests with absolute URLs in the requests. These requests
   * are generally sent by clients to forward/explicit proxies. This allows clients to configure
   * envoy as their HTTP proxy. In Unix, for example, this is typically done by setting the
   * ``http_proxy`` environment variable.
   */
  allow_absolute_url?:
    | boolean
    | undefined;
  /**
   * Handle incoming HTTP/1.0 and HTTP/0.9 requests.
   * This is off by default, and not fully standards compliant. There is support for pre-HTTP/1.1
   * style connect logic, dechunking, and handling lack of client host iff
   * ``default_host_for_http_10`` is configured.
   */
  accept_http_10?:
    | boolean
    | undefined;
  /**
   * A default host for HTTP/1.0 requests. This is highly suggested if ``accept_http_10`` is true as
   * Envoy does not otherwise support HTTP/1.0 without a Host header.
   * This is a no-op if ``accept_http_10`` is not true.
   */
  default_host_for_http_10?:
    | string
    | undefined;
  /**
   * Describes how the keys for response headers should be formatted. By default, all header keys
   * are lower cased.
   */
  header_key_format?:
    | Http1ProtocolOptions_HeaderKeyFormat
    | undefined;
  /**
   * Enables trailers for HTTP/1. By default the HTTP/1 codec drops proxied trailers.
   *
   * .. attention::
   *
   *   This only happens when Envoy is chunk encoding which occurs when:
   *   - The request is HTTP/1.1.
   *   - Is neither a ``HEAD`` only request nor a HTTP Upgrade.
   *   - Not a response to a ``HEAD`` request.
   *   - The ``Content-Length`` header is not present.
   */
  enable_trailers?:
    | boolean
    | undefined;
  /**
   * Allows Envoy to process requests/responses with both ``Content-Length`` and ``Transfer-Encoding``
   * headers set. By default such messages are rejected, but if option is enabled - Envoy will
   * remove ``Content-Length`` header and process message.
   * See `RFC7230, sec. 3.3.3 <https://tools.ietf.org/html/rfc7230#section-3.3.3>`_ for details.
   *
   * .. attention::
   *
   *   Enabling this option might lead to request smuggling vulnerability, especially if traffic
   *   is proxied via multiple layers of proxies.
   * [#comment:TODO: This field is ignored when the
   * :ref:`header validation configuration <envoy_v3_api_field_extensions.filters.network.http_connection_manager.v3.HttpConnectionManager.typed_header_validation_config>`
   * is present.]
   */
  allow_chunked_length?:
    | boolean
    | undefined;
  /**
   * Allows invalid HTTP messaging. When this option is false, then Envoy will terminate
   * HTTP/1.1 connections upon receiving an invalid HTTP message. However,
   * when this option is true, then Envoy will leave the HTTP/1.1 connection
   * open where possible.
   * If set, this overrides any HCM :ref:`stream_error_on_invalid_http_messaging
   * <envoy_v3_api_field_extensions.filters.network.http_connection_manager.v3.HttpConnectionManager.stream_error_on_invalid_http_message>`.
   */
  override_stream_error_on_invalid_http_message?:
    | boolean
    | undefined;
  /**
   * Allows sending fully qualified URLs when proxying the first line of the
   * response. By default, Envoy will only send the path components in the first line.
   * If this is true, Envoy will create a fully qualified URI composing scheme
   * (inferred if not present), host (from the host/:authority header) and path
   * (from first line or :path header).
   */
  send_fully_qualified_url?:
    | boolean
    | undefined;
  /**
   * [#not-implemented-hide:] Hiding so that field can be removed after BalsaParser is rolled out.
   * If set, force HTTP/1 parser: BalsaParser if true, http-parser if false.
   * If unset, HTTP/1 parser is selected based on
   * envoy.reloadable_features.http1_use_balsa_parser.
   * See issue #21245.
   *
   * @deprecated
   */
  use_balsa_parser?:
    | boolean
    | undefined;
  /**
   * [#not-implemented-hide:] Hiding so that field can be removed.
   * If true, and BalsaParser is used (either `use_balsa_parser` above is true,
   * or `envoy.reloadable_features.http1_use_balsa_parser` is true and
   * `use_balsa_parser` is unset), then every non-empty method with only valid
   * characters is accepted. Otherwise, methods not on the hard-coded list are
   * rejected.
   * Once UHV is enabled, this field should be removed, and BalsaParser should
   * allow any method. UHV validates the method, rejecting empty string or
   * invalid characters, and provides :ref:`restrict_http_methods
   * <envoy_v3_api_field_extensions.http.header_validators.envoy_default.v3.HeaderValidatorConfig.restrict_http_methods>`
   * to reject custom methods.
   */
  allow_custom_methods?:
    | boolean
    | undefined;
  /**
   * Ignore HTTP/1.1 upgrade values matching any of the supplied matchers.
   *
   * .. note::
   *
   *   ``h2c`` upgrades are always removed for backwards compatibility, regardless of the
   *   value in this setting.
   */
  ignore_http_11_upgrade?: StringMatcher[] | undefined;
}

/** [#next-free-field: 9] */
export interface Http1ProtocolOptions_HeaderKeyFormat {
  $type: "envoy.config.core.v3.Http1ProtocolOptions.HeaderKeyFormat";
  header_format?:
    | //
    /**
     * Formats the header by proper casing words: the first character and any character following
     * a special character will be capitalized if it's an alpha character. For example,
     * ``"content-type"`` becomes ``"Content-Type"``, and ``"foo$b#$are"`` becomes ``"Foo$B#$Are"``.
     *
     * .. note::
     *
     *   While this results in most headers following conventional casing, certain headers
     *   are not covered. For example, the ``"TE"`` header will be formatted as ``"Te"``.
     */
    { $case: "proper_case_words"; proper_case_words: Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords }
    | //
    /**
     * Configuration for stateful formatter extensions that allow using received headers to
     * affect the output of encoding headers. E.g., preserving case during proxying.
     * [#extension-category: envoy.http.stateful_header_formatters]
     */
    { $case: "stateful_formatter"; stateful_formatter: TypedExtensionConfig }
    | undefined;
}

export interface Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords {
  $type: "envoy.config.core.v3.Http1ProtocolOptions.HeaderKeyFormat.ProperCaseWords";
}

export interface KeepaliveSettings {
  $type: "envoy.config.core.v3.KeepaliveSettings";
  /**
   * Send HTTP/2 PING frames at this period, in order to test that the connection is still alive.
   * If this is zero, interval PINGs will not be sent.
   */
  interval?:
    | Duration
    | undefined;
  /**
   * How long to wait for a response to a keepalive PING. If a response is not received within this
   * time period, the connection will be aborted.
   *
   * .. note::
   *
   *   In order to prevent the influence of Head-of-line (HOL) blocking the timeout period is extended when *any* frame is received on
   *   the connection, under the assumption that if a frame is received the connection is healthy.
   */
  timeout?:
    | Duration
    | undefined;
  /**
   * A random jitter amount as a percentage of interval that will be added to each interval.
   * A value of zero means there will be no jitter.
   * The default value is ``15%``.
   */
  interval_jitter?:
    | Percent
    | undefined;
  /**
   * If the connection has been idle for this duration, send a HTTP/2 ping ahead
   * of new stream creation, to quickly detect dead connections.
   * If this is zero, this type of PING will not be sent.
   * If an interval ping is outstanding, a second ping will not be sent as the
   * interval ping will determine if the connection is dead.
   *
   * The same feature for HTTP/3 is given by inheritance from QUICHE which uses :ref:`connection idle_timeout <envoy_v3_api_field_config.listener.v3.QuicProtocolOptions.idle_timeout>` and the current PTO of the connection to decide whether to probe before sending a new request.
   */
  connection_idle_interval?: Duration | undefined;
}

/** [#next-free-field: 19] */
export interface Http2ProtocolOptions {
  $type: "envoy.config.core.v3.Http2ProtocolOptions";
  /**
   * `Maximum table size <https://httpwg.org/specs/rfc7541.html#rfc.section.4.2>`_
   * (in octets) that the encoder is permitted to use for the dynamic HPACK table. Valid values
   * range from ``0`` to ``4294967295`` (``2^32 - 1``) and defaults to ``4096``. ``0`` effectively disables header
   * compression.
   */
  hpack_table_size?:
    | number
    | undefined;
  /**
   * `Maximum concurrent streams <https://httpwg.org/specs/rfc7540.html#rfc.section.5.1.2>`_
   * allowed for peer on one HTTP/2 connection. Valid values range from ``1`` to ``2147483647`` (``2^31 - 1``)
   * and defaults to ``1024`` for safety and should be sufficient for most use cases.
   *
   * For upstream connections, this also limits how many streams Envoy will initiate concurrently
   * on a single connection. If the limit is reached, Envoy may queue requests or establish
   * additional connections (as allowed per circuit breaker limits).
   *
   * This acts as an upper bound: Envoy will lower the max concurrent streams allowed on a given
   * connection based on upstream settings. Config dumps will reflect the configured upper bound,
   * not the per-connection negotiated limits.
   */
  max_concurrent_streams?:
    | number
    | undefined;
  /**
   * `Initial stream-level flow-control window
   * <https://httpwg.org/specs/rfc7540.html#rfc.section.6.9.2>`_ size. Valid values range from ``65535``
   * (``2^16 - 1``, HTTP/2 default) to ``2147483647`` (``2^31 - 1``, HTTP/2 maximum) and defaults to
   * ``16MiB`` (``16 * 1024 * 1024``).
   *
   * .. note::
   *
   *   ``65535`` is the initial window size from HTTP/2 spec. We only support increasing the default window size now,
   *   so it's also the minimum.
   *
   * This field also acts as a soft limit on the number of bytes Envoy will buffer per-stream in the
   * HTTP/2 codec buffers. Once the buffer reaches this pointer, watermark callbacks will fire to
   * stop the flow of data to the codec buffers.
   */
  initial_stream_window_size?:
    | number
    | undefined;
  /**
   * Similar to ``initial_stream_window_size``, but for connection-level flow-control
   * window. The default is ``24MiB`` (``24 * 1024 * 1024``).
   */
  initial_connection_window_size?:
    | number
    | undefined;
  /** Allows proxying Websocket and other upgrades over H2 connect. */
  allow_connect?:
    | boolean
    | undefined;
  /**
   * [#not-implemented-hide:] Hiding until Envoy has full metadata support.
   * Still under implementation. DO NOT USE.
   *
   * Allows sending and receiving HTTP/2 METADATA frames. See [metadata
   * docs](https://github.com/envoyproxy/envoy/blob/main/source/docs/h2_metadata.md) for more
   * information.
   */
  allow_metadata?:
    | boolean
    | undefined;
  /**
   * Limit the number of pending outbound downstream frames of all types (frames that are waiting to
   * be written into the socket). Exceeding this limit triggers flood mitigation and connection is
   * terminated. The ``http2.outbound_flood`` stat tracks the number of terminated connections due
   * to flood mitigation. The default limit is ``10000``.
   */
  max_outbound_frames?:
    | number
    | undefined;
  /**
   * Limit the number of pending outbound downstream frames of types ``PING``, ``SETTINGS`` and ``RST_STREAM``,
   * preventing high memory utilization when receiving continuous stream of these frames. Exceeding
   * this limit triggers flood mitigation and connection is terminated. The
   * ``http2.outbound_control_flood`` stat tracks the number of terminated connections due to flood
   * mitigation. The default limit is ``1000``.
   */
  max_outbound_control_frames?:
    | number
    | undefined;
  /**
   * Limit the number of consecutive inbound frames of types ``HEADERS``, ``CONTINUATION`` and ``DATA`` with an
   * empty payload and no end stream flag. Those frames have no legitimate use and are abusive, but
   * might be a result of a broken HTTP/2 implementation. The ``http2.inbound_empty_frames_flood``
   * stat tracks the number of connections terminated due to flood mitigation.
   * Setting this to ``0`` will terminate connection upon receiving first frame with an empty payload
   * and no end stream flag. The default limit is ``1``.
   */
  max_consecutive_inbound_frames_with_empty_payload?:
    | number
    | undefined;
  /**
   * Limit the number of inbound ``PRIORITY`` frames allowed per each opened stream. If the number
   * of ``PRIORITY`` frames received over the lifetime of connection exceeds the value calculated
   * using this formula::
   *
   *   ``max_inbound_priority_frames_per_stream`` * (1 + ``opened_streams``)
   *
   * the connection is terminated. For downstream connections the ``opened_streams`` is incremented when
   * Envoy receives complete response headers from the upstream server. For upstream connection the
   * ``opened_streams`` is incremented when Envoy sends the ``HEADERS`` frame for a new stream. The
   * ``http2.inbound_priority_frames_flood`` stat tracks
   * the number of connections terminated due to flood mitigation. The default limit is ``100``.
   */
  max_inbound_priority_frames_per_stream?:
    | number
    | undefined;
  /**
   * Limit the number of inbound ``WINDOW_UPDATE`` frames allowed per ``DATA`` frame sent. If the number
   * of ``WINDOW_UPDATE`` frames received over the lifetime of connection exceeds the value calculated
   * using this formula::
   *
   *   ``5 + 2 * (opened_streams +
   *            max_inbound_window_update_frames_per_data_frame_sent * outbound_data_frames)``
   *
   * the connection is terminated. For downstream connections the ``opened_streams`` is incremented when
   * Envoy receives complete response headers from the upstream server. For upstream connections the
   * ``opened_streams`` is incremented when Envoy sends the ``HEADERS`` frame for a new stream. The
   * ``http2.inbound_priority_frames_flood`` stat tracks the number of connections terminated due to
   * flood mitigation. The default ``max_inbound_window_update_frames_per_data_frame_sent`` value is ``10``.
   * Setting this to ``1`` should be enough to support HTTP/2 implementations with basic flow control,
   * but more complex implementations that try to estimate available bandwidth require at least ``2``.
   */
  max_inbound_window_update_frames_per_data_frame_sent?:
    | number
    | undefined;
  /**
   * Allows invalid HTTP messaging and headers. When this option is disabled (default), then
   * the whole HTTP/2 connection is terminated upon receiving invalid HEADERS frame. However,
   * when this option is enabled, only the offending stream is terminated.
   *
   * This is overridden by HCM :ref:`stream_error_on_invalid_http_messaging
   * <envoy_v3_api_field_extensions.filters.network.http_connection_manager.v3.HttpConnectionManager.stream_error_on_invalid_http_message>`
   * iff present.
   *
   * This is deprecated in favor of :ref:`override_stream_error_on_invalid_http_message
   * <envoy_v3_api_field_config.core.v3.Http2ProtocolOptions.override_stream_error_on_invalid_http_message>`
   *
   * See `RFC7540, sec. 8.1 <https://tools.ietf.org/html/rfc7540#section-8.1>`_ for details.
   *
   * @deprecated
   */
  stream_error_on_invalid_http_messaging?:
    | boolean
    | undefined;
  /**
   * Allows invalid HTTP messaging and headers. When this option is disabled (default), then
   * the whole HTTP/2 connection is terminated upon receiving invalid HEADERS frame. However,
   * when this option is enabled, only the offending stream is terminated.
   *
   * This overrides any HCM :ref:`stream_error_on_invalid_http_messaging
   * <envoy_v3_api_field_extensions.filters.network.http_connection_manager.v3.HttpConnectionManager.stream_error_on_invalid_http_message>`
   *
   * See `RFC7540, sec. 8.1 <https://tools.ietf.org/html/rfc7540#section-8.1>`_ for details.
   */
  override_stream_error_on_invalid_http_message?:
    | boolean
    | undefined;
  /**
   * [#not-implemented-hide:]
   * Specifies SETTINGS frame parameters to be sent to the peer, with two exceptions:
   *
   * 1. SETTINGS_ENABLE_PUSH (0x2) is not configurable as HTTP/2 server push is not supported by
   * Envoy.
   *
   * 2. SETTINGS_ENABLE_CONNECT_PROTOCOL (0x8) is only configurable through the named field
   * 'allow_connect'.
   *
   * .. note::
   *
   *   Custom parameters specified through this field can not also be set in the
   *   corresponding named parameters:
   *
   * .. code-block:: text
   *
   *   ID    Field Name
   *   ----------------
   *   0x1   hpack_table_size
   *   0x3   max_concurrent_streams
   *   0x4   initial_stream_window_size
   *
   * Collisions will trigger config validation failure on load/update. Likewise, inconsistencies
   * between custom parameters with the same identifier will trigger a failure.
   *
   * See `IANA HTTP/2 Settings
   * <https://www.iana.org/assignments/http2-parameters/http2-parameters.xhtml#settings>`_ for
   * standardized identifiers.
   */
  custom_settings_parameters?:
    | Http2ProtocolOptions_SettingsParameter[]
    | undefined;
  /**
   * Send HTTP/2 PING frames to verify that the connection is still healthy. If the remote peer
   * does not respond within the configured timeout, the connection will be aborted.
   */
  connection_keepalive?:
    | KeepaliveSettings
    | undefined;
  /**
   * [#not-implemented-hide:] Hiding so that the field can be removed after oghttp2 is rolled out.
   * If set, force use of a particular HTTP/2 codec: oghttp2 if true, nghttp2 if false.
   * If unset, HTTP/2 codec is selected based on envoy.reloadable_features.http2_use_oghttp2.
   */
  use_oghttp2_codec?:
    | boolean
    | undefined;
  /** Configure the maximum amount of metadata than can be handled per stream. Defaults to ``1 MB``. */
  max_metadata_size?:
    | number
    | undefined;
  /**
   * Controls whether to encode headers using huffman encoding.
   * This can be useful in cases where the cpu spent encoding the headers isn't
   * worth the network bandwidth saved e.g. for localhost.
   * If unset, uses the data plane's default value.
   */
  enable_huffman_encoding?: boolean | undefined;
}

/**
 * Defines a parameter to be sent in the SETTINGS frame.
 * See `RFC7540, sec. 6.5.1 <https://tools.ietf.org/html/rfc7540#section-6.5.1>`_ for details.
 */
export interface Http2ProtocolOptions_SettingsParameter {
  $type: "envoy.config.core.v3.Http2ProtocolOptions.SettingsParameter";
  /** The 16 bit parameter identifier. */
  identifier?:
    | number
    | undefined;
  /** The 32 bit parameter value. */
  value?: number | undefined;
}

/** [#not-implemented-hide:] */
export interface GrpcProtocolOptions {
  $type: "envoy.config.core.v3.GrpcProtocolOptions";
  http2_protocol_options?: Http2ProtocolOptions | undefined;
}

/**
 * A message which allows using HTTP/3.
 * [#next-free-field: 9]
 */
export interface Http3ProtocolOptions {
  $type: "envoy.config.core.v3.Http3ProtocolOptions";
  quic_protocol_options?:
    | QuicProtocolOptions
    | undefined;
  /**
   * Allows invalid HTTP messaging and headers. When this option is disabled (default), then
   * the whole HTTP/3 connection is terminated upon receiving invalid HEADERS frame. However,
   * when this option is enabled, only the offending stream is terminated.
   *
   * If set, this overrides any HCM :ref:`stream_error_on_invalid_http_messaging
   * <envoy_v3_api_field_extensions.filters.network.http_connection_manager.v3.HttpConnectionManager.stream_error_on_invalid_http_message>`.
   */
  override_stream_error_on_invalid_http_message?:
    | boolean
    | undefined;
  /**
   * Allows proxying Websocket and other upgrades over HTTP/3 CONNECT using
   * the header mechanisms from the `HTTP/2 extended connect RFC
   * <https://datatracker.ietf.org/doc/html/rfc8441>`_
   * and settings `proposed for HTTP/3
   * <https://datatracker.ietf.org/doc/draft-ietf-httpbis-h3-websockets/>`_
   *
   * .. note::
   *
   *   HTTP/3 CONNECT is not yet an RFC.
   */
  allow_extended_connect?:
    | boolean
    | undefined;
  /**
   * [#not-implemented-hide:] Hiding until Envoy has full metadata support.
   * Still under implementation. DO NOT USE.
   *
   * Allows sending and receiving HTTP/3 METADATA frames. See [metadata
   * docs](https://github.com/envoyproxy/envoy/blob/main/source/docs/h2_metadata.md) for more
   * information.
   */
  allow_metadata?:
    | boolean
    | undefined;
  /**
   * [#not-implemented-hide:] Hiding until Envoy has full HTTP/3 upstream support.
   * Still under implementation. DO NOT USE.
   *
   * Disables QPACK compression related features for HTTP/3 including:
   * No huffman encoding, zero dynamic table capacity and no cookie crumbling.
   * This can be useful for trading off CPU vs bandwidth when an upstream HTTP/3 connection multiplexes multiple downstream connections.
   */
  disable_qpack?:
    | boolean
    | undefined;
  /**
   * Disables connection level flow control for HTTP/3 streams. This is useful in situations where the streams share the same connection
   * but originate from different end-clients, so that each stream can make progress independently at non-front-line proxies.
   */
  disable_connection_flow_control_for_streams?: boolean | undefined;
}

/** A message to control transformations to the :scheme header */
export interface SchemeHeaderTransformation {
  $type: "envoy.config.core.v3.SchemeHeaderTransformation";
  transformation?:
    | //
    /**
     * Overwrite any Scheme header with the contents of this string.
     * If set, takes precedence over ``match_upstream``.
     */
    { $case: "scheme_to_overwrite"; scheme_to_overwrite: string }
    | undefined;
  /**
   * Set the Scheme header to match the upstream transport protocol. For example, should a
   * request be sent to the upstream over TLS, the scheme header will be set to ``"https"``. Should the
   * request be sent over plaintext, the scheme header will be set to ``"http"``.
   * If ``scheme_to_overwrite`` is set, this field is not used.
   */
  match_upstream?: boolean | undefined;
}

function createBaseTcpProtocolOptions(): TcpProtocolOptions {
  return { $type: "envoy.config.core.v3.TcpProtocolOptions" };
}

export const TcpProtocolOptions: MessageFns<TcpProtocolOptions, "envoy.config.core.v3.TcpProtocolOptions"> = {
  $type: "envoy.config.core.v3.TcpProtocolOptions" as const,

  encode(_: TcpProtocolOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TcpProtocolOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTcpProtocolOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): TcpProtocolOptions {
    return { $type: TcpProtocolOptions.$type };
  },

  toJSON(_: TcpProtocolOptions): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<TcpProtocolOptions>, I>>(base?: I): TcpProtocolOptions {
    return TcpProtocolOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TcpProtocolOptions>, I>>(_: I): TcpProtocolOptions {
    const message = createBaseTcpProtocolOptions();
    return message;
  },
};

messageTypeRegistry.set(TcpProtocolOptions.$type, TcpProtocolOptions);

function createBaseQuicKeepAliveSettings(): QuicKeepAliveSettings {
  return { $type: "envoy.config.core.v3.QuicKeepAliveSettings" };
}

export const QuicKeepAliveSettings: MessageFns<QuicKeepAliveSettings, "envoy.config.core.v3.QuicKeepAliveSettings"> = {
  $type: "envoy.config.core.v3.QuicKeepAliveSettings" as const,

  encode(message: QuicKeepAliveSettings, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.max_interval !== undefined) {
      Duration.encode(message.max_interval, writer.uint32(10).fork()).join();
    }
    if (message.initial_interval !== undefined) {
      Duration.encode(message.initial_interval, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuicKeepAliveSettings {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuicKeepAliveSettings();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.max_interval = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.initial_interval = Duration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuicKeepAliveSettings {
    return {
      $type: QuicKeepAliveSettings.$type,
      max_interval: isSet(object.max_interval) ? Duration.fromJSON(object.max_interval) : undefined,
      initial_interval: isSet(object.initial_interval) ? Duration.fromJSON(object.initial_interval) : undefined,
    };
  },

  toJSON(message: QuicKeepAliveSettings): unknown {
    const obj: any = {};
    if (message.max_interval !== undefined) {
      obj.max_interval = Duration.toJSON(message.max_interval);
    }
    if (message.initial_interval !== undefined) {
      obj.initial_interval = Duration.toJSON(message.initial_interval);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuicKeepAliveSettings>, I>>(base?: I): QuicKeepAliveSettings {
    return QuicKeepAliveSettings.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuicKeepAliveSettings>, I>>(object: I): QuicKeepAliveSettings {
    const message = createBaseQuicKeepAliveSettings();
    message.max_interval = (object.max_interval !== undefined && object.max_interval !== null)
      ? Duration.fromPartial(object.max_interval)
      : undefined;
    message.initial_interval = (object.initial_interval !== undefined && object.initial_interval !== null)
      ? Duration.fromPartial(object.initial_interval)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(QuicKeepAliveSettings.$type, QuicKeepAliveSettings);

function createBaseQuicProtocolOptions(): QuicProtocolOptions {
  return { $type: "envoy.config.core.v3.QuicProtocolOptions" };
}

export const QuicProtocolOptions: MessageFns<QuicProtocolOptions, "envoy.config.core.v3.QuicProtocolOptions"> = {
  $type: "envoy.config.core.v3.QuicProtocolOptions" as const,

  encode(message: QuicProtocolOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.max_concurrent_streams !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.max_concurrent_streams! },
        writer.uint32(10).fork(),
      ).join();
    }
    if (message.initial_stream_window_size !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.initial_stream_window_size! },
        writer.uint32(18).fork(),
      ).join();
    }
    if (message.initial_connection_window_size !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.initial_connection_window_size! },
        writer.uint32(26).fork(),
      ).join();
    }
    if (message.num_timeouts_to_trigger_port_migration !== undefined) {
      UInt32Value.encode({
        $type: "google.protobuf.UInt32Value",
        value: message.num_timeouts_to_trigger_port_migration!,
      }, writer.uint32(34).fork()).join();
    }
    if (message.connection_keepalive !== undefined) {
      QuicKeepAliveSettings.encode(message.connection_keepalive, writer.uint32(42).fork()).join();
    }
    if (message.connection_options !== undefined && message.connection_options !== "") {
      writer.uint32(50).string(message.connection_options);
    }
    if (message.client_connection_options !== undefined && message.client_connection_options !== "") {
      writer.uint32(58).string(message.client_connection_options);
    }
    if (message.idle_network_timeout !== undefined) {
      Duration.encode(message.idle_network_timeout, writer.uint32(66).fork()).join();
    }
    if (message.max_packet_length !== undefined) {
      UInt64Value.encode(
        { $type: "google.protobuf.UInt64Value", value: message.max_packet_length! },
        writer.uint32(74).fork(),
      ).join();
    }
    if (message.client_packet_writer !== undefined) {
      TypedExtensionConfig.encode(message.client_packet_writer, writer.uint32(82).fork()).join();
    }
    if (message.connection_migration !== undefined) {
      QuicProtocolOptions_ConnectionMigrationSettings.encode(message.connection_migration, writer.uint32(90).fork())
        .join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuicProtocolOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuicProtocolOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.max_concurrent_streams = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.initial_stream_window_size = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.initial_connection_window_size = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.num_timeouts_to_trigger_port_migration = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.connection_keepalive = QuicKeepAliveSettings.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.connection_options = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.client_connection_options = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.idle_network_timeout = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.max_packet_length = UInt64Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.client_packet_writer = TypedExtensionConfig.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.connection_migration = QuicProtocolOptions_ConnectionMigrationSettings.decode(
            reader,
            reader.uint32(),
          );
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuicProtocolOptions {
    return {
      $type: QuicProtocolOptions.$type,
      max_concurrent_streams: isSet(object.max_concurrent_streams) ? Number(object.max_concurrent_streams) : undefined,
      initial_stream_window_size: isSet(object.initial_stream_window_size)
        ? Number(object.initial_stream_window_size)
        : undefined,
      initial_connection_window_size: isSet(object.initial_connection_window_size)
        ? Number(object.initial_connection_window_size)
        : undefined,
      num_timeouts_to_trigger_port_migration: isSet(object.num_timeouts_to_trigger_port_migration)
        ? Number(object.num_timeouts_to_trigger_port_migration)
        : undefined,
      connection_keepalive: isSet(object.connection_keepalive)
        ? QuicKeepAliveSettings.fromJSON(object.connection_keepalive)
        : undefined,
      connection_options: isSet(object.connection_options) ? globalThis.String(object.connection_options) : undefined,
      client_connection_options: isSet(object.client_connection_options)
        ? globalThis.String(object.client_connection_options)
        : undefined,
      idle_network_timeout: isSet(object.idle_network_timeout)
        ? Duration.fromJSON(object.idle_network_timeout)
        : undefined,
      max_packet_length: isSet(object.max_packet_length) ? Number(object.max_packet_length) : undefined,
      client_packet_writer: isSet(object.client_packet_writer)
        ? TypedExtensionConfig.fromJSON(object.client_packet_writer)
        : undefined,
      connection_migration: isSet(object.connection_migration)
        ? QuicProtocolOptions_ConnectionMigrationSettings.fromJSON(object.connection_migration)
        : undefined,
    };
  },

  toJSON(message: QuicProtocolOptions): unknown {
    const obj: any = {};
    if (message.max_concurrent_streams !== undefined) {
      obj.max_concurrent_streams = message.max_concurrent_streams;
    }
    if (message.initial_stream_window_size !== undefined) {
      obj.initial_stream_window_size = message.initial_stream_window_size;
    }
    if (message.initial_connection_window_size !== undefined) {
      obj.initial_connection_window_size = message.initial_connection_window_size;
    }
    if (message.num_timeouts_to_trigger_port_migration !== undefined) {
      obj.num_timeouts_to_trigger_port_migration = message.num_timeouts_to_trigger_port_migration;
    }
    if (message.connection_keepalive !== undefined) {
      obj.connection_keepalive = QuicKeepAliveSettings.toJSON(message.connection_keepalive);
    }
    if (message.connection_options !== undefined) {
      obj.connection_options = message.connection_options;
    }
    if (message.client_connection_options !== undefined) {
      obj.client_connection_options = message.client_connection_options;
    }
    if (message.idle_network_timeout !== undefined) {
      obj.idle_network_timeout = Duration.toJSON(message.idle_network_timeout);
    }
    if (message.max_packet_length !== undefined) {
      obj.max_packet_length = message.max_packet_length;
    }
    if (message.client_packet_writer !== undefined) {
      obj.client_packet_writer = TypedExtensionConfig.toJSON(message.client_packet_writer);
    }
    if (message.connection_migration !== undefined) {
      obj.connection_migration = QuicProtocolOptions_ConnectionMigrationSettings.toJSON(message.connection_migration);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuicProtocolOptions>, I>>(base?: I): QuicProtocolOptions {
    return QuicProtocolOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuicProtocolOptions>, I>>(object: I): QuicProtocolOptions {
    const message = createBaseQuicProtocolOptions();
    message.max_concurrent_streams = object.max_concurrent_streams ?? undefined;
    message.initial_stream_window_size = object.initial_stream_window_size ?? undefined;
    message.initial_connection_window_size = object.initial_connection_window_size ?? undefined;
    message.num_timeouts_to_trigger_port_migration = object.num_timeouts_to_trigger_port_migration ?? undefined;
    message.connection_keepalive = (object.connection_keepalive !== undefined && object.connection_keepalive !== null)
      ? QuicKeepAliveSettings.fromPartial(object.connection_keepalive)
      : undefined;
    message.connection_options = object.connection_options ?? undefined;
    message.client_connection_options = object.client_connection_options ?? undefined;
    message.idle_network_timeout = (object.idle_network_timeout !== undefined && object.idle_network_timeout !== null)
      ? Duration.fromPartial(object.idle_network_timeout)
      : undefined;
    message.max_packet_length = object.max_packet_length ?? undefined;
    message.client_packet_writer = (object.client_packet_writer !== undefined && object.client_packet_writer !== null)
      ? TypedExtensionConfig.fromPartial(object.client_packet_writer)
      : undefined;
    message.connection_migration = (object.connection_migration !== undefined && object.connection_migration !== null)
      ? QuicProtocolOptions_ConnectionMigrationSettings.fromPartial(object.connection_migration)
      : undefined;
    return message;
  },
};

messageTypeRegistry.set(QuicProtocolOptions.$type, QuicProtocolOptions);

function createBaseQuicProtocolOptions_ConnectionMigrationSettings(): QuicProtocolOptions_ConnectionMigrationSettings {
  return { $type: "envoy.config.core.v3.QuicProtocolOptions.ConnectionMigrationSettings" };
}

export const QuicProtocolOptions_ConnectionMigrationSettings: MessageFns<
  QuicProtocolOptions_ConnectionMigrationSettings,
  "envoy.config.core.v3.QuicProtocolOptions.ConnectionMigrationSettings"
> = {
  $type: "envoy.config.core.v3.QuicProtocolOptions.ConnectionMigrationSettings" as const,

  encode(
    message: QuicProtocolOptions_ConnectionMigrationSettings,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.migrate_idle_connections !== undefined) {
      QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings.encode(
        message.migrate_idle_connections,
        writer.uint32(10).fork(),
      ).join();
    }
    if (message.max_time_on_non_default_network !== undefined) {
      Duration.encode(message.max_time_on_non_default_network, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuicProtocolOptions_ConnectionMigrationSettings {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuicProtocolOptions_ConnectionMigrationSettings();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.migrate_idle_connections =
            QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings.decode(
              reader,
              reader.uint32(),
            );
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.max_time_on_non_default_network = Duration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuicProtocolOptions_ConnectionMigrationSettings {
    return {
      $type: QuicProtocolOptions_ConnectionMigrationSettings.$type,
      migrate_idle_connections: isSet(object.migrate_idle_connections)
        ? QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings.fromJSON(
          object.migrate_idle_connections,
        )
        : undefined,
      max_time_on_non_default_network: isSet(object.max_time_on_non_default_network)
        ? Duration.fromJSON(object.max_time_on_non_default_network)
        : undefined,
    };
  },

  toJSON(message: QuicProtocolOptions_ConnectionMigrationSettings): unknown {
    const obj: any = {};
    if (message.migrate_idle_connections !== undefined) {
      obj.migrate_idle_connections = QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings
        .toJSON(message.migrate_idle_connections);
    }
    if (message.max_time_on_non_default_network !== undefined) {
      obj.max_time_on_non_default_network = Duration.toJSON(message.max_time_on_non_default_network);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuicProtocolOptions_ConnectionMigrationSettings>, I>>(
    base?: I,
  ): QuicProtocolOptions_ConnectionMigrationSettings {
    return QuicProtocolOptions_ConnectionMigrationSettings.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuicProtocolOptions_ConnectionMigrationSettings>, I>>(
    object: I,
  ): QuicProtocolOptions_ConnectionMigrationSettings {
    const message = createBaseQuicProtocolOptions_ConnectionMigrationSettings();
    message.migrate_idle_connections =
      (object.migrate_idle_connections !== undefined && object.migrate_idle_connections !== null)
        ? QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings.fromPartial(
          object.migrate_idle_connections,
        )
        : undefined;
    message.max_time_on_non_default_network =
      (object.max_time_on_non_default_network !== undefined && object.max_time_on_non_default_network !== null)
        ? Duration.fromPartial(object.max_time_on_non_default_network)
        : undefined;
    return message;
  },
};

messageTypeRegistry.set(
  QuicProtocolOptions_ConnectionMigrationSettings.$type,
  QuicProtocolOptions_ConnectionMigrationSettings,
);

function createBaseQuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings(): QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings {
  return {
    $type: "envoy.config.core.v3.QuicProtocolOptions.ConnectionMigrationSettings.MigrateIdleConnectionSettings",
  };
}

export const QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings: MessageFns<
  QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings,
  "envoy.config.core.v3.QuicProtocolOptions.ConnectionMigrationSettings.MigrateIdleConnectionSettings"
> = {
  $type: "envoy.config.core.v3.QuicProtocolOptions.ConnectionMigrationSettings.MigrateIdleConnectionSettings" as const,

  encode(
    message: QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.max_idle_time_before_migration !== undefined) {
      Duration.encode(message.max_idle_time_before_migration, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number,
  ): QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.max_idle_time_before_migration = Duration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings {
    return {
      $type: QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings.$type,
      max_idle_time_before_migration: isSet(object.max_idle_time_before_migration)
        ? Duration.fromJSON(object.max_idle_time_before_migration)
        : undefined,
    };
  },

  toJSON(message: QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings): unknown {
    const obj: any = {};
    if (message.max_idle_time_before_migration !== undefined) {
      obj.max_idle_time_before_migration = Duration.toJSON(message.max_idle_time_before_migration);
    }
    return obj;
  },

  create<
    I extends Exact<DeepPartial<QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings>, I>,
  >(base?: I): QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings {
    return QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings.fromPartial(
      base ?? ({} as any),
    );
  },
  fromPartial<
    I extends Exact<DeepPartial<QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings>, I>,
  >(object: I): QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings {
    const message = createBaseQuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings();
    message.max_idle_time_before_migration =
      (object.max_idle_time_before_migration !== undefined && object.max_idle_time_before_migration !== null)
        ? Duration.fromPartial(object.max_idle_time_before_migration)
        : undefined;
    return message;
  },
};

messageTypeRegistry.set(
  QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings.$type,
  QuicProtocolOptions_ConnectionMigrationSettings_MigrateIdleConnectionSettings,
);

function createBaseUpstreamHttpProtocolOptions(): UpstreamHttpProtocolOptions {
  return { $type: "envoy.config.core.v3.UpstreamHttpProtocolOptions" };
}

export const UpstreamHttpProtocolOptions: MessageFns<
  UpstreamHttpProtocolOptions,
  "envoy.config.core.v3.UpstreamHttpProtocolOptions"
> = {
  $type: "envoy.config.core.v3.UpstreamHttpProtocolOptions" as const,

  encode(message: UpstreamHttpProtocolOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.auto_sni !== undefined && message.auto_sni !== false) {
      writer.uint32(8).bool(message.auto_sni);
    }
    if (message.auto_san_validation !== undefined && message.auto_san_validation !== false) {
      writer.uint32(16).bool(message.auto_san_validation);
    }
    if (message.override_auto_sni_header !== undefined && message.override_auto_sni_header !== "") {
      writer.uint32(26).string(message.override_auto_sni_header);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpstreamHttpProtocolOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpstreamHttpProtocolOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.auto_sni = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.auto_san_validation = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.override_auto_sni_header = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpstreamHttpProtocolOptions {
    return {
      $type: UpstreamHttpProtocolOptions.$type,
      auto_sni: isSet(object.auto_sni) ? globalThis.Boolean(object.auto_sni) : undefined,
      auto_san_validation: isSet(object.auto_san_validation)
        ? globalThis.Boolean(object.auto_san_validation)
        : undefined,
      override_auto_sni_header: isSet(object.override_auto_sni_header)
        ? globalThis.String(object.override_auto_sni_header)
        : undefined,
    };
  },

  toJSON(message: UpstreamHttpProtocolOptions): unknown {
    const obj: any = {};
    if (message.auto_sni !== undefined) {
      obj.auto_sni = message.auto_sni;
    }
    if (message.auto_san_validation !== undefined) {
      obj.auto_san_validation = message.auto_san_validation;
    }
    if (message.override_auto_sni_header !== undefined) {
      obj.override_auto_sni_header = message.override_auto_sni_header;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpstreamHttpProtocolOptions>, I>>(base?: I): UpstreamHttpProtocolOptions {
    return UpstreamHttpProtocolOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpstreamHttpProtocolOptions>, I>>(object: I): UpstreamHttpProtocolOptions {
    const message = createBaseUpstreamHttpProtocolOptions();
    message.auto_sni = object.auto_sni ?? undefined;
    message.auto_san_validation = object.auto_san_validation ?? undefined;
    message.override_auto_sni_header = object.override_auto_sni_header ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(UpstreamHttpProtocolOptions.$type, UpstreamHttpProtocolOptions);

function createBaseAlternateProtocolsCacheOptions(): AlternateProtocolsCacheOptions {
  return { $type: "envoy.config.core.v3.AlternateProtocolsCacheOptions" };
}

export const AlternateProtocolsCacheOptions: MessageFns<
  AlternateProtocolsCacheOptions,
  "envoy.config.core.v3.AlternateProtocolsCacheOptions"
> = {
  $type: "envoy.config.core.v3.AlternateProtocolsCacheOptions" as const,

  encode(message: AlternateProtocolsCacheOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== undefined && message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.max_entries !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.max_entries! },
        writer.uint32(18).fork(),
      ).join();
    }
    if (message.key_value_store_config !== undefined) {
      TypedExtensionConfig.encode(message.key_value_store_config, writer.uint32(26).fork()).join();
    }
    if (message.prepopulated_entries !== undefined && message.prepopulated_entries.length !== 0) {
      for (const v of message.prepopulated_entries) {
        AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry.encode(v!, writer.uint32(34).fork()).join();
      }
    }
    if (message.canonical_suffixes !== undefined && message.canonical_suffixes.length !== 0) {
      for (const v of message.canonical_suffixes) {
        writer.uint32(42).string(v!);
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AlternateProtocolsCacheOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAlternateProtocolsCacheOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.max_entries = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.key_value_store_config = TypedExtensionConfig.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          if (message.prepopulated_entries === undefined) {
            message.prepopulated_entries = [];
          }
          const el = AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.prepopulated_entries!.push(el);
          }
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          if (message.canonical_suffixes === undefined) {
            message.canonical_suffixes = [];
          }
          const el = reader.string();
          if (el !== undefined) {
            message.canonical_suffixes!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AlternateProtocolsCacheOptions {
    return {
      $type: AlternateProtocolsCacheOptions.$type,
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      max_entries: isSet(object.max_entries) ? Number(object.max_entries) : undefined,
      key_value_store_config: isSet(object.key_value_store_config)
        ? TypedExtensionConfig.fromJSON(object.key_value_store_config)
        : undefined,
      prepopulated_entries: globalThis.Array.isArray(object?.prepopulated_entries)
        ? object.prepopulated_entries.map((e: any) =>
          AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry.fromJSON(e)
        )
        : undefined,
      canonical_suffixes: globalThis.Array.isArray(object?.canonical_suffixes)
        ? object.canonical_suffixes.map((e: any) => globalThis.String(e))
        : undefined,
    };
  },

  toJSON(message: AlternateProtocolsCacheOptions): unknown {
    const obj: any = {};
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.max_entries !== undefined) {
      obj.max_entries = message.max_entries;
    }
    if (message.key_value_store_config !== undefined) {
      obj.key_value_store_config = TypedExtensionConfig.toJSON(message.key_value_store_config);
    }
    if (message.prepopulated_entries?.length) {
      obj.prepopulated_entries = message.prepopulated_entries.map((e) =>
        AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry.toJSON(e)
      );
    }
    if (message.canonical_suffixes?.length) {
      obj.canonical_suffixes = message.canonical_suffixes;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AlternateProtocolsCacheOptions>, I>>(base?: I): AlternateProtocolsCacheOptions {
    return AlternateProtocolsCacheOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AlternateProtocolsCacheOptions>, I>>(
    object: I,
  ): AlternateProtocolsCacheOptions {
    const message = createBaseAlternateProtocolsCacheOptions();
    message.name = object.name ?? undefined;
    message.max_entries = object.max_entries ?? undefined;
    message.key_value_store_config =
      (object.key_value_store_config !== undefined && object.key_value_store_config !== null)
        ? TypedExtensionConfig.fromPartial(object.key_value_store_config)
        : undefined;
    message.prepopulated_entries =
      object.prepopulated_entries?.map((e) =>
        AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry.fromPartial(e)
      ) || undefined;
    message.canonical_suffixes = object.canonical_suffixes?.map((e) => e) || undefined;
    return message;
  },
};

messageTypeRegistry.set(AlternateProtocolsCacheOptions.$type, AlternateProtocolsCacheOptions);

function createBaseAlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry(): AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry {
  return { $type: "envoy.config.core.v3.AlternateProtocolsCacheOptions.AlternateProtocolsCacheEntry" };
}

export const AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry: MessageFns<
  AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry,
  "envoy.config.core.v3.AlternateProtocolsCacheOptions.AlternateProtocolsCacheEntry"
> = {
  $type: "envoy.config.core.v3.AlternateProtocolsCacheOptions.AlternateProtocolsCacheEntry" as const,

  encode(
    message: AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.hostname !== undefined && message.hostname !== "") {
      writer.uint32(10).string(message.hostname);
    }
    if (message.port !== undefined && message.port !== 0) {
      writer.uint32(16).uint32(message.port);
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number,
  ): AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hostname = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.port = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry {
    return {
      $type: AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry.$type,
      hostname: isSet(object.hostname) ? globalThis.String(object.hostname) : undefined,
      port: isSet(object.port) ? globalThis.Number(object.port) : undefined,
    };
  },

  toJSON(message: AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry): unknown {
    const obj: any = {};
    if (message.hostname !== undefined) {
      obj.hostname = message.hostname;
    }
    if (message.port !== undefined) {
      obj.port = Math.round(message.port);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry>, I>>(
    base?: I,
  ): AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry {
    return AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry>, I>>(
    object: I,
  ): AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry {
    const message = createBaseAlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry();
    message.hostname = object.hostname ?? undefined;
    message.port = object.port ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(
  AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry.$type,
  AlternateProtocolsCacheOptions_AlternateProtocolsCacheEntry,
);

function createBaseHttpProtocolOptions(): HttpProtocolOptions {
  return { $type: "envoy.config.core.v3.HttpProtocolOptions" };
}

export const HttpProtocolOptions: MessageFns<HttpProtocolOptions, "envoy.config.core.v3.HttpProtocolOptions"> = {
  $type: "envoy.config.core.v3.HttpProtocolOptions" as const,

  encode(message: HttpProtocolOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.idle_timeout !== undefined) {
      Duration.encode(message.idle_timeout, writer.uint32(10).fork()).join();
    }
    if (message.max_connection_duration !== undefined) {
      Duration.encode(message.max_connection_duration, writer.uint32(26).fork()).join();
    }
    if (message.max_headers_count !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.max_headers_count! },
        writer.uint32(18).fork(),
      ).join();
    }
    if (message.max_response_headers_kb !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.max_response_headers_kb! },
        writer.uint32(58).fork(),
      ).join();
    }
    if (message.max_stream_duration !== undefined) {
      Duration.encode(message.max_stream_duration, writer.uint32(34).fork()).join();
    }
    if (
      message.headers_with_underscores_action !== undefined &&
      message.headers_with_underscores_action !== HttpProtocolOptions_HeadersWithUnderscoresAction.ALLOW
    ) {
      writer.uint32(40).int32(
        httpProtocolOptions_HeadersWithUnderscoresActionToNumber(message.headers_with_underscores_action),
      );
    }
    if (message.max_requests_per_connection !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.max_requests_per_connection! },
        writer.uint32(50).fork(),
      ).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HttpProtocolOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttpProtocolOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.idle_timeout = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.max_connection_duration = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.max_headers_count = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.max_response_headers_kb = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.max_stream_duration = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.headers_with_underscores_action = httpProtocolOptions_HeadersWithUnderscoresActionFromJSON(
            reader.int32(),
          );
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.max_requests_per_connection = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HttpProtocolOptions {
    return {
      $type: HttpProtocolOptions.$type,
      idle_timeout: isSet(object.idle_timeout) ? Duration.fromJSON(object.idle_timeout) : undefined,
      max_connection_duration: isSet(object.max_connection_duration)
        ? Duration.fromJSON(object.max_connection_duration)
        : undefined,
      max_headers_count: isSet(object.max_headers_count) ? Number(object.max_headers_count) : undefined,
      max_response_headers_kb: isSet(object.max_response_headers_kb)
        ? Number(object.max_response_headers_kb)
        : undefined,
      max_stream_duration: isSet(object.max_stream_duration)
        ? Duration.fromJSON(object.max_stream_duration)
        : undefined,
      headers_with_underscores_action: isSet(object.headers_with_underscores_action)
        ? httpProtocolOptions_HeadersWithUnderscoresActionFromJSON(object.headers_with_underscores_action)
        : undefined,
      max_requests_per_connection: isSet(object.max_requests_per_connection)
        ? Number(object.max_requests_per_connection)
        : undefined,
    };
  },

  toJSON(message: HttpProtocolOptions): unknown {
    const obj: any = {};
    if (message.idle_timeout !== undefined) {
      obj.idle_timeout = Duration.toJSON(message.idle_timeout);
    }
    if (message.max_connection_duration !== undefined) {
      obj.max_connection_duration = Duration.toJSON(message.max_connection_duration);
    }
    if (message.max_headers_count !== undefined) {
      obj.max_headers_count = message.max_headers_count;
    }
    if (message.max_response_headers_kb !== undefined) {
      obj.max_response_headers_kb = message.max_response_headers_kb;
    }
    if (message.max_stream_duration !== undefined) {
      obj.max_stream_duration = Duration.toJSON(message.max_stream_duration);
    }
    if (message.headers_with_underscores_action !== undefined) {
      obj.headers_with_underscores_action = httpProtocolOptions_HeadersWithUnderscoresActionToJSON(
        message.headers_with_underscores_action,
      );
    }
    if (message.max_requests_per_connection !== undefined) {
      obj.max_requests_per_connection = message.max_requests_per_connection;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HttpProtocolOptions>, I>>(base?: I): HttpProtocolOptions {
    return HttpProtocolOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HttpProtocolOptions>, I>>(object: I): HttpProtocolOptions {
    const message = createBaseHttpProtocolOptions();
    message.idle_timeout = (object.idle_timeout !== undefined && object.idle_timeout !== null)
      ? Duration.fromPartial(object.idle_timeout)
      : undefined;
    message.max_connection_duration =
      (object.max_connection_duration !== undefined && object.max_connection_duration !== null)
        ? Duration.fromPartial(object.max_connection_duration)
        : undefined;
    message.max_headers_count = object.max_headers_count ?? undefined;
    message.max_response_headers_kb = object.max_response_headers_kb ?? undefined;
    message.max_stream_duration = (object.max_stream_duration !== undefined && object.max_stream_duration !== null)
      ? Duration.fromPartial(object.max_stream_duration)
      : undefined;
    message.headers_with_underscores_action = object.headers_with_underscores_action ?? undefined;
    message.max_requests_per_connection = object.max_requests_per_connection ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(HttpProtocolOptions.$type, HttpProtocolOptions);

function createBaseHttp1ProtocolOptions(): Http1ProtocolOptions {
  return { $type: "envoy.config.core.v3.Http1ProtocolOptions" };
}

export const Http1ProtocolOptions: MessageFns<Http1ProtocolOptions, "envoy.config.core.v3.Http1ProtocolOptions"> = {
  $type: "envoy.config.core.v3.Http1ProtocolOptions" as const,

  encode(message: Http1ProtocolOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.allow_absolute_url !== undefined) {
      BoolValue.encode(
        { $type: "google.protobuf.BoolValue", value: message.allow_absolute_url! },
        writer.uint32(10).fork(),
      ).join();
    }
    if (message.accept_http_10 !== undefined && message.accept_http_10 !== false) {
      writer.uint32(16).bool(message.accept_http_10);
    }
    if (message.default_host_for_http_10 !== undefined && message.default_host_for_http_10 !== "") {
      writer.uint32(26).string(message.default_host_for_http_10);
    }
    if (message.header_key_format !== undefined) {
      Http1ProtocolOptions_HeaderKeyFormat.encode(message.header_key_format, writer.uint32(34).fork()).join();
    }
    if (message.enable_trailers !== undefined && message.enable_trailers !== false) {
      writer.uint32(40).bool(message.enable_trailers);
    }
    if (message.allow_chunked_length !== undefined && message.allow_chunked_length !== false) {
      writer.uint32(48).bool(message.allow_chunked_length);
    }
    if (message.override_stream_error_on_invalid_http_message !== undefined) {
      BoolValue.encode({
        $type: "google.protobuf.BoolValue",
        value: message.override_stream_error_on_invalid_http_message!,
      }, writer.uint32(58).fork()).join();
    }
    if (message.send_fully_qualified_url !== undefined && message.send_fully_qualified_url !== false) {
      writer.uint32(64).bool(message.send_fully_qualified_url);
    }
    if (message.use_balsa_parser !== undefined) {
      BoolValue.encode(
        { $type: "google.protobuf.BoolValue", value: message.use_balsa_parser! },
        writer.uint32(74).fork(),
      ).join();
    }
    if (message.allow_custom_methods !== undefined && message.allow_custom_methods !== false) {
      writer.uint32(80).bool(message.allow_custom_methods);
    }
    if (message.ignore_http_11_upgrade !== undefined && message.ignore_http_11_upgrade.length !== 0) {
      for (const v of message.ignore_http_11_upgrade) {
        StringMatcher.encode(v!, writer.uint32(90).fork()).join();
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Http1ProtocolOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttp1ProtocolOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.allow_absolute_url = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.accept_http_10 = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.default_host_for_http_10 = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.header_key_format = Http1ProtocolOptions_HeaderKeyFormat.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.enable_trailers = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.allow_chunked_length = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.override_stream_error_on_invalid_http_message = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.send_fully_qualified_url = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.use_balsa_parser = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.allow_custom_methods = reader.bool();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          if (message.ignore_http_11_upgrade === undefined) {
            message.ignore_http_11_upgrade = [];
          }
          const el = StringMatcher.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.ignore_http_11_upgrade!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Http1ProtocolOptions {
    return {
      $type: Http1ProtocolOptions.$type,
      allow_absolute_url: isSet(object.allow_absolute_url) ? Boolean(object.allow_absolute_url) : undefined,
      accept_http_10: isSet(object.accept_http_10) ? globalThis.Boolean(object.accept_http_10) : undefined,
      default_host_for_http_10: isSet(object.default_host_for_http_10)
        ? globalThis.String(object.default_host_for_http_10)
        : undefined,
      header_key_format: isSet(object.header_key_format)
        ? Http1ProtocolOptions_HeaderKeyFormat.fromJSON(object.header_key_format)
        : undefined,
      enable_trailers: isSet(object.enable_trailers) ? globalThis.Boolean(object.enable_trailers) : undefined,
      allow_chunked_length: isSet(object.allow_chunked_length)
        ? globalThis.Boolean(object.allow_chunked_length)
        : undefined,
      override_stream_error_on_invalid_http_message: isSet(object.override_stream_error_on_invalid_http_message)
        ? Boolean(object.override_stream_error_on_invalid_http_message)
        : undefined,
      send_fully_qualified_url: isSet(object.send_fully_qualified_url)
        ? globalThis.Boolean(object.send_fully_qualified_url)
        : undefined,
      use_balsa_parser: isSet(object.use_balsa_parser) ? Boolean(object.use_balsa_parser) : undefined,
      allow_custom_methods: isSet(object.allow_custom_methods)
        ? globalThis.Boolean(object.allow_custom_methods)
        : undefined,
      ignore_http_11_upgrade: globalThis.Array.isArray(object?.ignore_http_11_upgrade)
        ? object.ignore_http_11_upgrade.map((e: any) => StringMatcher.fromJSON(e))
        : undefined,
    };
  },

  toJSON(message: Http1ProtocolOptions): unknown {
    const obj: any = {};
    if (message.allow_absolute_url !== undefined) {
      obj.allow_absolute_url = message.allow_absolute_url;
    }
    if (message.accept_http_10 !== undefined) {
      obj.accept_http_10 = message.accept_http_10;
    }
    if (message.default_host_for_http_10 !== undefined) {
      obj.default_host_for_http_10 = message.default_host_for_http_10;
    }
    if (message.header_key_format !== undefined) {
      obj.header_key_format = Http1ProtocolOptions_HeaderKeyFormat.toJSON(message.header_key_format);
    }
    if (message.enable_trailers !== undefined) {
      obj.enable_trailers = message.enable_trailers;
    }
    if (message.allow_chunked_length !== undefined) {
      obj.allow_chunked_length = message.allow_chunked_length;
    }
    if (message.override_stream_error_on_invalid_http_message !== undefined) {
      obj.override_stream_error_on_invalid_http_message = message.override_stream_error_on_invalid_http_message;
    }
    if (message.send_fully_qualified_url !== undefined) {
      obj.send_fully_qualified_url = message.send_fully_qualified_url;
    }
    if (message.use_balsa_parser !== undefined) {
      obj.use_balsa_parser = message.use_balsa_parser;
    }
    if (message.allow_custom_methods !== undefined) {
      obj.allow_custom_methods = message.allow_custom_methods;
    }
    if (message.ignore_http_11_upgrade?.length) {
      obj.ignore_http_11_upgrade = message.ignore_http_11_upgrade.map((e) => StringMatcher.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Http1ProtocolOptions>, I>>(base?: I): Http1ProtocolOptions {
    return Http1ProtocolOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Http1ProtocolOptions>, I>>(object: I): Http1ProtocolOptions {
    const message = createBaseHttp1ProtocolOptions();
    message.allow_absolute_url = object.allow_absolute_url ?? undefined;
    message.accept_http_10 = object.accept_http_10 ?? undefined;
    message.default_host_for_http_10 = object.default_host_for_http_10 ?? undefined;
    message.header_key_format = (object.header_key_format !== undefined && object.header_key_format !== null)
      ? Http1ProtocolOptions_HeaderKeyFormat.fromPartial(object.header_key_format)
      : undefined;
    message.enable_trailers = object.enable_trailers ?? undefined;
    message.allow_chunked_length = object.allow_chunked_length ?? undefined;
    message.override_stream_error_on_invalid_http_message = object.override_stream_error_on_invalid_http_message ??
      undefined;
    message.send_fully_qualified_url = object.send_fully_qualified_url ?? undefined;
    message.use_balsa_parser = object.use_balsa_parser ?? undefined;
    message.allow_custom_methods = object.allow_custom_methods ?? undefined;
    message.ignore_http_11_upgrade = object.ignore_http_11_upgrade?.map((e) => StringMatcher.fromPartial(e)) ||
      undefined;
    return message;
  },
};

messageTypeRegistry.set(Http1ProtocolOptions.$type, Http1ProtocolOptions);

function createBaseHttp1ProtocolOptions_HeaderKeyFormat(): Http1ProtocolOptions_HeaderKeyFormat {
  return { $type: "envoy.config.core.v3.Http1ProtocolOptions.HeaderKeyFormat", header_format: undefined };
}

export const Http1ProtocolOptions_HeaderKeyFormat: MessageFns<
  Http1ProtocolOptions_HeaderKeyFormat,
  "envoy.config.core.v3.Http1ProtocolOptions.HeaderKeyFormat"
> = {
  $type: "envoy.config.core.v3.Http1ProtocolOptions.HeaderKeyFormat" as const,

  encode(message: Http1ProtocolOptions_HeaderKeyFormat, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.header_format?.$case) {
      case "proper_case_words":
        Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords.encode(
          message.header_format.proper_case_words,
          writer.uint32(10).fork(),
        ).join();
        break;
      case "stateful_formatter":
        TypedExtensionConfig.encode(message.header_format.stateful_formatter, writer.uint32(66).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Http1ProtocolOptions_HeaderKeyFormat {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttp1ProtocolOptions_HeaderKeyFormat();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.header_format = {
            $case: "proper_case_words",
            proper_case_words: Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.header_format = {
            $case: "stateful_formatter",
            stateful_formatter: TypedExtensionConfig.decode(reader, reader.uint32()),
          };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Http1ProtocolOptions_HeaderKeyFormat {
    return {
      $type: Http1ProtocolOptions_HeaderKeyFormat.$type,
      header_format: isSet(object.proper_case_words)
        ? {
          $case: "proper_case_words",
          proper_case_words: Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords.fromJSON(object.proper_case_words),
        }
        : isSet(object.stateful_formatter)
        ? { $case: "stateful_formatter", stateful_formatter: TypedExtensionConfig.fromJSON(object.stateful_formatter) }
        : undefined,
    };
  },

  toJSON(message: Http1ProtocolOptions_HeaderKeyFormat): unknown {
    const obj: any = {};
    if (message.header_format?.$case === "proper_case_words") {
      obj.proper_case_words = Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords.toJSON(
        message.header_format.proper_case_words,
      );
    }
    if (message.header_format?.$case === "stateful_formatter") {
      obj.stateful_formatter = TypedExtensionConfig.toJSON(message.header_format.stateful_formatter);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Http1ProtocolOptions_HeaderKeyFormat>, I>>(
    base?: I,
  ): Http1ProtocolOptions_HeaderKeyFormat {
    return Http1ProtocolOptions_HeaderKeyFormat.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Http1ProtocolOptions_HeaderKeyFormat>, I>>(
    object: I,
  ): Http1ProtocolOptions_HeaderKeyFormat {
    const message = createBaseHttp1ProtocolOptions_HeaderKeyFormat();
    if (
      object.header_format?.$case === "proper_case_words" &&
      object.header_format?.proper_case_words !== undefined &&
      object.header_format?.proper_case_words !== null
    ) {
      message.header_format = {
        $case: "proper_case_words",
        proper_case_words: Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords.fromPartial(
          object.header_format.proper_case_words,
        ),
      };
    }
    if (
      object.header_format?.$case === "stateful_formatter" &&
      object.header_format?.stateful_formatter !== undefined &&
      object.header_format?.stateful_formatter !== null
    ) {
      message.header_format = {
        $case: "stateful_formatter",
        stateful_formatter: TypedExtensionConfig.fromPartial(object.header_format.stateful_formatter),
      };
    }
    return message;
  },
};

messageTypeRegistry.set(Http1ProtocolOptions_HeaderKeyFormat.$type, Http1ProtocolOptions_HeaderKeyFormat);

function createBaseHttp1ProtocolOptions_HeaderKeyFormat_ProperCaseWords(): Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords {
  return { $type: "envoy.config.core.v3.Http1ProtocolOptions.HeaderKeyFormat.ProperCaseWords" };
}

export const Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords: MessageFns<
  Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords,
  "envoy.config.core.v3.Http1ProtocolOptions.HeaderKeyFormat.ProperCaseWords"
> = {
  $type: "envoy.config.core.v3.Http1ProtocolOptions.HeaderKeyFormat.ProperCaseWords" as const,

  encode(
    _: Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttp1ProtocolOptions_HeaderKeyFormat_ProperCaseWords();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords {
    return { $type: Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords.$type };
  },

  toJSON(_: Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords>, I>>(
    base?: I,
  ): Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords {
    return Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords>, I>>(
    _: I,
  ): Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords {
    const message = createBaseHttp1ProtocolOptions_HeaderKeyFormat_ProperCaseWords();
    return message;
  },
};

messageTypeRegistry.set(
  Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords.$type,
  Http1ProtocolOptions_HeaderKeyFormat_ProperCaseWords,
);

function createBaseKeepaliveSettings(): KeepaliveSettings {
  return { $type: "envoy.config.core.v3.KeepaliveSettings" };
}

export const KeepaliveSettings: MessageFns<KeepaliveSettings, "envoy.config.core.v3.KeepaliveSettings"> = {
  $type: "envoy.config.core.v3.KeepaliveSettings" as const,

  encode(message: KeepaliveSettings, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.interval !== undefined) {
      Duration.encode(message.interval, writer.uint32(10).fork()).join();
    }
    if (message.timeout !== undefined) {
      Duration.encode(message.timeout, writer.uint32(18).fork()).join();
    }
    if (message.interval_jitter !== undefined) {
      Percent.encode(message.interval_jitter, writer.uint32(26).fork()).join();
    }
    if (message.connection_idle_interval !== undefined) {
      Duration.encode(message.connection_idle_interval, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeepaliveSettings {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeepaliveSettings();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.interval = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timeout = Duration.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.interval_jitter = Percent.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.connection_idle_interval = Duration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KeepaliveSettings {
    return {
      $type: KeepaliveSettings.$type,
      interval: isSet(object.interval) ? Duration.fromJSON(object.interval) : undefined,
      timeout: isSet(object.timeout) ? Duration.fromJSON(object.timeout) : undefined,
      interval_jitter: isSet(object.interval_jitter) ? Percent.fromJSON(object.interval_jitter) : undefined,
      connection_idle_interval: isSet(object.connection_idle_interval)
        ? Duration.fromJSON(object.connection_idle_interval)
        : undefined,
    };
  },

  toJSON(message: KeepaliveSettings): unknown {
    const obj: any = {};
    if (message.interval !== undefined) {
      obj.interval = Duration.toJSON(message.interval);
    }
    if (message.timeout !== undefined) {
      obj.timeout = Duration.toJSON(message.timeout);
    }
    if (message.interval_jitter !== undefined) {
      obj.interval_jitter = Percent.toJSON(message.interval_jitter);
    }
    if (message.connection_idle_interval !== undefined) {
      obj.connection_idle_interval = Duration.toJSON(message.connection_idle_interval);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KeepaliveSettings>, I>>(base?: I): KeepaliveSettings {
    return KeepaliveSettings.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KeepaliveSettings>, I>>(object: I): KeepaliveSettings {
    const message = createBaseKeepaliveSettings();
    message.interval = (object.interval !== undefined && object.interval !== null)
      ? Duration.fromPartial(object.interval)
      : undefined;
    message.timeout = (object.timeout !== undefined && object.timeout !== null)
      ? Duration.fromPartial(object.timeout)
      : undefined;
    message.interval_jitter = (object.interval_jitter !== undefined && object.interval_jitter !== null)
      ? Percent.fromPartial(object.interval_jitter)
      : undefined;
    message.connection_idle_interval =
      (object.connection_idle_interval !== undefined && object.connection_idle_interval !== null)
        ? Duration.fromPartial(object.connection_idle_interval)
        : undefined;
    return message;
  },
};

messageTypeRegistry.set(KeepaliveSettings.$type, KeepaliveSettings);

function createBaseHttp2ProtocolOptions(): Http2ProtocolOptions {
  return { $type: "envoy.config.core.v3.Http2ProtocolOptions" };
}

export const Http2ProtocolOptions: MessageFns<Http2ProtocolOptions, "envoy.config.core.v3.Http2ProtocolOptions"> = {
  $type: "envoy.config.core.v3.Http2ProtocolOptions" as const,

  encode(message: Http2ProtocolOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hpack_table_size !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.hpack_table_size! },
        writer.uint32(10).fork(),
      ).join();
    }
    if (message.max_concurrent_streams !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.max_concurrent_streams! },
        writer.uint32(18).fork(),
      ).join();
    }
    if (message.initial_stream_window_size !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.initial_stream_window_size! },
        writer.uint32(26).fork(),
      ).join();
    }
    if (message.initial_connection_window_size !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.initial_connection_window_size! },
        writer.uint32(34).fork(),
      ).join();
    }
    if (message.allow_connect !== undefined && message.allow_connect !== false) {
      writer.uint32(40).bool(message.allow_connect);
    }
    if (message.allow_metadata !== undefined && message.allow_metadata !== false) {
      writer.uint32(48).bool(message.allow_metadata);
    }
    if (message.max_outbound_frames !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.max_outbound_frames! },
        writer.uint32(58).fork(),
      ).join();
    }
    if (message.max_outbound_control_frames !== undefined) {
      UInt32Value.encode(
        { $type: "google.protobuf.UInt32Value", value: message.max_outbound_control_frames! },
        writer.uint32(66).fork(),
      ).join();
    }
    if (message.max_consecutive_inbound_frames_with_empty_payload !== undefined) {
      UInt32Value.encode({
        $type: "google.protobuf.UInt32Value",
        value: message.max_consecutive_inbound_frames_with_empty_payload!,
      }, writer.uint32(74).fork()).join();
    }
    if (message.max_inbound_priority_frames_per_stream !== undefined) {
      UInt32Value.encode({
        $type: "google.protobuf.UInt32Value",
        value: message.max_inbound_priority_frames_per_stream!,
      }, writer.uint32(82).fork()).join();
    }
    if (message.max_inbound_window_update_frames_per_data_frame_sent !== undefined) {
      UInt32Value.encode({
        $type: "google.protobuf.UInt32Value",
        value: message.max_inbound_window_update_frames_per_data_frame_sent!,
      }, writer.uint32(90).fork()).join();
    }
    if (
      message.stream_error_on_invalid_http_messaging !== undefined &&
      message.stream_error_on_invalid_http_messaging !== false
    ) {
      writer.uint32(96).bool(message.stream_error_on_invalid_http_messaging);
    }
    if (message.override_stream_error_on_invalid_http_message !== undefined) {
      BoolValue.encode({
        $type: "google.protobuf.BoolValue",
        value: message.override_stream_error_on_invalid_http_message!,
      }, writer.uint32(114).fork()).join();
    }
    if (message.custom_settings_parameters !== undefined && message.custom_settings_parameters.length !== 0) {
      for (const v of message.custom_settings_parameters) {
        Http2ProtocolOptions_SettingsParameter.encode(v!, writer.uint32(106).fork()).join();
      }
    }
    if (message.connection_keepalive !== undefined) {
      KeepaliveSettings.encode(message.connection_keepalive, writer.uint32(122).fork()).join();
    }
    if (message.use_oghttp2_codec !== undefined) {
      BoolValue.encode(
        { $type: "google.protobuf.BoolValue", value: message.use_oghttp2_codec! },
        writer.uint32(130).fork(),
      ).join();
    }
    if (message.max_metadata_size !== undefined) {
      UInt64Value.encode(
        { $type: "google.protobuf.UInt64Value", value: message.max_metadata_size! },
        writer.uint32(138).fork(),
      ).join();
    }
    if (message.enable_huffman_encoding !== undefined) {
      BoolValue.encode(
        { $type: "google.protobuf.BoolValue", value: message.enable_huffman_encoding! },
        writer.uint32(146).fork(),
      ).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Http2ProtocolOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttp2ProtocolOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hpack_table_size = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.max_concurrent_streams = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.initial_stream_window_size = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.initial_connection_window_size = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.allow_connect = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.allow_metadata = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.max_outbound_frames = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.max_outbound_control_frames = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.max_consecutive_inbound_frames_with_empty_payload = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.max_inbound_priority_frames_per_stream = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.max_inbound_window_update_frames_per_data_frame_sent =
            UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.stream_error_on_invalid_http_messaging = reader.bool();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.override_stream_error_on_invalid_http_message = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          if (message.custom_settings_parameters === undefined) {
            message.custom_settings_parameters = [];
          }
          const el = Http2ProtocolOptions_SettingsParameter.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.custom_settings_parameters!.push(el);
          }
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.connection_keepalive = KeepaliveSettings.decode(reader, reader.uint32());
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.use_oghttp2_codec = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.max_metadata_size = UInt64Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.enable_huffman_encoding = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Http2ProtocolOptions {
    return {
      $type: Http2ProtocolOptions.$type,
      hpack_table_size: isSet(object.hpack_table_size) ? Number(object.hpack_table_size) : undefined,
      max_concurrent_streams: isSet(object.max_concurrent_streams) ? Number(object.max_concurrent_streams) : undefined,
      initial_stream_window_size: isSet(object.initial_stream_window_size)
        ? Number(object.initial_stream_window_size)
        : undefined,
      initial_connection_window_size: isSet(object.initial_connection_window_size)
        ? Number(object.initial_connection_window_size)
        : undefined,
      allow_connect: isSet(object.allow_connect) ? globalThis.Boolean(object.allow_connect) : undefined,
      allow_metadata: isSet(object.allow_metadata) ? globalThis.Boolean(object.allow_metadata) : undefined,
      max_outbound_frames: isSet(object.max_outbound_frames) ? Number(object.max_outbound_frames) : undefined,
      max_outbound_control_frames: isSet(object.max_outbound_control_frames)
        ? Number(object.max_outbound_control_frames)
        : undefined,
      max_consecutive_inbound_frames_with_empty_payload: isSet(object.max_consecutive_inbound_frames_with_empty_payload)
        ? Number(object.max_consecutive_inbound_frames_with_empty_payload)
        : undefined,
      max_inbound_priority_frames_per_stream: isSet(object.max_inbound_priority_frames_per_stream)
        ? Number(object.max_inbound_priority_frames_per_stream)
        : undefined,
      max_inbound_window_update_frames_per_data_frame_sent:
        isSet(object.max_inbound_window_update_frames_per_data_frame_sent)
          ? Number(object.max_inbound_window_update_frames_per_data_frame_sent)
          : undefined,
      stream_error_on_invalid_http_messaging: isSet(object.stream_error_on_invalid_http_messaging)
        ? globalThis.Boolean(object.stream_error_on_invalid_http_messaging)
        : undefined,
      override_stream_error_on_invalid_http_message: isSet(object.override_stream_error_on_invalid_http_message)
        ? Boolean(object.override_stream_error_on_invalid_http_message)
        : undefined,
      custom_settings_parameters: globalThis.Array.isArray(object?.custom_settings_parameters)
        ? object.custom_settings_parameters.map((e: any) => Http2ProtocolOptions_SettingsParameter.fromJSON(e))
        : undefined,
      connection_keepalive: isSet(object.connection_keepalive)
        ? KeepaliveSettings.fromJSON(object.connection_keepalive)
        : undefined,
      use_oghttp2_codec: isSet(object.use_oghttp2_codec) ? Boolean(object.use_oghttp2_codec) : undefined,
      max_metadata_size: isSet(object.max_metadata_size) ? Number(object.max_metadata_size) : undefined,
      enable_huffman_encoding: isSet(object.enable_huffman_encoding)
        ? Boolean(object.enable_huffman_encoding)
        : undefined,
    };
  },

  toJSON(message: Http2ProtocolOptions): unknown {
    const obj: any = {};
    if (message.hpack_table_size !== undefined) {
      obj.hpack_table_size = message.hpack_table_size;
    }
    if (message.max_concurrent_streams !== undefined) {
      obj.max_concurrent_streams = message.max_concurrent_streams;
    }
    if (message.initial_stream_window_size !== undefined) {
      obj.initial_stream_window_size = message.initial_stream_window_size;
    }
    if (message.initial_connection_window_size !== undefined) {
      obj.initial_connection_window_size = message.initial_connection_window_size;
    }
    if (message.allow_connect !== undefined) {
      obj.allow_connect = message.allow_connect;
    }
    if (message.allow_metadata !== undefined) {
      obj.allow_metadata = message.allow_metadata;
    }
    if (message.max_outbound_frames !== undefined) {
      obj.max_outbound_frames = message.max_outbound_frames;
    }
    if (message.max_outbound_control_frames !== undefined) {
      obj.max_outbound_control_frames = message.max_outbound_control_frames;
    }
    if (message.max_consecutive_inbound_frames_with_empty_payload !== undefined) {
      obj.max_consecutive_inbound_frames_with_empty_payload = message.max_consecutive_inbound_frames_with_empty_payload;
    }
    if (message.max_inbound_priority_frames_per_stream !== undefined) {
      obj.max_inbound_priority_frames_per_stream = message.max_inbound_priority_frames_per_stream;
    }
    if (message.max_inbound_window_update_frames_per_data_frame_sent !== undefined) {
      obj.max_inbound_window_update_frames_per_data_frame_sent =
        message.max_inbound_window_update_frames_per_data_frame_sent;
    }
    if (message.stream_error_on_invalid_http_messaging !== undefined) {
      obj.stream_error_on_invalid_http_messaging = message.stream_error_on_invalid_http_messaging;
    }
    if (message.override_stream_error_on_invalid_http_message !== undefined) {
      obj.override_stream_error_on_invalid_http_message = message.override_stream_error_on_invalid_http_message;
    }
    if (message.custom_settings_parameters?.length) {
      obj.custom_settings_parameters = message.custom_settings_parameters.map((e) =>
        Http2ProtocolOptions_SettingsParameter.toJSON(e)
      );
    }
    if (message.connection_keepalive !== undefined) {
      obj.connection_keepalive = KeepaliveSettings.toJSON(message.connection_keepalive);
    }
    if (message.use_oghttp2_codec !== undefined) {
      obj.use_oghttp2_codec = message.use_oghttp2_codec;
    }
    if (message.max_metadata_size !== undefined) {
      obj.max_metadata_size = message.max_metadata_size;
    }
    if (message.enable_huffman_encoding !== undefined) {
      obj.enable_huffman_encoding = message.enable_huffman_encoding;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Http2ProtocolOptions>, I>>(base?: I): Http2ProtocolOptions {
    return Http2ProtocolOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Http2ProtocolOptions>, I>>(object: I): Http2ProtocolOptions {
    const message = createBaseHttp2ProtocolOptions();
    message.hpack_table_size = object.hpack_table_size ?? undefined;
    message.max_concurrent_streams = object.max_concurrent_streams ?? undefined;
    message.initial_stream_window_size = object.initial_stream_window_size ?? undefined;
    message.initial_connection_window_size = object.initial_connection_window_size ?? undefined;
    message.allow_connect = object.allow_connect ?? undefined;
    message.allow_metadata = object.allow_metadata ?? undefined;
    message.max_outbound_frames = object.max_outbound_frames ?? undefined;
    message.max_outbound_control_frames = object.max_outbound_control_frames ?? undefined;
    message.max_consecutive_inbound_frames_with_empty_payload =
      object.max_consecutive_inbound_frames_with_empty_payload ?? undefined;
    message.max_inbound_priority_frames_per_stream = object.max_inbound_priority_frames_per_stream ?? undefined;
    message.max_inbound_window_update_frames_per_data_frame_sent =
      object.max_inbound_window_update_frames_per_data_frame_sent ?? undefined;
    message.stream_error_on_invalid_http_messaging = object.stream_error_on_invalid_http_messaging ?? undefined;
    message.override_stream_error_on_invalid_http_message = object.override_stream_error_on_invalid_http_message ??
      undefined;
    message.custom_settings_parameters =
      object.custom_settings_parameters?.map((e) => Http2ProtocolOptions_SettingsParameter.fromPartial(e)) || undefined;
    message.connection_keepalive = (object.connection_keepalive !== undefined && object.connection_keepalive !== null)
      ? KeepaliveSettings.fromPartial(object.connection_keepalive)
      : undefined;
    message.use_oghttp2_codec = object.use_oghttp2_codec ?? undefined;
    message.max_metadata_size = object.max_metadata_size ?? undefined;
    message.enable_huffman_encoding = object.enable_huffman_encoding ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(Http2ProtocolOptions.$type, Http2ProtocolOptions);

function createBaseHttp2ProtocolOptions_SettingsParameter(): Http2ProtocolOptions_SettingsParameter {
  return { $type: "envoy.config.core.v3.Http2ProtocolOptions.SettingsParameter" };
}

export const Http2ProtocolOptions_SettingsParameter: MessageFns<
  Http2ProtocolOptions_SettingsParameter,
  "envoy.config.core.v3.Http2ProtocolOptions.SettingsParameter"
> = {
  $type: "envoy.config.core.v3.Http2ProtocolOptions.SettingsParameter" as const,

  encode(message: Http2ProtocolOptions_SettingsParameter, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.identifier !== undefined) {
      UInt32Value.encode({ $type: "google.protobuf.UInt32Value", value: message.identifier! }, writer.uint32(10).fork())
        .join();
    }
    if (message.value !== undefined) {
      UInt32Value.encode({ $type: "google.protobuf.UInt32Value", value: message.value! }, writer.uint32(18).fork())
        .join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Http2ProtocolOptions_SettingsParameter {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttp2ProtocolOptions_SettingsParameter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.identifier = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Http2ProtocolOptions_SettingsParameter {
    return {
      $type: Http2ProtocolOptions_SettingsParameter.$type,
      identifier: isSet(object.identifier) ? Number(object.identifier) : undefined,
      value: isSet(object.value) ? Number(object.value) : undefined,
    };
  },

  toJSON(message: Http2ProtocolOptions_SettingsParameter): unknown {
    const obj: any = {};
    if (message.identifier !== undefined) {
      obj.identifier = message.identifier;
    }
    if (message.value !== undefined) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Http2ProtocolOptions_SettingsParameter>, I>>(
    base?: I,
  ): Http2ProtocolOptions_SettingsParameter {
    return Http2ProtocolOptions_SettingsParameter.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Http2ProtocolOptions_SettingsParameter>, I>>(
    object: I,
  ): Http2ProtocolOptions_SettingsParameter {
    const message = createBaseHttp2ProtocolOptions_SettingsParameter();
    message.identifier = object.identifier ?? undefined;
    message.value = object.value ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(Http2ProtocolOptions_SettingsParameter.$type, Http2ProtocolOptions_SettingsParameter);

function createBaseGrpcProtocolOptions(): GrpcProtocolOptions {
  return { $type: "envoy.config.core.v3.GrpcProtocolOptions" };
}

export const GrpcProtocolOptions: MessageFns<GrpcProtocolOptions, "envoy.config.core.v3.GrpcProtocolOptions"> = {
  $type: "envoy.config.core.v3.GrpcProtocolOptions" as const,

  encode(message: GrpcProtocolOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.http2_protocol_options !== undefined) {
      Http2ProtocolOptions.encode(message.http2_protocol_options, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrpcProtocolOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrpcProtocolOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.http2_protocol_options = Http2ProtocolOptions.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrpcProtocolOptions {
    return {
      $type: GrpcProtocolOptions.$type,
      http2_protocol_options: isSet(object.http2_protocol_options)
        ? Http2ProtocolOptions.fromJSON(object.http2_protocol_options)
        : undefined,
    };
  },

  toJSON(message: GrpcProtocolOptions): unknown {
    const obj: any = {};
    if (message.http2_protocol_options !== undefined) {
      obj.http2_protocol_options = Http2ProtocolOptions.toJSON(message.http2_protocol_options);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrpcProtocolOptions>, I>>(base?: I): GrpcProtocolOptions {
    return GrpcProtocolOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrpcProtocolOptions>, I>>(object: I): GrpcProtocolOptions {
    const message = createBaseGrpcProtocolOptions();
    message.http2_protocol_options =
      (object.http2_protocol_options !== undefined && object.http2_protocol_options !== null)
        ? Http2ProtocolOptions.fromPartial(object.http2_protocol_options)
        : undefined;
    return message;
  },
};

messageTypeRegistry.set(GrpcProtocolOptions.$type, GrpcProtocolOptions);

function createBaseHttp3ProtocolOptions(): Http3ProtocolOptions {
  return { $type: "envoy.config.core.v3.Http3ProtocolOptions" };
}

export const Http3ProtocolOptions: MessageFns<Http3ProtocolOptions, "envoy.config.core.v3.Http3ProtocolOptions"> = {
  $type: "envoy.config.core.v3.Http3ProtocolOptions" as const,

  encode(message: Http3ProtocolOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.quic_protocol_options !== undefined) {
      QuicProtocolOptions.encode(message.quic_protocol_options, writer.uint32(10).fork()).join();
    }
    if (message.override_stream_error_on_invalid_http_message !== undefined) {
      BoolValue.encode({
        $type: "google.protobuf.BoolValue",
        value: message.override_stream_error_on_invalid_http_message!,
      }, writer.uint32(18).fork()).join();
    }
    if (message.allow_extended_connect !== undefined && message.allow_extended_connect !== false) {
      writer.uint32(40).bool(message.allow_extended_connect);
    }
    if (message.allow_metadata !== undefined && message.allow_metadata !== false) {
      writer.uint32(48).bool(message.allow_metadata);
    }
    if (message.disable_qpack !== undefined && message.disable_qpack !== false) {
      writer.uint32(56).bool(message.disable_qpack);
    }
    if (
      message.disable_connection_flow_control_for_streams !== undefined &&
      message.disable_connection_flow_control_for_streams !== false
    ) {
      writer.uint32(64).bool(message.disable_connection_flow_control_for_streams);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Http3ProtocolOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttp3ProtocolOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.quic_protocol_options = QuicProtocolOptions.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.override_stream_error_on_invalid_http_message = BoolValue.decode(reader, reader.uint32()).value;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.allow_extended_connect = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.allow_metadata = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.disable_qpack = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.disable_connection_flow_control_for_streams = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Http3ProtocolOptions {
    return {
      $type: Http3ProtocolOptions.$type,
      quic_protocol_options: isSet(object.quic_protocol_options)
        ? QuicProtocolOptions.fromJSON(object.quic_protocol_options)
        : undefined,
      override_stream_error_on_invalid_http_message: isSet(object.override_stream_error_on_invalid_http_message)
        ? Boolean(object.override_stream_error_on_invalid_http_message)
        : undefined,
      allow_extended_connect: isSet(object.allow_extended_connect)
        ? globalThis.Boolean(object.allow_extended_connect)
        : undefined,
      allow_metadata: isSet(object.allow_metadata) ? globalThis.Boolean(object.allow_metadata) : undefined,
      disable_qpack: isSet(object.disable_qpack) ? globalThis.Boolean(object.disable_qpack) : undefined,
      disable_connection_flow_control_for_streams: isSet(object.disable_connection_flow_control_for_streams)
        ? globalThis.Boolean(object.disable_connection_flow_control_for_streams)
        : undefined,
    };
  },

  toJSON(message: Http3ProtocolOptions): unknown {
    const obj: any = {};
    if (message.quic_protocol_options !== undefined) {
      obj.quic_protocol_options = QuicProtocolOptions.toJSON(message.quic_protocol_options);
    }
    if (message.override_stream_error_on_invalid_http_message !== undefined) {
      obj.override_stream_error_on_invalid_http_message = message.override_stream_error_on_invalid_http_message;
    }
    if (message.allow_extended_connect !== undefined) {
      obj.allow_extended_connect = message.allow_extended_connect;
    }
    if (message.allow_metadata !== undefined) {
      obj.allow_metadata = message.allow_metadata;
    }
    if (message.disable_qpack !== undefined) {
      obj.disable_qpack = message.disable_qpack;
    }
    if (message.disable_connection_flow_control_for_streams !== undefined) {
      obj.disable_connection_flow_control_for_streams = message.disable_connection_flow_control_for_streams;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Http3ProtocolOptions>, I>>(base?: I): Http3ProtocolOptions {
    return Http3ProtocolOptions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Http3ProtocolOptions>, I>>(object: I): Http3ProtocolOptions {
    const message = createBaseHttp3ProtocolOptions();
    message.quic_protocol_options =
      (object.quic_protocol_options !== undefined && object.quic_protocol_options !== null)
        ? QuicProtocolOptions.fromPartial(object.quic_protocol_options)
        : undefined;
    message.override_stream_error_on_invalid_http_message = object.override_stream_error_on_invalid_http_message ??
      undefined;
    message.allow_extended_connect = object.allow_extended_connect ?? undefined;
    message.allow_metadata = object.allow_metadata ?? undefined;
    message.disable_qpack = object.disable_qpack ?? undefined;
    message.disable_connection_flow_control_for_streams = object.disable_connection_flow_control_for_streams ??
      undefined;
    return message;
  },
};

messageTypeRegistry.set(Http3ProtocolOptions.$type, Http3ProtocolOptions);

function createBaseSchemeHeaderTransformation(): SchemeHeaderTransformation {
  return { $type: "envoy.config.core.v3.SchemeHeaderTransformation", transformation: undefined };
}

export const SchemeHeaderTransformation: MessageFns<
  SchemeHeaderTransformation,
  "envoy.config.core.v3.SchemeHeaderTransformation"
> = {
  $type: "envoy.config.core.v3.SchemeHeaderTransformation" as const,

  encode(message: SchemeHeaderTransformation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.transformation?.$case) {
      case "scheme_to_overwrite":
        writer.uint32(10).string(message.transformation.scheme_to_overwrite);
        break;
    }
    if (message.match_upstream !== undefined && message.match_upstream !== false) {
      writer.uint32(16).bool(message.match_upstream);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchemeHeaderTransformation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchemeHeaderTransformation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.transformation = { $case: "scheme_to_overwrite", scheme_to_overwrite: reader.string() };
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.match_upstream = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SchemeHeaderTransformation {
    return {
      $type: SchemeHeaderTransformation.$type,
      transformation: isSet(object.scheme_to_overwrite)
        ? { $case: "scheme_to_overwrite", scheme_to_overwrite: globalThis.String(object.scheme_to_overwrite) }
        : undefined,
      match_upstream: isSet(object.match_upstream) ? globalThis.Boolean(object.match_upstream) : undefined,
    };
  },

  toJSON(message: SchemeHeaderTransformation): unknown {
    const obj: any = {};
    if (message.transformation?.$case === "scheme_to_overwrite") {
      obj.scheme_to_overwrite = message.transformation.scheme_to_overwrite;
    }
    if (message.match_upstream !== undefined) {
      obj.match_upstream = message.match_upstream;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SchemeHeaderTransformation>, I>>(base?: I): SchemeHeaderTransformation {
    return SchemeHeaderTransformation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SchemeHeaderTransformation>, I>>(object: I): SchemeHeaderTransformation {
    const message = createBaseSchemeHeaderTransformation();
    if (
      object.transformation?.$case === "scheme_to_overwrite" &&
      object.transformation?.scheme_to_overwrite !== undefined &&
      object.transformation?.scheme_to_overwrite !== null
    ) {
      message.transformation = {
        $case: "scheme_to_overwrite",
        scheme_to_overwrite: object.transformation.scheme_to_overwrite,
      };
    }
    message.match_upstream = object.match_upstream ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(SchemeHeaderTransformation.$type, SchemeHeaderTransformation);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
