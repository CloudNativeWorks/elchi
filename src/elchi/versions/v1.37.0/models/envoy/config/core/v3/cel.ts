// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/config/core/v3/cel.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../typeRegistry";

export const protobufPackage = "envoy.config.core.v3";

/**
 * CEL expression evaluation configuration.
 * These options control the behavior of the Common Expression Language runtime for
 * individual CEL expressions.
 */
export interface CelExpressionConfig {
  $type: "envoy.config.core.v3.CelExpressionConfig";
  /**
   * Enable string conversion functions for CEL expressions. When enabled, CEL expressions
   * can convert values to strings using the ``string()`` function.
   *
   * .. attention::
   *
   *   This option is disabled by default to avoid unbounded memory allocation.
   *   CEL evaluation cost is typically bounded by the expression size, but converting
   *   arbitrary values (e.g., large messages, lists, or maps) to strings may allocate
   *   memory proportional to input data size, which can be unbounded and lead to
   *   memory exhaustion.
   */
  enable_string_conversion?:
    | boolean
    | undefined;
  /**
   * Enable string concatenation for CEL expressions. When enabled, CEL expressions
   * can concatenate strings using the ``+`` operator.
   *
   * .. attention::
   *
   *   This option is disabled by default to avoid unbounded memory allocation.
   *   While CEL normally bounds evaluation by expression size, enabling string
   *   concatenation allows building outputs whose size depends on input data,
   *   potentially causing large intermediate allocations and memory exhaustion.
   */
  enable_string_concat?:
    | boolean
    | undefined;
  /**
   * Enable string manipulation functions for CEL expressions. When enabled, CEL
   * expressions can use additional string functions:
   *
   * * ``replace(old, new)`` - Replaces all occurrences of ``old`` with ``new``.
   * * ``split(separator)`` - Splits a string into a list of substrings.
   * * ``lowerAscii()`` - Converts ASCII characters to lowercase.
   * * ``upperAscii()`` - Converts ASCII characters to uppercase.
   *
   * .. note::
   *
   *   Standard CEL string functions like ``contains()``, ``startsWith()``, and
   *   ``endsWith()`` are always available regardless of this setting.
   *
   * .. attention::
   *
   *   This option is disabled by default to avoid unbounded memory allocation.
   *   Although CEL generally bounds evaluation by expression size, functions such as
   *   ``replace``, ``split``, ``lowerAscii()``, and ``upperAscii()`` can allocate memory
   *   proportional to input data size. Under adversarial inputs this can lead to
   *   unbounded allocations and memory exhaustion.
   */
  enable_string_functions?: boolean | undefined;
}

function createBaseCelExpressionConfig(): CelExpressionConfig {
  return { $type: "envoy.config.core.v3.CelExpressionConfig" };
}

export const CelExpressionConfig: MessageFns<CelExpressionConfig, "envoy.config.core.v3.CelExpressionConfig"> = {
  $type: "envoy.config.core.v3.CelExpressionConfig" as const,

  encode(message: CelExpressionConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enable_string_conversion !== undefined && message.enable_string_conversion !== false) {
      writer.uint32(8).bool(message.enable_string_conversion);
    }
    if (message.enable_string_concat !== undefined && message.enable_string_concat !== false) {
      writer.uint32(16).bool(message.enable_string_concat);
    }
    if (message.enable_string_functions !== undefined && message.enable_string_functions !== false) {
      writer.uint32(24).bool(message.enable_string_functions);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CelExpressionConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCelExpressionConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enable_string_conversion = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.enable_string_concat = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.enable_string_functions = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CelExpressionConfig {
    return {
      $type: CelExpressionConfig.$type,
      enable_string_conversion: isSet(object.enable_string_conversion)
        ? globalThis.Boolean(object.enable_string_conversion)
        : undefined,
      enable_string_concat: isSet(object.enable_string_concat)
        ? globalThis.Boolean(object.enable_string_concat)
        : undefined,
      enable_string_functions: isSet(object.enable_string_functions)
        ? globalThis.Boolean(object.enable_string_functions)
        : undefined,
    };
  },

  toJSON(message: CelExpressionConfig): unknown {
    const obj: any = {};
    if (message.enable_string_conversion !== undefined) {
      obj.enable_string_conversion = message.enable_string_conversion;
    }
    if (message.enable_string_concat !== undefined) {
      obj.enable_string_concat = message.enable_string_concat;
    }
    if (message.enable_string_functions !== undefined) {
      obj.enable_string_functions = message.enable_string_functions;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CelExpressionConfig>, I>>(base?: I): CelExpressionConfig {
    return CelExpressionConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CelExpressionConfig>, I>>(object: I): CelExpressionConfig {
    const message = createBaseCelExpressionConfig();
    message.enable_string_conversion = object.enable_string_conversion ?? undefined;
    message.enable_string_concat = object.enable_string_concat ?? undefined;
    message.enable_string_functions = object.enable_string_functions ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(CelExpressionConfig.$type, CelExpressionConfig);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
