// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/transport_sockets/tls/cert_mappers/sni/v3/config.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../../../typeRegistry";

export const protobufPackage = "envoy.extensions.transport_sockets.tls.cert_mappers.sni.v3";

/** Uses the SNI value from the TLS client hello as the secret resource name. */
export interface SNI {
  $type: "envoy.extensions.transport_sockets.tls.cert_mappers.sni.v3.SNI";
  /** The value to use as the secret name when SNI is empty or absent. */
  default_value?: string | undefined;
}

function createBaseSNI(): SNI {
  return { $type: "envoy.extensions.transport_sockets.tls.cert_mappers.sni.v3.SNI" };
}

export const SNI: MessageFns<SNI, "envoy.extensions.transport_sockets.tls.cert_mappers.sni.v3.SNI"> = {
  $type: "envoy.extensions.transport_sockets.tls.cert_mappers.sni.v3.SNI" as const,

  encode(message: SNI, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.default_value !== undefined && message.default_value !== "") {
      writer.uint32(10).string(message.default_value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SNI {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSNI();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.default_value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SNI {
    return {
      $type: SNI.$type,
      default_value: isSet(object.default_value) ? globalThis.String(object.default_value) : undefined,
    };
  },

  toJSON(message: SNI): unknown {
    const obj: any = {};
    if (message.default_value !== undefined) {
      obj.default_value = message.default_value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SNI>, I>>(base?: I): SNI {
    return SNI.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SNI>, I>>(object: I): SNI {
    const message = createBaseSNI();
    message.default_value = object.default_value ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(SNI.$type, SNI);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
