// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/transport_sockets/tls/cert_selectors/on_demand_secret/v3/config.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../../../typeRegistry";
import { ConfigSource } from "../../../../../../config/core/v3/config_source";
import { TypedExtensionConfig } from "../../../../../../config/core/v3/extension";

export const protobufPackage = "envoy.extensions.transport_sockets.tls.cert_selectors.on_demand_secret.v3";

/**
 * Fetches the secret on-demand while allowing the parent cluster or listener to accept connections
 * without warming. During the handshake, a secret name is derived from the peer hello message, an
 * SDS resource request starts, and the handshake is paused. Once an SDS response is received with a
 * resource, the handshake is resumed with the provided certificate. If the SDS server indicates the
 * resource removal, the handshake is failed, and the SDS subscription to the resource is stopped.
 *
 * Similar to the regular SDS, the certificate is configured using the outer common TLS context,
 * e.g. by setting the FIPS compliance policy on the loaded certificate.
 */
export interface Config {
  $type: "envoy.extensions.transport_sockets.tls.cert_selectors.on_demand_secret.v3.Config";
  /** Defines the configuration source of the secrets. */
  config_source?:
    | ConfigSource
    | undefined;
  /**
   * Extension point to specify a function to compute the secret name. The extension is called
   * during the TLS handshake after receiving the "CLIENT HELLO" message from the client.
   * [#extension-category: envoy.tls.certificate_mappers]
   */
  certificate_mapper?:
    | TypedExtensionConfig
    | undefined;
  /**
   * A list of secret resource names to start fetching on configuration load (prior to receiving any
   * requests). The parent resource initializes immediately without waiting for the fetch to
   * complete.
   */
  prefetch_secret_names?: string[] | undefined;
}

function createBaseConfig(): Config {
  return { $type: "envoy.extensions.transport_sockets.tls.cert_selectors.on_demand_secret.v3.Config" };
}

export const Config: MessageFns<
  Config,
  "envoy.extensions.transport_sockets.tls.cert_selectors.on_demand_secret.v3.Config"
> = {
  $type: "envoy.extensions.transport_sockets.tls.cert_selectors.on_demand_secret.v3.Config" as const,

  encode(message: Config, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.config_source !== undefined) {
      ConfigSource.encode(message.config_source, writer.uint32(10).fork()).join();
    }
    if (message.certificate_mapper !== undefined) {
      TypedExtensionConfig.encode(message.certificate_mapper, writer.uint32(18).fork()).join();
    }
    if (message.prefetch_secret_names !== undefined && message.prefetch_secret_names.length !== 0) {
      for (const v of message.prefetch_secret_names) {
        writer.uint32(26).string(v!);
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Config {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.config_source = ConfigSource.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.certificate_mapper = TypedExtensionConfig.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          if (message.prefetch_secret_names === undefined) {
            message.prefetch_secret_names = [];
          }
          const el = reader.string();
          if (el !== undefined) {
            message.prefetch_secret_names!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Config {
    return {
      $type: Config.$type,
      config_source: isSet(object.config_source) ? ConfigSource.fromJSON(object.config_source) : undefined,
      certificate_mapper: isSet(object.certificate_mapper)
        ? TypedExtensionConfig.fromJSON(object.certificate_mapper)
        : undefined,
      prefetch_secret_names: globalThis.Array.isArray(object?.prefetch_secret_names)
        ? object.prefetch_secret_names.map((e: any) => globalThis.String(e))
        : undefined,
    };
  },

  toJSON(message: Config): unknown {
    const obj: any = {};
    if (message.config_source !== undefined) {
      obj.config_source = ConfigSource.toJSON(message.config_source);
    }
    if (message.certificate_mapper !== undefined) {
      obj.certificate_mapper = TypedExtensionConfig.toJSON(message.certificate_mapper);
    }
    if (message.prefetch_secret_names?.length) {
      obj.prefetch_secret_names = message.prefetch_secret_names;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Config>, I>>(base?: I): Config {
    return Config.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Config>, I>>(object: I): Config {
    const message = createBaseConfig();
    message.config_source = (object.config_source !== undefined && object.config_source !== null)
      ? ConfigSource.fromPartial(object.config_source)
      : undefined;
    message.certificate_mapper = (object.certificate_mapper !== undefined && object.certificate_mapper !== null)
      ? TypedExtensionConfig.fromPartial(object.certificate_mapper)
      : undefined;
    message.prefetch_secret_names = object.prefetch_secret_names?.map((e) => e) || undefined;
    return message;
  },
};

messageTypeRegistry.set(Config.$type, Config);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
