// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: envoy/extensions/filters/network/dynamic_modules/v3/dynamic_modules.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Any } from "../../../../../../google/protobuf/any";
import { messageTypeRegistry } from "../../../../../../typeRegistry";
import { DynamicModuleConfig } from "../../../../dynamic_modules/v3/dynamic_modules";

export const protobufPackage = "envoy.extensions.filters.network.dynamic_modules.v3";

/**
 * Configuration for the Dynamic Modules network filter. This filter allows loading shared object
 * files that can be loaded via ``dlopen`` to extend the network filter chain.
 *
 * A module can be loaded by multiple network filters; the module is loaded only once and shared
 * across multiple filters.
 *
 * Unlike HTTP filters which operate on structured headers, body, and trailers, network filters work
 * with raw TCP byte streams. The filter can:
 *
 * * Inspect, modify, or inject data into the downstream connection.
 * * Access connection-level information such as addresses and TLS status.
 * * Control connection lifecycle (e.g., close the connection).
 */
export interface DynamicModuleNetworkFilter {
  $type: "envoy.extensions.filters.network.dynamic_modules.v3.DynamicModuleNetworkFilter";
  /** Specifies the shared-object level configuration. */
  dynamic_module_config?:
    | DynamicModuleConfig
    | undefined;
  /**
   * The name for this filter configuration.
   *
   * This can be used to distinguish between different filter implementations inside a dynamic
   * module. For example, a module can have completely different filter implementations. When Envoy
   * receives this configuration, it passes the ``filter_name`` to the dynamic module's network
   * filter config init function together with the ``filter_config``. That way a module can decide
   * which in-module filter implementation to use based on the name at load time.
   */
  filter_name?:
    | string
    | undefined;
  /**
   * The configuration for the filter chosen by ``filter_name``.
   *
   * This is passed to the module's network filter initialization function. Together with the
   * ``filter_name``, the module can decide which in-module filter implementation to use and
   * fine-tune the behavior of the filter.
   *
   * For example, if a module has two filter implementations, one for echo and one for rate
   * limiting, ``filter_name`` is used to choose either echo or rate limiting. The
   * ``filter_config`` can be used to configure the echo behavior or the rate limiting parameters.
   *
   * ``google.protobuf.Struct`` is serialized as JSON before passing it to the module.
   * ``google.protobuf.BytesValue`` and ``google.protobuf.StringValue`` are passed directly
   * without the wrapper.
   *
   * .. code-block:: yaml
   *
   *  # Passing a string value
   *  filter_config:
   *    "@type": "type.googleapis.com/google.protobuf.StringValue"
   *    value: hello
   *
   *  # Passing raw bytes
   *  filter_config:
   *    "@type": "type.googleapis.com/google.protobuf.BytesValue"
   *    value: aGVsbG8=  # echo -n "hello" | base64
   */
  filter_config?:
    | Any
    | undefined;
  /**
   * If ``true``, the dynamic module is a terminal filter to use without an upstream connection.
   *
   * The dynamic module is responsible for creating and sending the response to downstream.
   *
   * Defaults to ``false``.
   */
  terminal_filter?: boolean | undefined;
}

function createBaseDynamicModuleNetworkFilter(): DynamicModuleNetworkFilter {
  return { $type: "envoy.extensions.filters.network.dynamic_modules.v3.DynamicModuleNetworkFilter" };
}

export const DynamicModuleNetworkFilter: MessageFns<
  DynamicModuleNetworkFilter,
  "envoy.extensions.filters.network.dynamic_modules.v3.DynamicModuleNetworkFilter"
> = {
  $type: "envoy.extensions.filters.network.dynamic_modules.v3.DynamicModuleNetworkFilter" as const,

  encode(message: DynamicModuleNetworkFilter, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dynamic_module_config !== undefined) {
      DynamicModuleConfig.encode(message.dynamic_module_config, writer.uint32(10).fork()).join();
    }
    if (message.filter_name !== undefined && message.filter_name !== "") {
      writer.uint32(18).string(message.filter_name);
    }
    if (message.filter_config !== undefined) {
      Any.encode(message.filter_config, writer.uint32(26).fork()).join();
    }
    if (message.terminal_filter !== undefined && message.terminal_filter !== false) {
      writer.uint32(32).bool(message.terminal_filter);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DynamicModuleNetworkFilter {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDynamicModuleNetworkFilter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dynamic_module_config = DynamicModuleConfig.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.filter_name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.filter_config = Any.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.terminal_filter = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DynamicModuleNetworkFilter {
    return {
      $type: DynamicModuleNetworkFilter.$type,
      dynamic_module_config: isSet(object.dynamic_module_config)
        ? DynamicModuleConfig.fromJSON(object.dynamic_module_config)
        : undefined,
      filter_name: isSet(object.filter_name) ? globalThis.String(object.filter_name) : undefined,
      filter_config: isSet(object.filter_config) ? Any.fromJSON(object.filter_config) : undefined,
      terminal_filter: isSet(object.terminal_filter) ? globalThis.Boolean(object.terminal_filter) : undefined,
    };
  },

  toJSON(message: DynamicModuleNetworkFilter): unknown {
    const obj: any = {};
    if (message.dynamic_module_config !== undefined) {
      obj.dynamic_module_config = DynamicModuleConfig.toJSON(message.dynamic_module_config);
    }
    if (message.filter_name !== undefined) {
      obj.filter_name = message.filter_name;
    }
    if (message.filter_config !== undefined) {
      obj.filter_config = Any.toJSON(message.filter_config);
    }
    if (message.terminal_filter !== undefined) {
      obj.terminal_filter = message.terminal_filter;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DynamicModuleNetworkFilter>, I>>(base?: I): DynamicModuleNetworkFilter {
    return DynamicModuleNetworkFilter.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DynamicModuleNetworkFilter>, I>>(object: I): DynamicModuleNetworkFilter {
    const message = createBaseDynamicModuleNetworkFilter();
    message.dynamic_module_config =
      (object.dynamic_module_config !== undefined && object.dynamic_module_config !== null)
        ? DynamicModuleConfig.fromPartial(object.dynamic_module_config)
        : undefined;
    message.filter_name = object.filter_name ?? undefined;
    message.filter_config = (object.filter_config !== undefined && object.filter_config !== null)
      ? Any.fromPartial(object.filter_config)
      : undefined;
    message.terminal_filter = object.terminal_filter ?? undefined;
    return message;
  },
};

messageTypeRegistry.set(DynamicModuleNetworkFilter.$type, DynamicModuleNetworkFilter);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
