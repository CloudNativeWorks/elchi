// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.7
//   protoc               unknown
// source: contrib/envoy/extensions/filters/http/alpn/v3/alpn.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../../../../../../../typeRegistry";

export const protobufPackage = "istio.envoy.config.filter.http.alpn.v2alpha1";

/** FilterConfig is the config for Istio-specific filter. */
export interface FilterConfig {
  $type: "istio.envoy.config.filter.http.alpn.v2alpha1.FilterConfig";
  /** Map from upstream protocol to list of ALPN */
  alpn_override?: FilterConfig_AlpnOverride[] | undefined;
}

/** Upstream protocols */
export enum FilterConfig_Protocol {
  HTTP10 = "HTTP10",
  HTTP11 = "HTTP11",
  HTTP2 = "HTTP2",
}

export function filterConfig_ProtocolFromJSON(object: any): FilterConfig_Protocol {
  switch (object) {
    case 0:
    case "HTTP10":
      return FilterConfig_Protocol.HTTP10;
    case 1:
    case "HTTP11":
      return FilterConfig_Protocol.HTTP11;
    case 2:
    case "HTTP2":
      return FilterConfig_Protocol.HTTP2;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum FilterConfig_Protocol");
  }
}

export function filterConfig_ProtocolToJSON(object: FilterConfig_Protocol): string {
  switch (object) {
    case FilterConfig_Protocol.HTTP10:
      return "HTTP10";
    case FilterConfig_Protocol.HTTP11:
      return "HTTP11";
    case FilterConfig_Protocol.HTTP2:
      return "HTTP2";
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum FilterConfig_Protocol");
  }
}

export function filterConfig_ProtocolToNumber(object: FilterConfig_Protocol): number {
  switch (object) {
    case FilterConfig_Protocol.HTTP10:
      return 0;
    case FilterConfig_Protocol.HTTP11:
      return 1;
    case FilterConfig_Protocol.HTTP2:
      return 2;
    default:
      throw new globalThis.Error("Unrecognized enum value " + object + " for enum FilterConfig_Protocol");
  }
}

export interface FilterConfig_AlpnOverride {
  $type: "istio.envoy.config.filter.http.alpn.v2alpha1.FilterConfig.AlpnOverride";
  /** Upstream protocol */
  upstream_protocol?:
    | FilterConfig_Protocol
    | undefined;
  /** A list of ALPN that will override the ALPN for upstream TLS connections. */
  alpn_override?: string[] | undefined;
}

function createBaseFilterConfig(): FilterConfig {
  return { $type: "istio.envoy.config.filter.http.alpn.v2alpha1.FilterConfig" };
}

export const FilterConfig: MessageFns<FilterConfig, "istio.envoy.config.filter.http.alpn.v2alpha1.FilterConfig"> = {
  $type: "istio.envoy.config.filter.http.alpn.v2alpha1.FilterConfig" as const,

  encode(message: FilterConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.alpn_override !== undefined && message.alpn_override.length !== 0) {
      for (const v of message.alpn_override) {
        FilterConfig_AlpnOverride.encode(v!, writer.uint32(10).fork()).join();
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FilterConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFilterConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          if (message.alpn_override === undefined) {
            message.alpn_override = [];
          }
          const el = FilterConfig_AlpnOverride.decode(reader, reader.uint32());
          if (el !== undefined) {
            message.alpn_override!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FilterConfig {
    return {
      $type: FilterConfig.$type,
      alpn_override: globalThis.Array.isArray(object?.alpn_override)
        ? object.alpn_override.map((e: any) => FilterConfig_AlpnOverride.fromJSON(e))
        : undefined,
    };
  },

  toJSON(message: FilterConfig): unknown {
    const obj: any = {};
    if (message.alpn_override?.length) {
      obj.alpn_override = message.alpn_override.map((e) => FilterConfig_AlpnOverride.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FilterConfig>, I>>(base?: I): FilterConfig {
    return FilterConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FilterConfig>, I>>(object: I): FilterConfig {
    const message = createBaseFilterConfig();
    message.alpn_override = object.alpn_override?.map((e) => FilterConfig_AlpnOverride.fromPartial(e)) || undefined;
    return message;
  },
};

messageTypeRegistry.set(FilterConfig.$type, FilterConfig);

function createBaseFilterConfig_AlpnOverride(): FilterConfig_AlpnOverride {
  return { $type: "istio.envoy.config.filter.http.alpn.v2alpha1.FilterConfig.AlpnOverride" };
}

export const FilterConfig_AlpnOverride: MessageFns<
  FilterConfig_AlpnOverride,
  "istio.envoy.config.filter.http.alpn.v2alpha1.FilterConfig.AlpnOverride"
> = {
  $type: "istio.envoy.config.filter.http.alpn.v2alpha1.FilterConfig.AlpnOverride" as const,

  encode(message: FilterConfig_AlpnOverride, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.upstream_protocol !== undefined && message.upstream_protocol !== FilterConfig_Protocol.HTTP10) {
      writer.uint32(8).int32(filterConfig_ProtocolToNumber(message.upstream_protocol));
    }
    if (message.alpn_override !== undefined && message.alpn_override.length !== 0) {
      for (const v of message.alpn_override) {
        writer.uint32(18).string(v!);
      }
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FilterConfig_AlpnOverride {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFilterConfig_AlpnOverride();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.upstream_protocol = filterConfig_ProtocolFromJSON(reader.int32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          if (message.alpn_override === undefined) {
            message.alpn_override = [];
          }
          const el = reader.string();
          if (el !== undefined) {
            message.alpn_override!.push(el);
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FilterConfig_AlpnOverride {
    return {
      $type: FilterConfig_AlpnOverride.$type,
      upstream_protocol: isSet(object.upstream_protocol)
        ? filterConfig_ProtocolFromJSON(object.upstream_protocol)
        : undefined,
      alpn_override: globalThis.Array.isArray(object?.alpn_override)
        ? object.alpn_override.map((e: any) => globalThis.String(e))
        : undefined,
    };
  },

  toJSON(message: FilterConfig_AlpnOverride): unknown {
    const obj: any = {};
    if (message.upstream_protocol !== undefined) {
      obj.upstream_protocol = filterConfig_ProtocolToJSON(message.upstream_protocol);
    }
    if (message.alpn_override?.length) {
      obj.alpn_override = message.alpn_override;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FilterConfig_AlpnOverride>, I>>(base?: I): FilterConfig_AlpnOverride {
    return FilterConfig_AlpnOverride.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FilterConfig_AlpnOverride>, I>>(object: I): FilterConfig_AlpnOverride {
    const message = createBaseFilterConfig_AlpnOverride();
    message.upstream_protocol = object.upstream_protocol ?? undefined;
    message.alpn_override = object.alpn_override?.map((e) => e) || undefined;
    return message;
  },
};

messageTypeRegistry.set(FilterConfig_AlpnOverride.$type, FilterConfig_AlpnOverride);

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
